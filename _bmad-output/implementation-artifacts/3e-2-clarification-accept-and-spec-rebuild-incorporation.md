# Story 3e.2: Clarification Accept + Spec-Rebuild Incorporation

Status: done

<!-- Proposed via systematic-debugging diagnosis 2026-06-23; Epic 3e formally slotted via sprint-change-proposal-2026-06-23.md (correct-course). Depends on 3e-1 (creation seam). -->
<!-- 2026-06-24 (bmad-create-story finalize): dependency 3e-1 is DONE (CREATE half shipped). Context-engine pass re-verified ALL back-half seams this story touches against the live codebase — they HELD (3e-1's churn was in runner-contracts / both runner.mjs / new ClarificationIngestService / RunnerBroker.handleSuccess, NOT the sweep/lifecycle/bundle files here). Re-verified anchors 2026-06-24: sweep call `ArtifactOperationService.sweepAfterSpecRebuild(...)` @ ~L397 (inside `markArtifactAvailable`); `markAccepted` @ `ClarificationLifecycleService.java:138` STILL zero production callers; `acknowledgesQuestion` STUB @ `ClarificationLifecycleOrchestrator.java:263` (lineage scope `lineageArtifactIds` @ :237); the `newVersion(parentArtifactId,...)` graft @ `ArtifactOperationService.java:493` STILL has zero production callers (`createNextVersion` IS reached via the UPDATE/REPLACE path @ :765 and the prOutput-enrich path, but the explicit-parent graft method is dormant — R3 still holds); `retrySpecGeneration` @ `WorkflowOrchestrationService.java:299`; `assembleForSpecInvestigation` @ `ContextBundleService.java:775`; `collectExecutionFeedbackReferences` @ :1015; `AllowedAction.ANSWER_CLARIFICATION` @ `AllowedAction.java:8` (no accept/regenerate yet); answer REST `POST …/clarifications/{id}/answer` @ `WorkflowController.java:966` (method @ :998); `WorkflowCommandService.answerClarification` @ :184; CLI `WorkflowCommands.answerClarification` @ :1633. ⚠️ ONE AC CORRECTION applied — see R2/AC2: `retrySpecGeneration` RE-DISPATCHES ONLY and MUST NOT re-transition (Trap T8, verified `WorkflowOrchestrationService.java:291-298`); the `WaitingForSpecApproval → Investigating` transition is done by the COMMAND first (mirror `ApprovalService.rejectSpec` @ :456-477), THEN retry is called. Anchor by SEMANTIC markers, re-grep before editing. -->

> **⚠️ READ FIRST — this CLOSES the FR10 loop; most of the back half already exists but is starved of inputs.** Story 2.12 built the full incorporation machinery — `ClarificationLifecycleService` (`answered→accepted`, `accepted→incorporated`, `accepted→superseded`, `answered→rejected_invalid`) and `ClarificationLifecycleOrchestrator.sweepAfterSpecRebuild`, which is **already wired** into `ArtifactOperationService.markAvailable` and fires on **every** SPEC artifact becoming available (verified `ArtifactOperationService.java:395–405`). But the loop is **starved at three points**: (1) **nothing transitions `answered → accepted`** — there is no `accept_clarification` AllowedAction and `markAccepted` has **zero production callers**, so the sweep's `accepted`-only filter always finds nothing; (2) **`acknowledgesQuestion` is an explicit STUB** — a case-sensitive substring scan of the new spec's bytes for the literal `questionId`, with a `TODO(epic-3-runner-contracts): replace with a structured clarification_acknowledgements block` (verified `ClarificationLifecycleOrchestrator.java:251–285`); (3) **a re-dispatched spec does NOT graft onto the original spec lineage**, so the original clarification's pinned `artifactId` is not in the new spec's lineage and the sweep **skips it** (see R3 — the central architectural trap).
>
> **3e-2 builds: an explicit accept action → a spec-rebuild action that feeds answered clarifications into the spec context bundle → a structured `clarification_acknowledgements` channel the runner emits → the sweep consuming it (replacing the substring stub) → the rebuilt spec grafting as a `newVersion` so lineage holds.** Depends on **3e-1** (open clarifications must exist before they can be answered/accepted).
>
> **Depends on 3e-1. Same proposed Epic 3e. Ratify via correct-course before `dev-story`.**
>
> **Four reconciliations the live codebase forces (read all four before coding):**
>
> 1. **The sweep already fires — on `markAvailable`, not `newVersion`.** The orchestrator's own javadoc says "hooked into `newVersion`" but the **actual** call site is `ArtifactOperationService.markAvailable` (L395), which runs for every SPEC version (incl. v1, via the broker's `markArtifactAvailable`). So you do NOT need to wire the sweep — you need to (a) give it `accepted` rows to act on and (b) replace its acknowledgement oracle. The "newVersion" javadoc is **stale** — fix it.
> 2. **`markAccepted` has no trigger.** `AllowedAction` has only `answer_clarification` (no accept). `ClarificationLifecycleService.markAccepted` is called by nobody. Add an `accept_clarification` action + command + REST/CLI + a `WorkflowCommandService.acceptClarification` that calls `markAccepted` (the answer twin of `submitAnswer`). See R1. (Alternative considered + rejected: auto-accept on answer — it collapses the `answered`/`accepted` distinction the sweep and 2.12 audit depend on. R1.)
> 3. **A re-dispatched spec mints a FRESH lineage, breaking the sweep's lineage scope.** The broker ingests every spec via `recordOperation(CREATE)` → a brand-new `art_` with `parentArtifactId = null`. `newVersion`/`createNextVersion` (the graft) has **zero production callers** (verified `ArtifactOperationService.java:492–512`). The sweep scopes by `lineageArtifactIds(newSpec)` (parent-walk) ∩ `accepted` clarifications — a clarification pinned to v1 is invisible to a v2 in a different lineage. **This is the crux (R3):** the spec-rebuild path must produce the new spec as a `newVersion` graft of the prior spec, OR the sweep scope must widen from lineage to workflow-run. Recommend the graft.
> 4. **Answers reach the runner by reference, never inline — they must be MATERIALIZED into the input bundle.** `priorFeedbackReferences` carries `{referenceId, kind}` only (no content) and the runner reads referenced content from its mounted input dir. So feeding an answer to the rebuild = adding a `clarification.answered` feedback ref **and** materializing the (redacted) question+answer as an input file the runner can read — not embedding answer text in the bundle JSON. See R4 + the redaction gate.

## Story

As a product reviewer,
I want to accept my clarification answers and trigger a spec regeneration that incorporates them, with the runner reporting which questions it actually addressed,
so that the visible-incorporation lifecycle (2.12) closes for real: accepted answers drive a rebuilt spec, and each clarification is marked `incorporated` or `superseded` based on what the new spec genuinely did — not a brittle substring guess.

## Acceptance Criteria

> Reconciled against the live codebase (2.12 machinery present but input-starved). Where epic/FR10 wording assumes a working loop, the reconciled AC below is authoritative; rationale in Dev Notes (R1–R7).

1. **Given** allowed-actions (2.14) and a `WaitingForSpecApproval` run with an `answered` clarification, **Then** a new `AllowedAction ACCEPT_CLARIFICATION("accept_clarification")` is registered (drift: enum + `allowed-actions.placeholder.json` + `AllowedActionRegistryPinTest` wire pin) and surfaced by `WorkflowInspectionService.computeActionMatrix` for the reviewer role (`product_reviewer`/`workflow_owner`) at `WaitingForSpecApproval`; a REST `POST /workflows/{id}/clarifications/{clarificationId}/accept` + CLI parity drives `WorkflowCommandService.acceptClarification` → `ClarificationLifecycleService.markAccepted` (`answered → accepted`). Re-accepting an already-`accepted` row is idempotent-friendly; accepting an `open`/terminal row surfaces the existing lifecycle guard error. Mirrors the `answer_clarification` wiring (2.11/2.13).

2. **Given** an accepted clarification, **Then** a new `AllowedAction REGENERATE_SPEC("regenerate_spec_with_clarifications")` (same drift sites) lets the reviewer trigger a spec rebuild from `WaitingForSpecApproval`. The command's executor must do the transition + re-dispatch in the **same** `@Transactional` boundary, in the exact order `ApprovalService.rejectSpec` uses (verified `ApprovalService.java:456–477`): (a) `workflowTransitionService.transition(runId, WorkflowState.INVESTIGATING, actor, "regenerate specification", idempotencyKey, details)` performs the `WaitingForSpecApproval → Investigating` transition (the transition service appends `workflow.stateChanged` itself — do NOT append a second one); THEN (b) `WorkflowOrchestrationService.retrySpecGeneration(runId, correlationId)` **re-dispatches ONLY** — it is **reused, not duplicated**, increments the existing `specRejectionLoopCount` for a deterministic idempotency key, and no-ops an in-flight dispatch. ⚠️ `retrySpecGeneration` **MUST NOT** be expected to transition: its contract is re-dispatch-only (Trap T8, `WorkflowOrchestrationService.java:291–298` — an `INVESTIGATING → INVESTIGATING` transition is illegal/duplicate). All-or-nothing: a dispatch failure rolls back the transition.

3. **Given** the spec rebuild dispatch, **Then** `ContextBundleService.assembleForSpecInvestigation` (L770+) adds, to `priorFeedbackReferences`, one `{referenceId: clarificationId, kind: "clarification.answered"}` entry per `accepted` clarification of the run (alongside the existing `spec.rejection` entries), **and** the bundle composer materializes each accepted clarification's `questionId`+`questionText`+`answerText` as a **redaction-policed** input file under the runner input dir (referenced, never inlined into the bundle JSON) so the runner can read the answer. The materialized content passes the existing adversarial redaction fixture gate (no secret leakage; answer text is reviewer-authored but still policed).

4. **Given** the runner-result contract, **Then** `runner-result.v1.schema.json` `specArtifact` gains an **optional** `clarificationAcknowledgements` array (declared in `properties`, not `required`, **no** `schemaVersion` bump — same additive discipline as 3e-1's `questions`). Each item: `{ "questionId": string (same pattern), "addressed": boolean }`. Both `runner.mjs` files (BYTE-IDENTICAL) emit it at `stage === 'spec'` from a fenced ` ```clarificationAcknowledgements ` block (mirror 3e-1's fence-split); both offline mocks emit a deterministic acknowledgement (`addressed:true` for any question id present in the input bundle) so the loop IT is deterministic.

5. **Given** the rebuilt spec is ingested, **Then** it is recorded as a **`newVersion` graft of the prior spec** (NOT a fresh lineage) so the prior clarification's pinned `artifactId` is in the new spec's `lineageArtifactIds` and the sweep considers it. (Implementation: route the spec-rebuild ingest through `ArtifactOperationService.newVersion`/`createNextVersion` for a run that already has a spec, OR widen `ClarificationLifecycleOrchestrator` lineage scope to the workflow-run — **R3 decides; the graft is recommended**. Whichever is chosen, an IT proves a v1-pinned clarification is swept on v2 availability.)

6. **Given** the sweep oracle, **Then** `ClarificationLifecycleOrchestrator.acknowledgesQuestion(...)` (the documented stub seam, L251–285) is **replaced** by consuming the structured `clarificationAcknowledgements` from the rebuilt spec's runner result (`addressed == true` ⇒ `markIncorporated`, else `markSuperseded` with `no_effect_reason = "spec_runner_skipped_question"` — already in the allowed vocabulary, `ClarificationLifecycleService.java:74–80`). The acknowledgements must be **plumbed to the sweep**: persisted on/with the spec artifact at ingest (the sweep runs inside `markAvailable` which has only the artifact, not the runner result) — e.g. an `artifact_clarification_acknowledgements` side-store keyed by artifactId, or carried on the artifact metadata read at sweep time. The substring scan + its `TODO` comment are deleted; the stale "hooked into newVersion" javadoc is corrected to "markAvailable".

7. **Given** the rebuilt spec, **When** the sweep runs, **Then** the FE incorporation lifecycle (2.18 `ClarificationRegion`) reflects each clarification moving `accepted → incorporated`/`superseded`, and a subsequent EXECUTION-stage context bundle includes `incorporated` clarifications via the existing `collectExecutionFeedbackReferences` (`ContextBundleService.java:1034–1038`, already present) — proving the full chain end-to-end.

8. **Given** tests, **Then** a **full-loop IT** (Failsafe + Testcontainers) proves: 3e-1 creates an `open` clarification → `/answer` → `/accept` (`accepted`) → `regenerate_spec_with_clarifications` re-dispatches → mock spec v2 ingested as a graft carrying `clarificationAcknowledgements:[{Q, addressed:true}]` → `markAvailable` sweep marks the clarification `incorporated`; a parallel case with `addressed:false` ⇒ `superseded`; lineage-scope proven (v1-pinned clarification swept on v2). Unit: `acceptClarification` wiring; the new sweep oracle over structured acknowledgements; contract valid/invalid for `clarificationAcknowledgements`; runner emission (both runners). Drift: 2 new AllowedActions + any new event types green. Redaction fixture green for the materialized answer file. `application.*` ≥80% coverage.

## Tasks / Subtasks

- [x] **Task 1 — `accept_clarification` action + command + REST/CLI** (AC: 1)
  - [x] `domain/registry/AllowedAction.java` — add `ACCEPT_CLARIFICATION("accept_clarification")`. Drift: `allowed-actions.placeholder.json` + a wire pin in `architecture/AllowedActionRegistryPinTest`.
  - [x] New command `application/workflow/commands/AcceptClarificationCommand.java` (mirror `SubmitClarificationCommand`; carries `workflowRunId`, `clarificationId`, actor, idempotencyKey, correlationId — version-binding optional, `markAccepted` is on the answered row). Register per the WorkflowCommand fan-out recipe ([[epic3b-command-and-approval-wiring-fanout]] / `docs/patterns/registry-recipe.md §2`).
  - [x] `WorkflowCommandService.acceptClarification(...)` (`@Transactional`) → `ClarificationLifecycleService.markAccepted(workflowRunId, clarificationId, actor)`.
  - [x] REST `WorkflowController` `POST /{workflowRunId}/clarifications/{clarificationId}/accept` (mirror `answerClarification` L959–1034) + `WorkflowCommands` CLI parity (HUMAN actor). OpenAPI regen (this DOES change `openapi.json` — a new operation; regen `schema.d.ts` too — [[openapi-regen-frontend-client-drift-cascade]]).
  - [x] `computeActionMatrix` — add `ACCEPT_CLARIFICATION` to the `WaitingForSpecApproval` reviewer-role action set (next to `ANSWER_CLARIFICATION`). Matrix test cases.

- [x] **Task 2 — `regenerate_spec_with_clarifications` action → reuse `retrySpecGeneration`** (AC: 2)
  - [x] `domain/registry/AllowedAction.java` — add `REGENERATE_SPEC("regenerate_spec_with_clarifications")` (same drift sites).
  - [x] Command + `WorkflowCommandService.regenerateSpecWithClarifications(...)` (`@Transactional`) — **mirror `ApprovalService.rejectSpec` (`ApprovalService.java:456–477`) exactly**: FIRST `workflowTransitionService.transition(runId, WorkflowState.INVESTIGATING, actor, "regenerate specification", idempotencyKey, details)` (transition service appends `workflow.stateChanged` — don't double-append), THEN `WorkflowOrchestrationService.retrySpecGeneration(runId, correlationId)`. `retrySpecGeneration` (`WorkflowOrchestrationService.java:299–335`) **re-dispatches ONLY — it does NOT transition (Trap T8)**: it no-ops an in-flight dispatch, builds `specDispatchKey(runId, specRejectionLoopCount)`, enqueues INVESTIGATION. The `WaitingForSpecApproval → Investigating` edge is already legal (the reject→retry loop uses it). Do NOT duplicate dispatch logic; do NOT call `retrySpecGeneration` expecting it to move state.
  - [x] `computeActionMatrix` — surface `REGENERATE_SPEC` at `WaitingForSpecApproval` for the reviewer role (gate: only meaningful when ≥1 `accepted` clarification exists — surface always, let the service no-op/inform if none, OR compute presence; note the choice).

- [x] **Task 3 — Feed accepted clarifications into the spec rebuild bundle** (AC: 3)
  - [x] `application/runner/ContextBundleService.java#assembleForSpecInvestigation` (L770+) — after the `priorRejections` loop (L793–798), append one `priorFeedbackReferences` entry `{referenceId: clarification.publicId(), kind: "clarification.answered"}` per `accepted` clarification (read via the injected `ClarificationReadPort.listByWorkflowRunId`, filter `STATUS_ACCEPTED`). The method signature/caller must pass or fetch them — thread an `acceptedClarifications` list from the assembler's caller (the spec-investigation bundle build site), mirroring how `priorRejections` is threaded.
  - [x] **Materialize** each accepted clarification as a referenced input file (questionId + questionText + answerText) under the runner input dir via the existing artifact/input materialization path (find where `priorFeedbackReferences` referenced content is written for `spec.rejection` — mirror it). **Route through the redaction policy** (`RedactionClassificationPolicy` / the secret-fixture gate, story 1.10/2.24) so the materialized file is policed exactly like artifact content. Never inline `answerText` into the bundle JSON (trap: bundle 2KB payload cap — [[context-bundle-2kb-payload-cap.md]] — and the redaction contract).
  - [x] Bundle composition test: an accepted clarification appears as a `clarification.answered` feedback ref + a materialized, redaction-clean input file; `open`/`answered`-but-not-`accepted` clarifications do NOT (only `accepted` feed the rebuild).

- [x] **Task 4 — `clarificationAcknowledgements` contract + runner emission** (AC: 4)
  - [x] `runner-result.v1.schema.json` `specArtifact` — add optional `clarificationAcknowledgements` array (items `{questionId (same pattern), addressed (boolean)}`, `additionalProperties:false`); not `required`; no `schemaVersion` bump (3e-1 discipline). Valid (with/without) + invalid fixtures.
  - [x] Both `runner.mjs` (BYTE-IDENTICAL) — fence-split ` ```clarificationAcknowledgements ` (sibling of 3e-1's `clarifications` split); set `artifact.clarificationAcknowledgements` only when non-empty at `stage === 'spec'`. Offline mocks: for each question id materialized in the input bundle, emit `{questionId, addressed:true}` (deterministic). Non-fatal on malformed JSON.
  - [x] Runner unit tests (both runners).

- [x] **Task 5 — Graft the rebuilt spec + plumb acknowledgements to the sweep** (AC: 5, 6)
  - [x] **Graft (R3):** ensure a spec result for a run that ALREADY has a spec is ingested as a `newVersion` of the prior spec (so lineage holds). Options — pick one, record in Completion Notes: (a) in `RunnerBroker.handleSuccess`, when `artifactType==SPEC` and a prior active spec exists for the run, route to `ArtifactOperationService.newVersion(priorSpecId, payloadRef, actor)` instead of `recordOperation(CREATE)`; or (b) widen `ClarificationLifecycleOrchestrator` scope from `lineageArtifactIds` to all run clarifications. **Recommend (a)** — it preserves spec version lineage the 2.12 supersession semantics already assume. Add an IT proving a v1-pinned clarification is swept on v2.
  - [x] **Plumb acknowledgements:** the sweep runs inside `markAvailable` which sees only the artifact. Persist the rebuilt spec's `clarificationAcknowledgements` at ingest so the sweep can read them by artifactId. Recommended: a small side-store (`spec_clarification_acknowledgements(artifact_id, question_id, addressed)`, additive Flyway, next free version) written in `handleSuccess` next to artifact ingest; the orchestrator reads it in `sweepAfterSpecRebuild`. (Avoid stuffing into artifact payload bytes — keeps the redaction/cap contract clean.)
  - [x] **Replace the oracle:** `ClarificationLifecycleOrchestrator.acknowledgesQuestion(bytes, questionId)` → `wasAddressed(acknowledgements, questionId)`; `addressed==true` ⇒ `markIncorporated`, else `markSuperseded(... "spec_runner_skipped_question" ...)`. Delete the substring scan + `TODO`. Correct the stale class javadoc ("hooked into `newVersion`" → "`markAvailable`").

- [x] **Task 6 — FE: accept + regenerate controls; incorporation reflection** (AC: 1, 2, 7)
  - [x] Regenerate `schema.d.ts` FIRST (new endpoints/actions → check:api drift gate — [[openapi-regen-frontend-client-drift-cascade]], [[story-3c-9-projects-management-ui-reconciliations]]).
  - [x] `ClarificationRegion`/decision-bar — add Accept + Regenerate-spec governed buttons gated by the new AllowedActions (no bare role text — the `no-bare-actor-role-text` eslint rule); reuse `useWorkflowMutation`. The incorporation lifecycle chips already exist (2.18) — verify they reflect `accepted`/`incorporated`/`superseded`.
  - [x] Vitest + axe; (optional) extend the critical-journey e2e with the accept→regenerate→incorporated path.

- [x] **Task 7 — Tests** (AC: 8, all)
  - [x] **Full-loop IT** (Failsafe + Testcontainers) — the create→answer→accept→regenerate→graft-v2(+acknowledgements)→sweep chain; assert `incorporated` (addressed:true) and `superseded` (addressed:false); lineage-scope proven.
  - [x] Unit: `acceptClarification`/`markAccepted` wiring; new sweep oracle over structured acknowledgements; bundle injection of accepted clarifications + redaction.
  - [x] Contract: `clarificationAcknowledgements` valid/invalid; runner emission (both runners).
  - [x] Drift: 2 new AllowedActions (placeholder + pins), any new event/Flyway (FlywaySchemaContractTest if a side-store table is added), OpenAPI + schema.d.ts (new endpoints — NOT byte-identical this time).
  - [x] Redaction fixture green over the materialized answer input file.
  - [x] Naming/tier: `@SpringBootTest`+Testcontainers ⇒ `*IT` via the lifecycle phase ([[maven-arglineation-goal-crash]]).

- [x] **Logging instrumentation** (cross-cutting)
  - [x] Accept, regenerate-dispatch, bundle injection, sweep-oracle decision — instrument (NOT N/A). Never log `questionText`/`answerText`/payload bytes (trap T12). Pin ≥1 line per new branch.

## Dev Notes

### Why these ACs are reconciled (epic/FR wording vs. the live codebase)

| Wording assumption | Reality (verified 2026-06-23) | Reconciliation |
|---|---|---|
| "accepted answers are incorporated into a rebuilt spec" (FR10/2.12) | `markAccepted` has **zero** prod callers; no `accept_clarification` action ⇒ the sweep's `accepted` filter is always empty. | Add `accept_clarification` action + command + REST/CLI + `acceptClarification` (R1, Task 1). |
| "the spec runner acknowledges which questions it addressed" | `acknowledgesQuestion` is a substring-scan **stub** with a `TODO(epic-3-runner-contracts)`. | Add optional `clarificationAcknowledgements` to the contract, emit from both runners, consume in the sweep (Task 4–5, R5). |
| "a rebuilt spec supersedes the old one and the sweep runs over it" | The sweep IS wired (on `markAvailable`, not `newVersion` — stale javadoc), but a re-dispatched spec mints a **fresh lineage** so the prior clarification is out of scope; `newVersion` has zero callers. | Graft the rebuilt spec via `newVersion` (or widen sweep scope) — R3, Task 5. |
| "answers flow to the runner" | `priorFeedbackReferences` is reference-by-id (no content); runner reads referenced files from its input dir. | Add a `clarification.answered` feedback ref + **materialize** the answer as a redaction-policed input file (R4, Task 3). |

### Previous Story (3e-1) Intelligence — reuse these landed seams, inherit these traps

3e-1 (DONE 2026-06-24) shipped the CREATE half and left 3e-2 a set of reusable patterns and **one hard-won trap**. Read before coding:

- **🚨 TRAP — duplicate-key flush poisons the shared broker tx (3e-1's CRITICAL review fix; [[caught-idempotency-conflict-poisons-shared-tx]]).** 3e-1's first cut caught the `IDEMPOTENCY_KEY_CONFLICT` from a duplicate `questionId` but the `saveAndFlush` had **already poisoned the Hibernate session** inside the broker's shared tx → the next flush `AssertionFailure` stranded the run `Investigating` (reproduced on Testcontainers PG; the "best-effort catch" did NOT heal it). Fix was **de-dup by `questionId` (first-wins) BEFORE insert**. **3e-2 writes a NEW `spec_clarification_acknowledgements` side-store at ingest in that SAME shared broker tx (Task 5)** — it MUST de-dup acknowledgements by `questionId` before insert and must NOT rely on catching a flush conflict. Same failure mode, same fix. Pin it with an IT that ingests a result with a duplicate `questionId` acknowledgement and asserts the run still reaches `WaitingForSpecApproval`.
- **Reuse the broker injection pattern for the acknowledgement-persist seam.** 3e-1's `ClarificationIngestService` is `@Transactional(MANDATORY)`, injected into `RunnerBroker` via a **lazy `Supplier`/`ObjectProvider`** ([[broker-orchestration-lazy-supplier]]) defaulting to `() -> null` in the package-private test ctors; its `ingestSpecClarifications` seam sits in `handleSuccess` **immediately after the SPEC `markArtifactAvailable` block**. 3e-2's acknowledgement-persist sits in the SAME place — reuse the lazy-supplier injection and remember the **ctor fan-out** ([[docker-adapter-ctor-dep-fans-out]] / `RunnerBrokerUnitTest` full-ctor call sites each need a `() -> null`).
- **Reuse the just-ingested spec artifactId/version capture (3e-1 R5).** The spec `opResult.artifact().publicId()` + `.version()` are already captured in the spec-ingest loop — the acknowledgement side-store is keyed by the NEW spec `artifactId`, and the **graft (R3)** needs the prior spec's id. Capture, don't re-query.
- **The acknowledgements fence-split is the EXACT sibling of `splitClarificationsFence`** (byte-identical, SHA-equal 2313 chars across both `runner.mjs`). Mirror the helper AND 3e-1's review-hardened rules: strip the fence from `spec.md` **only on a successful parse with ≥1 usable item**; **slice the original string at the fence-line offsets** (preserve CRLF + trailing newline byte-for-byte); non-fatal on malformed JSON; keep the two test files in lockstep; verify SHA-equal across both files. Mocks gate on the non-secret `DELIVERYLINE_RUNNER_STAGE` and emit deterministically at `stage === 'spec'`.
- **Line drift (3e-1 finalize note, re-confirmed here):** anchor SEMANTICALLY. `RunnerBroker.handleSuccess` @ ~L1652; spec artifactId/version capture @ ~L1774; both `runner.mjs` spec artifact-build blocks @ ~L346 & ~L411. Re-grep before editing.
- **Conventions to carry:** deterministic idempotency keys (`runner-result-clarification:<rex>:<questionId>` is the 3e-1 shape — keep any 3e-2 key deterministic per `(rex, questionId)`); the idempotency-key validator requires **≥16-char opaque keys** (size IT keys accordingly); a new `WorkflowEventType` → **both** fixture sites ([[new-workfloweventtype-fixture-sites]]); verify with `mvnw -pl deliveryline-backend -am verify` (the `-am` matters — [[runner-contracts-schema-stale-in-m2]]).

### R1 — Explicit accept action (auto-accept rejected)
2.12 modeled `answered → accepted` as a distinct, audited PM judgment ("I accept this answer for incorporation"), and the sweep acts ONLY on `accepted`. Auto-accepting on `/answer` was considered and rejected: it erases the `answered`/`accepted` distinction (a reviewer could answer to think out loud without committing it to a rebuild), breaks the 2.12 event audit, and makes the sweep fire on un-vetted answers. Add an explicit `accept_clarification` — the structural twin of `answer_clarification` (2.11/2.13) and of how `accept_implementation`/`reject` wrap a lifecycle service.

### R2 — Reuse `retrySpecGeneration`, but the COMMAND owns the transition (not retry)
**Resolved 2026-06-24 against the live code — this corrects the earlier AC2 phrasing.** `retrySpecGeneration` (`WorkflowOrchestrationService.java:299–335`) is **re-dispatch-ONLY and MUST NOT re-transition** — its own javadoc (`:291–298`) calls out Trap T8: "the caller performed any required transition … this method re-dispatches ONLY … an `INVESTIGATING → INVESTIGATING` transition would be illegal / a duplicate event." The canonical caller proves the pattern: `ApprovalService.rejectSpec` (`:456–477`) FIRST does `workflowTransitionService.transition(runId, WorkflowState.INVESTIGATING, actor, "reject specification", idempotencyKey, details)` (which appends `workflow.stateChanged` itself), THEN calls `retrySpecGeneration` inside the SAME mandatory tx (all-or-nothing: a dispatch failure rolls back the transition + counter). So `regenerateSpecWithClarifications` is the structural twin of `rejectSpec`: transition-then-redispatch, reusing `retrySpecGeneration` for the dispatch half only. The `WaitingForSpecApproval → Investigating` edge is already legal (the reject loop uses it — no transition-table change). Do NOT build a second dispatch path; do NOT expect `retrySpecGeneration` to move state.

### R3 — The lineage trap is the crux of this story
`ClarificationLifecycleOrchestrator.sweepAfterSpecRebuild` considers a clarification only if `c.artifactId ∈ lineageArtifactIds(newSpec)` (parent-walk, L237–249) AND `c.status == accepted`. A clarification is pinned to the v1 spec's `artifactId`. The broker ingests every spec via `recordOperation(CREATE)` → fresh `art_`, `parentArtifactId=null` → v2 is in its OWN lineage → v1's clarification is NEVER swept. `newVersion`/`createNextVersion` (the graft that sets `parentArtifactId`) exists but has **zero production callers** — it was built (2.12) as exactly this seam and left dormant. Resolve by grafting the rebuilt spec (recommended — preserves the version lineage the supersession semantics assume) or by widening the sweep scope to the workflow-run. Either way, an IT MUST prove a v1-pinned clarification is acted on when v2 becomes available. This is the single most likely place for the loop to silently no-op.

### R4 — Answers are materialized, not inlined (redaction + 2KB cap)
The bundle JSON is reference-by-id and capped (`CONTEXT_BUNDLE_MAX_PAYLOAD_BYTES`, [[context-bundle-2kb-payload-cap.md]]); answer text must NOT be embedded in it. Mirror how `spec.rejection` feedback content is made available to the runner (referenced input file under the mounted input dir) and run the materialized answer file through the **redaction classification policy** (1.10/2.24) and the adversarial secret-fixture gate — reviewer-authored answer text is still untrusted content for redaction purposes. The runner reads the file by following the `clarification.answered` reference.

### R5 — Acknowledgements must be persisted at ingest because the sweep can't see the runner result
The sweep runs inside `ArtifactOperationService.markAvailable` (L395), which has the artifact but NOT the runner-result JSON (that lives in the broker). So the structured `clarificationAcknowledgements` the runner emits must be persisted at broker ingest (recommended: a small additive `spec_clarification_acknowledgements` side-store keyed by artifactId, next free Flyway version) and read by the orchestrator at sweep time. Do not push them through the artifact payload bytes (keeps the redaction/cap contract clean and avoids re-parsing spec markdown). `no_effect_reason="spec_runner_skipped_question"` is already in the allowed vocabulary (`ClarificationLifecycleService.java:74–80`) for the not-addressed case.

### R6 — The execution-stage feedback path already consumes `incorporated` clarifications
`ContextBundleService.collectExecutionFeedbackReferences` (L1010–1038) already adds `{clarification.incorporated}` refs for incorporated clarifications to the EXECUTION bundle. So once 3e-2 produces `incorporated` rows, the downstream implementation stage automatically carries them — no further wiring. AC7 just proves this end-to-end; do not rebuild it.

### R7 — Stale documentation to fix
`ClarificationLifecycleOrchestrator` class javadoc (L24–35) says "Hooked into `ArtifactOperationService.newVersion`" — the real hook is `markAvailable` (L395). Correct it while you're replacing the oracle, so the next reader isn't misled about where the sweep fires.

### Verified seams (file:line, 2026-06-23)
- Sweep is real + wired on availability — `application/artifact/ArtifactOperationService.java:395–405` (NOT `newVersion`).
- `markAccepted` zero callers — `application/clarification/ClarificationLifecycleService.java:138–171`.
- Acknowledgement STUB + `TODO(epic-3-runner-contracts)` — `application/clarification/ClarificationLifecycleOrchestrator.java:251–285`; lineage scope `:237–249`; `accepted` filter `:96–102`.
- `newVersion`/`createNextVersion` graft, zero callers — `application/artifact/ArtifactOperationService.java:492–512`.
- Spec re-dispatch reuse — `application/workflow/WorkflowOrchestrationService.java:300–331` (`retrySpecGeneration`).
- Spec bundle feedback seam — `application/runner/ContextBundleService.java:770–810`; execution incorporated-feedback `:1010–1038`.
- Only `answer_clarification` action — `domain/registry/AllowedAction.java:8`; answer REST `WorkflowController.java:959–1034`.

> **Re-verified 2026-06-24** (3e-1 landed): the seams above HELD — sweep call @ `ArtifactOperationService.java:~397`; `markAccepted` @ `ClarificationLifecycleService.java:138`; `acknowledgesQuestion` @ `ClarificationLifecycleOrchestrator.java:263` (lineage `:237`); graft `newVersion` @ `ArtifactOperationService.java:493` (still zero prod callers); `retrySpecGeneration` @ `WorkflowOrchestrationService.java:299` (re-dispatch-only, T8); `assembleForSpecInvestigation` @ `ContextBundleService.java:775`, exec feedback @ `:1015`. The answer twins to mirror: `WorkflowCommandService.answerClarification` @ `:184`, REST @ `WorkflowController.java:966/998`, CLI `WorkflowCommands.answerClarification` @ `:1633`.

## Dev Agent Record

### Agent Model Used

Opus 4.8 (1M context) — `claude-opus-4-8[1m]` via `bmad-dev-story`.

### Debug Log References

- Full-loop IT idempotency conflict (`spec-dispatch:run:0`): `regenerateSpecWithClarifications` must increment `spec_rejection_loop_count` BEFORE the transition (mirror `ApprovalService.rejectSpec`'s increment→transition→retry order) so `retrySpecGeneration`'s snapshot read sees the bumped count and mints a distinct dispatch key.
- Rebuild bundle `RUNNER_CONTRACT_VIOLATION`: the backend-only `mvnw verify` (no `-am`) used the stale `.m2` runner-contracts jar (old context-bundle schema). Build with `-am` so the new `acceptedClarifications` schema is on the classpath ([[runner-contracts-schema-stale-in-m2]]).
- `ContextBundleServiceSpecInvestigationTest` / `SpecStageRepoContextIT` regression: Task 3 made `createForSpecInvestigation` call `clarificationReadPort`, so the legacy 5-arg ctor's `UnwiredClarificationReadPort` now throws — switched those sites to the 6-arg ctor with a `mock(ClarificationReadPort.class)` (Mockito returns an empty list by default).
- `RegistryContractTest.publicIdPrefixesStayAlignedWithSqlChecksAndParsingHelpers`: a new `PublicIdPrefixes` value must also be added to the API placeholder manifest `registry-api-schema-placeholders.json`.

### Completion Notes List

- **AC3 reconciliation (USER DECISION).** R4/AC3 assumed an existing "materialize feedback as a referenced input FILE" seam to mirror — it does not exist (the runner reads ONLY `context-bundle.v1.json`; all feedback is reference-by-id; there is no `RunnerWorkspaceStore` arbitrary-file-write method). With the user's approval, accepted clarifications are carried INLINE in the bundle as an additive `acceptedClarifications` array (`{clarificationId, questionId, questionText, answerText}`) + the `{kind:'clarification.answered'}` references. The whole bundle root is already policed by the single `RedactionPolicyService.redact(root, SHAREABLE_REDACTED)` pass, so the reviewer-authored answer text is redacted exactly like artifact content; the cap is 256KB (not the stale 2KB the story cited), and `answerText` is `@Size(max=8192)`. Both `runner.mjs` read the field and inject the answers into the prompt.
- **AC5/R3 graft (chosen: option a).** `RunnerBroker.handleSuccess` routes a re-ingested SPEC through `ArtifactOperationType.UPDATE` (→ `createNextVersion`, parent = prior spec) instead of `CREATE` when `artifactOperationService.hasActiveLineage(run, SPEC)` is true. This keeps the v1-pinned accepted clarification inside `lineageArtifactIds(v2)` so the sweep acts on it. (Uses the existing UPDATE path, which returns the uniform `RecordArtifactOperationResult`, rather than the `newVersion(...)` overload whose return shape doesn't fit the broker's `markArtifactAvailable`/ingest flow.) Also FIXES the previously-broken reject→retry v2 ingest (CREATE rejected a re-ingest with `ARTIFACT_LINEAGE_ALREADY_EXISTS`).
- **AC6 oracle + reason vocabulary.** The substring-scan stub (`acknowledgesQuestion` + helpers) is deleted; the sweep now reads structured acks from the V25 `spec_clarification_acknowledgements` side-store via `SpecClarificationAcknowledgementReadPort` (`addressed==true ⇒ markIncorporated`; absent OR `addressed==false ⇒ markSuperseded`). AC6's `no_effect_reason="spec_runner_skipped_question"` is in `REJECTED_INVALID_REASONS`, NOT `SUPERSEDED_REASONS`, so `markSuperseded` would reject it — kept the in-vocabulary `clarification_not_addressed` (the value the sweep already used; the accepted→superseded edge is the only valid one here). The stale "hooked into newVersion" class javadoc is corrected to `markAvailable`.
- **AC6/R5 plumbing.** Acks are persisted at broker ingest by a new `ClarificationIngestService.persistSpecClarificationAcknowledgements` (reuses the existing lazy-`Supplier` broker seam — no new broker ctor dependency), keyed by the just-ingested spec `artifactId`, BEFORE `markArtifactAvailable` (the sweep runs synchronously inside it). De-dups by questionId (first-wins) + pre-flight `existsBy...` probe so a re-harvest never flushes a `(artifact, question)` UNIQUE conflict into the shared broker tx (the 3e-1 session-poison trap; same defense as `createOpenFromSpec`).
- **AC1/AC2 matrix gating.** Accept + regenerate surface for the REVIEWER roles only (`product_reviewer` + `workflow_owner`) at `WaitingForSpecApproval`; `developer` keeps view+answer only. Both are surfaced unconditionally (not gated on a pending/accepted count) — the executors no-op/guard when there's nothing to act on (Completion Notes "surface always").
- **AC2 loop-count semantics.** `regenerateSpecWithClarifications` increments `spec_rejection_loop_count` (conflating regenerate with the reject loop) purely to mint a fresh deterministic dispatch key — there's no DB CHECK / escalation side-effect from the increment itself (escalation is `rejectSpec`-only).
- **Task 4 mock.** Both offline mocks emit a deterministic `clarificationAcknowledgements` fence for the fixed `Q-MOCK-001` (addressed:true) at the spec stage — schema-valid + deterministic for the runner-image/node tiers. The full-loop incorporation IT drives addressed true/false via pre-built runner-result scenario JSON (the 3e-1 broker-IT precedent), so the mock's exact behaviour does not gate it.
- **Verification:** backend unit suite 1326/0/0; `SpecRebuildIncorporationIT` 2/2 (incorporated + superseded, graft lineage proven, side-store row asserted); `FlywaySchemaContractTest` (V25 + dedup unique), `RegistryContractTest`, `AllowedActionRegistryPinTest` green; runner-contracts 11/11 (acks valid/invalid fixtures); runner node tier both runners green (acks fence-split SHA-equal); frontend 1169/1169 + `npm run build` + `npm run lint` + `check:api` green; OpenAPI snapshot + `schema.d.ts` regenerated.

### File List

**Backend — main**

- `deliveryline-backend/src/main/java/org/dradgo/domain/registry/AllowedAction.java` — +`ACCEPT_CLARIFICATION`, +`REGENERATE_SPEC`
- `deliveryline-backend/src/main/java/org/dradgo/domain/id/PublicIdPrefixes.java` — +`SPEC_CLARIFICATION_ACKNOWLEDGEMENT` (`sca_`)
- `deliveryline-backend/src/main/java/org/dradgo/application/workflow/commands/AcceptClarificationCommand.java` — NEW
- `deliveryline-backend/src/main/java/org/dradgo/application/workflow/commands/RegenerateSpecCommand.java` — NEW
- `deliveryline-backend/src/main/java/org/dradgo/application/workflow/commands/WorkflowCommand.java` — +2 permits
- `deliveryline-backend/src/main/java/org/dradgo/application/idempotency/WorkflowCommandFingerprintFactory.java` — +2 fingerprint cases
- `deliveryline-backend/src/main/java/org/dradgo/application/workflow/WorkflowCommandService.java` — `acceptClarification` + `regenerateSpecWithClarifications` (+`ClarificationLifecycleService`, `WorkflowRunRejectionLoopPort` deps) + replay wiring
- `deliveryline-backend/src/main/java/org/dradgo/application/workflow/WorkflowInspectionService.java` — surface accept+regenerate at `WaitingForSpecApproval` (reviewer roles)
- `deliveryline-backend/src/main/java/org/dradgo/adapters/rest/WorkflowController.java` — `POST .../accept` + `POST .../regenerate-spec`
- `deliveryline-backend/src/main/java/org/dradgo/adapters/rest/ClarificationAcceptResponse.java` — NEW
- `deliveryline-backend/src/main/java/org/dradgo/adapters/cli/WorkflowCommands.java` — `accept-clarification` + `regenerate-spec` CLI
- `deliveryline-backend/src/main/java/org/dradgo/application/runner/ContextBundleService.java` — feed accepted clarifications into the spec-rebuild bundle (inline + refs)
- `deliveryline-backend/src/main/java/org/dradgo/application/clarification/ClarificationIngestService.java` — +`persistSpecClarificationAcknowledgements` (+ack write port dep)
- `deliveryline-backend/src/main/java/org/dradgo/application/clarification/ClarificationLifecycleOrchestrator.java` — structured-acks oracle (substring stub deleted, javadoc corrected)
- `deliveryline-backend/src/main/java/org/dradgo/application/clarification/SpecClarificationAcknowledgement.java` — NEW
- `deliveryline-backend/src/main/java/org/dradgo/application/clarification/spi/SpecClarificationAcknowledgementReadPort.java` — NEW
- `deliveryline-backend/src/main/java/org/dradgo/application/clarification/spi/SpecClarificationAcknowledgementWritePort.java` — NEW
- `deliveryline-backend/src/main/java/org/dradgo/application/artifact/ArtifactOperationService.java` — +`hasActiveLineage`
- `deliveryline-backend/src/main/java/org/dradgo/application/runner/RunnerBroker.java` — graft (UPDATE) + `ingestSpecAcknowledgements` seam
- `deliveryline-backend/src/main/java/org/dradgo/adapters/persistence/entity/SpecClarificationAcknowledgementEntity.java` — NEW
- `deliveryline-backend/src/main/java/org/dradgo/adapters/persistence/repository/SpecClarificationAcknowledgementRepository.java` — NEW
- `deliveryline-backend/src/main/java/org/dradgo/adapters/persistence/SpecClarificationAcknowledgementPersistenceAdapter.java` — NEW
- `deliveryline-backend/src/main/resources/db/migration/V25__add_spec_clarification_acknowledgements.sql` — NEW
- `deliveryline-backend/src/main/resources/openapi/openapi.json` — regenerated (2 new operations)

**Runner contracts**

- `deliveryline-runner-contracts/src/main/resources/schemas/runner-result.v1.schema.json` — +optional `clarificationAcknowledgements`
- `deliveryline-runner-contracts/src/main/resources/schemas/context-bundle.v1.schema.json` — +optional `acceptedClarifications`
- `deliveryline-runner-contracts/src/test/resources/fixtures/valid/runner-result.v1.spec-with-acknowledgements.valid.json` — NEW
- `deliveryline-runner-contracts/src/test/resources/fixtures/invalid/runner-result.v1.invalid-acknowledgement-missing-addressed.json` — NEW
- `deliveryline-runner-contracts/src/test/resources/fixtures/invalid/runner-result.v1.invalid-acknowledgement-malformed-id.json` — NEW
- `deliveryline-runner-contracts/src/test/resources/fixtures/fixture-expectations.json` — +2 invalid registrations

**Runners (byte-identical helper / lockstep tests)**

- `runners/claude/lib/runner.mjs`, `runners/codex/lib/runner.mjs` — +`splitClarificationAcknowledgementsFence` (SHA-equal) + emit + bundle `acceptedClarifications` prompt injection
- `runners/claude/test/mock-claude.sh`, `runners/codex/test/mock-codex.sh` — deterministic acks fence at spec stage
- `runners/claude/test/runner-clarification-acknowledgements-fence.test.mjs`, `runners/codex/test/runner-clarification-acknowledgements-fence.test.mjs` — NEW (twins)

**Backend — test**

- `deliveryline-backend/src/test/resources/contracts/frontend/allowed-actions.placeholder.json` — +2 actions
- `deliveryline-backend/src/test/resources/contracts/openapi/registry-api-schema-placeholders.json` — +`sca_` prefix
- `deliveryline-backend/src/test/java/org/dradgo/architecture/AllowedActionRegistryPinTest.java` — +2 pins
- `deliveryline-backend/src/test/java/org/dradgo/contract/FlywaySchemaContractTest.java` — +CORE_TABLE + V25 probe
- `deliveryline-backend/src/test/java/org/dradgo/application/clarification/ClarificationLifecycleOrchestratorTest.java` — rewritten for the structured oracle
- `deliveryline-backend/src/test/java/org/dradgo/application/clarification/ClarificationIngestServiceTest.java` — +ack-persist tests
- `deliveryline-backend/src/test/java/org/dradgo/application/workflow/WorkflowInspectionServiceAllowedActionsTest.java` — matrix expectations
- `deliveryline-backend/src/test/java/org/dradgo/application/workflow/WorkflowCommandServiceReplayRefTest.java`, `WorkflowCommandServiceCreateBindingTest.java` — ctor deps
- `deliveryline-backend/src/test/java/org/dradgo/application/runner/ContextBundleServiceSpecInvestigationTest.java`, `workspace/SpecStageRepoContextIT.java` — 6-arg ctor
- `deliveryline-backend/src/test/java/org/dradgo/application/workflow/SpecRebuildIncorporationIT.java` — NEW (full-loop IT)
- `deliveryline-backend/src/test/resources/runner-scenarios/happy-spec-rebuild-acknowledged.json`, `happy-spec-rebuild-not-acknowledged.json` — NEW

**Frontend**

- `deliveryline-frontend/src/lib/api/schema.d.ts` — regenerated
- `deliveryline-frontend/src/features/workflows/hooks/useAcceptClarification.ts`, `useRegenerateSpec.ts` — NEW
- `deliveryline-frontend/src/features/workflows/components/ClarificationRegion.tsx` — gated accept + regenerate controls
- `deliveryline-frontend/src/features/workflows/components/ClarificationRegionContainer.tsx` — wire the two mutations + allowed actions
- `deliveryline-frontend/src/features/workflows/components/ClarificationRegion.test.tsx`, `ClarificationRegionContainer.test.tsx` — +accept/regenerate coverage

## Change Log

| Date | Change |
|---|---|
| 2026-06-24 | `bmad-dev-story` (Opus 4.8 [1m]): implemented the full FR10 incorporation loop. accept_clarification + regenerate_spec actions/commands/REST/CLI; accepted-clarification spec-rebuild bundle feed (INLINE per user decision — no materialized-file seam exists); `clarificationAcknowledgements` runner contract + both runners + mocks + fence tests; rebuilt-spec GRAFT via UPDATE; V25 `spec_clarification_acknowledgements` side-store + ingest seam; structured sweep oracle replacing the substring stub; FE accept/regenerate controls. OpenAPI + schema.d.ts regenerated. Full-loop IT + units + contract/drift green; backend 1326/0/0, FE 1169/1169. Status `in-progress → review`. |
| 2026-06-24 | Story contexted via `bmad-create-story`: re-verified all back-half seams against the live codebase post-3e-1 (held), corrected AC2/Task2/R2 (`retrySpecGeneration` is re-dispatch-only T8 — the command owns the `WaitingForSpecApproval → Investigating` transition, mirroring `ApprovalService.rejectSpec`), folded in 3e-1 previous-story intelligence (incl. the duplicate-key shared-tx-poison trap that now threatens the new acknowledgement side-store write). Status `backlog → ready-for-dev`. |

### Review Findings

<!-- bmad-code-review 2026-06-24 (Opus 4.8 [1m]): Blind Hunter + Edge Case Hunter + Acceptance Auditor (3 layers, all completed). 1 decision / 5 patch / 2 defer / 3 dismissed. -->

- [x] [Review][Patch] APPLIED (Decision → option (a)): added `DomainErrorCode.REGENERATE_NOT_APPLICABLE` (enum + `ProblemDetailsCatalog` CONFLICT mapping + `registry-api-schema-placeholders.json` manifest), a guard at the TOP of `regenerateSpecWithClarificationsInternal` (rejects BEFORE the loop-count bump/transition/re-dispatch when zero `accepted` clarifications), and an IT case `regenerateIsRejectedWhenNoClarificationHasBeenAccepted` (asserts the code + that the run stays WaitingForSpecApproval with only v1). — Regenerate executor lacks the zero-accepted no-op/guard its own contract claims — `WorkflowInspectionService.computeActionMatrix` (`WorkflowInspectionService.java:921-934`) surfaces `REGENERATE_SPEC` unconditionally and its comment asserts "the canonical executors no-op/guard when there is … no accepted clarification to rebuild from", but `regenerateSpecWithClarificationsInternal` (`WorkflowCommandService.java:507-543`) has NO accepted-count check: it unconditionally bumps `spec_rejection_loop_count`, transitions `WaitingForSpecApproval → Investigating`, and re-dispatches a full spec run with an empty `acceptedClarifications`. AC2 Task (story L59) explicitly left the choice open ("surface always, let the service no-op/inform if none, OR compute presence — note the choice"); the chosen "surface always" path requires the service to no-op/inform, which it does not. DECISION: (a) guard — throw a domain error when zero `accepted`; (b) silent no-op + info log; (c) accept current behavior (a spurious no-incorporation spec re-run is acceptable) and fix the misleading comment instead. (source: edge+auditor)

- [x] [Review][Patch] APPLIED (HIGH) — made the broker's graft decision IDEMPOTENT by REUSING the operation_type already recorded for the idempotency key instead of recomputing it from mutable lineage state. New type-agnostic `ArtifactOperationPort.findRecordedOperation` (+ derived repo query + adapter) → `ArtifactOperationService.recordedOperationType(run, type, key)`; `RunnerBroker` now does `recordedOperationType(...).orElseGet(() -> hasActiveLineage ? UPDATE : CREATE)`, so a re-harvest reuses the prior CREATE (replay hits, no spurious v2) while the first harvest computes fresh. Verified: 2 new pinning ITs in `ArtifactOperationServiceContractTest` (reuse replays to v1 / a flipped UPDATE mints v2 — proving why the guard is needed), both green; full `SpecRebuildIncorporationIT` 3/3 green. Spec graft `operationType` is non-idempotent across re-harvest → spurious duplicate v2 + broken recovery [`RunnerBroker.java:1785-1788`, `ArtifactOperationService.java:530-537`] — the CREATE-vs-UPDATE graft decision is computed live from mutable state (`hasActiveLineage(run, SPEC)`), but `replayIfPresent` keys the idempotency replay on `operationTypeValue`. On any re-harvest of the FIRST spec (at-least-once delivery, or the documented best-effort `markArtifactAvailable`-failed→re-mark path, `RunnerBroker.java:1854`), `hasActiveLineage` flips `false→true`, so the same `runner-result:<execId>:<artifactId>` key is recomputed as UPDATE; `findReplay(…, "UPDATE")` misses the stored CREATE row → NOT a replay → `createNextVersion` mints a SPURIOUS v2 graft of v1 instead of re-marking v1 available. HIGH — confirmed by trace. Fix: make the graft decision idempotent (e.g. replay-lookup independent of operationType, or base CREATE/UPDATE on whether THIS runner execution already recorded a spec, not on lineage presence). Needs a pinning IT (re-harvest the first spec ⇒ no v2). (source: blind)

- [x] [Review][Defer] **DEFERRED from Patch (latent + Flyway-contract ripple)** — `FlywaySchemaContractTest.java:826-834` asserts the *named constraint* `uq_spec_clarification_acknowledgements_artifact_question` exists; converting it to a partial unique INDEX would break that assertion and require rewriting the contract probe. Nothing in the change ever sets `archived_at` on this table, so the mismatch is unreachable today. Deferred to a focused change. — V25 `(spec_artifact_id, question_id)` UNIQUE is total but the table is soft-deletable [`V25__add_spec_clarification_acknowledgements.sql`, `SpecClarificationAcknowledgementRepository.java`] — `uq_spec_clarification_acknowledgements_artifact_question` is a total unique, yet the entity carries `archived_at` and every read filters `archived_at IS NULL`. Per the repo's own [[one-active-per-key-needs-partial-unique-index]] pattern, an archived row can never be re-inserted and the existence-probe (total) vs read (archived-filtered) disagree → a re-persist-after-archive is silently suppressed → questions treated not-addressed → superseded. Latent today (no archive path), but introduced here. Fix: make the unique partial (`… where archived_at is null`) and/or scope the probe to `archived_at IS NULL`. (source: blind+edge)

- [x] [Review][Defer] **DEFERRED from Patch (semantic choice + byte-identical dual-runner edit)** — the fix requires choosing last-wins vs reject-on-conflict de-dup (a judgment, not a mechanical fix) and editing both BYTE-IDENTICAL `runner.mjs` files in lockstep plus a runner test. Left for a focused change. — Acknowledgement fence keeps only the FIRST block and de-dups first-wins, silently discarding a conflicting `addressed:false` [`runners/claude/lib/runner.mjs`, `runners/codex/lib/runner.mjs` (`splitClarificationAcknowledgementsFence`), `ClarificationIngestService` (`dedupeAcknowledgementsByQuestionId`/`putIfAbsent`)] — a second `clarificationAcknowledgements` fence is left un-stripped in `spec.md` and ignored; a repeated questionId with `addressed:true` then `addressed:false` keeps `true` (first-wins) → a question the spec ultimately did NOT address can be recorded incorporated instead of superseded. Fix: last-wins (or reject-on-conflict) de-dup; detect/strip duplicate fences. (source: edge)

- [x] [Review][Patch] APPLIED — `handleAcceptClarification` now calls `accept.reset()` when retargeting a DIFFERENT clarification (`activeAcceptId !== id`), so a settled prior-row success/error never attaches to the newly-targeted row; same-row re-accepts keep their in-place feedback. Frontend `activeAcceptId` is never reset → stale accept success/error banner can mis-attach [`ClarificationRegionContainer.tsx`] — `setActiveAcceptId(id)` on click is never cleared; accept feedback is keyed solely on a never-cleared id + the persisted React Query mutation result rather than per-row mutation scoping. Reset on success and on selection change. LOW. (source: blind)

- [x] [Review][Patch] APPLIED — the inline `acceptedClarifications` loop now skips (with a `log.warn`) any accepted clarification whose `answerText` is null/blank, so a contract-violating blank never emits a JSON-null leaf and aborts the whole bundle; its by-id `clarification.answered` feedback ref is still recorded. `acceptedClarifications.answerText` written to the bundle with no null/blank guard [`ContextBundleService.java` (`assembleForSpecInvestigation`)] — `clarificationNode.put("answerText", accepted.answerText())`; a null/blank answer emits JSON null/empty, violating `context-bundle.v1.schema.json` (`answerText` `type:string`, `minLength:1`) and failing the WHOLE spec-investigation bundle build. Defended today only by upstream `@NotBlank`; add a skip/substitute when blank. LOW. (source: edge)

- [x] [Review][Defer] Absent acknowledgement block silently supersedes ALL accepted clarifications — deferred, design-as-specified — `wasAddressed` (`ClarificationLifecycleOrchestrator.java:983`) returns false for any questionId not in the map, and `ingestSpecAcknowledgements` (`RunnerBroker.java:2223`) early-returns on an empty/missing array. Per AC6 (`addressed==true ⇒ incorporated, else superseded`) absent==not-addressed is the SPECIFIED semantic, but a real runner that simply forgets to emit the fence terminally supersedes every accepted PM answer with only an info log. Recommend a louder signal (warn/metric) when accepted clarifications exist but zero acknowledgements were emitted. (source: blind)

- [x] [Review][Defer] Regenerate during an in-flight spec dispatch bumps + transitions but no-ops the re-dispatch — deferred, low reachability — if a prior INVESTIGATION dispatch is in flight, `retrySpecGeneration` short-circuits to the in-flight no-op AFTER the loop-count bump + `WaitingForSpecApproval → Investigating` transition already committed (`WorkflowCommandService.java:531-535`). Largely unreachable from `WaitingForSpecApproval` (the spec being approved is already produced; the transition immediately leaves the state; a double-click is rejected by the transition guard and rolled back), so deferred rather than patched. (source: edge)

<!-- Dismissed (3): (1) AC3 inline-vs-materialized-file — INLINE `acceptedClarifications` in the bundle root instead of a referenced redaction-policed input file; recorded USER DECISION in Completion Notes (no materialized-file seam exists; redaction via the whole-bundle redact pass + 256KB cap + @Size). (2) AC6 supersede reason `clarification_not_addressed` vs the AC's literal `spec_runner_skipped_question` — the AC's named token lives in REJECTED_INVALID_REASONS, not SUPERSEDED_REASONS, so it is INVALID for markSuperseded; the code chose a valid superseded-vocabulary token — correct resolution of a spec self-contradiction. (3) Best-effort acknowledgement-write partial-failure poisoning the shared tx before the synchronous sweep — defended by the pre-flight existence probe + first-wins dedup (the shared-tx-poison guard the story called out); residual risk is narrow. -->
