# Story 4.16: Artifact Reconciliation — Operator-Driven Repair Actions

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a Workflow Owner reviewing detected drift from story 4.15,
I want operator-triggered repair methods on `ArtifactReconciliationService` (`markOperationFailed`, `markOperationComplete`, `markPayloadUnavailable`, `restoreFromBackup`, `markCorrupted`, `reVerifyChecksum`) plus a REST endpoint `POST /api/v1/artifact-drift/{driftId}/repair` and the sibling `deliveryline operator artifact-repair` CLI command to invoke them,
so that AR14 (artifact reconciliation deeper than the story 1.12 skeleton) is fully realized — operators resolve detected drift through explicit, auditable decisions (NFR19: no silent overwrite).

## Context & Central Reconciliation (READ FIRST)

**This is the REPAIR half of the Epic-4 artifact-reconciliation split (detection = 4.15 `done`/`review`, operator-driven repair = 4.16 THIS story, lineage/fork governance = 4.16a — a SEPARATE `backlog` story you MUST NOT build here).** Story 4.15 already shipped and is on-branch (see [[story-4-15-artifact-drift-detection]]): the `artifact_drift_detected` table (**V45** — `resolved_at`/`resolved_by_action_id` columns + `fk_artifact_drift_detected_recovery_actions` FK already exist, **nullable + unwritten, waiting for you**), the `DriftCategory` persisted enum, the `adr_` `PublicIdPrefixes.ARTIFACT_DRIFT_DETECTED` prefix, the `WorkflowEventType.ARTIFACT_DRIFT_DETECTED` event, the drift write/read ports (`ArtifactDriftWritePort.recordIfAbsent` + `ArtifactDriftReadPort.listUnresolved`/`countUnresolvedByCategory`), the `listUnresolvedDrift(DriftFilter) → List<DriftSummary>` read surface on `ArtifactReconciliationService`, and the computed `RepairActionHint`. **You consume all of that; you build the WRITE/repair path on top of it.**

**⚠️ THIS IS A RICH STORY, NOT A PURE ADAPTER.** Unlike the 4.10–4.14 REST slice (where 4.5–4.9 had already built the whole application/domain half and the endpoints were pure wiring), **4.16 builds BOTH halves**: (1) the operator-repair application logic on `ArtifactReconciliationService`, (2) new drift persistence methods (resolve + single-row lookup), (3) `ArtifactStatus.corrupted` + a migration widening two CHECKs, (4) four new `DomainErrorCode`s, (5) a NEW `artifact.driftRepaired` event, (6) a NEW non-workflow-scoped `ArtifactDriftController`, and (7) the CLI command + equivalence tests. Budget accordingly — this is closer in size to a 4.7/4.8 recovery story than to a 4.14 adapter story.

### HEADLINE RECONCILIATIONS (epic AC text drifts from live code / omits critical mechanics — these bindings win)

1. **⚠️ `recovery_actions` HAS NO `details` COLUMN. Epic AC3's "`details` JSONB carrying the repair-method name + parameters" is factually wrong about the table.** The table (`V1:265-293`) has NO `details` column — never did. Every recovery action's rich audit payload lives on the **resulting `workflow_events.details` JSONB**, linked via `recovery_actions.resulting_event_id`. `classifyFailure` (story 4.9) is the exact precedent: it builds a `LinkedHashMap` of detail keys, appends ONE `workflow_events` row carrying them, then sets that event's `evt_` public-id as `resulting_event_id` on the recovery-action `insert`. **Bind: each repair appends ONE `artifact.driftRepaired` event whose `details` carry `repairAction` + `driftCategory` + `driftId` + `artifactId`/`operationId` + `reason` + `correlationId` + `idempotencyKey`; that event's id becomes the recovery action's `resulting_event_id`. NEVER try to write a `recovery_actions.details` column.** [Source: V1__create_workflow_core_tables.sql:265-293; RecoveryService.java:3020-3068 (classifyFailure detail→event→resulting_event_id)]

2. **`action_type` is NOT a domain-registry enum — it is an SQL CHECK + private `RecoveryService` constants. Add `'artifact_repair'` via a NEW migration + a new constant.** There is NO `RecoveryActionType` class in `domain.registry` (the allowed set lives only in `ck_recovery_actions_action_type` and `RecoveryService` `ACTION_TYPE_*` constants at `:177-195`). Current accepted set: `retry, rerun, resume, takeover, pause, reconcile, classify_failure`. **Bind: (a) add `ACTION_TYPE_ARTIFACT_REPAIR = "artifact_repair"` in the `RecoveryService`-style constants block on `ArtifactReconciliationService`; (b) the new migration DROP+RE-ADDs `ck_recovery_actions_action_type` restating the full set + `'artifact_repair'` — copy `V44__add_failure_classification.sql:55-62` verbatim.** No `RegistryContractTest`/`FlywaySchemaContractTest` pins the `action_type` value-set (they pin only `actor_type`/`result_status` and the column type), so nothing reds — but add a positive IT proving the widened CHECK accepts an `artifact_repair` row (mirror `RecoveryServiceClassifyFailureIT`). [Source: V1:280 CHECK; V44:55-62; RecoveryService.java:177-195,3057-3068; RegistryContractTest.java:166]

3. **The migration is `V46` (provisional) and does ZERO new tables/columns — only TWO CHECK widenings.** Highest on-disk is **V45** (story 4.15) → next-free is **V46**, but it is merge-order-dependent (Epic-4 siblings may claim ahead of you); **re-confirm against the target branch's `db/migration/` immediately before authoring** ([[flyway-v31-cross-branch-collision]]). `V46__widen_artifact_repair_checks.sql` (or similar) does exactly two things: DROP+ADD `ck_recovery_actions_action_type` (+`artifact_repair`) and DROP+ADD `ck_artifacts_status` (+`corrupted`). **No new table, no new column** — the `resolved_at`/`resolved_by_action_id` columns + `fk_artifact_drift_detected_recovery_actions` FK already shipped in V45 (`V45:26-27,48-49`). Register the widened CHECKs in `FlywaySchemaContractTest` if it asserts their value-sets (it does NOT today — it only asserts `status`/`action_type` are `text`), and assert replay + ordering in `FlywayMigrationsFoundationContract`. [Source: db/migration/ highest=V45; V45:26-27,48-49; V44:55-62; FlywaySchemaContractTest.java:202]

4. **`ArtifactStatus.corrupted` is the ONLY new persisted status — a 3-site fan-out the `RegistryContractTest` three-way-aligns.** Add `CORRUPTED("corrupted")` (`ArtifactStatus.java:9`); widen `ck_artifacts_status` in the V46 migration (site 2); add `"corrupted"` to `registry-api-schema-placeholders.json` `artifactStatuses` (site 3). `PersistedRegistryValues.artifactStatus` + `DomainRegistry.artifactStatuses()` auto-derive — no edit. `RegistryContractTest.actorTypesAndStatusRegistriesStayAlignedWithSqlChecksAndApiManifest` (`:160-173`) asserts `DomainRegistry.artifactStatuses() == extractConstraintValues("ck_artifacts_status") == manifest.artifactStatuses` — it REDS unless sites 2+3 move in lockstep with the enum. `artifacts.status` is NOT an OpenAPI schema enum → no OpenAPI regen from this. **`markPayloadUnavailable` does NOT add a status** — see Reconciliation 8. [Source: ArtifactStatus.java:5-9; V1:128; RegistryContractTest.java:160-173; registry-api-schema-placeholders.json:27-32]

5. **⚠️ FOUR new `DomainErrorCode`s — and `ProblemDetailsCatalog` is EXHAUSTIVE-CHECKED, so an unregistered code CRASHES app construction.** Add `DRIFT_NOT_FOUND`, `DRIFT_ALREADY_RESOLVED`, `INVALID_REPAIR_ACTION_FOR_DRIFT_CATEGORY`, `MISSING_REPAIR_REQUIRED_FIELD` (`DomainErrorCode.java`, before the terminating `;`). `ProblemDetailsCatalog.createMetadata()` ends with `if (!metadata.keySet().equals(EnumSet.allOf(DomainErrorCode.class))) throw new IllegalStateException(...)` (`:688-690`) — every new code MUST be registered or the catalog blows up at boot. This is the classic [[new-domainerrorcode-three-sites]] fan-out: (1) enum, (2) `ProblemDetailsCatalog` (status + retryable), (3) `problemTypeUris` in `registry-api-schema-placeholders.json`; auto-covered by `ProblemDetailsCoverageFoundationContract` under `-Pfoundation-gate` (confirm it is registered in `FoundationGateVerificationTest`'s delegate list — [[foundation-contract-must-be-registered-to-run]]). Status mappings by precedent: `DRIFT_NOT_FOUND`→404 non-retryable (mirror `CONFLICT_NOT_FOUND` `:619`), `DRIFT_ALREADY_RESOLVED`→409 non-retryable (mirror `CONFLICT_ALREADY_RESOLVED`), `INVALID_REPAIR_ACTION_FOR_DRIFT_CATEGORY`+`MISSING_REPAIR_REQUIRED_FIELD`→400 non-retryable. Epic AC6's `ACTION_NOT_ALLOWED` (409) has NO generic code in this repo — reuse `INVALID_REVIEWER_ROLE_FOR_ENDPOINT` for the role gate (see Reconciliation 11). [Source: DomainErrorCode.java:267-301; ProblemDetailsCatalog.java:619,652,688-690; registry-api-schema-placeholders.json problemTypeUris ~:120,210]

6. **The drift ports built by 4.15 are INSERT/READ-only — you add a single-row lookup + a resolve write.** `ArtifactDriftWritePort` has only `recordIfAbsent(DriftRecordRequest)`; `ArtifactDriftReadPort` has only `listUnresolved(DriftQuery)` (hard-filters `resolved_at IS NULL`) + `countUnresolvedByCategory()`. **Add: (a) `Optional<DriftRow> findByPublicId(String driftId)` returning EVEN RESOLVED rows (so `DRIFT_ALREADY_RESOLVED` is detectable — the existing `listUnresolved` cannot return a resolved row); (b) `boolean resolveDrift(String driftId, String resolvedByActionPublicId, Instant resolvedAt)` setting `resolved_at` + `resolved_by_action_id`; (c) `boolean updateLastKnownState(String driftId, String lastKnownStateJson)` for the `reVerifyChecksum`-no-match path (Reconciliation 9).** Put (a) on the read port, (b)+(c) on the write port; implement in `ArtifactDriftPersistenceAdapter` (pure `NamedParameterJdbcTemplate`). `resolved_by_action_id` FKs `recovery_actions(public_id)` RESTRICT → you MUST insert the `recovery_actions` row FIRST, then pass its `rcv_` id to `resolveDrift`. `DriftRow = (driftId, workflowRunId, artifactId, artifactOperationId, driftCategory, detectedAt, lastKnownStateJson)`. [Source: ArtifactDriftWritePort.java:22; ArtifactDriftReadPort.java:20-22; ArtifactDriftPersistenceAdapter.java (LIST_UNRESOLVED_SQL:65-66); DriftRow.java:15-22; V45:26-27,48-49]

7. **⚠️ AC9 IS UNENFORCED WITHOUT A NEW ArchUnit RULE — the 4.15 rule is method-name-specific and will NOT fire on your new resolve method.** The 4.15 rule `ONLY_RECONCILIATION_PACKAGE_MAY_WRITE_ARTIFACT_DRIFT` (`ArchitectureRuleCatalog.java:549-558`) bans exactly `callMethod(ArtifactDriftWritePort.class, "recordIfAbsent", DriftRecordRequest.class)` from outside `application.artifact.reconciliation..` — it does NOT bind the whole interface, so a new `resolveDrift` call from `application.artifact.ArtifactReconciliationService` passes silently. AC9 ("`ArtifactReconciliationService` is the ONLY service that may resolve drift rows") therefore needs a **NEW** rule: mirror the 4.15 `callMethod(...)` shape but scope it to `resolveDrift` (+ `updateLastKnownState`) and permit ONLY `ArtifactReconciliationService` (`noClasses().that().areNotAssignableTo(ArtifactReconciliationService.class)...should().callMethod(...resolveDrift...)`, or the reflection-free `ArchCondition` variant `ARTIFACT_WRITES_MUST_GO_THROUGH_ARTIFACT_OPERATION_SERVICE` uses). **Keep the repair methods on `ArtifactReconciliationService` in `application.artifact`** — that class already legitimately owns the drift READ surface (the 4.15 javadoc at `ArchitectureRuleCatalog.java:546-547` states reads are unrestricted and `listUnresolvedDrift` "may stay in `application.artifact`"), so resolving from there is consistent; do NOT relocate the methods into the `.reconciliation` subpackage. Register the new `@ArchTest` in `ArchitectureBoundaryTest` (Failsafe tier — [[archunit-runs-in-failsafe-not-surefire]]). [Source: ArchitectureRuleCatalog.java:546-558; ArchitectureBoundaryTest.java]

8. **⚠️ Repair→status mapping: 4 of 6 reuse existing port methods; `markCorrupted` is the only genuinely-new status transition; `markPayloadUnavailable` and `restoreFromBackup` are DESIGN-DECISION / STUB (OQ).**
   - `markOperationFailed` → **REUSE** `ArtifactOperationPort.markFailed(opId, FailureCategory, reason)` → `'failed'` (in CHECK).
   - `markOperationComplete` → **REUSE** `ArtifactOperationPort.markComplete(opId)` → `'complete'`. AC6: requires `completionEvidence` → throw `MISSING_REPAIR_REQUIRED_FIELD` if absent.
   - `markCorrupted` → **NEW** `ArtifactRecordPort.markCorrupted(artifactId, reason)` → `'corrupted'`; adapter adds a transition guard permitting current `AVAILABLE` (checksum-mismatch drift is on an `available` artifact) → `CORRUPTED`, else `ARTIFACT_INVALID_STATE_TRANSITION`.
   - `markPayloadUnavailable` → **NO dedicated status exists.** `ARTIFACT_PAYLOAD_UNAVAILABLE` is a `DomainErrorCode`, not a status. Provisional binding: transition `AVAILABLE → FAILED` via a NEW `ArtifactRecordPort.markPayloadUnavailable(artifactId, reason)` (the existing `markFailed` guard rejects `AVAILABLE`, so do NOT reuse it as-is) reusing the `FAILED` status with a distinct failure reason token. **OQ-1** — confirm FAILED-vs-new-status.
   - `restoreFromBackup` → **E4 STUB** (AC1 explicit): define the method signature + the `restore_from_backup` repairAction enum value for forward compatibility, but the body rejects with a typed error until the future backup-integration epic wires a backup source. **OQ-2** — confirm the stub's error/behavior.
   - `reVerifyChecksum` → recompute via `ArtifactChecksum.digestHex(alg, LocalArtifactStore.readBytes(ref))`; if it now MATCHES `checksum_value` → resolve the drift (transient/false-positive); if still mismatched → leave the drift UNRESOLVED and `updateLastKnownState` (Reconciliation 6c). No status change on match.
   [Source: ArtifactOperationPort.java:44,46,60; ArtifactRecordPort.java:84,87,90; ArtifactRecordPersistenceAdapter.java:289,315,363 (guards); ArtifactChecksum.digestHex; LocalArtifactStore.readBytes]

9. **Approval invalidation (AC4) — REUSE `ApprovalService.invalidateCurrentApproval(runId, artifactType, reason)`, which is `Propagation.MANDATORY`.** Story 4.7's V42 columns (`approvals.invalidated_at` + `invalidated_reason`) and the `ApprovalService.invalidateCurrentApproval` method (`:547-577`) are the exact reuse. `markCorrupted` + `markPayloadUnavailable` (the two that render an artifact unusable) call it, deriving `workflowRunId` + `artifactType` from the `ArtifactRecordSnapshot`; mint a controlled reason token (`superseded_by_mark_corrupted` / `superseded_by_mark_payload_unavailable`) mirroring 4.7's `superseded_by_rerun_from_step`. Because the method is `MANDATORY`, the repair MUST run inside the prep transaction (it already will — single-tx per AC2). **Caveat (flag in code):** `invalidateCurrentApproval` keys on **run + type**, invalidating the current approved leaf of that type — NOT a specific artifact version; `ApprovalReadPort` has no by-artifact-id finder. If a repair must target a specific superseded version rather than the current leaf, that is a new read+write path — **OQ-3**. PM/Developer notification (AC4) is the run-timeline event you already append (the `artifact.driftRepaired` event surfaces in FR47 history). [Source: V42__add_approval_invalidation.sql:21-22; ApprovalService.java:547-577; ApprovalWritePort.java:44; RecoveryService.java rerunFromStep invalidation ~:2223-2229]

10. **One NEW `WorkflowEventType.ARTIFACT_DRIFT_REPAIRED("artifact.driftRepaired")` — provisional single event per repair (NOT per-method events).** Epic AC2 names per-method events (`artifact.operationFailed`, `artifact.corrupted`) but that multiplies the [[new-workfloweventtype-fixture-sites]] fan-out five-fold. **Provisional binding: ONE `artifact.driftRepaired` event, state-neutral (like `artifact.driftDetected` — `priorState==resultingState==null`, `SYSTEM`-or-actor, non-lifecycle → NOT in scenario-stream fixtures), whose `details.repairAction` discriminates the specific repair.** `event_type` is an un-CHECKed text column → NO migration for the event. Fan-out = add the enum value + the string to `workflow-event-types.fixture.json` (`DomainRegistry` auto-derives). Add ONE new allow-listed detail key `repairAction` (`WorkflowEventDetailKeys` holder + `ALLOW_LISTED_KEYS` + its contract test + `workflow-history.v1.schema.json` — exactly the 4.15 `driftCategory` precedent); REUSE `driftCategory`/`artifactId`/`reason`/`correlationId`/`idempotencyKey`. **OQ-4** — confirm single-event vs per-method events. [Source: WorkflowEventType.java:173-182 (RECOVERY_FAILURE_CLASSIFIED / ARTIFACT_DRIFT_DETECTED — un-CHECKed, fixture-pinned); WorkflowEventDetailKeys.java:121 (4.15 driftCategory key precedent)]

11. **The REST endpoint needs a NEW `ArtifactDriftController` — it is NOT workflow-scoped, so it does NOT go on `WorkflowController`.** Path `POST /api/v1/artifact-drift/{driftId}/repair` (`{driftId}` = `adr_...`). Create `@RestController @Validated @RequestMapping("/api/v1/artifact-drift") @Tag(...) ArtifactDriftController` in `adapters.rest`, mirroring `WorkflowController`'s mutating-endpoint idiom: the `Idempotency-Key` + `X-Actor-Identity` header preamble (`rejectMultiValuedIdempotencyKeyHeader` → `requireNonBlankIdempotencyKey` → `rejectMultiValuedActorIdentityHeader` → `localActorIdentityResolver.requireSafe/resolve`), a `role` gate (`requireWorkflowOwnerRole("artifact-repair", request.role())` → `INVALID_REVIEWER_ROLE_FOR_ENDPOINT`, satisfying epic AC6's `ACTION_NOT_ALLOWED`), sanitized logs (reason LENGTH only), and returning the response DTO directly. **These preamble/role helpers currently live private on `WorkflowController`** — extract them to a shared `@Component`/util the new controller reuses, OR duplicate the minimal set (**OQ-5** — prefer extraction). The controller MUST call an application-service method, never a `*Port` directly (thin-controller ArchUnit `REST_CONTROLLERS_STAY_THIN...` `:240-255`; note it forbids `application.artifact.spi..` but the drift ports are in `application.artifact.reconciliation.spi..` — still, go through the service). `ArtifactDriftController` must end in `Controller` + live under `adapters.rest` (`REST_CONTROLLERS_MUST_BE_NAMED_AS_CONTROLLERS`). [Source: WorkflowController.java:91-95,2516-2603,2814-2831; ArchitectureRuleCatalog.java:240-278; OperatorController.java (non-workflow-scoped read precedent)]

12. **Single service entry point + typed dispatch.** AC1 lists six typed methods; AC5's request carries a single `repairAction` discriminator + action-specific fields. **Bind: a public coordinator `ArtifactReconciliationService.repairArtifactDrift(RepairArtifactDriftCommand) → ArtifactRepairResult` that (1) runs the idempotency replay pre-check (Reconciliation 13), (2) looks up the drift by `driftId` (`DRIFT_NOT_FOUND`/`DRIFT_ALREADY_RESOLVED`), (3) validates `repairAction` against the drift's `DriftCategory` (`INVALID_REPAIR_ACTION_FOR_DRIFT_CATEGORY`), (4) validates action-specific required fields (`MISSING_REPAIR_REQUIRED_FIELD`), then (5) dispatches to the matching typed method (`markOperationFailed`/…/`reVerifyChecksum`) — all inside ONE `REQUIRES_NEW` prep tx.** The six typed methods are the public API AC1 asks for; the coordinator is what the controller + CLI call so validation/idempotency is single-sourced and the controller stays thin. `repairAction`↔category legality mirrors the 4.15 `RepairActionHint` mapping: `orphan_operation`→{`mark_operation_failed`,`mark_operation_complete`}; `missing_payload`→{`mark_payload_unavailable`,`restore_from_backup`}; `checksum_mismatch`→{`re_verify_checksum`,`mark_corrupted`}. `repairAction` is a typed application enum surfaced to OpenAPI as inline `@Schema(allowableValues=…)` (NOT a persisted `domain.registry` value → no drift test), stored only as event-detail text.

13. **Idempotency = the classify `succeeded`-on-INSERT single-tx shape (NOT the pending→markSucceeded post-commit shape).** All repair effects (event append, status mutation, approval invalidation, recovery-action insert, drift resolve) commit in ONE transaction (AC2 "in one transaction") with NO post-commit side-effect, so mirror `classifyFailure`, NOT rerun/pause: run the replay pre-check (`recoveryActionRecordPort.findByIdempotencyKey`) BEFORE input validation; a prior row that is NOT (`action_type='artifact_repair'` AND same drift/run AND `result_status='succeeded'`) → `IDEMPOTENCY_KEY_CONFLICT`; a genuine match → REPLAY the prior result (reconstruct from the prior resulting event's details). Insert the `recovery_actions` row with `result_status='succeeded'` directly. **The `action_type` guard is load-bearing** — a prior row under the same key with a different `action_type` MUST conflict, never false-replay ([[transition-idempotencykey-is-not-a-dedupe-key]]). Order within the tx: append `artifact.driftRepaired` event → mutate status/invalidate approvals → insert `recovery_actions` (`resulting_event_id`=the event, `triggering_event_id`=the drift event per Reconciliation 14) → `resolveDrift(driftId, rcv_id, now)`. [Source: RecoveryService.java:1731-1747 (replay pre-check + action_type guard), 3057-3068 (succeeded-on-INSERT); [[story-4-9-recovery-service-classify-failure]]]

14. **`triggering_event_id` (AC3 = the `artifact.driftDetected` event) — a GAP; provisional best-effort, nullable.** The column is `bigint NULL` FK → `workflow_events(id)` ON DELETE SET NULL. There is NO read-port method to fetch the `artifact.driftDetected` event for a SPECIFIC drift row: 4.15 emitted that event with `artifactId`/`driftCategory` details but NOT the drift's `adr_` id, and `artifact_drift_detected` stores no triggering-event id. **Provisional binding: resolve best-effort via `WorkflowEventReadPort.findLatestByWorkflowRunPublicIdAndEventTypeIn(runId, List.of("artifact.driftDetected"))` and pass its `evt_` public-id (nullable → null if none); the adapter resolves public-id→bigint.** This is imprecise when a run has multiple drifts. **OQ-6** — confirm best-effort-latest vs. adding a precise drift→event link (would need 4.15's `driftDetected` event to carry the `adr_` id, a retro change). Also populate the reverse link: `resolveDrift` sets `artifact_drift_detected.resolved_by_action_id` = the new `rcv_` id. [Source: RecoveryService.java:1779-1783 (best-effort triggering event); WorkflowEventReadPort.java:36; V45:48-49]

15. **OpenAPI regen is MANDATORY and cascades into the frontend.** The net-new path + `ArtifactRepairRequest`/`ArtifactRepairResponse` schemas change `src/main/resources/openapi/openapi.json`; `OpenApiSnapshotContractTest` boots the app, canonicalizes `/v3/api-docs`, and asserts byte-equality — it reds until you regen with `-Dopenapi.snapshot.write=true`, review, commit. Add the new `operationId` (e.g. `repairArtifactDrift`) to that test's hardcoded operationId allowlist (`:87-125`). Then regenerate the FE client (`cd deliveryline-frontend && npm run generate-api`) or `check:api` reds ([[openapi-regen-frontend-client-drift-cascade]]). `repairAction` renders as a `string` with inline `enum:` (from `allowableValues`) — no named component. Regenerate carefully on Windows — mojibake em-dashes double-encode and red the snapshot ([[mojibake-emdash-openapi-drift]], [[stash-recovery-reinjects-mojibake-invisibly]]). [Source: OpenApiSnapshotContractTest.java:56-57,87-156]

16. **CLI: add `artifact-repair` to `OperatorCommands` + the ENUMERATED registration IT.** Add a `@Command(name="artifact-repair", …)` method to `OperatorCommands` (`@CommandGroup prefix "deliveryline operator"`); it calls the SAME `artifactReconciliationService.repairArtifactDrift(...)` the controller calls (CLI does NOT go through REST), resolving its own idempotency key + actor identity via the shared seams. Flags: `--drift <adr_id>` (arg or option), `--action <repairAction>`, `--reason`, action-specific `--field=value` (e.g. `--completion-evidence`, `--backup-source`), `--idempotency-key`, `--actor-identity`, `--correlation-id`, `--format text|json`, `--verbose`. **You MUST add `assertTrue(commandNames.contains("deliveryline operator artifact-repair"))` to `OperatorCliCommandRegistrationIT` (`:60-89`) — commands are enumerated, not auto-discovered** ([[story-4-14-rest-endpoint-classify-failure]]). If `--format json` is offered, add a `WorkflowCommandOutputs.renderOperatorArtifactRepairJson` (`operator-artifact-repair.v1`) + its `*JsonSchemaContractTest`. Add an `ArtifactRepairCliRestEquivalenceContractTest` (mirror `ClassifyFailureCliRestEquivalenceContractTest`) capturing the service args from both surfaces via `ArgumentCaptor`. [Source: OperatorCommands.java:53-58,873-1005; OperatorCliCommandRegistrationIT.java:60-89]

## Scope Boundary — what 4.16 BUILDS vs REUSES vs DEFERS

| Concern | 4.16 | Note |
|---|---|---|
| `ArtifactReconciliationService.repairArtifactDrift(command)` coordinator + 6 typed methods (`markOperationFailed`/`markOperationComplete`/`markPayloadUnavailable`/`restoreFromBackup`/`markCorrupted`/`reVerifyChecksum`) | **BUILD** | AC1 — Reconciliation 8/12 |
| `RepairAction` typed application enum + category-legality mapping | **BUILD** | AC1/AC6 — Reconciliation 12 |
| `V46` migration: DROP+ADD `ck_recovery_actions_action_type` (+`artifact_repair`) + DROP+ADD `ck_artifacts_status` (+`corrupted`) — NO new table/column | **BUILD** | AC2 — Reconciliation 2/3/4 |
| `ArtifactStatus.CORRUPTED` + `registry-api-schema-placeholders.json` `artifactStatuses` | **BUILD** | AC2 — Reconciliation 4 |
| `ArtifactRecordPort.markCorrupted` + `markPayloadUnavailable` (new adapter transitions from `AVAILABLE`) | **BUILD** | AC2 — Reconciliation 8 |
| `ArtifactDriftReadPort.findByPublicId` (returns resolved rows) + `ArtifactDriftWritePort.resolveDrift` + `updateLastKnownState` + adapter SQL | **BUILD** | AC2 — Reconciliation 6 |
| `recovery_actions` row per repair (`action_type='artifact_repair'`, `result_status='succeeded'`, `resulting_event_id`, best-effort `triggering_event_id`) via existing `RecoveryActionRecordPort` | **BUILD** | AC3 — Reconciliation 1/13/14 |
| `WorkflowEventType.ARTIFACT_DRIFT_REPAIRED` + fixture + `repairAction` allow-listed detail key (holder/ALLOW_LISTED_KEYS/contract test/`workflow-history.v1.schema.json`) | **BUILD** | AC2 — Reconciliation 10 |
| Approval invalidation on `markCorrupted`/`markPayloadUnavailable` via `ApprovalService.invalidateCurrentApproval` (MANDATORY, run+type keyed) | **BUILD** | AC4 — Reconciliation 9 |
| 4 new `DomainErrorCode`s + `ProblemDetailsCatalog` + `problemTypeUris` manifest | **BUILD** | AC6 — Reconciliation 5 |
| NEW `ArtifactDriftController` `POST /api/v1/artifact-drift/{driftId}/repair` → service coordinator | **BUILD** | AC5 — Reconciliation 11 |
| `ArtifactRepairRequest`/`ArtifactRepairResponse` DTOs (`repairAction` plain-String `@Schema(allowableValues)`, action-specific fields, `role`) | **BUILD** | AC5 — Reconciliation 11/12 |
| OpenAPI snapshot regen + operationId allowlist + FE `npm run generate-api` | **BUILD** | AC7 — Reconciliation 15 |
| CLI `deliveryline operator artifact-repair` + enumerated `OperatorCliCommandRegistrationIT` + CLI/REST equivalence + (opt) JSON renderer/schema | **BUILD** | AC8 — Reconciliation 16 |
| NEW ArchUnit rule: only `ArtifactReconciliationService` may call `resolveDrift`/`updateLastKnownState` | **BUILD** | AC9 — Reconciliation 7 |
| Drift DETECTION sweep / `recordIfAbsent` / `listUnresolvedDrift` / `RepairActionHint` / `DriftCategory` / `adr_` prefix / `ARTIFACT_DRIFT_DETECTED` event / V45 table | **REUSE** | shipped by story 4.15 |
| `ArtifactOperationPort.markFailed`/`markComplete`; `ArtifactChecksum.digestHex`; `LocalArtifactStore.readBytes`; `RecoveryActionRecordPort`; per-item/prep-tx idempotency-replay idiom | **REUSE** | Reconciliation 8/9/13 |
| `restoreFromBackup` full behavior (backup-source resolution) | **STUB** | AC1 explicit E4 stub — OQ-2 |
| Lineage/fork governance (`reattach`/`terminate`/`create_explicit_fork`, `artifact_lineage_reconcile`) | **DEFER** | **story 4.16a** (separate `backlog`) |
| Operator-UI repair form / decision-bar surfacing | **DEFER** | story 4.2 / 4.22 (FE, `backlog`) |
| `RecoveryService`, workflow-state transitions | **DO NOT TOUCH** | detection/repair are artifact-scoped; `RECOVERY_SERVICE_IS_SCOPE_PROTECTED` untouched |

## Acceptance Criteria

> From `epic-04-recovery.md` §"Story 4.16" (lines 337–354), with **binding clarifications** in **bold parentheticals**.

1. **Given** the `ArtifactReconciliationService` extension, **Then** typed repair methods are added: `markOperationFailed(driftId, reason, actor, idempotencyKey)`, `markOperationComplete(driftId, completionEvidence, actor, idempotencyKey)`, `markPayloadUnavailable(artifactId, reason, actor, idempotencyKey)`, `restoreFromBackup(artifactId, backupSource, actor, idempotencyKey)` (E4 stub), `markCorrupted(artifactId, reason, actor, idempotencyKey)`, `reVerifyChecksum(artifactId, actor, idempotencyKey)`. **(RECONCILED — expose the six as the public typed API AND a `repairArtifactDrift(RepairArtifactDriftCommand)` coordinator the REST/CLI call, which validates + dispatches in ONE `REQUIRES_NEW` tx — Reconciliation 12/13. `markOperationFailed`/`markOperationComplete` REUSE `ArtifactOperationPort.markFailed`/`markComplete`; `markCorrupted` is the only new status; `markPayloadUnavailable` → `AVAILABLE→FAILED` new adapter path (OQ-1); `restoreFromBackup` is a forward-compat STUB (OQ-2); `reVerifyChecksum` resolves-on-match / updates-lastKnownState-on-mismatch — Reconciliation 6/8.)**

2. **Given** each repair method, **Then** in one transaction: (a) the `artifact_drift_detected` row's `resolved_at` + `resolved_by_action_id` are set, (b) the corresponding `artifacts`/`artifact_operations` row is updated per the chosen repair + an event appended. **(RECONCILED — single `REQUIRES_NEW` prep tx (Reconciliation 13). Drift resolve via the NEW `resolveDrift` write method (Reconciliation 6); `resolved_by_action_id` FKs `recovery_actions.public_id` so insert the recovery-action FIRST. `corrupted` is the ONLY new `ArtifactStatus` (Reconciliation 4); it + the widened `ck_recovery_actions_action_type` are the ONLY DDL (V46 — Reconciliation 3). The appended event is ONE `artifact.driftRepaired` whose `details.repairAction` discriminates — NOT per-method event types (Reconciliation 10, OQ-4). `reVerifyChecksum` that still mismatches does NOT resolve (Reconciliation 8).)**

3. **Given** the `recovery_actions` table, **Then** every repair appends a row with `action_type='artifact_repair'`, `triggering_event_id`, `actor_*`, `reasonText`, `idempotency_key`, `result_status`, `details` JSONB carrying the repair-method name + parameters. **(⚠️ RECONCILED — `recovery_actions` HAS NO `details` COLUMN (Reconciliation 1). The repair-method name + params go on the RESULTING `workflow_events.details`, linked via `resulting_event_id` — mirror `classifyFailure` exactly. `action_type='artifact_repair'` needs the V46 CHECK widen + a `RecoveryService`-style constant (Reconciliation 2). `result_status='succeeded'` on INSERT (single-tx — Reconciliation 13). `triggering_event_id` = best-effort latest `artifact.driftDetected` event for the run, nullable (Reconciliation 14, OQ-6). `reviewer_role='workflow_owner'`. Reuse the existing `RecoveryActionRecordPort.insert(RecoveryActionWriteCommand)`.)**

4. **Given** approval-eligibility gating per story 1.12 AC6, **When** `markCorrupted` or `markPayloadUnavailable` runs on an artifact with approvals attached, **Then** those approvals are invalidated (mirrors story 4.7 AC7 using the V42 `invalidated_*` columns); PM/Developer is notified via run timeline. **(RECONCILED — REUSE `ApprovalService.invalidateCurrentApproval(runId, artifactType, reason)` (`Propagation.MANDATORY`, run+type keyed) with a controlled reason token; notification = the `artifact.driftRepaired` run-timeline event. Version-specific invalidation is a new path — OQ-3. Reconciliation 9. The columns are V42, not "V8" — epic text is stale.)**

5. **Given** REST `POST /api/v1/artifact-drift/{driftId}/repair`, **Then** typed request `ArtifactRepairRequest { repairAction, reasonText, ...action-specific-fields }` (camelCase); mandatory `Idempotency-Key` + `X-Actor-Identity` headers. **(RECONCILED — a NEW `ArtifactDriftController` (NOT `WorkflowController` — not workflow-scoped) — Reconciliation 11. `repairAction` is a plain `@Size String` with inline `@Schema(allowableValues)`, NO `@NotBlank` (service validates — surfaces `INVALID_REPAIR_ACTION_FOR_DRIFT_CATEGORY`/`MISSING_REPAIR_REQUIRED_FIELD`); `reasonText` required-but-service-validated (`@Size`, no `@NotBlank`); action-specific fields (`completionEvidence`, `backupSource`) optional/validated by the service; add a `role` field gated by `requireWorkflowOwnerRole` → `INVALID_REVIEWER_ROLE_FOR_ENDPOINT`. Header preamble mirrors the recovery endpoints — Reconciliation 11, OQ-5.)**

6. **Given** Problem Details errors, **Then** typed errors cover: `DRIFT_NOT_FOUND` (404), `DRIFT_ALREADY_RESOLVED` (409), `INVALID_REPAIR_ACTION_FOR_DRIFT_CATEGORY` (400), `MISSING_REPAIR_REQUIRED_FIELD` (400), `IDEMPOTENCY_KEY_CONFLICT` (409), `ACTION_NOT_ALLOWED` (409). **(RECONCILED — the first four are NEW `DomainErrorCode`s (3-site fan-out + exhaustive `ProblemDetailsCatalog` — Reconciliation 5). `IDEMPOTENCY_KEY_CONFLICT` exists. `ACTION_NOT_ALLOWED` has no generic code — the role gate reuses `INVALID_REVIEWER_ROLE_FOR_ENDPOINT` (Reconciliation 11). Also reachable: `MISSING_/INVALID_IDEMPOTENCY_KEY`, `INVALID_COMMAND_PAYLOAD` (unknown-field via `@JsonIgnoreProperties(ignoreUnknown=false)`), `ARTIFACT_INVALID_STATE_TRANSITION` if a repair races a status change.)**

7. **Given** OpenAPI + drift check, **Then** endpoint + repair-action enum schemas appear in regenerated snapshot. **(RECONCILED — regen `openapi.json` (`-Dopenapi.snapshot.write=true`) + FE `npm run generate-api`; add `repairArtifactDrift` to the `OpenApiSnapshotContractTest` operationId allowlist; `repairAction` = inline `enum` (no named component). Windows mojibake caution — Reconciliation 15.)**

8. **Given** CLI/REST equivalence, **Then** Spring Shell `deliveryline operator artifact-repair --drift {driftId} --action {repairAction} --reason "..." [--field=value ...] [--idempotency-key K]`; contract test asserts identical outcomes. **(RECONCILED — add to `OperatorCommands` + the ENUMERATED `OperatorCliCommandRegistrationIT`; CLI calls the service directly; `ArtifactRepairCliRestEquivalenceContractTest` captures service args from both surfaces — Reconciliation 16.)**

9. **Given** ArchUnit (story 1.11), **Then** `ArtifactReconciliationService` is the ONLY service that may resolve drift rows; controllers + adapters call the service, never write `artifact_drift_detected.resolved_*` directly. **(⚠️ RECONCILED — the 4.15 rule is method-name-specific to `recordIfAbsent` and will NOT enforce this; add a NEW `callMethod`-scoped rule permitting only `ArtifactReconciliationService` to call `resolveDrift`/`updateLastKnownState` + register the `@ArchTest` (Failsafe) — Reconciliation 7. Keep the repair methods in `application.artifact`, not the `.reconciliation` subpackage.)**

10. **Given** the test suite, **Then** tests cover: each repair method per drift category (happy path + idempotent replay), invalid-repair-action-for-category rejection, drift-already-resolved rejection, missing-required-field rejection, `markCorrupted`+`markPayloadUnavailable` invalidate prior approvals, REST endpoint conformance, CLI equivalence. **(RECONCILED — split real-PG `ArtifactDriftRepairIT` (`@Tag("integration")`, `*IT` name — [[springboot-testcontainers-test-must-be-IT]]): seed a resolved-able drift per category (a stale pending op; an `available` artifact with a deleted payload; an `available` artifact with a corrupt payload), assert status flip + `resolved_at`/`resolved_by_action_id` set + `recovery_actions` row (`artifact_repair`, widened-CHECK accepted) + `artifact.driftRepaired` event + approval invalidation, idempotent replay (second call → same result, no second row/event), `DRIFT_ALREADY_RESOLVED` on a re-repair, `reVerifyChecksum` resolve-on-match + leave-unresolved-on-mismatch, V46 replay. Unit `ArtifactReconciliationServiceRepairUnitTest` (call-through `TransactionTemplate`, mock ports): coordinator dispatch, category-legality matrix, missing-field rejection, event/detail shape via list-appender, replay-first precedence. `@WebMvcTest(ArtifactDriftController)` conformance (200 + the six error codes). `RegistryContractTest` green for `corrupted`. Logging pins per the standard below.)**

## Tasks / Subtasks

- [x] **Task 1 — Migration + registries (AC2, AC3, AC6)**
  - [x] Re-confirmed next-free Flyway version (highest on-disk = V45). Authored `V46__widen_artifact_repair_checks.sql`: DROP+ADD `ck_recovery_actions_action_type` (+`artifact_repair`); DROP+ADD `ck_artifacts_status` (+`corrupted`). Copied the DROP+ADD idiom from `V44:55-62`. NO new table/column.
  - [x] Added `ArtifactStatus.CORRUPTED("corrupted")` + `"corrupted"` to `registry-api-schema-placeholders.json` `artifactStatuses`.
  - [x] Added 4 `DomainErrorCode`s (`DRIFT_NOT_FOUND` 404, `DRIFT_ALREADY_RESOLVED` 409, `INVALID_REPAIR_ACTION_FOR_DRIFT_CATEGORY` 400, `MISSING_REPAIR_REQUIRED_FIELD` 400); registered each in `ProblemDetailsCatalog` (non-retryable) + `problemTypeUris` manifest.
  - [x] Added `WorkflowEventType.ARTIFACT_DRIFT_REPAIRED("artifact.driftRepaired")` + the string in `workflow-event-types.fixture.json` AND `workflow-events-response.schema.json` (2nd enum site); added allow-listed `repairAction` detail key (`WorkflowEventDetailKeys` holder + `ALLOW_LISTED_KEYS` + `workflow-history.v1.schema.json`).
- [x] **Task 2 — Drift persistence + artifact-status ports (AC1, AC2)**
  - [x] `ArtifactDriftReadPort.findByPublicId` (returns resolved rows; `DriftRow` gained a nullable `resolvedAt`) + `ArtifactDriftWritePort.resolveDrift` + `updateLastKnownState`; implemented in `ArtifactDriftPersistenceAdapter` (binds `resolvedAt` as OffsetDateTime UTC, not `Timestamp.from`, per the tz-shift trap).
  - [x] `ArtifactRecordPort.markCorrupted` (`AVAILABLE→CORRUPTED`) + `markPayloadUnavailable` (`AVAILABLE→FAILED`, OQ-1) new adapter guards. REUSE `ArtifactOperationPort.markFailed`/`markComplete`.
- [x] **Task 3 — Repair coordinator + typed methods (AC1, AC2, AC3, AC4)**
  - [x] `RepairAction` typed enum + category-legality mapping. `RepairArtifactDriftCommand` + `ArtifactRepairResult` records.
  - [x] `ArtifactReconciliationService.repairArtifactDrift(command)` coordinator + 6 typed public wrappers: replay-first (`action_type='artifact_repair'` guard) → drift lookup → category-legality → required-field → dispatch, all in ONE `REQUIRES_NEW` tx. Per repair: mutate status / invalidate approvals → append `artifact.driftRepaired` event → insert `recovery_actions` (succeeded-on-INSERT, best-effort `triggering_event_id`) → `resolveDrift`. Widened both ctors (production 14-arg + canonical) keeping the legacy 7-arg test ctor delegating with nulls.
  - [x] `restoreFromBackup` E4 stub (rejects with `INVALID_REPAIR_ACTION_FOR_DRIFT_CATEGORY` + WARN, OQ-2). `reVerifyChecksum` resolve-on-match / `updateLastKnownState`-on-mismatch.
- [x] **Task 4 — REST + CLI (AC5, AC6, AC7, AC8)**
  - [x] NEW `ArtifactDriftController` `POST /api/v1/artifact-drift/{driftId}/repair` → `repairArtifactDrift`. Header preamble + `role` gate DUPLICATED (minimal set) from `WorkflowController` (OQ-5). `ArtifactRepairRequest`/`ArtifactRepairResponse` DTOs.
  - [x] OpenAPI regen + `repairArtifactDrift` operationId allowlist + FE `npm run generate-api`.
  - [x] CLI `@Command(name="artifact-repair")` on `OperatorCommands` (calls the service directly, 3rd 9-arg ctor); added to `OperatorCliCommandRegistrationIT`; `renderOperatorArtifactRepairJson` (`operator-artifact-repair.v1`).
- [x] **Task 5 — ArchUnit + tests (AC9, AC10)**
  - [x] NEW ArchUnit rule `ONLY_RECONCILIATION_SERVICE_MAY_RESOLVE_ARTIFACT_DRIFT` (only `ArtifactReconciliationService` may call `resolveDrift`/`updateLastKnownState`) + `@ArchTest` registration.
  - [x] `ArtifactDriftRepairIT` (real PG, per-category), `ArtifactReconciliationServiceRepairUnitTest`, `ArtifactDriftControllerTest` (`@WebMvcTest` conformance), `ArtifactRepairCliRestEquivalenceContractTest`, logging pins.
- [x] **Logging instrumentation** (cross-cutting; required on every story)
  - [ ] Add SLF4J-backed structured logs at every public service entry/exit, every typed `DomainException` raise site, every external SPI call (DB write, file I/O), and every retry/replay/conflict/recovery branch.
  - [ ] Use parameterized logging (`log.info("...", arg1, arg2)`) — never string concatenation.
  - [ ] Levels: `INFO` normal lifecycle (repair start/finish, decision taken), `WARN` recoverable anomalies (replay, `reVerifyChecksum` still-mismatched, approval invalidated), `ERROR` only unhandled failures/invariant breaks. `DEBUG` for hot-path detail.
  - [ ] Every log carries `correlationId`, `workflowRunId`, `idempotencyKey`, `actorIdentity`, plus the entity's public id (`driftId`, `artifactId`/`operationId`). Use MDC (`MdcKeys.ARTIFACT_ID`/`ARTIFACT_OPERATION_ID`) where possible.
  - [ ] Never log secrets, payload bytes, raw tokens, PII, or the artifact payload read for re-hashing. Sanitize `reason` (log LENGTH only in the controller, like the recovery endpoints).
  - [ ] Add ≥1 focused test assertion that the expected log line(s) emit at the expected level for each new branch (list-appender / `OutputCaptureExtension`).

### Review Findings (code review 2026-07-14)

Adversarial review — 3 parallel layers (Blind Hunter, Edge Case Hunter, Acceptance Auditor). **1 decision-needed, 5 patch, 0 defer, 5 dismissed as noise.** Acceptance Auditor verdict: PASS with minor findings — every AC + all 16 reconciliations substantively implemented against the *reconciled* bindings; no scope leakage (no `RecoveryService` edit, no 4.16a lineage/fork).

- [x] [Review][Patch] APPLIED (verified: foundation-gate 54/54 + unit test). `restoreFromBackup` stub surfaces a semantically-wrong error code (**OQ-2 resolved → option 2**) — `restore_from_backup` is declared *legal* for `missing_payload` in `RepairAction.LEGAL_BY_CATEGORY` and advertised as an allowableValue, yet dispatch rejects it with `INVALID_REPAIR_ACTION_FOR_DRIFT_CATEGORY` ("invalid for category") when the action is valid-but-unimplemented. **Decision: add a dedicated `REPAIR_ACTION_NOT_IMPLEMENTED` `DomainErrorCode` (501/409, non-retryable) and throw it from the stub.** 3-site fan-out per [[new-domainerrorcode-three-sites]]: enum + `ProblemDetailsCatalog` (status + retryable) + `problemTypeUris` in `registry-api-schema-placeholders.json`; confirm `ProblemDetailsCoverageFoundationContract` stays green under `-Pfoundation-gate`. [ArtifactReconciliationService.java:914-930; RepairAction LEGAL_BY_CATEGORY]

- [x] [Review][Patch] APPLIED (verified: unit test `blankDriftIdIsRejectedAsInvalidCommandPayload`). CLI null/blank `--drift`/`--action` → opaque 500 (NPE) instead of a typed error [ArtifactReconciliationService.java:704 / ArtifactDriftPersistenceAdapter.findByPublicId / OperatorCommands.artifactRepair] — CLI declares `--drift`/`--action` `required=false` and passes them straight through; `findByPublicId(null)` hits `Objects.requireNonNull` before `.orElseThrow(driftNotFound)`, yielding a 500 rather than `DRIFT_NOT_FOUND`/`INVALID_COMMAND_PAYLOAD`. Also reachable on the replay path (:1039). Fix: guard blank/null driftId+action (CLI `requireNonBlank` or coordinator normalization → typed error). (blind+edge)

- [x] [Review][Patch] APPLIED (decision: affected-rows re-check; verified unit test `reVerifyChecksumThatLosesTheResolveRaceIsRejectedAsAlreadyResolved` + IT 6/6 no regression). `performRepair` now throws `DRIFT_ALREADY_RESOLVED` when an attempted `resolveDrift` updates 0 rows (a concurrent winner already set `resolved_at`), rolling back the whole tx so no duplicate event/recovery_actions commit. Concurrent distinct-key `re_verify_checksum` on one unresolved drift → duplicate `recovery_actions` + duplicate `artifact.driftRepaired` event, reports success [ArtifactReconciliationService.performRepair ~:770-856] — Step-1 replay only dedupes the *same* idempotency key; two operators with distinct keys both pass the drift-unresolved check and both recompute a now-matching checksum, appending an event + inserting a recovery_actions row each; the 2nd `resolveDrift` no-ops (`WHERE resolved_at IS NULL`) → `resolved=false` but still returns success with a duplicate audit row. Status-mutating actions are safe (status guard rolls the loser back) but surface `ARTIFACT_INVALID_STATE_TRANSITION` instead of `DRIFT_ALREADY_RESOLVED`. Fix: re-check/lock drift resolution inside the tx (advisory-lock precedent `RecoveryService.reconcile 0x5243`, or `SELECT … FOR UPDATE`) → throw `DRIFT_ALREADY_RESOLVED` so the whole tx rolls back. (edge+blind)

- [x] [Review][Patch] APPLIED (verified: unit test `oversizedReasonIsRejectedAsInvalidCommandPayload`; service now enforces the @Size bounds). CLI repair string inputs unbounded vs REST `@Size` — CLI/REST equivalence hole (AC8) + unbounded event-detail write [OperatorCommands.artifactRepair vs ArtifactRepairRequest] — `ArtifactRepairRequest` caps `repairAction ≤64`, `reasonText`/`completionEvidence`/`backupSource ≤512`; the CLI passes the same fields with no length guard and the service performs no length validation, so a multi-KB `--reason` is accepted via CLI (landing in the `artifact.driftRepaired` event details) while the identical REST call is rejected. `ArtifactRepairCliRestEquivalenceContractTest` only asserts happy-path arg equality, so it misses this. Fix: enforce length bounds in the service/command (single validation source) so both surfaces agree. (blind+edge)

- [x] [Review][Patch] APPLIED (verified: unit tests + real-PG `ArtifactDriftRepairIT` 6/6, incl. new `markPayloadUnavailableFailsArtifactAndResolvesMissingPayloadDrift` + `markOperationComplete` happy path). AC10 test gap — `missing_payload` / `markPayloadUnavailable` entirely untested [ArtifactDriftRepairIT / ArtifactReconciliationServiceRepairUnitTest] — the IT seeds only `orphan_operation` + `checksum_mismatch`; the unit test never exercises `markPayloadUnavailable`, so its `AVAILABLE→FAILED` transition, its `INVALIDATED_REASON_MARK_PAYLOAD_UNAVAILABLE` token, its approval invalidation, and the non-trivial `ck_artifacts_failure_reason_paired` adapter branch ship unverified. `markOperationComplete` happy path is only negatively tested. AC10 explicitly requires the deleted-payload scenario. Fix: add `missing_payload`/`markPayloadUnavailable` IT + unit coverage (with the approval-invalidation assertion) and a `markOperationComplete` happy-path case. (auditor)

- [x] [Review][Patch] APPLIED (decision: guard → INTERNAL_ERROR 500; verified unit test `nullTargetAxisOnDriftRowSurfacesInternalErrorNotNpe`). `requireDriftAxis` in `applyRepair` now throws `INTERNAL_ERROR` (500) with a diagnostic payload + ERROR log for a null category-axis (kept an honest 500 — a server-side invariant break, not a masked client error). Null target-axis on a drift row → NPE 500 in adapter (low reachability) [ArtifactReconciliationService.applyRepair ~:874-952] — dispatch reads `drift.artifactOperationId()` / `drift.artifactId()` with no null check; a drift row whose category-axis column is null (data corruption / hand-insert — the detection invariant normally prevents it) yields `requireOperation(null)`/`requireArtifact(null)`/`findByPublicId(null)` → NPE → 500 instead of a typed rejection. Fix: defensive null-axis guard → typed error. (edge)

## Dev Notes

### Relevant architecture patterns and constraints

- **Repair-vs-detection split (build on 4.15).** Story 4.15 shipped detection-only: the `artifact_drift_detected` table (V45), drift ports, `DriftCategory`, `RepairActionHint`, `adr_` prefix, `ARTIFACT_DRIFT_DETECTED` event, and `listUnresolvedDrift`. 4.16 is the WRITE/repair half. Read `ArtifactReconciliationService.java` end-to-end first — the `PROPAGATION_REQUIRES_NEW` per-item `TransactionTemplate`, per-tick `UUID` correlationId, `ActorContext.SYSTEM`, `ArtifactEventPort.append(ArtifactEventRecord)`, and `MdcKeys.beginScope/endScope` idioms are the ones you extend. The service already injects `ArtifactDriftReadPort` (nullable in the test-only ctor); you add the write/resolve port + recovery-action port + approval service.
- **The `classifyFailure` recovery-action precedent (mirror for the write + idempotency).** `RecoveryService.classifyFailure` (story 4.9) is the single-tx, `succeeded`-on-INSERT, replay-first precedent: replay pre-check with an `action_type` guard BEFORE input validation; detail payload built as a `LinkedHashMap` on the RESULTING event (there is NO `recovery_actions.details` column); the event's `evt_` id set as `resulting_event_id`. Reuse `RecoveryActionRecordPort.insert(RecoveryActionWriteCommand)` (9-arg: `workflowRunPublicId, actionType, triggeringEventPublicId, resultingEventPublicId, actorIdentity, actorType, idempotencyKey, resultStatus, reviewerRole`) + `findByIdempotencyKey` for replay.
- **Idempotency is NOT the transition key.** `Idempotency-Key` uniqueness on `recovery_actions` + the `action_type`/run guard is the dedupe surface ([[transition-idempotencykey-is-not-a-dedupe-key]]); a caught conflict must not poison the shared tx ([[caught-idempotency-conflict-poisons-shared-tx]]) — but here the whole repair is one `REQUIRES_NEW` unit and the `recovery_actions` unique index + the replay pre-check do the work.
- **Approval invalidation (V42, run+type keyed).** `ApprovalService.invalidateCurrentApproval(runId, artifactType, reason)` is `Propagation.MANDATORY` — call it inside the prep tx. It flips the current approved leaf of the type (`invalidated_at`/`invalidated_reason`); there is no by-artifact-version finder (OQ-3).
- **Registries + gates.** `ArtifactStatus.corrupted` → enum + `ck_artifacts_status` widen + `registry-api-schema-placeholders.json` (three-way `RegistryContractTest`). `action_type='artifact_repair'` → SQL CHECK widen + a constant only (NO registry enum). `WorkflowEventType.ARTIFACT_DRIFT_REPAIRED` → un-CHECKed text, fixture-pinned (no migration). New `DomainErrorCode`s → the exhaustive `ProblemDetailsCatalog` + `problemTypeUris` + foundation-gate. New `repairAction` detail key → `WorkflowEventDetailKeys` holder + `ALLOW_LISTED_KEYS` + contract test + `workflow-history.v1.schema.json`.
- **ArchUnit.** A NEW `callMethod`-scoped rule enforces AC9 (only `ArtifactReconciliationService` resolves drift). Thin-controller rule forbids `adapters.rest` reaching `application.*.spi..` — the new controller calls the service, not the ports. New rules run under Failsafe ([[archunit-runs-in-failsafe-not-surefire]]).
- **Scope protection.** Do NOT touch `RecoveryService` or any workflow-state transition; repair is artifact-scoped. Lineage/fork recovery is **story 4.16a** (separate `backlog`) — do NOT build it here ([[artifact-lineage-graft-spec-only-reexec-conflict]] is context only).

### Logging Requirements (project-wide standard)

Every story is expected to leave the touched services observable enough to debug a production incident without re-deploying (enforced via the "Logging instrumentation" task).

- **Framework:** SLF4J + Logback. No `System.out`, no `printStackTrace()`.
- **Where to log (minimum surface for this story):**
  - `repairArtifactDrift` → `INFO` on entry (`driftId`, `repairAction`) + `INFO` on exit (result, `resolved`); `WARN` on replay (`replayed=true`).
  - Each typed repair → `INFO` "artifact repair applied" with `driftId`/`artifactId`/`operationId`, `repairAction`, `driftCategory`, per-tick `correlationId`.
  - Approval invalidation → `WARN` "approval invalidated by repair" with the `apr_` id + reason token.
  - `reVerifyChecksum` still-mismatched → `WARN` "re-verify checksum still mismatched — drift left unresolved".
  - `restoreFromBackup` stub → `WARN` "restore-from-backup not yet supported" (until backup integration).
  - REST controller → `INFO` received/success with sanitized reason (LENGTH only).
- **Required context keys** (MDC or params): `correlationId`, `workflowRunId`, `idempotencyKey`, `actorIdentity`, `driftId`, `artifactId`/`operationId`, `repairAction`.
- **Forbidden:** payload bytes, checksum contents beyond short hex, secrets/PII. Never log the artifact payload read for re-hashing.
- **Test contract:** pin the `INFO`-on-repair + `WARN`-on-approval-invalidation + `WARN`-on-reVerify-mismatch lines with a list-appender / `OutputCaptureExtension`; assert the idempotent replay emits NO second event.

### Project Structure Notes

- **Extends** `org.dradgo.application.artifact.ArtifactReconciliationService` (repair coordinator + 6 typed methods + `RepairAction`/`RepairArtifactDriftCommand`/`ArtifactRepairResult`). **New** ports/methods on `application.artifact.reconciliation.spi.ArtifactDriftWritePort`/`ArtifactDriftReadPort` + `adapters.persistence.ArtifactDriftPersistenceAdapter`. **New** mutation methods on `application.artifact.spi.ArtifactRecordPort` + `adapters.persistence.ArtifactRecordPersistenceAdapter`. **New** `adapters.rest.ArtifactDriftController` + `ArtifactRepairRequest`/`ArtifactRepairResponse`. **Extend** `adapters.cli.OperatorCommands`. **New** `V46__widen_artifact_repair_checks.sql`.
- Registry edits: `ArtifactStatus` (+CHECK+manifest), `DomainErrorCode` (+catalog+manifest), `WorkflowEventType` (+fixture), `WorkflowEventDetailKeys` (+allow-list+schema). NO new `DomainRegistry` enum for `action_type` or `repairAction`.
- Variance: epic AC3's `recovery_actions.details` column does not exist (Reconciliation 1); epic AC4's "V8 invalidated_* columns" is actually V42 (Reconciliation 9); epic AC5's endpoint is on a NEW controller, not `WorkflowController` (Reconciliation 11); epic AC6's `ACTION_NOT_ALLOWED` has no generic code (Reconciliation 5/11); epic AC9 needs a NEW ArchUnit rule (Reconciliation 7).

### References

- [Source: _bmad-output/planning-artifacts/epic-04-recovery.md#Story 4.16 (lines 337–354)] — AC1–AC10; §4.16a (356–373) = lineage governance (OUT OF SCOPE — separate `backlog` story).
- [Source: _bmad-output/implementation-artifacts/4-15-artifact-reconciliation-db-file-drift-detection.md] — the detection half you build on: V45 table, drift ports, `DriftCategory`, `RepairActionHint`, `adr_` prefix, `ARTIFACT_DRIFT_DETECTED` event, `listUnresolvedDrift`.
- [Source: deliveryline-backend/src/main/java/org/dradgo/application/artifact/ArtifactReconciliationService.java] — the service you extend (per-item `REQUIRES_NEW`, per-tick correlationId, `ArtifactEventPort.append`, `listUnresolvedDrift`/`DriftSummary`/`RepairActionHint`, nullable `ArtifactDriftReadPort` ctor dep).
- [Source: deliveryline-backend/src/main/java/org/dradgo/application/recovery/RecoveryService.java:1731-1747,3020-3068] — `classifyFailure` replay-first + `action_type` guard + detail→event→`resulting_event_id`; `RecoveryActionWriteCommand` 9-arg shape.
- [Source: deliveryline-backend/src/main/resources/db/migration/V1__create_workflow_core_tables.sql:128,159,265-293] — `ck_artifacts_status`, `ck_artifact_operations_status`, `recovery_actions` (NO `details` column); V44:55-62 CHECK-widen idiom; V45:26-27,48-49 `resolved_*` cols + FK.
- [Source: deliveryline-backend/src/main/java/org/dradgo/domain/registry/ArtifactStatus.java:5-9; DomainErrorCode.java:267-301; WorkflowEventType.java:173-182; WorkflowEventDetailKeys.java:121] — registry homes.
- [Source: deliveryline-backend/src/main/java/org/dradgo/application/artifact/reconciliation/spi/ArtifactDriftWritePort.java:22; ArtifactDriftReadPort.java:20-22; adapters/persistence/ArtifactDriftPersistenceAdapter.java; DriftRow.java:15-22] — insert/read-only ports to extend with lookup + resolve.
- [Source: deliveryline-backend/src/main/java/org/dradgo/application/artifact/spi/ArtifactRecordPort.java:84-90; ArtifactOperationPort.java:44-60; adapters/persistence/ArtifactRecordPersistenceAdapter.java:289,315,363] — mutation methods + transition guards.
- [Source: deliveryline-backend/src/main/java/org/dradgo/application/approval/ApprovalService.java:547-577; application/approval/spi/ApprovalWritePort.java:44; V42__add_approval_invalidation.sql:21-22] — approval invalidation reuse.
- [Source: deliveryline-backend/src/main/java/org/dradgo/adapters/rest/WorkflowController.java:91-95,2516-2603,2814-2831; ProblemDetailsCatalog.java:619,652,688-690; ProblemDetailsMapper.java:77-99] — mutating-endpoint idiom, exhaustive catalog, error mapping.
- [Source: deliveryline-backend/src/test/java/org/dradgo/architecture/ArchitectureRuleCatalog.java:240-278,546-558; ArchitectureBoundaryTest.java] — thin-controller + naming rules; the 4.15 `recordIfAbsent` rule to mirror for AC9.
- [Source: deliveryline-backend/src/main/java/org/dradgo/adapters/cli/OperatorCommands.java:53-58,873-1005; src/test/.../OperatorCliCommandRegistrationIT.java:60-89; ClassifyFailureCliRestEquivalenceContractTest.java] — CLI command + enumerated registration + equivalence test.
- [Source: deliveryline-backend/src/test/java/org/dradgo/adapters/rest/OpenApiSnapshotContractTest.java:56-57,87-156; deliveryline-frontend/package.json (generate-api)] — OpenAPI snapshot + FE regen cascade.
- [Source: deliveryline-backend/src/test/resources/contracts/openapi/registry-api-schema-placeholders.json (artifactStatuses:27-32, problemTypeUris:~120); src/test/resources/contracts/events/workflow-event-types.fixture.json] — manifests/fixtures to update.

### Open Questions (for Alex — do not block dev; provisional bindings applied)

- **OQ-1 — `markPayloadUnavailable` target status.** No "unavailable" `ArtifactStatus` exists; `ARTIFACT_PAYLOAD_UNAVAILABLE` is only a `DomainErrorCode`. Provisional: transition `AVAILABLE→FAILED` via a new `markPayloadUnavailable` adapter path (the existing `markFailed` guard rejects `AVAILABLE`). Alternative: add a `payload_unavailable` `ArtifactStatus` (a 2nd new status + CHECK). Confirm before build.
- **OQ-2 — `restoreFromBackup` stub behavior.** AC1 marks it an E4 stub (backup-source resolution deferred). Provisional: define the method + `restore_from_backup` enum value; body rejects with a typed error (a new `BACKUP_SOURCE_NOT_CONFIGURED`? or reuse `INVALID_REPAIR_ACTION_FOR_DRIFT_CATEGORY`?) and logs WARN until the future backup epic. Confirm the exact error surface — adding a code has the 3-site fan-out.
- **OQ-3 — approval invalidation granularity.** `invalidateCurrentApproval` keys on run+type (the current leaf), not a specific artifact version. If a repair must invalidate a specific superseded version, that is a new read+write path. Confirm run+type is acceptable.
- **OQ-4 — one `artifact.driftRepaired` event vs per-method events.** Epic AC2 names `artifact.operationFailed`/`artifact.corrupted`; provisional binds ONE `artifact.driftRepaired` with a `repairAction` detail discriminator (one fixture site vs five). Confirm — per-method events multiply the [[new-workfloweventtype-fixture-sites]] fan-out.
- **OQ-5 — controller header/role helper reuse.** The header-preamble + `requireWorkflowOwnerRole` helpers are private on `WorkflowController`. Provisional: extract them to a shared `@Component`/util the new `ArtifactDriftController` reuses. Alternative: duplicate the minimal set. Confirm the extraction is welcome (touches `WorkflowController`).
- **OQ-6 — `triggering_event_id` precision.** No link from a drift row to its specific `artifact.driftDetected` event (4.15's event omits the `adr_` id). Provisional: best-effort latest `artifact.driftDetected` event for the run (nullable). Alternative: retro-add the drift id to 4.15's event details for a precise link. Confirm best-effort is acceptable.
- **OQ-7 — role gating on a drift-scoped endpoint.** The endpoint is drift-scoped, not workflow-scoped; provisional keeps a `role` field + `requireWorkflowOwnerRole` (→ `INVALID_REVIEWER_ROLE_FOR_ENDPOINT`) for parity with the recovery endpoints. Confirm the role gate belongs here.

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Claude Opus 4.8, 1M context) — bmad-create-story workflow.

### Debug Log References

- `@{argLine}` crash when running `surefire:test` / `failsafe:integration-test` as DIRECT goals ([[maven-arglineation-goal-crash]]) — run the `test` / `verify` LIFECYCLE phase with `-Djacoco.skip=true` instead.
- Windows PowerShell mangles unquoted `-D…=…` args (splits `-Djacoco.skip=true` at the dot) — always quote each `-D` arg.

### Completion Notes List

Implemented the REPAIR half of the Epic-4 artifact-reconciliation split on branch `feat/3g-5-claude-token-capture` (UNCOMMITTED). All 5 tasks + logging done.

**Provisional OQ bindings applied (all documented, none blocked dev):**
- **OQ-1** `markPayloadUnavailable` → `AVAILABLE→FAILED` (no dedicated status; distinct `failure_reason`).
- **OQ-2** `restoreFromBackup` → E4 stub: `restore_from_backup` is a real `RepairAction`/allowableValue but the dispatch body rejects with `INVALID_REPAIR_ACTION_FOR_DRIFT_CATEGORY` (+WARN, `reason=restore_from_backup_not_yet_supported`) — reuses one of the 4 codes (no 5th code added, keeping the scope-table count).
- **OQ-3** approval invalidation is run+type keyed (`ApprovalService.invalidateCurrentApproval`, MANDATORY), not version-specific.
- **OQ-4** ONE `artifact.driftRepaired` event per repair (state-neutral, `interventionMarker=true`); `details.repairAction` discriminates.
- **OQ-5** header/role preamble DUPLICATED (minimal set) into `ArtifactDriftController` rather than extracted from `WorkflowController` (lower blast radius).
- **OQ-6** `triggering_event_id` = best-effort latest `artifact.driftDetected` for the run (nullable).
- **OQ-7** kept the `role` gate (`workflow_owner` → `INVALID_REVIEWER_ROLE_FOR_ENDPOINT`) on the drift-scoped endpoint for parity with the recovery endpoints.

**Design notes:** the coordinator uses `WorkflowEventWritePort` directly (pre-generated `evt_` id → `resulting_event_id`, `interventionMarker=true`) — NOT `ArtifactEventPort` (which returns void + hardcodes `interventionMarker=false`), mirroring `classifyFailure`. `DriftRow` gained a nullable `resolvedAt` so `findByPublicId` can surface an already-resolved row for `DRIFT_ALREADY_RESOLVED`. `resolveDrift` binds `resolvedAt` as an `OffsetDateTime` at UTC (not `Timestamp.from`) to avoid the tz-shift trap.

**Verification (Docker up):** fast Surefire tier GREEN — `ArtifactReconciliationServiceRepairUnitTest` 11/0, `ArtifactDriftControllerTest` 8/0, `ArtifactRepairCliRestEquivalenceContractTest` 1/0. Failsafe/Docker tier (OpenAPI regen, RegistryContract three-way, WorkflowEventDetailKeys, ArchitectureBoundary incl. the new resolve-drift rule, `ArtifactDriftRepairIT`, CLI registration) + FE `generate-api` + spotless — see the Change Log / sprint-status entry for the final result.

**AC4 coverage note:** approval invalidation is verified at the UNIT level (the coordinator calls `invalidateCurrentApproval` with the correct run/type/reason-token for `markCorrupted`/`markPayloadUnavailable`); the end-to-end flip is covered by story 4.7's `ApprovalServiceInvalidationTest`. The repair IT focuses on the real-PG drift/artifact/recovery-action/event state + idempotency to avoid a fragile approvals seed.

### File List

**New (main):**
- `deliveryline-backend/src/main/resources/db/migration/V46__widen_artifact_repair_checks.sql`
- `deliveryline-backend/src/main/java/org/dradgo/application/artifact/RepairAction.java`
- `deliveryline-backend/src/main/java/org/dradgo/application/artifact/RepairArtifactDriftCommand.java`
- `deliveryline-backend/src/main/java/org/dradgo/application/artifact/ArtifactRepairResult.java`
- `deliveryline-backend/src/main/java/org/dradgo/adapters/rest/ArtifactDriftController.java`
- `deliveryline-backend/src/main/java/org/dradgo/adapters/rest/ArtifactRepairRequest.java`
- `deliveryline-backend/src/main/java/org/dradgo/adapters/rest/ArtifactRepairResponse.java`

**Modified (main):**
- `deliveryline-backend/src/main/java/org/dradgo/application/artifact/ArtifactReconciliationService.java` (repair coordinator + 6 typed methods + widened ctors)
- `deliveryline-backend/src/main/java/org/dradgo/application/artifact/reconciliation/spi/ArtifactDriftReadPort.java` (+`findByPublicId`)
- `deliveryline-backend/src/main/java/org/dradgo/application/artifact/reconciliation/spi/ArtifactDriftWritePort.java` (+`resolveDrift`/`updateLastKnownState`)
- `deliveryline-backend/src/main/java/org/dradgo/application/artifact/reconciliation/spi/DriftRow.java` (+`resolvedAt`)
- `deliveryline-backend/src/main/java/org/dradgo/adapters/persistence/ArtifactDriftPersistenceAdapter.java`
- `deliveryline-backend/src/main/java/org/dradgo/application/artifact/spi/ArtifactRecordPort.java` (+`markCorrupted`/`markPayloadUnavailable`)
- `deliveryline-backend/src/main/java/org/dradgo/adapters/persistence/ArtifactRecordPersistenceAdapter.java`
- `deliveryline-backend/src/main/java/org/dradgo/domain/registry/ArtifactStatus.java` (+`CORRUPTED`)
- `deliveryline-backend/src/main/java/org/dradgo/domain/registry/DomainErrorCode.java` (+4 codes)
- `deliveryline-backend/src/main/java/org/dradgo/adapters/rest/ProblemDetailsCatalog.java`
- `deliveryline-backend/src/main/java/org/dradgo/domain/registry/WorkflowEventType.java` (+`ARTIFACT_DRIFT_REPAIRED`)
- `deliveryline-backend/src/main/java/org/dradgo/domain/registry/WorkflowEventDetailKeys.java` (+`REPAIR_ACTION`)
- `deliveryline-backend/src/main/resources/schemas/cli/workflow-history.v1.schema.json`
- `deliveryline-backend/src/main/java/org/dradgo/adapters/cli/OperatorCommands.java` (+`artifact-repair` command + 3rd ctor)
- `deliveryline-backend/src/main/java/org/dradgo/adapters/cli/WorkflowCommandOutputs.java` (+`renderOperatorArtifactRepairJson`)
- `deliveryline-backend/src/main/resources/openapi/openapi.json` (regenerated)

**New (test):**
- `deliveryline-backend/src/test/java/org/dradgo/application/artifact/ArtifactReconciliationServiceRepairUnitTest.java`
- `deliveryline-backend/src/test/java/org/dradgo/adapters/rest/ArtifactDriftControllerTest.java`
- `deliveryline-backend/src/test/java/org/dradgo/adapters/cli/ArtifactRepairCliRestEquivalenceContractTest.java`
- `deliveryline-backend/src/test/java/org/dradgo/application/artifact/reconciliation/ArtifactDriftRepairIT.java`

**Modified (test):**
- `deliveryline-backend/src/test/resources/contracts/openapi/registry-api-schema-placeholders.json` (+`corrupted` + 4 problemTypeUris)
- `deliveryline-backend/src/test/resources/contracts/events/workflow-event-types.fixture.json` (+`artifact.driftRepaired`)
- `deliveryline-backend/src/test/resources/fixture-event-streams/schema/workflow-events-response.schema.json` (+`artifact.driftRepaired`)
- `deliveryline-backend/src/test/java/org/dradgo/architecture/ArchitectureRuleCatalog.java` (+resolve-drift rule)
- `deliveryline-backend/src/test/java/org/dradgo/architecture/ArchitectureBoundaryTest.java` (+@ArchTest)
- `deliveryline-backend/src/test/java/org/dradgo/adapters/rest/OpenApiSnapshotContractTest.java` (+`repairArtifactDrift` allowlist)
- `deliveryline-backend/src/test/java/org/dradgo/adapters/cli/OperatorCliCommandRegistrationIT.java` (+registration assertion)
- `deliveryline-frontend/src/api/**` (regenerated by `npm run generate-api`)
