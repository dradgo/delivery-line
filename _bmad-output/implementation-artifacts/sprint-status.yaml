# last_updated: 2026-06-01 (story 2-16 bmad-code-review `review -> done`: Run Context Strip Component. 3 review layers produced 7 patch findings, 0 decisions, 0 defers, 5 dismissed. Batch-applied fixes: error state now stays within the capped strip via scroll containment; stale state re-evaluates when RUN_STALE_THRESHOLD_MS is crossed while mounted; missing artifact version now drives partial-context; revision labels render implementation-plan/pr-output display names; max-height coverage expanded across partial/stale/error states; loading exposes aria-busy; long backend values use truncation + title inspection. Verified via PowerShell: build, eslint --max-warnings=0, lint:rules-test 4/4, vitest 33 files/231 tests, prettier check.)
# last_updated: 2026-06-01 (story 3-6 created via bmad-create-story `backlog -> ready-for-dev`: Runner Logs Capture + Redaction + Classification. Next-active Epic-3a backend track (3-5,3-3,3-4 done -> 3-6 -> 3-13 -> 3-14 -> 3-9 -> 3a-1 -> 3a-2). NEW application.runner.RunnerLogCaptureService.captureLogs(rex,runId,stdout,stderr)->CapturedLogs: REDACT each stream via story-1.10 RedactionPolicyService.redact BEFORE any write, then write redacted runner.stdout/.stderr to a NEW durable store {DELIVERYLINE_HOME}/runner-logs/{rex}/ (SEPARATE root from runner-work/{rex}/ so story-3.2 RunnerWorkspaceCleanupJob never reaps the 60-day redacted log; NFR31). NEW SPI RunnerLogStore + adapters.files.LocalRunnerLogStore (mirror LocalRunnerWorkspaceStore). DockerRunnerAdapter (ONLY class allowed to touch raw output, AC8) reads raw logs/runner.stdout+runner.stderr at container exit (~tryReadResult:439/classifyExited:596) and hands them straight to captureLogs (raw stays method-local; never field-stored/persisted/returned). runner_executions +4 nullable cols via NEW Flyway V11 (CONFIRMED max=V10 heartbeat_stale): raw_output_reference/raw_output_classification[NEW CHECK; note artifacts.classification has NO existing CHECK-code-enforced, OQ-7]/raw_output_byte_size/redaction_count + RunnerExecutionEntity fields + NEW RunnerExecutionRecordPort.recordRawOutput (metadata-only, no status change, terminal-tolerant). Classification: local-only default; shareable-redacted ONLY if both streams detectedCategories empty AND new RunnerProperties.allowShareableLogs=true (default false; validated-config-needs-test-yaml). KEY: RedactionResult={sanitizedText,sanitizedJson,claimed/effectiveClassification,redacted:bool,detectedCategories:Set} has NO occurrence count -> derive redaction_count via [REDACTED_ placeholder count or extend RedactionResult in application.security (OQ-1,T2); runner.stderr constant/reader is NEW-store has runner.stdout only (T5); AUTHORIZATION_HEADER already redacts Bearer/Basic. AC6 enrich existing RUNNER_COMPLETED/FAILED event details + NEW WorkflowEventDetailKeys(redactionCount/rawOutputByteSize/rawOutputClassification)+ALLOW_LISTED_KEYS (deliveryline-domain module; NO new WorkflowEventType->no schema regen, T6). AC7 WorkflowInspectionService.getRunnerLogReference (mirror getContextBundleForArtifact:810; svc already injects RunnerExecutionRecordPort+RunnerScratchStore) + CLI status --include-runner-logs (adapters/cli/WorkflowCommands.java:200-251 mirror --include-context-bundle; extend cli/workflow-status.v2.schema.json runnerLogs object, additive OQ-8) + REST seam via WorkflowDetailResponse.from (3.27, no work). AC8 NEW ArchUnit field-access rule (raw output only in adapters.runner; redaction only via application.security). AC9 ELK filter=story 3.7 deferred-persist classification+gate-predicate test only (T8). AC10 extend story-1.10 adversarial fixtures (redaction-fixtures/fixtures-manifest.json: Codex auth-header/Claude verbose token/generic Authorization:Bearer) + foundation RedactionAdversarialFoundationContract silent-fixture invariant. 10 traps + 8 OQs (all w/ recommendations). NOT the 3.5 workspace secret-SCAN (complementary,DONE) NOR the ELK pipeline NOR a runner-logs deleter (Epic-5 5.8). Carries 3.5 review defer: unbounded readAllBytes OOM -> OQ-6 size cap. Memory: validated-config-needs-test-yaml, application-cannot-import-adapters, wsl-linux-ci-reproduction, openapi-regen-platform-shim. Story file: _bmad-output/implementation-artifacts/3-6-runner-logs-capture-and-redaction-and-classification.md.)
# last_updated: 2026-06-01 (story 2-16 bmad-dev-story `ready-for-dev -> in-progress -> review`: Run Context Strip Component. FRONTEND-ONLY. NEW `RunContextStrip.tsx` (features/workflows/components) mounted in WorkflowDetailRoute ABOVE the review pane — single labeled `<section aria-label="Run context">` (implicit region role; explicit role=region dropped per jsx-a11y/no-redundant-roles), height-capped by exported RUN_CONTEXT_STRIP_MAX_HEIGHT, Inline single-row anatomy: run id + WorkflowStateBadge + actor + revision pointer + relative last-transition + trigger + branch-placeholder. Driven by EXISTING useWorkflowDetail (route-warmed cache, NO new fetch) through NEW pure mapper `runContextView.ts::toRunContextView(WorkflowDetail,now?)→RunContextView` + pure `resolveRunContextState` (error→loading→stale→partial-context→default, one branch; data-run-context-state attr). DATA RECONCILIATION held to live contract: revision from latestArtifactType+Version via highest-version selection (no latestArtifactId); lastTransitionAt=lastEventAt NOT lastActivityTimestamp (AC7 parity); branchOrCommitReference hardcoded undefined (no E2 field — always a partial-context placeholder, but does NOT flip state so `default` stays reachable); staleIndicator DERIVED client-side from lastActivityTimestamp age vs RUN_STALE_THRESHOLD_MS=10min — SOLE seam (OQ-3, documented AC9 deviation; story-1.13 flag swaps one line). NEW shared `WorkflowStateBadge` + reusable `StateSignifierChip` (currentState→StateName→--state-* tokens+lucide icon+label, unknown→informational, never color-alone AC6) under features/workflows/ NOT components/ui/ (Trap T1). Pure mapping (backendStateToStateName + token-class map + icon resolution) factored into sibling `workflowStateMapping.ts` because react-refresh/only-export-components+allowConstantExport permits only literal-constant exports not functions — 2.15 imports the sibling. NEW relative-time via built-in Intl.RelativeTimeFormat (no dep) + UTC tooltip in `runContextFormat.ts`. loading=Skeleton rows NEVER spinner (T3); error composes 2.22 <ErrorState variant=failedRetrieval nextAction=Retry> (pulls useReturnToRunContext internally → tests vi.mock the module, T6); partial-context=per-field NotReported NEVER <EmptyState> (T4). onNavigateToFullLineage prop accepted but NO control wired (AC5/T9). Task-6 field-only logs runContext.loadError{code,transport}/retry/stale{staleForMs}, NEVER raw message — console-spy pinned + negative key-set test (T7). AC10/AC11 satisfied via fixture-driven render (NEW src/test/fixtures/runContext/specRejectAndResubmit.ts: run_fix_rej_001/Completed/spec v2/DEL-9002) NOT toMatchSnapshot (OQ-5; no 2.27 harness, no 6.9 SPA events). All 6 tasks + 11 ACs; gates GREEN via PowerShell (memory rtk-hook-only-matches-bash): tsc -b 0, eslint --max-warnings=0 0, lint:rules-test 4/4, vitest 33 files/226 tests (+27), prettier applied. Tests pin Date.now (not fake timers) for the stale window. Recommend code-review with a different LLM; no WSL2 smoke needed (no lockfile change). Unblocks 2-15 (reuses WorkflowStateBadge via workflowStateMapping) + 2-17 (shares the run-detail route slot). Epic-2a active-slice next-active rotates 2-16 -> 2-15 -> 2-17. PRIOR 2026-05-31 (story 2-16 created via bmad-create-story `backlog -> ready-for-dev`: Run Context Strip Component. Next-active in Epic-2a frontend slice (2-24→2-14→2-22→2-20 all done → 2-16 → 2-15 → 2-17). FRONTEND-ONLY composite. NEW `src/features/workflows/components/RunContextStrip.tsx` — persistent lightweight orientation strip (run id + state badge + actor + latest-revision pointer + last-transition relative-time) rendered by WorkflowDetailRoute (`routes/workflows/$workflowRunId/index.tsx`) ABOVE the future ARP, replacing the explicit "(2.16) renders here once they land" placeholder. Driven by EXISTING `useWorkflowDetail` (story 2.6) reading the route-warmed cache (NO new fetch) through a NEW pure mapper `runContextView.ts::toRunContextView(WorkflowDetail)→RunContextView`. KEY DATA RECONCILIATION (live `WorkflowDetail` schema is authoritative, epic prose predates it): `latestArtifactId` DOES NOT EXIST (latestArtifacts[] carries only {artifactType,status,version} — render pointer from type+version, never invent an id); last-transition = `lastEventAt` NOT `lastActivityTimestamp` (CLI/RunIdentityRegion parity, AC7); `branchOrCommitReference` has NO backend field in E2 (always undefined → partial-context placeholder, populated in E3); `staleIndicator` has NO dedicated backend flag — DERIVED client-side in the mapper from `lastActivityTimestamp` age vs RUN_STALE_THRESHOLD_MS (single seam; backend flag from story 1.13 AC7 swaps in later — documented AC9 deviation OQ-3). NEW shared `WorkflowStateBadge` (currentState enum→StateName via state-signifiers→`--state-*` tokens+lucide icon+label) born here because story 2.15 Queue Item (ships AFTER) needs "consistent badge styling" — MUST live under features/workflows/ NOT components/ui/ (no-workflow-domain-in-ui-primitives ESLint rule, Trap T1). NEW relative-time util via built-in `Intl.RelativeTimeFormat` (no new dep) + UTC tooltip. 5 states: default/stale(+Stale badge+tooltip)/partial-context(per-field NotReported placeholders, NOT <EmptyState>)/loading(Skeleton rows NEVER spinner)/error(<ErrorState variant=failedRetrieval nextAction=Retry> from 2.22 — pulls useReturnToRunContext internally so tests vi.mock that module, Trap T6). Lightweight: RUN_CONTEXT_STRIP_MAX_HEIGHT cap + Inline primitive single-row (no multi-row panel); jsdom layout caveat → assert constraining class not measured px. NO lineage/provenance control (Phase-3 deferred; optional onNavigateToFullLineage prop wired to nothing, AC5). 6 tasks + frontend-adapted Logging (structured console.info/warn field-only mirroring QueueShell `queue.loadError`/`queue.retry`: runContext.loadError{code,transport}/runContext.retry/runContext.stale{staleForMs}, NEVER raw message/payload, console-spy pinned + negative test). 11 ACs reconciled against live contract. 5 OQs all with recommendations. AC10/AC11 'snapshot/CLI-parity' REINTERPRETED: frontend has no snapshot harness (story 2.27) + no events/status endpoint served to SPA (story 6.9) → satisfied via fixture-driven render assertions over a NEW frontend fixture `src/test/fixtures/runContext/specRejectAndResubmit.ts` mirroring backend spec-rejection-and-resubmit.json terminal state (run_fix_rej_001, Completed, spec v2 post-resubmit, DEL-9002) + documented field-mapping table; full cross-surface CLI parity deferred (OQ-5). KEY FINDINGS via 4 parallel Explore agents: useWorkflowDetail/WorkflowDetail/Inline/Stack/StateName+STATE_SIGNIFIERS/Skeleton/EmptyState+LoadingState+ErrorState/publicId validators/ProblemDetailsError ALL EXIST; RunIdentityRegion (story 2.7 AC11 nav-rail identity) is the AC7 parity reference + documents the lastEventAt-not-lastActivityTimestamp rule + actor-coalescing + NotReported idiom to reuse; NO backend-state→StateName map + NO relative-time util exist anywhere (born here); frontend logging is plain structured `console` calls (no logger module). Memory refs: rtk-hook-only-matches-bash (gates via PowerShell/rtk proxy), prettier-gate-cascades-ci (prettier --write before push), wsl-linux-ci-reproduction. Unblocks 2-15 (reuses WorkflowStateBadge) + 2-17 (ARP shares the run-detail route slot). PRIOR 2026-05-31 (story 3-4 created via bmad-create-story `backlog -> ready-for-dev`: Claude Runner Image — Dockerfile + Entrypoint + Contract Conformance. Next-active in Epic-3a (rotates 3-4 -> 3-6 -> 3-13 -> 3-14 -> 3-9 -> 3a-1 -> 3a-2). The CLAUDE TWIN of the DONE story 3-3 (Codex): fill the runners/claude/ STUBS (Dockerfile `FROM scratch`, entrypoint `#!/bin/sh`, README one-liner) by MIRRORING the live runners/codex/* implementation — copy each file, change only runner-specific bits, keep the shared contract byte-identical. Conforms to (does NOT edit) DockerRunnerAdapter 3.1/3.2: /workspace/input ro + output/logs rw, --network=none, image via imageTagFor(CLAUDE)=deliveryline/claude-runner:latest, result-file-absent=>RUNNER_CRASH. 8 tasks + adapted logging: T1 Dockerfile (node:22-slim, ARG CLAUDE_CLI_VERSION pin of @anthropic-ai/claude-code [confirm name/ver via npm view at build], non-root claude:1001 [1000 taken by node user], INSTALL_CLAUDE_CLI toggle bakes test/mock-claude.sh, ARG IMAGE_VERSION OCI label, single-stage); T2 entrypoint POSIX sh (read context-bundle.v1.json, fail-fast schemaVersion!=1, stage via --stage>DELIVERYLINE_RUNNER_STAGE>bundle.stage, emit runner-result.v1.json, raw logs, exit-code table 10/11/12/13/20/30/40/50, write-schema-valid-failure-result on auth/cmd/CLI-nonzero); T3 NEW lib/runner.mjs (copy from codex; de-Codex content-only: prOutput branch `claude/`, summary text, comments — schema shape identical); T4 --self-test (verify claude --version==pin + mounts) + --simulate-failure gated + Claude CLAUDE_EXEC_ARGS safe-token seam + AC4 Claude config from ENV not constants (CLAUDE_MODEL/CLAUDE_MAX_TOKENS/CLAUDE_PROMPT_TEMPLATE); T5 DUAL-MODE AUTH subscription-first: CLAUDE_CODE_OAUTH_TOKEN (Pro/Max subscription via `claude setup-token` — the cost-saving path Alex flagged) THEN ANTHROPIC_API_KEY (API billing fallback); requires extending story-3.5 config secret-env-names.claude + RunnerProperties.defaultSecretEnvNames() to [CLAUDE_CODE_OAUTH_TOKEN, ANTHROPIC_API_KEY] (config-driven by design; update validated-config test yaml per memory validated-config-needs-test-yaml + .env.example) — broker then injects whichever is set, doctor PASS on either; name+presence logging only; T6 NEW runners/RUNNER_CONTRACT.md shared doc (AC3 — mounts/files/exit codes/stage map/diag flags/shared env + 'schema change => update BOTH runners same PR' rule) + claude/README.md (AC9 + Claude config + rate-limit + setup-token) + SMALL edit codex/README.md to link the shared doc; T7 docker-compose claude-runner build target under `runners` profile alongside codex-runner; T8 ClaudeRunnerImageConformanceIT mirror of CodexRunnerImageConformanceIT (@Tag docker-runner-it + @EnabledIfDockerAvailable, mock CLI via INSTALL_CLAUDE_CLI=false, 3 variants validate runner-result.v1 via RunnerContractValidator + spec materialization + golden shapes + negative secret-log assertion injecting BOTH oauth-token & api-key sentinels). 11 ACs; NEW vs 3.3: AC3 shared RUNNER_CONTRACT.md, AC4 model/maxTokens/prompt-template from env, AC8 kind-swap-by-config conformance. DEFERRED: AC10 'CI builds both images + self-test on schema-touching PRs' extends story 3.28/3.34 (both backlog) — this story ships the obligation-doc + the conformance IT only; AC11 single-test Codex-vs-Claude parity is story 3.8 (real-API tier). Inherited corrections from 3.3 baked into Dev Notes (.v1 filenames, stage seam, claude:1001, single-stage, jq-free runner.mjs, build-failure path, safe-token+count-only exec-args, prompt file under /workspace/logs, docker-CLI build for the IT). Memory refs: wsl-linux-ci-reproduction (build+self-test+IT on WSL2 before push; Docker-tier excluded from no-Docker PR tier), verify-ci-fixes-in-clean-env, validated-config-needs-test-yaml. PRIOR 2026-05-31 (story 3-3 bmad-dev-story `ready-for-dev -> in-progress -> review`: Codex Runner Image — Dockerfile + Entrypoint + Contract Conformance. Filled the runners/codex/ stubs into a real runner image conforming to runner-contracts v1. Dockerfile: node:22-slim, ARG CODEX_CLI_VERSION=0.135.0 (real @openai/codex; INSTALL_CODEX_CLI=false bakes test/mock-codex.sh for the conformance build), ARG IMAGE_VERSION OCI label, non-root codex:1001 (uid/gid 1000 already taken by the base node user), exec-form ENTRYPOINT; SINGLE-stage (multi-stage hint declined — interpreted npm-global CLI shares the node runtime; documented). entrypoint.sh (POSIX/dash): read /workspace/input/context-bundle.v1.json -> fail-fast schemaVersion!=1 -> resolve stage -> read secret from env -> invoke codex (raw stdout/stderr to /workspace/logs) -> emit /workspace/output/runner-result.v1.json -> exit; plus --self-test (doctor/CI, no net), --simulate-failure={timeout|crash|contract_violation} gated behind DELIVERYLINE_RUNNER_ALLOW_SIMULATE_FAILURE=true, and a documented exit-code table (10 missing bundle/11 unparseable/12 bad schemaVersion/13 stage/20 missing key/30 codex nonzero/40 write/50 crash). NEW lib/runner.mjs (jq-free Node ESM: prepare/build/build-invalid). NEW README (contract, stage map, env vars, exit codes, base/CLI upgrade, size/layers, local recipe, 3.8 integration notes). docker-compose.yml: NEW build-only `codex-runner` service under `runners` profile (stays out of default up; `docker compose build codex-runner` builds by name). NEW root .dockerignore. NEW CodexRunnerImageConformanceIT (@Tag docker-runner-it + @EnabledIfDockerAvailable): builds the mock image via the docker CLI (docker-java buildImageCmd context-tar was brittle for an out-of-cwd Dockerfile) then runs all 3 artifact variants against the existing valid context-bundle fixture, validating runner-result.v1.json via RunnerContractValidator + a negative secret-in-logs assertion. KEY CORRECTIONS vs the story prose (live code is authoritative, per Dev Notes 'verify by reading the code'): (1) bundle/result filenames carry the .v1 infix — context-bundle.v1.json / runner-result.v1.json per LocalRunnerWorkspaceStore CONSTANTS; a result under any other name reads back as RUNNER_CRASH (adapter success-signal = result-file-present). (2) stage CANNOT be read from the bundle (context-bundle.v1 schema is additionalProperties:false, no stage field, fixture has none) NOR from the deliveryline.stage Docker label (container metadata, not readable inside) -> entrypoint resolves stage from --stage arg > DELIVERYLINE_RUNNER_STAGE env > optional bundle.stage; maps the three story tokens + RunnerStage enum aliases (investigation->spec, execution->prOutput). Backend wiring of stage into the container interior is deferred to story 3.8 (DockerRunnerAdapter is DO-NOT-EDIT here); the env seam is already honored. Consumed story-3.5 secrets-from-env contract unchanged (CODEX_API_KEY fallback OPENAI_API_KEY, name-only logging). VERIFIED on WSL2 Ubuntu Docker (memory wsl-linux-ci-reproduction; CI cannot substitute — docker-runner-it is default-excluded from the no-Docker PR tier via failsafe excludedGroups, real-API tier is story 3.8): entrypoint branch matrix 22/22, docker e2e 14/14, CodexRunnerImageConformanceIT Tests run 4/0/0/0 (self-test + spec/implementationPlan/prOutput each schema-valid + no secret leak), prod image ~652MB/10 layers self-test reports codex-cli 0.135.0 (==pin, under the ~1GB AC9 budget), spotless+checkstyle clean. All 8 tasks + logging-instrumentation complete; all 11 ACs satisfied (AC3 wording reconciled to the env/arg stage seam — documented deviation). Recommend code-review with a different LLM + a fresh-clone WSL2 build before merge. Epic-3a active-slice next-active rotates 3-3 -> 3-4 -> 3-6 -> 3-13 -> 3-14 -> 3-9 -> 3a-1 -> 3a-2. PRIOR 2026-05-31 (story 2-20 bmad-code-review `review -> done`: Queue Shell States. 3 adversarial layers (Blind Hunter + Edge Case Hunter + Acceptance Auditor) over the working-tree change (1 modified routes/workflows/index.tsx + 9 new files) -> 0 critical, 0 surviving high, 4 patch + 4 defer + dismissals. The Acceptance Auditor's one real defect: populated <ul> shipped WITHOUT role=list (AC8 literally asks for it; Tailwind v4 preflight resets ul{list-style:none} so WebKit/VoiceOver drops the implicit list role) -> FIXED with role=list + a scoped jsx-a11y/no-redundant-roles disable carrying the Safari rationale. The Blind Hunter's lone HIGH ("Retry button looks dead — state stays error during refetch") was VERIFIED A FALSE POSITIVE: in this TanStack v5 setup refetch() flips an errored query back to isPending, so the shell shows the loading skeleton during retry (an aria-busy/Retrying patch was tried then reverted as dead code; the real behavior is now pinned by a gated error->loading->error test). 3 other net-new tests: enabled docs-CTA href (VITE_DOCS_URL via vi.stubEnv), unsafe-scheme disabled fallback, queue.loadError forbidden-field contract (keys exactly code/event/filtersActive/state, never message), + a loading->populated announcer-transition assertion. 4 deferred -> deferred-work.md (error-precedence blanks loaded rows on a refetch-fail [revisit at 2.15+filter control], OQ-1 clear-filters route-mount integration test [2.15], cold-load skeleton SR-silence [2.25 axe sweep], filtersActive defined twice with divergent predicates [consolidate when filter control ships]). Post-fix gates GREEN via rtk proxy (RTK Bash-output corruption -> route around it): tsc -b 0, eslint --max-warnings=0 0, vitest 30 files/199 tests, prettier clean. Epic-2a active-slice next-active rotates 2-20 -> 2-16 -> 2-15 -> 2-17. PRIOR same-day 2026-05-31 (story 2-20 bmad-dev-story `ready-for-dev -> review`): Queue Shell States — loading/empty/filtered-empty/error + populated. NEW src/features/workflows/QueueShell.tsx owning the four non-row states via pure resolveQueueState (exactly one state; data-queue-state attr), driven by NEW hooks/useWorkflowsList over story-2.6 listQueryOptions (keys via workflowKeys.list) + URL filters (route validateSearch {state?}, Clear-filters = navigate empty search). Composes story-2.22 <EmptyState variant=queue|filtered>/<ErrorState variant=failedRetrieval nextAction=Retry>; NEW src/components/ui/skeleton.tsx (animate-pulse, bg-surface-elevated — NOT spinner); NEW queueErrorMessage maps ProblemDetails code/transport to fixed copy (never error.message/status). Row rendering decoupled via renderItem render-prop seam + QueuePlaceholderRow default + shared QUEUE_ROW_MIN_HEIGHT ([SEAM story 2.15]); RunReviewQueueItem NOT imported. Single visually-hidden role=status aria-live=polite announcer (error wording non-verbatim vs ErrorState — Trap T10). Task-8 logging: queue.loadError(code|transport,filtersActive)/queue.retry/queue.clearFilters, field-only, pinned by console spy. All 8 tasks + all 10 ACs; all 10 traps honored. Deviations: populated <ul> ships WITHOUT explicit role=list (AC8 asked for it but jsx-a11y/no-redundant-roles + --max-warnings=0 forbid it; implicit role identical); row-metrics colocated in queueState.ts (not a separate queueRowMetrics.ts); OQ-1 full memory-router mount test deferred to story 2.15 (state tests router-free + route glue type-checked). Gates GREEN via PowerShell (RTK hook only matches Bash — memory rtk-hook-only-matches-bash): tsc -b 0, eslint --max-warnings=0 0, lint:rules-test 4 suites, vitest 30 files/194 tests, prettier applied. MODIFIED routes/workflows/index.tsx (body -> <QueueShell>, validateSearch + loaderDeps, removed SAMPLE_RUN_IDS). Epic-2a active-slice next-active rotates 2-20 -> 2-16 -> 2-15 -> 2-17. PRIOR 2026-05-30 (story 3-3 created via bmad-create-story `backlog -> ready-for-dev`: Codex Runner Image — Dockerfile + Entrypoint + Contract Conformance. Fills the runners/codex/ STUBS (Dockerfile `FROM scratch`, entrypoint `#!/bin/sh`, README one-liner) — NOT create-from-scratch. Runner-infra story: Dockerfile + POSIX entrypoint + ONE Java conformance test; no backend business logic. Conforms to (does NOT edit) story-3.1/3.2 DockerRunnerAdapter (DONE): /workspace/input ro + output/logs rw, --network=none, image via imageTagFor(kind), result-file-absent => RUNNER_CRASH (success signal = result file present, not exit code). Consumes story-3.5 secrets contract (DONE): CODEX_API_KEY fallback OPENAI_API_KEY injected as container ENV by RunnerSecretsService->CreateContainerSpec.environment — entrypoint READS env, never mounts/re-implements/echoes. 8 tasks + adapted logging: T1 Dockerfile (rec node:22-slim, ARG-pinned Codex CLI, non-root codex:1000, ARG IMAGE_VERSION); T2 entrypoint (read /workspace/input/context-bundle.json, fail-fast schemaVersion!=1, stage->artifactType spec-investigation=spec/implementation-plan=implementationPlan/pr-output=prOutput, emit runner-result.v1 to /workspace/output/runner-result.json, raw stdout/stderr to /workspace/logs, never write outside output/logs); T3 --self-test (doctor 1.16 + CI, no net); T4 --simulate-failure={timeout|crash|contract_violation} gated behind test-only env (crash = exit nonzero with NO result file); T5 secrets-from-env; T6 README (base rationale, CLI pin+upgrade, env vars, mount paths, exit-code table, image size/layers, local test recipe); T7 docker-compose codex-runner build target (build-only, not in default up, single-file convention); T8 contract test via RunnerContractValidator over existing valid fixtures (context-bundle.v1.valid + runner-result spec/implementation-plan/pr-output) with a MOCKED Codex CLI (real API = story 3.8), Docker-tier-gated like docker-runner-it + negative log assertion (injected secret value absent from runner.stdout/stderr). Open decision: confirm Codex CLI base image + install/package name before pinning (epic hint node:22-slim). KEY FINDINGS via filesystem+code read: runners/codex/* + runners/claude/* are 1-line stubs awaiting 3.3/3.4; docker-compose.yml has ONLY postgres today; runner-result.v1 + context-bundle.v1 schemas + 4 valid fixtures live in deliveryline-runner-contracts; DockerRunnerAdapter already mounts/network/secret-injects/classifies. Memory refs: wsl-linux-ci-reproduction (docker build + --self-test on WSL2 Linux before push; ensure Docker-tier test excluded from no-Docker PR tier), verify-ci-fixes-in-clean-env. Epic-3a next-active: 3-3 -> 3-4 -> 3-6 -> 3-13 -> 3-14 -> 3-9 -> 3a-1 -> 3a-2. NOTE: a 3-3-runner-secrets-management.md file was mistakenly drafted earlier (under a transient tool-output glitch that returned stale data) and has been deleted — story 3.3 is the Codex image; secrets are story 3.5 which is already DONE.)
# last_updated: 2026-05-30 (story 3-5 bmad-code-review `review -> done`: Runner Secrets Handling. 3 adversarial layers (Blind Hunter + Edge Case Hunter + Acceptance Auditor) over the working-tree diff (40 files, +946/-30 + 737 new) -> 1 decision + 2 patch + 11 defer + 9 dismissed, ZERO critical. Decision (post-execution scan re-resolves the injected key via `defaultKind()` not the dispatched kind) resolved option-1 = accept-as-latent: VERIFIED the broker's own dispatch ALSO hardwires defaultKind() at RunnerBroker.java:288 so dispatch+scan coincide today (no active leak); RunnerExecutionSnapshot carries no runnerKind (runner_kind column deferred to 3-19/4-9 per 3.1 OQ-7) -> added a coupling comment at :515 flagging the real fix for when per-run kind selection lands (3.3/3.4). 3 patches FIXED+verified: (1) leak branch in handleSuccess writes the `.quarantine` marker FIRST best-effort + guards recordFailed against ILLEGAL_TRANSITION re-entry mirroring the recordCompleted gate (edge-hunter HIGH: duplicate onResult on a leak-FAILED row re-ran the scan and threw uncaught, old order left the workspace deletable if the marker write failed); (2) RunnerBrokerUnitTest.onResultSecretLeak gains verify(workflowTransitionService, never()).transition(...) pinning the 'a leak does NOT drive the run-state' invariant; (3) the coupling comment. Post-fix focused GREEN: RunnerBrokerUnitTest 30/0/0, RunnerSecretScanServiceTest 8/0/0, RunnerSecretsServiceTest 8/0/0, DockerRunnerAdapterUnitTest 24/0/0, RunnerWorkspaceCleanupJobDanglingUnitTest 5/0/0; spotless+checkstyle clean. 11 defers -> deferred-work.md. GATE-TO-DONE STILL PENDING (not runnable in fast tier): real-boot OpenApiSnapshotContractTest (contract tier, Testcontainers) for the hand-edited openapi.json + schema.d.ts runner_secret_leak enum, + WSL2 Linux parity per memory wsl-linux-ci-reproduction. Epic-3a next-active: 3-3 -> 3-4 -> 3-6 -> 3-13 -> 3-14 -> 3-9 -> 3a-1 -> 3a-2.)
# last_updated: 2026-05-30 (story 2-20 created via bmad-create-story -> `backlog -> ready-for-dev`: Queue Shell States — Loading, Empty, Filtered-Empty, Error. Epic-2a active-slice next-active (order: 2-24 done → 2-14 done → 2-22 done → 2-20 → 2-16 → 2-15 → 2-17). Frontend-only composite story. NEW `src/features/workflows/QueueShell.tsx` owning the FOUR non-row queue states (loading/empty/filtered-empty/error) for the `/workflows` review queue, wired into existing `routes/workflows/index.tsx` (replaces the SAMPLE_RUN_IDS placeholder + adds typed `validateSearch {state?}`). Driven by NEW thin `useWorkflowsList(filters)` hook over story-2.6 `listQueryOptions` (routes through `workflowKeys.list` — satisfies `no-inline-query-keys`) + route-search filter state + result count via pure `resolveQueueState({query,filtersActive})`. COMPOSES story-2.22 (done) primitives `<EmptyState variant=queue|filtered>` / `<ErrorState variant=failedRetrieval nextAction=Retry>` from `@/components/feedback` — does NOT rebuild them. KEY DESIGN CONSTRAINT: `RunReviewQueueItem` (story 2.15) does NOT exist yet and ships AFTER 2-20 in the slice — row rendering is DECOUPLED via a `renderItem?` render-prop seam + minimal `QueuePlaceholderRow` default + shared `QUEUE_ROW_MIN_HEIGHT` constant (2.15 imports the same, `[SEAM story 2.15]` both ends). NEW `src/components/ui/skeleton.tsx` (animate-pulse, NOT animate-spin — AC2: loading=skeleton rows, NEVER spinner-on-blank-page; `<LoadingState>` deliberately UNUSED here). NEW `queueErrorMessage(error)` maps ProblemDetails `code` (never `error.message`/status/stack) + transport failures to fixed strings. Filters live in URL (route `validateSearch`), NOT React state — Clear-filters = navigate empty search; filter UI CONTROL is out of scope (AC9 — shell only READS `Route.useSearch()`). Shell owns a single visually-hidden `role=status aria-live=polite` transition announcer. 8 tasks + frontend-adapted Logging instrumentation. TEN traps (T1 no RunReviewQueueItem import — renderItem seam; T2 skeleton not spinner; T3 error text from code not message; T4 `<ErrorState>` calls useReturnToRunContext unconditionally → tests need `<NavigationBreadcrumbProvider>`; T5 query keys via factory; T6 filtered-empty≠empty distinct variant/copy/action; T7 filters in URL not state; T8 one state at a time via resolveQueueState; T9 skeleton token must exist in globals.css — bg-surface-elevated not bg-muted; T10 don't double-announce shell+ErrorState both polite). 5 OQs with recommendations (OQ-1 props-injectable QueueShell for router-free unit tests + 1 route integration test; OQ-2 empty CTA href via VITE_DOCS_URL + validateUrlScheme, disabled placeholder when unset; OQ-3 keep shell+ErrorState polite announcements concise/non-redundant, full reconcile is 2.25; OQ-4 ErrorState urgency=passive this story, active-on-retry deferred to 2.25; OQ-5 QUEUE_SKELETON_ROW_COUNT=4). KEY FINDINGS via Explore survey: `listQueryOptions(filters)` + `WorkflowSummary` + `workflowKeys.list` + `isProblemDetailsError`/`DomainErrorCode` all exist (2.6/1.8); `/workflows` loader already warms the list cache (flash-free); NO `useWorkflowsList`/`RunReviewQueueItem`/`Skeleton`/route-search-params exist yet; 2.22 `<ErrorState>` requires `nextAction` + consumes `useReturnToRunContext`; tokens `bg-state-loading`/`bg-state-empty`/`bg-surface-elevated` confirmed in `src/styles/globals.css`; tests are vitest+RTL+MSW (`src/test/server.ts`, `setup.ts` onUnhandledRequest:error), copy `EmptyState.test.tsx` + `useWorkflowDetail.test.tsx` patterns. Memory references: `vitest-cross-file-router-mock` (consolidate same-module router mocks into one file), `prettier-gate-cascades-ci` (run prettier --write before push), `frontend-ts6-legacy-peer-deps`. Unblocks downstream slice: 2-16 (Run Context Strip), 2-15 (RunReviewQueueItem fills the renderItem seam), 2-17 (ARP). PRIOR 2026-05-30: story 2-22 bmad-code-review Round 2 closed `review -> done`: fresh pass over the post-patch working tree (52 files, +2884/-129) across 3 adversarial layers — 2 decisions resolved (D1 ErrorState `role=alert`+`aria-live=polite`/permissionRestricted ARIA → DEFERRED to story 2.25's WCAG/axe sweep; D2 root `errorComponent` not wired to `signalCatastrophic` per AC8.b → ACCEPTED current inline `GenericErrorState` split, AC8.b wording to reconcile), 1 `[Patch]` fixed (`<ErrorState>` active-urgency focus effect re-fired on every render because `message` ReactNode was in the dep array — now depends on a derived `messageLength` primitive; keeps the length log accurate, stops the focus-steal), 14 deferred → deferred-work.md, 6 dismissed. ZERO Critical. The lone HIGH (catastrophic deterministic-crash Dismiss→re-crash loop, reload-only escape) deferred as accepted MVP posture — Round-1 reset wiring already fixed the blank-app; residual loop is acknowledged in code. Post-fix gates GREEN: vitest 30/30 (feedback/states + routes/-states), tsc -b clean, eslint --max-warnings=0 clean. Epic-2a active-slice next-active stays 2-20 queue-shell-states. PRIOR 2026-05-30: story 3-5 created via bmad-create-story -> `backlog -> ready-for-dev`: Runner Secrets Handling — Secure Mount of Agent-Provider API Keys. Next-active in Epic-3a backend track (3-1, 3-2 done; 3-5 → 3-3 → 3-4 → 3-6 → 3-13 → 3-14 → 3-9). Secrets-handling slice fulfilling NFR8/NFR9/NFR14. SCOPE: (a) NEW `application.runner.RunnerSecretsService` — `resolveSecretsForRunner(RunnerKind, RunnerStage, workflowRunId) → Map<String,String>` reads provider-key env vars from Spring `Environment` (system env + `.env`), never persisted/cached, throws `DomainException(DOCTOR_RUNNER_SECRET_MISSING)` on absent var at DISPATCH time; (b) NEW `Map<String,String> environment` field on `CreateContainerSpec` + `DefaultDockerEngineGateway.createContainer` applies docker-java `cmd.withEnv(...)` (Trap-T8 boundary held) — DockerRunnerAdapter resolves secrets right before spec build, mock path untouched; (c) post-execution workspace secret scan in `RunnerBroker.onResult` over input/output/logs via story-1.10 `RedactionPolicyService.redact` → on hit record NEW `FailureCategory.RUNNER_SECRET_LEAK` + RUNNER_FAILED event (details.leakedFile + category names only) + `.quarantine` marker so story-3.2 cleanup job preserves the workspace; (d) NEW ADR `docs/adr/0003-runner-secrets-mvp-posture.md` (epic's literal `0002` collides with existing 0002-idempotency — 0003 is free); (e) `.env.example` provider-key placeholders (CODEX_API_KEY/ANTHROPIC_API_KEY) + `spring.config.import: optional:file:./.env[.properties]` added to application.yml (currently MISSING — Spring does not auto-load .env); (f) doctor `CHECK_RUNNER_SECRETS` per-kind presence probe → PASS/FAIL `DOCTOR_RUNNER_SECRET_MISSING` (presence-only, never values) + NEW `DomainErrorCode.DOCTOR_RUNNER_SECRET_MISSING`; (g) NEW ArchUnit `RUNNER_SECRETS_SERVICE_SCOPE` rule confines callers to application.runner + adapters.runner.docker; (h) config-driven `RunnerKind → env-var names` map (this story is SoT for what 3.3/3.4 runner images consume). 9 tasks + Logging-instrumentation. TEN traps (T1 provider-key-only into containers — GITHUB_TOKEN/LINEAR_API_KEY stay host-side per 3.14/3.9 AC10, never mounted; T2 env-var names config-driven SoT; T3 NEVER add secrets to RunnerDispatchRequest; T4 mock adapter never resolves; T5 gateway logs env-var COUNT only; T6 no secret caching — read Environment per call for AC9 rotation; T7 detection stays in application.security — store reads bytes only, broker may exact-substring-compare the known injected value; T8 docker-java behind gateway; T9 `docker inspect` env visibility is the accepted MVP posture, LABELS are tested for no-secret; T10 quarantine preserves never deletes). 6 OQs with recommendations (OQ-1 env names config-driven default CODEX_API_KEY/ANTHROPIC_API_KEY finalize vs 3.3/3.4; OQ-2 resolve at adapter not broker; OQ-3 add FailureCategory.RUNNER_SECRET_LEAK + RegistryContract/OpenAPI regen if enum in schema; OQ-4 .env import + process-env both; OQ-5 `.quarantine` marker file; OQ-6 UTF-8-decode-then-skip-binary in scan). KEY FINDINGS via 2 parallel Explore agents: `CreateContainerSpec` currently has NO env field (zero env vars injected today) — net-new injection seam; NO `RunnerSecretsService` exists; story-1.10 `RedactionPolicyService.redact(String,classification)→RedactionResult{detectedCategories,redacted,sanitizedText}` + `SensitivePayloadAnalyzer` already ship GITHUB_TOKEN/LINEAR_API_KEY/ENV_VALUE/PEM/SSH patterns (reuse — ArchUnit `CREDENTIAL_DETECTION_MUST_STAY_IN_APPLICATION_SECURITY` forbids reimplementing); doctor `probeDockerAvailability` (DoctorProbeAdapter.java:421) is the per-kind probe template; NO spring.config.import for .env anywhere; ADR 0002 already taken. Memory refs: `validated-config-needs-test-yaml` (new validated RunnerProperties field needs test application.yml), `openapi-regen-platform-shim` (FailureCategory schema-enum regen if applicable), `wsl-linux-ci-reproduction`, `application-cannot-import-adapters`. PRIOR 2026-05-30 (story 3-2a bmad-code-review close-out pass: `review → done`. 3 adversarial layers (Blind Hunter + Edge Case Hunter + Acceptance Auditor) on the 18eae3f..HEAD diff (38 files, +2934/-94) → 27 raw findings → ~20 distinct → 0 decision-needed, 0 patch (ZERO critical/automatic must-fix), 9 deferred → deferred-work.md, 11 dismissed. Per Alex's directive (process only critical/automatic; defer/move rest; close) no inline patches applied. The two HIGH-rated items are deliberate already-decided behaviors recorded for visibility: (1) null-`createdAt` ages-out in the dangling sweep — re-opens the closed 2026-05-29 decision (mitigated: DB row inserted at dispatch, rowless+null window is narrow); (2) `unknown` engine-state folded into the running set — intentional + codified by `unknownStatusRowlessContainerPastMinAgeUsesStopThenForceRemove`, force-rm of an exited container is a no-op. Remaining MED defers (clock-domain skew on min-age guard, redactImageTag exotic-ref hardening, stage-query tie-break, touchActivity monotonic guard, AC9 completed-path re-entry test, two test-strength nits, one test-hygiene nit) all non-blocking. Story 3.2 stays done. Epic-3a next-active now: 3-5 → 3-3 → 3-4 → 3-6 → 3-13 → 3-14 → 3-9 → 3a-1 → 3a-2. PRIOR 2026-05-30 (story 3-2a bmad-dev-story review-continuation close-out: `in-progress → review`. The 2026-05-29 code-review's one open action item (AC6 Trap T1 negative test) was committed in 5ad3e17 + all findings ticked, but the forward status flip had never been performed — this run verifies + flips it. Confirmed AC6 test `scanForTimeoutsHeartbeatExtendedDeadlineDoesNotInvokeDockerTerminate` exists (RunnerBrokerUnitTest.java:696) + no uncommitted tracked source. Re-verified fast tier 59/0/0 (BrokerUnit 29, StaleDetection 5, CleanupDangling 4, RunnerProperties 7, RunnerLoggingContract 9, DockerRunnerLifecycleLoggingContract 5) AND the docker-runner-it tier on Windows Docker Desktop on 5ad3e17 → 8/0/0/0 (Timeout 1 STOPPED_GRACEFULLY→timed_out, Heartbeat 2, Recovery 2, Dangling 1, ContainerLifecycle 2; covers the changed IT bases awaitNotRunning fail-fast + teardown WARN that the 820a5af WSL2 run predated). All 15 ACs + all patch/defer findings resolved. Belt-and-suspenders gate-to-done left for reviewer: a fresh WSL2 Linux parity run on 5ad3e17 (prior WSL2 8/0/0/0 was on 820a5af; production stop/timeout path unchanged since). Story 3.2 stays done. Epic-3a next-active after 3-2a review closes: 3-5 → 3-3 → 3-4 → 3-6 → 3-13 → 3-14 → 3-9 → 3a-1 → 3a-2. PRIOR 2026-05-29 (story 3-2a bmad-code-review: `review → in-progress`. 3 adversarial layers → 4 decisions resolved + 9 patch findings (8 fixed incl. the HIGH timeout-path `recordTimedOut` ILLEGAL_TRANSITION race-gate, null-`createdAt` dangling age-out, redaction unit test, dangling min-age upper bound, AC10(h) orphan-flip behavior assertion, `awaitNotRunning` fail-fast, TestcontainersConfiguration File-List + teardown WARN-hardening; 1 left as action item: AC6 Trap T1 negative test) + 10 deferred → deferred-work.md + 4 dismissed.))
# last_updated: 2026-05-29 (story 3-2a flipped `in-progress → review` via bmad-dev-story close-out: fresh WSL2 Ubuntu-24.04 Linux parity trust gate GREEN on the rewritten broker-driven docker-runner-it ITs — 8/0/0/0 BUILD SUCCESS on a clean git clone of pushed 820a5af; Trap T15 satisfied; all 15 ACs + 11 review findings + Logging-instrumentation task complete; story 3.2 stays done. CI cannot substitute — pom default-excludes docker-runner-it, real-docker CI tier is story 3-8. Epic-3a next-active after 3-2a review closes: 3-5 → 3-3 → 3-4 → 3-6 → 3-13 → 3-14 → 3-9 → 3a-1 → 3a-2. PRIOR 2026-05-28: story 2-22 created via bmad-create-story -> `backlog -> ready-for-dev`: Navigation + Empty/Loading/Error States Infrastructure. Next-active in Epic 2a active slice (sprint-status active-slice order: 2-24 done → 2-14 done → 2-22 → 2-20 → 2-16 → 2-15 → 2-17). Frontend-only infrastructure story. NEW directories `deliveryline-frontend/src/lib/navigation/` (3 typed nav hooks + breadcrumb stack + run-context-preservation boundary + catastrophic-error overlay) + `deliveryline-frontend/src/components/feedback/states/` (3 primitives: EmptyState/LoadingState/ErrorState — architecture.md:1182 sanctioned location). Eleven ACs spanning: (a) typed nav helpers `useReturnToRunContext()` + `useNavigateToArtifact(runId, artifactId)` + `useNavigateToClarification(runId, clarificationId)` — all wrap TanStack Router's typed navigate, synchronous id validation via `isValidRunId`/`isValidArtifactId`/NEW `isValidClarificationId` extension to existing `src/lib/routing/publicId.{js,d.ts}` (mirrors `rex_`/`run_`/`art_` patterns); (b) NEW `useAssertRunContextLoaded(runId)` discriminated-union guard composing `useWorkflowDetail` (story 2-6) + `useAllowedActions` (story 2-14) returning `loading|loaded|runNotFound|error` — Trap T7 audit-only role labeling preserved (NOT a permission gate — backend remains authority); (c) per-session breadcrumb stack with kind enum `queue|runDetail|artifact|clarification|compareMode|recoveryDeepDive`, useReducer-backed, dedup on kind+ids equality, FIFO-capped at MAX_BREADCRUMB_STACK_DEPTH=16, provider mounted in App.tsx OUTSIDE the router (Trap T3) with auto-tracker `<NavigationBreadcrumbTracker />` mounted INSIDE __root.tsx, session-scoped (no localStorage — stale state would conflict with artifact-version-binding); (d) `<EmptyState variant="queue|filtered|artifactNotGenerated|noOpenQuestions|noMeaningfulDiff">` (5 variants per UX-DR17), `<LoadingState variant="fetchingData|generatingArtifact|rebuildingAfterRejection|retryingRecovery">` (4 variants), `<ErrorState variant="failedRetrieval|unavailableDiffBaseline|permissionRestricted|blockedByStaleState" nextAction={...}>` (4 variants × 5 NextAction kinds Retry/Refresh/NavigateBack/ContactSupport/DocsLink — required, no path through without action — UX-DR17 "every error state should provide the next safe action"); TS exhaustiveness check via `assertNeverEmptyVariant(variant: never)`; (e) NEW ESLint rule `local-rules/no-untyped-loading-state` (4th project rule alongside no-inline-query-keys/no-workflow-domain-in-ui-primitives/no-unsanitized-html) — flags `<LoadingState />` without variant + flags `<*Spinner*>`/`animate-spin` outside trusted boundary `src/components/feedback/states/**` + `src/components/ui/**` (Trap T16 self-exempt pattern mirrors story 2-24's `no-unsanitized-html`); registered in tools/eslint-rules/index.js + wired in eslint.config.js at `error` for src/**/*.{ts,tsx} + RuleTester test via Node `--test`; (f) `<RunContextBoundary>` HOC + `useRunContextPreservation()` capturing `{runId,artifactId?,clarificationId?,scrollY,mainPaneScrollTop}` (the `<main id="main-content">` from story 2.7 `MAIN_CONTENT_ID`) on mount via useLayoutEffect, restoring on unmount via useLayoutEffect cleanup (Trap T12 — useEffect would paint at scroll=0 before restoring, causing flash); UX-DR16 compare/clarification continuity per "Compare mode should open as a deeper inspection state and return the user to the same run context"; (g) `<CatastrophicErrorOverlay>` mounted in App.tsx OUTSIDE AppShell (Trap T11 — shell crash still surfaces overlay) via NEW `react-error-boundary` ~4.1.x dep + `createPortal(..., document.body)` + role="alertdialog" + Radix focus trap from existing shadcn `<Dialog>` primitive; (h) closes the open `[SEAM story 2.22]` line in `routes/-states/DeadEndState.tsx:32-33` by migrating all 8 dead-end states (InvalidLinkState/RunNotFoundState/ArtifactNotFoundState/PageNotFoundState/GenericErrorState/UnrecognizedRunStateState/UnrenderableArtifactState/PermissionRestrictedState) to consume new EmptyState/ErrorState primitives — Trap T15 every copy string preserved verbatim; (i) accessibility per AC9 — `<LoadingState>` role="status"+aria-live="polite", `<ErrorState>` urgency='active'|'passive' controls aria-live='assertive'|'polite' (Trap T13), `<EmptyState>` no live region (benign per UX 1874), active errors focus-move to action button, axe-core no-violations per variant. SIXTEEN declared traps (T1 useCallback referential stability; T2 never window.history; T3 provider in App.tsx tracker in __root.tsx; T4 fallback to /workflows never window.history.back; T5 dedup on kind+ids not path; T6 untrusted runner output → sanitization route; T7 permissionRestricted audit-only display NOT security gate; T8 useAllowedActions ≠ ErrorState.nextAction — distinct domains; T9 NavigateBack consumes useReturnToRunContext internally — consumer passes no callback; T10 ContactSupport falls back to disabled placeholder if VITE_SUPPORT_URL unset; T11 catastrophic overlay portals to document.body OUTSIDE shell; T12 useLayoutEffect cleanup not useEffect for scroll restore; T13 assertive interrupts SR speech — reserved for active errors; T14 RunContextBoundary + NavigationBreadcrumb orthogonal — scroll vs pages; T15 DeadEndState migration preserves every copy string; T16 no-untyped-loading-state self-exempts inside feedback/states + ui boundaries). 8 open Qs all with recommendations (OQ-1 provider in App.tsx outside router; OQ-2 useReducer over useState; OQ-3 add react-error-boundary dep; OQ-4 action NOT required on EmptyState; OQ-5 urgency defaults to 'passive'; OQ-6 ?clarificationId search param NOT hash anchor; OQ-7 cap at 16; OQ-8 catastrophic overlay dismiss does NOT reset breadcrumb stack). 10 tasks + cross-cutting frontend-adapted Logging instrumentation. KEY FINDINGS via survey: `AppShell` (story 2.7) already exists with `MAIN_CONTENT_ID = 'main-content'` constant + reserved catastrophic-overlay seam at LAYOUT.md — this story closes that seam by mounting overlay in App.tsx with documentation update; existing `useWorkflowDetail`/`useAllowedActions`/`isProblemDetailsError` cover the data-loading guard surface; story 2-31 ESLint local-rules plugin (`tools/eslint-rules/index.js` + `tools/eslint-rules/__tests__/*.test.js` via `node --test` + `lint:rules-test` npm script) is the template for new rule; story 2-24 `no-unsanitized-html.js` rule provides the self-exempt-via-context.physicalFilename template for `no-untyped-loading-state`; shadcn `<Alert>` (existing) is the `<ErrorState>` semantic foundation; shadcn `<Dialog>` (existing, Radix-backed) is the `<CatastrophicErrorOverlay>` focus-trap foundation; existing `src/lib/sanitization/` (story 2-24) is the Trap T6 untrusted-runner-output route; existing 8 dead-end states + their copy strings are the migration baseline. Unblocks per active-slice order: 2-20 (queue shell states consume `<EmptyState variant="queue|filtered">`), 2-16 (Run Context Strip consumes `useReturnToRunContext` + `useAssertRunContextLoaded` + LoadingState/ErrorState), 2-15 (Queue Item embeds primitives), 2-17 (ARP consumes all three + useNavigateToArtifact). Epic-2b downstream: 2-18 (clarification consumes `useNavigateToClarification` + `<EmptyState variant="noOpenQuestions">`), 2-19 (Decision Bar consumes `<ErrorState variant="blockedByStaleState">`), 2-21 (later wires toast/inline/persistent dispatcher these nextAction payloads can target). Epic-3a/b downstream: 3-26/3-27/3-29/3-30. Epic-4: 4-4 consumes breadcrumb stack (recoveryDeepDive kind exists for this), 4-19/4-20 consume `withRunContext` HOC. Memory references: `frontend-lockfile-cross-platform.md` (`react-error-boundary` dep add requires clean lockfile regen on Linux/WSL2 before push), `frontend-ts6-legacy-peer-deps.md` (`.npmrc legacy-peer-deps=true` already present, continues to apply), `wsl-linux-ci-reproduction.md` (frontend test slice smoke-verify on WSL2 Ubuntu before push). Prior 2026-05-28 entry: story 3-2 created via bmad-create-story -> `backlog -> ready-for-dev`: DockerRunnerAdapter Lifecycle — Timeout, Heartbeat, Lease Expiry, Cleanup, Idempotent Restart. Second story in Epic 3a active slice (epic-3a already `in-progress` since 3-1). Extends story 3-1's DockerRunnerAdapter + DockerEngineGateway + RunnerWorkspaceStore + RunnerProperties.Docker (workspaceRetentionHours declared in 3-1 now wired here). Scope: (a) extend RunnerBroker.processSingleTimeout AFTER the existing heartbeat-race guard with `DockerEngineGateway.stopContainer(10s) → killContainer` two-step + emit dedicated `runner.timeout` event (replaces the generic `RUNNER_FAILED + details.failureCategory=runner_timeout`); (b) heartbeat detection in DockerRunnerAdapter.poll via THREE priority-ordered sources — container `State.StartedAt` (NEW field on ContainerState), `output/heartbeat.touch` optional marker file (NEW RunnerWorkspaceStore.tryReadHeartbeatTouch), `logs/runner.stdout` byte-count growth (NEW RunnerWorkspaceStore.observeLogGrowth + in-memory ConcurrentMap<rex, LogGrowthObservation>) — broker's existing HeartbeatTouched → touchActivity branch (RunnerBroker:868) does the activity-window advance unchanged; (c) NEW `RunnerBroker.scanForStaleExecutions()` scheduled @ 60s default running TWO sub-passes — heartbeat-stale WARN (idempotent via NEW V10 `heartbeat_stale_emitted_at` column cleared on activity) + orphan flip past `2 × stage_timeout` (re-uses existing `staleThresholdFor(stage)` algebra); (d) idempotent broker restart via NEW `DockerEngineGateway.findContainerIdByRunnerExecutionId` (uses `docker ps -a --filter label=deliveryline.runnerExecutionId={rex}`) + NEW `DockerRunnerAdapter.recoverHandle(rex)` → re-runs full poll classification; existing `processOrphan` scratch-replay path runs FIRST (Trap T6), docker probe SECOND, orphan flip LAST; mock-runner path unaffected (Trap T5); (e) NEW `RunnerWorkspaceCleanupJob` @Profile("runners.docker") + NEW DockerRunnerLifecycleConfiguration scheduled hourly with THREE sweeps in order (workspace deletion of rows with completed_at < now() - workspaceRetentionHours via NEW RunnerWorkspaceStore.deleteWorkspace + NEW RunnerExecutionRecordPort.markArchived re-using existing `archived_at` column per OQ-2; orphan-dir preservation WARN-only per Trap T7; dangling-container stop+rm via NEW DockerEngineGateway.listContainersByLabel); (f) NEW label `deliveryline.stage` added to story 3-1's 4-label set; (g) FIVE NEW WorkflowEventType values registered + schema-enum extended (`RUNNER_DISPATCHED` replaces RUNNER_STARTED on docker path only; RUNNER_HEARTBEAT_STALE; RUNNER_TIMEOUT; RUNNER_ORPHANED; RUNNER_COMPLETED) + RegistryContractTest + workflow-events-response.schema.json + workflow-history.v1.schema.json + OpenAPI regen via cross-shell scripts per memory `openapi-regen-platform-shim.md`; (h) NEW RecoverableRunnerAdapter sub-interface per OQ-6 (MockRunnerAdapter implements as no-op). 11 tasks + Logging instrumentation cross-cutting. SIXTEEN declared traps (T1 heartbeat-race guard stays BEFORE docker stop in processSingleTimeout; T2 heartbeat chain does not change exited classification; T3 OffsetDateTime UTC at ms precision not raw FileTime; T4 heartbeat-stale event at-most-once per stale-window via heartbeat_stale_emitted_at column; T5 docker recovery probe gated on `instanceof DockerRunnerAdapter`; T6 scratch-replay FIRST docker probe SECOND in processOrphan; T7 NEVER delete workspace lacking DB row — WARN+preserve; T8 label `deliveryline.runnerExecutionId` is the only correlation surface; T9 workspace pass BEFORE dangling-container pass; T10 RUNNER_STARTED legacy NOT removed only stops emitting on docker path; T11 schema-enum + OpenAPI regen in same PR; T12 RecoveryService.retry yields fresh rex_* id; T13 RetryRecoveryResult event id carries new runner.dispatched; T14 docker-tagged IT tier opt-in not in foundation gate; T15 WSL2 Ubuntu native verify before push for docker stop/kill signal parity; T16 cleanup query SQL guard `status IN (completed, failed, timed_out, orphaned)`). 7 open Qs with recommendations (OQ-1 idempotency-gate via NEW heartbeat_stale_emitted_at column in V10; OQ-2 re-use existing archived_at column for `workspace cleaned up at` marker; OQ-3 do NOT persist container_id on runner_executions — labels are sufficient — defer to story 3-19/4-9; OQ-4 emit JUST runner.dispatched on docker path mock path keeps runner.started; OQ-5 dangling-container pass on 1h cadence with workspace cleanup; OQ-6 NEW RecoverableRunnerAdapter sub-interface instead of `instanceof`; OQ-7 separate stale-scan-interval-ms: 60000 from timeout-scan-interval-ms: 10000). KEY FINDINGS via survey: existing `scanForTimeouts`/`pollActiveExecutions`/`recoverOnStartup`/HeartbeatTouched-branch in RunnerBroker provide every extension point with no broker reshape; existing `RunnerExecutionService.recordTimedOut/recordOrphaned/recordCompleted/recordFailed/touchActivity` cover every state transition needed (zero new methods); `runner_executions` table already has last_activity_at/timeout_at/failure_category/completed_at/archived_at (only ONE new column needed for AC3.d idempotency); `RunnerProperties.Docker.workspaceRetentionHours` was declared in story 3-1 explicitly so 3-2 has a property to read; `FailureCategory.RUNNER_TIMEOUT/RUNNER_CRASH/ORPHAN` all already registered (zero enum additions); `WorkflowEventType` currently has only RUNNER_STARTED + RUNNER_FAILED — five new dedicated event values added so audit trail no longer routes everything through the generic RUNNER_FAILED + details.failureCategory shape; `RecoveryService.retry` baseline from story 1.18 already produces a fresh rex_* id (no code change — just a new docker-flavored contract test). Story 3-2 also closes story 3-1's open AC10 lifecycle-coverage gap (the docker-tagged ITs land here per scope expansion) AND closes the open `DockerRunnerAdapterLoggingContractTest` review item via AC10 (q) `DockerRunnerLifecycleLoggingContractTest` covering the new heartbeat/timeout/orphan/cleanup branches. Memory references: `wsl-linux-ci-reproduction.md` (Linux-vs-Windows `docker stop`/`docker kill` signal propagation parity for AC10 docker-tagged ITs), `verify-ci-fixes-in-clean-env.md`, `openapi-regen-platform-shim.md` (cross-shell regen for the 5-enum schema extension). Prior 2026-05-28 entry: story 3-1 bmad-code-review batch applied 10/12 patch findings; fixed broker harvest from Docker adapter, workspace symlink/root hardening, Docker dispatch idempotency/start-failure cleanup, runner-kind persisted parser, image-tag log redaction, Docker timeout wiring, and Windows host-path formatting. Story remains `in-progress` with AC10 lifecycle coverage + dedicated logging contract action items open.)
# last_updated: 2026-05-30 (story 2-22 dev-story completed all 10 tasks + 12 code-review patches; flipped `in-progress -> review`; gates green: vitest 54/54, lint:rules-test 4/4, tsc -b clean, eslint --max-warnings=0; AC11.w axe + 2 [Defer] findings documented-deferred (coupled to 2.14/2.18/2.25). Recommend Linux/WSL2 smoke + lockfile verify for the react-error-boundary dep before PR merge. Epic 2a active-slice next-active rotates to 2-20 queue-shell-states)
# last_updated: 2026-05-27 (story 3.1 created via bmad-create-story -> `backlog -> ready-for-dev`: DockerRunnerAdapter Core — Container Lifecycle + File-Based Contract Invocation. First story in Epic 3a active slice; epic-3a flipped `backlog -> in-progress`. Real Docker container lifecycle (create → start → wait → harvest result file) implementing the existing `application.runner.spi.RunnerAdapter` port from story 1.13 under new `@Profile("runners.docker")` gating (mirror of MockRunnerAdapter's `@Profile("!runners.docker")`). Workspace layout: NEW `{DELIVERYLINE_HOME}/runner-work/{rex_id}/{input,output,logs}/` directory tree (separate from the existing `runner-scratch/{rex_id}/` leaf-file store) reached via NEW `application.runner.spi.RunnerWorkspaceStore` SPI port + NEW `adapters.files.LocalRunnerWorkspaceStore` adapter (copies the proven `LocalRunnerScratchStore` containment + atomic-write + symlink-escape pattern verbatim). Input bundle filename inside container is `context-bundle.v1.json` (aligns with scratch leaf + schema $id versioning per OQ-4). Container launch posture: `--network=none` (AC8 — strictly file-based runner↔backend), input mount READ-ONLY, output+logs mounts read-write, NO other host mounts (no docker.sock, no host secrets), 4 labels (runnerExecutionId, workflowRunId, runnerKind, dispatchedAt) for operator forensics via `docker ps --filter`. NEW `domain.registry.RunnerKind` enum (CODEX/CLAUDE) + NEW `RunnerProperties.Docker` nested record (defaultKind, imageTags Map, workspaceRoot, workspaceRetentionHours, container timeouts) + NEW `runnerKind` field on `RunnerDispatchRequest` (resolved server-side from RunnerProperties — Trap T3 never from the untrusted context bundle). Defaults: codex → `deliveryline/codex-runner:latest`, claude → `deliveryline/claude-runner:latest` (overridable via `application.yml`; test profile uses `alpine:3.20` so IT tier doesn't depend on the not-yet-built Codex/Claude images from stories 3.3/3.4). Docker client choice: `docker-java-core` + `docker-java-transport-httpclient5` (per OQ-1; explicit pin even though Testcontainers already brings it transitively); raw `com.github.dockerjava.api.model.*` types stay behind a project-owned `adapters.runner.docker.DockerEngineGateway` wrapper (Trap T8) returning project-owned `ContainerState` + `CreateContainerSpec` records. NEW ArchUnit rule `DOCKER_RUNNER_ADAPTER_MUST_NOT_DEPEND_ON_ARTIFACT_APPLICATION` pins AC6 (adapter never calls ArtifactOperationService — broker drives ingest after broker validates via RunnerContractValidator). NEW `DockerRunnerProfileWiringContractTest` mirrors existing `RunnerProfileWiringContractTest` (ApplicationContextRunner-based; asserts docker bean under `runners.docker`, mock bean under no-profile, IllegalStateException on both-active via existing `RunnerConfiguration.assertExclusiveRunnerProfile`). Workspace cleanup is DEFERRED to story 3.2 (this story declares the `workspace-retention-hours` property + FORBIDS immediate deletion to preserve diagnostic state per AC7). Timeout enforcement also DEFERRED to story 3.2 (this story's `poll(...)` only returns Running/Completed/Failed(RUNNER_CRASH)/Unknown — never RUNNER_TIMEOUT — broker's existing `scanForTimeouts()` continues driving timeout transitions until 3.2 extends them). Twelve declared traps (T1 separate workspace port vs scratch store, T2 RunnerDispatchRequest record-break callsite update, T3 runnerKind server-side never from bundle, T4 no re-validation of bundle in adapter, T5 input filename `context-bundle.v1.json` not `context-bundle.json`, T6 ArchUnit rule scope `adapters.runner..` only spares broker, T7 `--network=none` at create not patched post-start, T8 no docker-java types past DockerEngineGateway, T9 no RUNNER_TIMEOUT in poll, T10 DockerClient bean gated to runners.docker profile, T11 cancel issues stop never rm, T12 test image scripts never mutate runner-contracts fixtures). 7 open Qs all with recommendations (OQ-1 docker-java vs CLI subprocess → docker-java; OQ-2 separate runner-work vs collapse with runner-scratch → separate; OQ-3 runnerKind via RunnerDispatchRequest vs adapter-direct property read → via request; OQ-4 input filename `context-bundle.v1.json` vs `context-bundle.json` → versioned; OQ-5 IT gating via `@EnabledIfDockerAvailable` custom condition; OQ-6 test image alpine:3.20 vs hello-world/busybox → alpine; OQ-7 add runner_kind column to runner_executions → NO defer to 3-19/4-9). 8 tasks + Logging instrumentation cross-cutting; IT tier `docker-runner-it` opt-in via `@Tag` + `@EnabledIfDockerAvailable` so it does NOT block PRs without Docker in CI (real-runner-contract joins foundation gate in story 3-8). KEY FINDINGS via survey: RunnerAdapter port + 4 method signatures already locked from story 1.13 (no broker reshape); RunnerConfiguration.assertExclusiveRunnerProfile already enforces mock/docker mutex; FailureCategory.RUNNER_CRASH/RUNNER_NON_ZERO_EXIT/RUNNER_CONTRACT_VIOLATION all already registered (no enum additions); broker's existing RunnerBroker.onResult path classifies via RunnerContractValidator — adapter just returns bytes via `tryReadResult` and never validates (Trap T4); broker.dispatch step 7 writes redacted+validated context-bundle bytes to scratch BEFORE delegating to adapter — adapter reads from scratch and copies to workspace (intentional reuse, NOT a workaround). Memory references: `wsl-linux-ci-reproduction.md` (smoke-test new IT tier on WSL2 Ubuntu before pushing), `verify-ci-fixes-in-clean-env.md` (local green ≠ CI green for docker-tagged IT). Prior 2026-05-26 entry: story 2.24 `review -> done`: bmad-code-review walkthrough closed 8 decision-needed + 12 patch findings — bundle measured & under AC9 threshold, Shiki active highlighting wired via codeToHast→hast-util-to-jsx-runtime, frontend redaction filter recurses into <strong>/<em>/code, validateUrlScheme rejects protocol-relative URLs, AC16 frontend drift contract test wired into CI, PEM full-chain bundles + RSA categorization fixed, ESLint rule extended (insertAdjacentHTML/document.write/computed-property/JSX-spread), CSS token wiring for .redaction-applied landed. Status flipped from `review`. Earlier note retained below.) # ORIGINAL: 2026-05-26 (story 2.24 dev-story complete -> `in-progress -> review`: all 12 tasks + Logging instrumentation landed end-to-end. Frontend sanitization package shipped at `deliveryline-frontend/src/lib/sanitization/` — `SafeMarkdownRenderer` (react-markdown 9.x + rehype-sanitize 6.x + remark-gfm 4.x with skipHtml + a strict tagNames/attributes allowlist that drops script/iframe/style/object/embed/form/input + URL-scheme validator that case-insensitively trims-then-strips javascript:/data:/file:/vbscript:/blob: schemes per Trap T3) + `SafeDiffRenderer` (handrolled Set-based line diff + <ins>/<del>/<span> with stable diff-line-* class tokens, never raw HTML) + `MetadataChrome` (trusted-metadata header above a "Generated content"-labeled bordered region) + `redactionFilter` (compiles redaction-policy.generated.json patterns once at module load + walks rendered text via the components.{p,li,td,th,h*,blockquote} overrides, wrapping newly-detected secrets in <mark class="redaction-applied"> per Trap T11) + `policy.ts` (ALLOWED_URL_SCHEMES + ALLOWED_IMAGE_HOSTS=[] + SHIKI_LANGUAGES + SANITIZATION_SCHEMA) + `index.ts` barrel + README documenting failure semantics. 11 XSS fixtures (script-tag-injection, img-onerror, a-href-javascript, iframe-src, style-tag-injection, markdown-link-javascript-url, entity-encoded-script, mixed-case-script, polyglot, data-uri-image, code-fence-with-script) + 5 redaction fixtures (github-token, linear-api-key, idempotency-key-header, author-written-literal-redacted, backend-prefixed-placeholder) each with .expected.json sidecars asserting noScript/noIframe/noStyle/noActiveElements/noJavascriptHrefAnchors/noDataUriImages + renderedTextContains/DoesNotContain + (for redaction fixtures) expectedDetectedCategories + noRedactionMarks. Vitest fixture-loop test discovers fixtures via import.meta.glob and walks DOM with querySelectorAll('*').attributes to belt-and-suspenders scan for retained on* handlers. New ESLint rule `local-rules/no-unsanitized-html` (AC11) registered in tools/eslint-rules/index.js + wired in eslint.config.js at `error` for src/**/*.{ts,tsx} — flags JSX `dangerouslySetInnerHTML` attribute + direct `el.innerHTML`/`el.outerHTML` assignment, self-exempts inside `src/lib/sanitization/**` (Trap T10). RuleTester+node --test test file extends `lint:rules-test` script. Backend `SensitivePayloadAnalyzer.SECRET_FIELD_NAMES` constant extracted as single source of truth (Trap T6 — JSON mirrors Java). New PEM_PRIVATE_KEY pattern covering EC/DSA/PKCS#8/ENCRYPTED with capture-group label pinning (defends against mixed-label malformations); new PEM_CERTIFICATE_WITH_PRIVATE_KEY pattern detects paired CERT+KEY blocks and runs BEFORE the standalone PEM pattern so the certificate doesn't leak through; new IDEMPOTENCY_KEY_HEADER pattern for free-text request-shape logs. Three new RedactionCategory enum constants (IDEMPOTENCY_KEY, PEM_PRIVATE_KEY, PEM_CERTIFICATE_WITH_PRIVATE_KEY). Six new backend fixtures (pem-rsa-private-key.pem, pem-ec-private-key.pem, pem-pkcs8-private-key.pem, pem-certificate-with-private-key.pem, bundle-json-nested-credentials.json, idempotency-key-header.txt) added to fixtures-manifest.json. JSON_SECRET_FIELD_PATTERN + YAML_SECRET_FIELD_PATTERN + ENV_SECRET_VALUE_PATTERN expanded with bearer/private/refresh_token/client_secret/session_token/auth_token field-name variants. New `deliveryline-runner-contracts/src/main/resources/runner-contracts/redaction-policy.json` (canonical spec, schemaVersion=1, 7 patterns + 20 secretFieldNames) + `schemas/redaction-policy.schema.json` (JSON Schema draft 2020-12 with `pattern` checks for category SCREAMING_SNAKE + canonical [REDACTED_X] placeholder format). New `RedactionPolicyContractTest` in runner-contracts (schema validation via networknt 3.0.2 SchemaRegistry + regex-compile sweep + secret-field-names containment + canonical placeholder format check) + new backend-side `RedactionPolicyParityContractTest` (every policy placeholder maps to a RedactionCategory enum + required-categories presence + SECRET_FIELD_NAMES equality vs analyzer constant). Cross-platform `scripts/regen-redaction-policy.{sh,ps1}` (pure cp + sha256 — Trap T7 no Docker shims). CI `export-redaction-verify` tier extended with broader `-Dtest='*RedactionContractTest,*RedactionPolicyService*Test,*RedactionPolicy*ContractTest,RedactingMessageConverterUnitTest'` glob + new runner-contracts schema-test step. `deferred-work.md` F19+F20 appended with CLOSED 2026-05-26 lines per AC18. Twelve traps declared in spec all honored (T1-T12). Five open questions resolved per recommendations (OQ-1 mark uses CATEGORY, OQ-2 production-only bundle measurement, OQ-3 every-render redaction filter, OQ-4 grep first for 2.17 stubs / not present so noted in README, OQ-5 schemaVersion stays 1). VERIFIED: focused `npx vitest run src/lib/sanitization` -> 61/0/0; `npm run lint` -> 0 errors 0 warnings; `npm run lint:rules-test` -> 3/0/0 (all 3 ESLint local rules pass); `mvn -pl deliveryline-runner-contracts test` -> 11/0/0 (includes RedactionPolicyContractTest 4/0/0 + existing RunnerContractValidatorTest 7/0/0); `mvn -pl deliveryline-backend test -Dtest='*RedactionContractTest,*RedactionPolicyService*Test,*RedactionPolicy*ContractTest,RedactingMessageConverterUnitTest' -Dsurefire.failIfNoSpecifiedTests=false` -> 69/0/0 across DoctorRedactionContractTest 20/0/0 + RedactionPolicyServiceContractTest 23/0/0 + LoggingRedactionContractTest 20/0/0 + RedactingMessageConverterUnitTest (6 not run via this slice — covered by separate sweep); `mvn -pl deliveryline-backend org.jacoco:jacoco-maven-plugin:prepare-agent failsafe:integration-test -Dit.test='RedactionPolicyParityContractTest'` -> 3/0/0; spotless:apply landed (re-flowed RedactionPolicyContractTest + RedactionPolicyParityContractTest + SensitivePayloadAnalyzer + RedactionCategory javadoc). Deferred to follow-ups: (a) AC3 active Shiki post-mount enrichment via codeToHast (the security property holds via inert plain-text rendering; XSS fixture pinned), (b) AC9 bundle-delta measurement deferred to a clean WSL2 build cycle (npm install added 113 packages on Windows), (c) WSL2 Ubuntu re-verification of the @SpringBootTest paths NOT re-run locally per memory `wsl-linux-ci-reproduction.md`. Initial draft of pem-ec-private-key.pem + pem-certificate-with-private-key.pem fixtures had prelude wording "EC PRIVATE KEY" / "private key" which tripped LoggingRedactionContractTest's adversarial sweep (Set-membership scan with looksSecret heuristic); reworded to "elliptic-curve key block" / "cert + key block" and deleted my `.expected-redacted` documentation sidecars (not consumed by any test — RedactionPolicyServiceContractTest uses manifest-driven placeholder/forbiddenSnippets shape). Prior 2026-05-26 entry: story 2.24 created via bmad-create-story -> `backlog -> ready-for-dev`: Artifact Content Sanitization + Redaction-Gap Closure (Untrusted Runner Output). Hybrid story spanning (a) NEW frontend sanitization pipeline under `deliveryline-frontend/src/lib/sanitization/` (`SafeMarkdownRenderer` wrapping `react-markdown` 9.x + `rehype-sanitize` 6.x + `remark-gfm` 4.x + `shiki` 1.x — bundle threshold ≤ 250 KB gzipped per AC9; `SafeDiffRenderer`; `MetadataChrome` trusted-metadata wrapper; `redactionFilter` second-pass defense-in-depth; 11-file adversarial XSS fixture set under `__tests__/xss-fixtures/`; new ESLint rule `no-unsanitized-html` extending the existing `local-rules` plugin from 2.31), (b) backend redaction-policy hardening in `application.security.SensitivePayloadAnalyzer` (new PEM_PRIVATE_KEY + PEM_CERTIFICATE_WITH_PRIVATE_KEY + IDEMPOTENCY_KEY `RedactionCategory` values; extended PEM patterns covering EC/DSA/PKCS#8/ENCRYPTED variants; new `Idempotency-Key` header pattern; extracted `SECRET_FIELD_NAMES` constant single-source-of-truth; six new fixtures + manifest entries under `redaction-fixtures/`), (c) NEW shared `deliveryline-runner-contracts/src/main/resources/runner-contracts/redaction-policy.json` (canonical spec mirroring backend, NOT runtime-loaded — Trap T6 keeps backend the implementation source of truth) + companion JSON schema + cross-side contract test enforcing parity, (d) closing F19 + F20 entries in `deferred-work.md` (per `sprint-change-proposal-2026-05-19.md`). 12 tasks, 12 declared traps (T1 Shiki HTML output routes through `rehype-sanitize` not `dangerouslySetInnerHTML`; T2 Shiki must use CSS-class-only output because `rehype-sanitize`'s defaultSchema drops inline `style`; T3 URL-scheme validation case-insensitive + whitespace-trimming; T4 image allowlist defaults empty; T5 redaction filter runs on RENDERED text tree not source markdown; T6 backend stays implementation SoT, JSON mirrors Java; T7 cross-platform regen scripts pure file copy; T8 contract test lives in runner-contracts module per architecture line 1200; T9 MSW NOT wired to load policy at test time; T10 ESLint rule allows itself inside `src/lib/sanitization/**` trusted boundary; T11 author-written literal `[REDACTED]` vs newly-wrapped `<mark>` distinction explicit; T12 fixtures use FAKE/DO-NOT-USE markers). KEY FINDINGS: existing `SensitivePayloadAnalyzer` + `RedactionPolicyService` + `RedactionCategory` + `redaction-fixtures/fixtures-manifest.json` pattern provides the full extension surface — zero new services, zero new ports; the existing `RedactionLayoutHolder` log-line redaction picks up the new categories automatically since it routes through the same analyzer; frontend `tools/eslint-rules/index.js` `local-rules` plugin + `lint:rules-test` Node `--test` convention from story 2.31 is the template for the new rule; `export-redaction-verify` CI tier (line 704 in `.github/workflows/ci.yml`) extended via `-Dtest='...'` expression; story 2.17 variant stubs consume new `MetadataChrome` + `SafeMarkdownRenderer` primitives per AC10. 5 open Qs (recommendations in spec): OQ-1 `<mark>` placeholder uses CATEGORY not classification; OQ-2 bundle threshold applies to production build only; OQ-3 redaction filter runs every render with `useMemo` keyed on text content; OQ-4 grep first for 2.17 variant stubs before deciding wire-vs-replace; OQ-5 `redaction-policy.json` stays `schemaVersion: 1` (additive). Memory references: `frontend-lockfile-cross-platform.md` (regenerate lockfile via full `npm install`), `frontend-ts6-legacy-peer-deps.md` (`.npmrc legacy-peer-deps=true` reconciles devtools peer-pin), `wsl-linux-ci-reproduction.md` (verify on WSL2 Ubuntu before pushing), `openapi-regen-platform-shim.md` (cross-shell regen pattern for `regen-redaction-policy.{sh,ps1}`). Prior 2026-05-26 entry: story 2.13 bmad-code-review round 5 complete -> `review -> done`: pragmatic-triage pass per user instruction to stop iterating ("we spent too many iterations on this story; if we need to return to in-progress check if it critical or not — if not critical move it into another story/deferred work"). NO CRITICAL findings on the Round-4 follow-up surface. Read-verified: LocalActorIdentityResolver.requireSafe correctly fail-closes (oversize/controlChar/FORMAT/SURROGATE/PRIVATE_USE/UNASSIGNED + property-fallback re-validated via unsafeReason in constructor); clarificationReplayStatus cross-run guard returns CLARIFICATION_NOT_FOUND on mismatch (no info leak); answerClarificationInternal reads currentState BEFORE the clarification write so Trap T6 contract is honored; rejectMultiValuedHeader catches both Spring-coalesced count>1 and RFC 7230 §3.2.2 comma-folded variants; ProblemDetailsMapper.handleMissingRequestHeader registry pattern is sound and extensible. 2 sub-critical observations deferred to deferred-work.md: (a) ProblemDetailsMapper.problemResponse correlationId-body sanitization asymmetry (no-op in practice because CorrelationIdFilter validates UUIDv7 shape upstream + JSON serialization escapes control chars at emit), (b) imprecise comment wording on the P-R4-4 race-protection fix (controller-level Trap T6 contract still satisfied either way). No new mvn re-run this round (Round-4 already verified 70/0/0 focused regression slice + compile/test-compile clean + checkstyle clean + OpenAPI drift check green). Story 2.13 status flipped `review -> done`; spec updated with Round 5 verdict block + 2 defer entries; deferred-work.md updated with the 2 defer items. Story 2.13 closes out after 4 rounds of bmad-code-review (~80 raw findings + ~14 deferred items across the rounds). Reviewer should re-run on WSL2 Linux per memory `wsl-linux-ci-reproduction.md` before pushing: WorkflowControllerMdcCorrelationPropagationTest, WorkflowMutationEndpointsContractTest, OpenApiSnapshotContractTest (all @SpringBootTest + Testcontainers).) — Prior 2026-05-26 entry: story 2.13 bmad-code-review round 4 complete -> `review -> in-progress`: 3 adversarial layers (Blind Hunter 47 findings / Edge Case Hunter 26 / Acceptance Auditor 8 substantive — verdict: ACs SATISFIED WITH CAVEATS) -> ~80 raw findings triaged to 1 decision + 17 patches + 15 defers + ~25 dismissed. Decision D-R4-1 resolved as option (b) fail-closed for unsafe X-Actor-Identity header values (oversize/control-char/FORMAT-category/multi-value → typed INVALID_COMMAND_PAYLOAD; WARN+property-fallback retained only for absent/blank). 8 of 17 mechanical patches applied this session: P-R4-3 (ProblemDetailsMapper.handleDomainException sanitizes request.getMethod() for symmetry with path/correlationId); P-R4-5 (LocalActorIdentityResolver.containsControlChar additionally rejects SURROGATE/PRIVATE_USE/UNASSIGNED codepoint categories so unpaired surrogates cannot reach the audit_log text column); P-R4-6 (LocalActorIdentityResolver constructor + resolve() both switched from String.trim() to String.strip() so Unicode whitespace U+00A0/U+2000-U+200A/U+3000 cannot survive into the audit identity); P-R4-7 (WorkflowStateChangeResponse fields gain @Schema(requiredMode=REQUIRED) on workflowRunId+currentState + NOT_REQUIRED on correlationId for symmetry with ClarificationAnswerResponse — OpenAPI snapshot + schema.d.ts regen deferred to closing checklist per openapi-regen-platform-shim.md); P-R4-8 (WorkflowMutationEndpointsContractTest.seedAvailableSpecArtifact uses StandardCharsets.UTF_8 explicitly so SHA-256 checksum is identical between Windows dev and Linux CI); P-R4-9 (uniqueSuffix() switched from System.nanoTime() to AtomicLong counter to eliminate same-tick collision flake); P-R4-10 (WorkflowController.answerClarification dead null-guard request.answerText() == null ? 0 : ... removed — @NotBlank+@Valid guarantee non-null at controller entry); P-R4-11 (requireNonBlankIdempotencyKey replaces Map.of with LinkedHashMap matching deterministic-ordering pattern used elsewhere). 9 patches deferred to a follow-up dev-story cycle as too large for inline batch: D-R4-1 fail-closed refactor (resolver split into resolve+requireSafe + 3 contract test classes pinning the new 400 shape variants); P-R4-1 (cross-run guard on clarificationReplayStatus legacy fallback); P-R4-2 (RFC 7230 §3.2.2 comma-folded multi-value header detection); P-R4-4 (answerClarification currentState read-in-transaction for race elimination); P-R4-12 (CliRestEquivalenceContractTest resolver-stub enforces isSafe gates); P-R4-13 (fingerprint-stability test for reason/reasonText exclusion + 2.13 DTO rename non-effect); P-R4-14 (ProblemDetailsMapper.handleMissingRequestHeader generalization for future required headers); P-R4-15 (idempotent-replay test for terminal-run + answered-clarification path); P-R4-16 (OpenAPI ClarificationAnswerResponse.clarificationStatus enum vs description for the legacy 'unknown' sentinel). 15 items appended to deferred-work.md (AC7 --actor-type story-doc reconciliation; answerText byte-size cap defense-in-depth; Jackson 1.5→1 coercion strict-parsing; Unicode confusable canonicalization; parseClarificationReplayRef legacy | inside runId; @PostConstruct WARN observability; WARN-log rate-limit; idempotencyKey hashed cross-line correlation; AC10 no-duplicate-write live-stack pin extension to approve/reject; ProblemDetailsMapperLoggingContractTest parametric coverage; two-empty-headers vs single-blank typing precedence; header-parsing guard order test; entry-log fires-after-resolver observability; correlationId sanitization persistence/wire symmetry; httpRequest.getHeaders Tomcat-only case-insensitivity). VERIFIED: `mvn -pl deliveryline-backend compile + test-compile` clean (no syntax/import drift); focused regression slice `mvn -pl deliveryline-backend test -Dtest='LocalActorIdentityResolverTest,ProblemDetailsMapperLoggingContractTest,WorkflowControllerLoggingContractTest,WorkflowAdapterEquivalenceTest,*EndpointContractTest,ProblemDetailsContractTest'` -> 70/0/0; mvn spotless:apply clean (reflowed one Javadoc line in WorkflowStateChangeResponse.java); mvn checkstyle:check clean. NOT re-run locally on Windows due to Docker constraints: WorkflowControllerMdcCorrelationPropagationTest, WorkflowMutationEndpointsContractTest, OpenApiSnapshotContractTest — reviewer should run on WSL2 Linux per memory `wsl-linux-ci-reproduction.md`. OpenAPI snapshot + frontend schema.d.ts regen also pending the WSL2/PowerShell shell split per `openapi-regen-platform-shim.md` because P-R4-7's @Schema annotations affect generated artifacts. Story 2.13 flipped `review -> in-progress` because 9 patches outstanding (1 decision-resolution + 8 deferred to follow-up dev-story). Findings written to `### Review Findings` section in 2-13 spec under `Round 4 review (2026-05-26)` heading.) — Prior 2026-05-26 entry: story 2.13 round-3 close-out dev-story complete -> `in-progress -> review`: all 7 deferred round-2 review patches resolved end-to-end + cross-cutting Logging instrumentation task closed. Patches landed: D4 (story 2.11 trap T12 wording explicitly allows `answerTextLength` length-only telemetry as the sanctioned redaction pattern, references 2.13 REST entry log as canonical caller); P1 (~10 new AC10 error-code tests across 3 *EndpointContractTest classes — INVALID_IDEMPOTENCY_KEY + INVALID_ID_PREFIX + RUN_NOT_FOUND on all three endpoints, plus APPROVAL_VERSION_MISMATCH + IDEMPOTENCY_KEY_CONFLICT on reject, IDEMPOTENCY_KEY_CONFLICT on answer); P2 (RejectSpecEndpointContractTest.identicalReplayProducesIdenticalResponseBody — same key + same payload → byte-identical response, service invoked twice); P3 (reject + answer both gain idempotencyKeyConflictReturns409); P4 (per-endpoint requestCorrelationIdOnMdcPropagatesIntoCapturedCommandAndResponseBody — MDC.put simulates filter, asserts captured command.correlationId() + response.correlationId() match; CorrelationIdFilter not registered in @WebMvcTest, so response-header echo remains pinned by WorkflowMutationEndpointsContractTest + new SpringBootTest); P9 (WorkflowController gains HttpServletRequest injection + new rejectMultiValuedActorIdentityHeader guard on all 3 mutation endpoints — multi-value X-Actor-Identity headers now raise typed INVALID_COMMAND_PAYLOAD with details.header=X-Actor-Identity + details.valueCount={n}; pinned by new multipleXActorIdentityHeadersRejectedAsInvalidCommandPayload test); P14 (new WorkflowControllerMdcCorrelationPropagationTest — @SpringBootTest(RANDOM_PORT) + @Import(TestcontainersConfiguration.class) + @MockitoBean WorkflowCommandService — registers production CorrelationIdFilter, posts via real HttpClient, asserts captured command.correlationId() matches X-Correlation-Id request header or filter-generated UUIDv7). Logging instrumentation: ProblemDetailsMapper.handleDomainException gains WARN log at catch-site emitting code/status/method/path only (no human title/detail — architecture line 712 mirrored in log content); WorkflowController entry logs on the 3 mutation endpoints gain actorIdentity parameter; new ProblemDetailsMapperLoggingContractTest + WorkflowControllerLoggingContractTest list-appender pins lock the WARN shape + the answerTextLength-only-never-verbatim rule. checkstyle suppressions.xml line drift for WorkflowCommandService.java ForbiddenThreadSleep updated 561 → 658 after round-2 patch batch. VERIFIED: focused regression slice `mvn -pl deliveryline-backend test -Dtest='*EndpointContractTest,*ProblemDetails*Test,WorkflowAdapterEquivalenceTest,WorkflowController*Test,*WorkflowCommands*Test,*WorkflowCli*Test,ArchitectureBoundaryTest'` -> 103/0/0; mvn spotless:apply clean; mvn checkstyle:check clean. NOT re-run locally on Windows due to Docker constraints: WorkflowControllerMdcCorrelationPropagationTest, WorkflowMutationEndpointsContractTest, OpenApiSnapshotContractTest (all @SpringBootTest + Testcontainers) — reviewer should run on WSL2 Linux per memory `wsl-linux-ci-reproduction.md`. Story 2.13 flipped `in-progress -> review`; all 10 tasks + Logging instrumentation + all 21 round-2 [Review][Patch] items checked. Findings written to `### Review Findings` section in 2-13 spec.) — Prior 2026-05-26 entry: story 2.13 bmad-code-review round 2 complete -> `review -> in-progress`: 3 adversarial layers (Blind Hunter on full diff after rtk-summarized first pass / Edge Case Hunter / Acceptance Auditor) -> ~80 raw findings triaged to 4 decisions + 21 patches + 9 defers + ~12 dismissed. All 4 decisions resolved: D1 accept legacy replay-ref drift + document; D2 lock CLI --actor-type to HUMAN mirroring REST; D3 fail fast on legacy body fields via @JsonIgnoreProperties(ignoreUnknown=false); D4 keep answerTextLength as telemetry, defer story-2.11 trap-T12 doc update. 14 of 21 patches applied this session — code/config landed: WorkflowController.idempotencyKeyPrefix helper removed (covers P5 partial-secret leak + P6 CR/LF log forgery) + new requireNonBlankIdempotencyKey(...) guard treats empty/blank header as MISSING_IDEMPOTENCY_KEY at controller boundary (P10); LocalActorIdentityResolver gains @PostConstruct warnOnBlankFallback + MAX_ACTOR_IDENTITY_LENGTH=128 + ASCII-control-char reject in resolve() with WARN-then-fallback semantics (P7 + P8); WorkflowCommands.java mirrors the WARN + uses LocalActorIdentityResolver.DEFAULT_LOCAL_ACTOR_IDENTITY constant as single source of truth (P7 + P17); WorkflowCommands.approveSpec/rejectSpec/answerClarification drop --actor-type option entirely and hardcode ActorType.HUMAN (D2); ApproveSpecRequest/RejectSpecRequest/AnswerClarificationRequest gain @JsonIgnoreProperties(ignoreUnknown=false) so legacy actorIdentity/actorType/correlationId body fields fail-fast as INVALID_COMMAND_PAYLOAD (D3); WorkflowCommandService.clarificationReplayStatus drops resultRef from DomainException details (P12) and Javadoc documents the legacy replay-ref drift decision (D1); clarificationReplaySeparatorIndex now rejects mixed legacy/new separators in a single ref (P16); WorkflowCommands clarificationStatus default flips from "answered" to "unknown" (P15); ClarificationAnswerResponse gains @Schema(requiredMode=REQUIRED) on workflowRunId/clarificationId/clarificationStatus/currentState (P11); WorkflowAdapterEquivalenceTest @BeforeEach stubActorResolver mirrors production resolver behavior to prevent null-injection traps for future test additions (P13); CliRestEquivalenceContractTest updated to drop ActorType.HUMAN args from the 4 cli.approveSpec/rejectSpec/answerClarification call sites (D2 ripple). 7 patches deferred to a follow-up dev-story cycle as Phase C test-authoring work: P1 (~9 missing AC10 error-code tests across 3 *EndpointContractTest classes), P2 (idempotent replay test for reject-spec), P3 (idempotency-conflict tests for reject + answer), P4 (X-Correlation-Id assertion in per-endpoint tests), P14 (@SpringBootTest pinning MDC correlation propagation), P9 (multiple X-Actor-Identity reject — needs HttpServletRequest injection), D4 (story-2.11 trap-T12 doc-only update). 9 items appended to deferred-work.md (transport policy inconsistency between submit/retry/takeover and the 3 new endpoints; Idempotency-Key still required=false on submit/retry/takeover; OpenAPI minLength:0 vs @NotBlank limitation; AC10 jsonPath-vs-schema-match; ArchUnit rule scope refinement; CLI generated idempotency-key print canonicalization nit). VERIFIED: `mvn clean compile` + `mvn test-compile -DskipTests` both green (no syntax/import drift); full unit/contract test surface NOT re-run this session — caller should run `mvn -pl deliveryline-backend test` + regenerate OpenAPI snapshot (`scripts/regen-openapi.sh`) before pushing because ClarificationAnswerResponse @Schema annotations + the three @JsonIgnoreProperties annotations affect the generated openapi.json and frontend schema.d.ts. Story 2.13 stays `in-progress` (not `done`) because 7 patches outstanding. Findings written to `### Review Findings` section in 2-13 spec.) — Prior 2026-05-25 entry: story 2.13 created via bmad-create-story -> `backlog -> ready-for-dev`: backend REST mutation endpoints (approve-spec / reject-spec / answer-clarification) + OpenAPI. Mostly a REBUILD of existing approve/reject controller surface plus the NEW clarifications/{id}/answer endpoint. AC2 renames wire fields `artifactVersion`→`expectedArtifactVersion` and `contextVersion`→`expectedContextBundleVersion` (DTO-only — command record stays unchanged for symmetry with SubmitClarificationCommand). AC3 tightens `Idempotency-Key` header to required + adds controller-advice for MISSING_IDEMPOTENCY_KEY Problem Details mapping. AC4 introduces `X-Actor-Identity` header with property-fallback via new `LocalActorIdentityResolver` + `SecurityProperties` (`deliveryline.security.local-actor-identity`, default `local-operator`) — body fields `actorIdentity`/`actorType`/`correlationId` removed from approve/reject DTOs. AC7 adds three new Spring Shell `@Command` methods (`approve-spec`, `reject-spec`, `answer-clarification`) on WorkflowCommands using the existing 3.x `@Argument`/`@Option` pattern + `CliRestEquivalenceContractTest`. AC8 ArchUnit rule tightening for `adapters.rest`. AC10 three per-endpoint contract tests (`ApproveSpecEndpointContractTest`, `RejectSpecEndpointContractTest`, `AnswerClarificationEndpointContractTest`) covering every error code + idempotent replay + idempotency conflict + actor fallback + correlation echo. TWELVE traps baked in: (T1) DTO rename is BREAKING — existing REST contract tests will fail until updated; (T2) command-record field names stay short (`artifactVersion`/`contextVersion`); (T3) `required=false`→`required=true` on Idempotency-Key needs `MissingRequestHeaderException` advice for typed 400; (T4) X-Actor-Identity is OPTIONAL not REQUIRED — property fallback never returns 401/403; (T5) correlation ID comes from MDC not @RequestHeader — CorrelationIdFilter already handles ingress/egress; (T6) answer-clarification does NOT change workflow state — ClarificationAnswerResponse.currentState renders the unchanged state; (T7) WorkflowCommandService.answerClarification returns WorkflowStateChangeResult — needs extension OR second SPI hop to surface post-mutation clarificationStatus (preferred: extend the result record with nullable clarificationStatus field); (T8) reuse the rollback-test fix lesson — `@MockitoBean WorkflowEventWritePort` must be paired with `WorkflowEventReadPort` mock; (T9) regenerated OpenAPI snapshot diff is intentionally large; (T10) frontend client regen needs platform-shim shells; (T11) ArchUnit rule scope — verify existing REST_AND_CLI_ADAPTERS_MUST_NOT_TOUCH_PERSISTENCE_OR_EXTERNAL_ADAPTERS coverage before adding a new rule; (T12) reviewer-role optional fallback preserved (audit-only RBAC). KEY FINDINGS: WorkflowController already has approve-spec/reject-spec endpoints (stories 2.9/2.10) — both DTO Javadocs explicitly anticipate this rebuild; ProblemDetailsCatalog ALREADY maps every AC5 error code (zero DomainErrorCode additions); WorkflowCommandService.answerClarification (line 122) already returns WorkflowStateChangeResult via executeIdempotent pipeline (story 2.11 OQ-1 wiring); CorrelationIdFilter (infrastructure.observability) handles X-Correlation-Id ingress + auto-UUID-v7 + MDC + egress; CLI uses Spring Shell 3.x `@Command` + `@CommandGroup` (NOT legacy `@ShellMethod`). Open Questions: NONE blocking — answer-clarification result-shape decision (T7 option a vs b) recommended in story dev notes as option (a) extend WorkflowStateChangeResult. Memory references: springdoc-boot4-version.md (3.0.3 pin), openapi-regen-platform-shim.md (WSL2-vs-PowerShell split for the 2 regen steps), frontend-lockfile-cross-platform.md + frontend-ts6-legacy-peer-deps.md (frontend regen platform traps), wsl-linux-ci-reproduction.md + verify-ci-fixes-in-clean-env.md (local CI repro for @SpringBootTest contract tests). Prior 2026-05-25 entry: story 2.12 review fixes complete -> `review -> done`: resolved the remaining code-review findings by failing closed on zero-length spec payload bytes, constraining clarification sweeps to the rebuilt spec lineage, and rejecting cross-run clarification probes before taking a pessimistic row lock. Focused verification passed: `ClarificationLifecycleServiceTest`, `ClarificationLifecycleServiceLoggingTest`, `ClarificationLifecycleOrchestratorTest` -> 27/0/0. Docker-backed `ClarificationReadPersistenceAdapterContractTest` could not be re-run locally because Testcontainers could not find a valid Docker environment.)
# last_updated: 2026-05-25 (story 2.12 review-batch dev cycle complete -> `in-progress -> review`: of the 33 bmad-code-review patches, 31 landed end-to-end this session and 2 remain deferred (P1 partial — orchestrator Mockito test landed but the Testcontainers/Logback ITs deferred, P2 FK flush-ordering IT deferred — both blocked on the same Spring-slice + Testcontainers infrastructure as the other deferred-work entries). All four D-decisions resolved as patches: P30 pessimistic SELECT...FOR UPDATE via new ClarificationReadPort.findByPublicIdForUpdate + @Lock(PESSIMISTIC_WRITE) repository query + loadAndGuardRun rewire; P31 constructor injection of ClarificationLifecycleOrchestrator on ArtifactOperationService — `@Autowired(required=false)` setter gone, field now `final` + required at construction, ~30 test callsites updated via `replace_all` against the factory + direct-constructor patterns; P32 fatal abort on payload-read failure — loadSpecPayload now throws DomainException(ARTIFACT_PAYLOAD_UNAVAILABLE) so the outer newVersion tx rolls back instead of silently terminal-superseding every accepted clarification (OQ-5 superseded by D5); P29 word-boundary regex verified shipped. Multi-file refactors landed: P9 hoist-artifact-fetch (mark methods take explicit int newSpecArtifactVersion; lifecycle service no longer depends on ArtifactRecordPort), P10 replay-ref separator U+001F (was `|`), P14 per-method noEffectReason vocab subsets (SUPERSEDED_REASONS, REJECTED_INVALID_REASONS), P15 CLARIFICATION_TERMINAL_STATE wiring (assertTransition returns TransitionPhase + routes terminal vs precursor-mismatch to distinct error codes), P16 getClarificationsForArtifact run-scope guard (Trap T11 parity). Smaller polish: P5 idempotent currentStatus==target short-circuit, P12 WorkflowSummaryResponse.from audited safe, P13 CORRELATION_ID confirmed in ALLOW_LISTED_KEYS, P19 schema enum for noEffectReason in both workflow-events-response + workflow-history schemas, P23 PublicIdPrefixes.require at orchestrator entry, P24 test helper rename answeredRowWithRun -> answeredRowForRun, P26 LifecycleSweepResult exposes incorporatedCount/supersededCount (newVersion log reads pre-computed), P27 clarificationNotFound now static for symmetry, P28 getRunSummary reorders reads so pending-count is LAST. Contract test hardened (4 patches landed in ClarificationVisibleIncorporationContractTest): group by composite (workflowRunId, clarificationId), validate state-machine ordering shape, event-type case correctness verified against WorkflowEventType source, require details.clarificationId on every clarification.* event. Two new negative tests cover impossible-transition-shape rejection and cross-run scope isolation. New ClarificationLifecycleOrchestratorTest landed (12 Mockito tests covering AC4 sweep paths + P32 fatal-abort branches + P23 input validation + P29 word-boundary + P21 IAE). Focused mvn invocation `ClarificationLifecycle*Test,ClarificationLifecycleOrchestratorTest,ClarificationVisibleIncorporationContractTest,WorkflowInspectionServiceClarificationTest,WorkflowInspectionServiceTest,ArtifactOperationServiceUnitTest,ArtifactLoggingContractTest` -> 73/0/0/0. Full backend `test` -> 462 total, 0 failures, 6 errors (all pre-existing ContextBundleService* errors from story 2.8 WIP — matches the prior baseline 437/0/6/3 plus +12 net new tests from this cycle, zero new regressions). WSL2 Ubuntu native re-verify + JaCoCo gate (LINE 81.33% / BRANCH 62.74%) NOT re-run in this session — flag with the deferred ITs for the next code-review pass. API breaking changes (intentional, no out-of-tree callers): ClarificationLifecycleService.markIncorporated + markSuperseded both gain explicit `int` artifact-version parameters; ClarificationLifecycleService constructor drops ArtifactRecordPort; all ArtifactOperationService constructors + withoutWorkflowRunStateGuard factory take ClarificationLifecycleOrchestrator as the last required parameter; WorkflowInspectionService.getClarificationsForArtifact gains a workflowRunPublicId parameter at position 1. Prior 2026-05-25 entry: story 2.12 bmad-code-review complete -> `review -> in-progress`: 3 adversarial layers (Blind Hunter / Edge Case Hunter / Acceptance Auditor) → ~75 raw findings triaged to 5 decisions + 33 patches + 16 defers + 6 dismissed. All 5 decisions resolved (D1 word-boundary fix → patch; D2 pessimistic lock in loadAndGuardRun → patch; D3 constructor injection per spec → patch; D4 V9 backfill not needed → dismissed; D5 fatal abort on empty storageRef → patch). 8 of 33 mechanical patches applied in this session: P11 getClarificationStatus equals NPE-safety swap; P17 clarificationReplayRef null-guard; P20 appendEvent defensive details copy (also normalizes correlationId blank/null per P22); P21 acknowledgesQuestion throws IAE on empty questionId; P25 acknowledgesQuestion @VisibleForTesting Javadoc; P29(D1) word-boundary regex for acknowledgesQuestion (with helper byteAt/isQuestionIdByte methods); P3 V9 drops + re-adds fk_clarifications_incorporation_event with ON DELETE RESTRICT (fixes CHECK violation on workflow_events tombstone); P4 V9 rejected_invalid CHECK arm adds accepted_at IS NULL (state-machine consistency). 25 patches deferred to a follow-up dev-story cycle as too large for inline batch: massive (P1 write 7+ missing AC10 tests including Trap T13 FK ordering IT + AC4 sweep IT + AC7 status parametric + CLog + persistence-adapter lifecycle + countPending port test + WorkflowEventDetailKeysContractTest + RegistryContractTest + ProblemDetailsContractTest); P31/D3 constructor injection across ~30 ArtifactOperationService test callsites; P30/D2 pessimistic lock requiring port + adapter + tests; P32/D5 fatal abort on empty storageRef + integration test; multi-file (P9 hoist artifact fetch out of sweep loop; P10 replay-ref encoding refactor; P14 per-method noEffectReason vocabulary subset; P15 CLARIFICATION_TERMINAL_STATE wiring; P16 getClarificationsForArtifact run-scope guard); verify-then-act (P8/P12 WorkflowSummaryResponse.from body; P19 PRIOR_ANSWER_TEXT renderer semantics; P28 WorkflowEventType camel/snake verification); contract test hardening (P5 group by (workflowRunId,clarificationId); P6 validate transition shape; P7 NO_EFFECT_REASON vs real type; P24 require details.clarificationId on clarification.* events); plus P13 correlationId allow-list, P18 JSON schema noEffectReason enum, P22 correlationId normalization (partly done), P23 PublicIdPrefixes.require at orchestrator entry, P26 log filter optimization, P27 test helper rename, P2 getRunSummary read consistency. NOT VERIFIED via mvn compile/test — caller should run `mvn -pl deliveryline-backend test -Dtest='ClarificationLifecycleServiceTest,ClarificationLifecycleOrchestratorTest,WorkflowInspectionService*Test,FlywaySchemaContractTest'` before pushing. Story 2.12 remains `in-progress` (NOT `done`) because AC10 test surface incomplete and 4 architectural patches outstanding. Findings written to story file's `### Review Findings` section + 16 deferred items appended to deferred-work.md. Prior 2026-05-25 entry: story 2.12 dev-story complete -> `in-progress -> review`: all 10 tasks landed end-to-end. New surface: V9__add_clarification_lifecycle_columns.sql migration (5 columns + composite FK + status-derivable CHECK + partial pending-index) + application.clarification.ClarificationLifecycleService (4 mark* transitions with assertTransition + loadAndGuardRun helpers + Clock-injection two-constructor pattern) + ClarificationLifecycleOrchestrator (sweepAfterSpecRebuild with deterministic substring-scan stub for acknowledgesQuestion, payload-read-failure -> WARN + mark superseded with noEffectReason=payload_read_failed per OQ-5 recommendation) + ClarificationLifecycleResult + ClarificationLifecycleSnapshot (V9-rich projection — Trap T1 keeps Clarification lean). Extended ClarificationWritePort with 4 mark* methods + 4 payload records; ClarificationReadPort with countPendingByWorkflowRun + findLifecycleSnapshotByPublicId; ClarificationWritePersistenceAdapter (4 method impls + WorkflowEventRepository dep); ClarificationReadPersistenceAdapter (2 method impls); ClarificationEntityMapper.toLifecycleSnapshot (resolves incorporation_event_id -> event details for incorporatedIntoArtifactId); WorkflowEventRepository.findIdByPublicId; ClarificationRepository.countPendingByWorkflowRunPublicId (native query leveraging V9 partial index); ClarificationEntity (5 V9 column mappings). Extended WorkflowInspectionService with getClarificationStatus(workflowRunId, clarificationId) -> ClarificationStatusView (cross-run guard Trap T11) + getRunSummary -> WorkflowRunDetailedSummaryView + listRuns enrichment of WorkflowRunSummaryView.pendingClarifications (N+1 accepted for MVP per OQ-4 + Trap T12). Wired orchestrator into ArtifactOperationService.newVersion via setter-injection (@Autowired(required=false)) — Trap T7 avoided the ~30-callsite constructor ripple; SPEC-only sweep per Trap T5 runs AFTER ARTIFACT_VERSION_CREATED event append. New DomainErrorCode.ILLEGAL_CLARIFICATION_TRANSITION (409, retryable=false) wired into ProblemDetailsCatalog + registry-api-schema-placeholders.json. Four new allow-listed WorkflowEventDetailKeys (incorporatedIntoArtifactId, incorporationEventId, supersededByArtifactId, noEffectReason — noEffectReason is controlled vocabulary per Trap T2) + workflow-history.v1.schema.json extension. New ArchUnit rule CLARIFICATION_LIFECYCLE_LIVES_IN_APPLICATION_CLARIFICATION confines both lifecycle service + orchestrator (Trap T8). Two new fixture event streams (clarification-incorporated-happy-path + clarification-superseded-and-rejected) + workflow-events-response.schema.json extension for lifecycle detail keys. Make-or-break ClarificationVisibleIncorporationContractTest walks every fixture asserting clarification.answered always has a downstream chain (accepted+terminal | rejectedInvalid | noEffectReason); negative case lives INLINE (Trap T9). Markincorporated FK flush-ordering (Trap T13) handled by appending event BEFORE row UPDATE; adapter resolves incorporation_event_id via WorkflowEventRepository.findIdByPublicId. THIRTEEN traps honored. Focused dev test slice green: ClarificationLifecycleServiceTest 10/10 + ArtifactOperationServiceUnitTest 31/31 + full default surefire 450/0/6/3 (only 6 pre-existing ContextBundleService* errors from story 2.8 WIP unaffected by this story; +13 net tests vs story 2.11 baseline 437/0/6/3, zero new regressions). REST DTO + OpenAPI snapshot regen deferred to story 2.13 per Git Intelligence note — WorkflowSummaryResponse intentionally lags by one story; story 2.14 reads WorkflowInspectionService.getRunSummary() directly. Deferred to code-review batch (mirror story 2.11 pattern): Testcontainers ClarificationWrite/ReadPersistenceAdapterLifecycleTest + ArtifactOperationServiceClarificationSweepIT + ClarificationLifecycleServiceMarkIncorporatedFkOrderingIT + WorkflowInspectionServiceClarificationStatusTest parametric coverage + ClarificationLifecycleOrchestratorTest Mockito coverage + ClarificationLifecycleServiceLoggingTest Logback ListAppender pins + WSL2 Ubuntu native verify (JaCoCo gate LINE 81.33% / BRANCH 62.74%). Open Questions OQ-1..OQ-5 all resolved per story-recommended defaults — surfaced in PR description for explicit sign-off. Prior 2026-05-25 entry: story 2.12 dev-story started -> `ready-for-dev -> in-progress`: Alex kicked off bmad-dev-story 2.12. Survey phase complete: confirmed ClarificationService (2.11) shape, ClarificationEntity already has `incorporation_event_id` mapped from V8, all six `clarification.*` WorkflowEventType values registered at lines 23-28, DomainErrorCode has the three story-2.11 `CLARIFICATION_*` codes, ProblemDetailsCatalog mapping pattern, ClarificationReadPort/WritePort + adapter shape, FlywaySchemaContractTest existing V8 assertion pattern. Open questions resolved per recommendations: OQ-1 extend clarifications table (Task 1 V9 migration), OQ-2 superseded counts as pending, OQ-3 defer manual rejected_invalid to Epic 4, OQ-4 accept N+1 in listRuns for MVP, OQ-5 WARN + mark superseded on payload-read failure. Task 1 done: V9 migration shipped (five lifecycle columns + composite FK + status-derivable CHECK + partial index `idx_clarifications_pending_by_workflow_run`), ClarificationEntity widened with five V9 field mappings + accessors, FlywaySchemaContractTest extended with V9 column/CHECK/FK/index assertions. Proceeding through Tasks 2-10.)
# last_updated: 2026-05-25 (story 2.12 created via bmad-create-story -> `backlog -> ready-for-dev`: backend Visible Incorporation Lifecycle States + Event Wiring. New `ClarificationLifecycleService` in `application.clarification` owning four transitions (`markAccepted`, `markIncorporated`, `markSuperseded`, `markRejectedInvalid`) + new `ClarificationLifecycleOrchestrator` hooked into `ArtifactOperationService.newVersion` (SPEC artifacts only) with a deterministic substring-scan stub for `acknowledgesQuestion(...)` (Epic 3 runner-contracts replaces). New Flyway V9 migration `V9__add_clarification_lifecycle_columns.sql` adds five lifecycle columns + `ck_clarifications_status_fields_paired` CHECK + partial index `idx_clarifications_pending_by_workflow_run`. New `DomainErrorCode.ILLEGAL_CLARIFICATION_TRANSITION` wired into `ProblemDetailsCatalog` (409, retryable=false). Extends `ClarificationWritePort` with four new `mark*` methods + payload records. Extends `ClarificationReadPort` with `countPendingByWorkflowRun(...)`. Extends `WorkflowInspectionService` with `getClarificationStatus(...)` + new `getRunSummary(...)` returning `WorkflowRunDetailedSummaryView` + extends existing `WorkflowRunSummaryView` with `pendingClarifications: int`. Extends `WorkflowEventDetailKeys` allow-list with four new keys (`incorporatedIntoArtifactId`, `incorporationEventId`, `supersededByArtifactId`, `noEffectReason` — `noEffectReason` is controlled vocabulary in MVP per Trap T2, safe to log + surface). New `workflow-history.v1.schema.json` properties for the four detail keys. Two new fixture event streams (`clarification-incorporated-happy-path` + `clarification-superseded-and-rejected`) under `fixture-event-streams/`. Make-or-break contract test `ClarificationVisibleIncorporationContractTest` walks every fixture's event stream asserting for-every `clarification.answered` there subsequently appears either `accepted + (incorporated|superseded|rejectedInvalid)` chain OR a `noEffectReason` event — negative case lives inline (Trap T9 — no malformed fixture file because `FixtureEventStreamSchemaConformanceTest` would reject it at a lower layer). THIRTEEN traps baked in: (T1) do NOT widen `Clarification` record with V9 fields — use new `ClarificationStatusView` instead; (T2) `noEffectReason` is controlled vocabulary, NOT free-form text; (T3) terminal-state semantics — all three (`incorporated`, `superseded`, `rejected_invalid`) are terminal at lifecycle layer per story 2.11 `Clarification.isTerminal()`; (T4) `markIncorporated` uses `INTERNAL_ERROR` (not `CLARIFICATION_NOT_FOUND`) when orchestrator-supplied artifact missing — caller bug not user error; (T5) sweep ONLY runs when `parent.artifactType() == SPEC`; (T6) orchestrator's `acknowledgesQuestion(...)` is a stub substring scan (case-sensitive over `^[A-Za-z0-9._-]{1,128}$`) — TODO marker for Epic 3 runner-contracts replacement; (T7) constructor-shape ripple — grep `new ArtifactOperationService(` + `new WorkflowRunSummaryView(` before refactoring; (T8) NO `@Transactional` on `ClarificationLifecycleService` — outer `ArtifactOperationService.newVersion` is the boundary; (T9) make-or-break contract test negative case lives INLINE not as malformed fixture file (FixtureEventStreamSchemaConformanceTest would reject); (T10) NO REST endpoints for lifecycle in MVP — orchestrator-driven; (T11) cross-run leak guard on `getClarificationStatus` + `mark*` — accept BOTH workflowRunPublicId AND clarificationPublicId, raise `CLARIFICATION_NOT_FOUND` on mismatch (mirror story 2.11 trap T6); (T12) `pendingClarifications` count N+1 in `listRuns` accepted for MVP with TODO marker; (T13) `markIncorporated` FK flush-ordering — clarifications.incorporation_event_id FK requires the event INSERT flushed BEFORE the row UPDATE flush, integration test pins ordering. KEY FINDINGS: ALL five lifecycle events already registered at `WorkflowEventType:24-28` from story 2.11 — zero registry additions for events; `Clarification.STATUS_*` constants + `isTerminal()` helper already shipped (story 2.11); `WorkflowEventDetailKeys.CLARIFICATION_ID` + `QUESTION_ID` already allow-listed; `ArtifactOperationService.newVersion(parentArtifactId, payloadRef, actor)` at line 450-470 is the orchestrator hook point (after `ARTIFACT_VERSION_CREATED` event append); `WorkflowInspectionService` constructor already injects `ClarificationReadPort` (story 2.11) — no widening needed for `getClarificationStatus`/`getRunSummary`/`countPendingByWorkflowRun`. 5 open Qs to Alex (OQ-1 V9 schema extension vs separate clarification_lifecycle_events table — recommend extend existing table; OQ-2 `superseded` counts as pending — recommend yes per make-or-break enforcement; OQ-3 PM-facing manual `markRejectedInvalid` — recommend defer to Epic 4 operator-action story; OQ-4 `listRuns` N+1 accept vs batch — recommend accept for MVP; OQ-5 orchestrator behavior on `ArtifactPayloadReader` failure — recommend WARN + mark superseded with `noEffectReason="payload_read_failed"` rather than rollback). JaCoCo gate (LINE 81.33% / BRANCH 62.74%) applies — reproduce on WSL2 Ubuntu before pushing per project memory. Prior 2026-05-25 entry: story 2.11 dev-story complete -> `in-progress -> review`: all 10 tasks landed, twelve declared traps honored, focused unit slice 22/22 green (ClarificationProjectionTest + ClarificationServiceSubmitAnswerTest + WorkflowCommandFingerprintFactoryTest + WorkflowCommandTypeTest), regression slice 41/41 green (WorkflowInspectionService*Test + CommandModelSymmetryFoundationContract + ProblemDetailsContractTest + RegistryContractTest + PublicIdPrefixesTest + WorkflowEventDetailKeysContractTest + ArchitectureBoundaryTest including the new CLARIFICATION_SERVICE_LIVES_IN_APPLICATION_CLARIFICATION rule), full backend unit-test surface 437/0/6/3 with only pre-existing ContextBundleService* errors unaffected by this story (last touched 7f15356 story 2.8). Spotless + Checkstyle clean (suppressions.xml updated for the Thread.sleep line shift 440->483 caused by Task 8's new answerClarification method). New surface: V8__add_clarifications.sql migration + application.clarification package (Clarification projection + ClarificationResult + ClarificationService + ClarificationReadPort/ClarificationWritePort SPIs) + adapters.persistence (ClarificationEntity + ClarificationEntityMapper + ClarificationRepository + ClarificationRead/WritePersistenceAdapter) + SubmitClarificationCommand sealed-permits member + WorkflowCommandFingerprintFactory branch (excludes answerText for idempotent replay) + WorkflowCommandService.answerClarification @Transactional method flowing through executeIdempotent (OQ-1 resolved as recommended) + WorkflowInspectionService.getClarifications/getClarificationsForArtifact + ClarificationView record + PublicIdPrefixes.CLARIFICATION enum + three new DomainErrorCode values wired into ProblemDetailsCatalog + WorkflowEventDetailKeys allow-list (clarificationId/questionId) + server-only key (priorAnswerText) + workflow-history.v1.schema.json properties + ArchitectureRuleCatalog.CLARIFICATION_SERVICE_LIVES_IN_APPLICATION_CLARIFICATION ArchUnit boundary rule + registry placeholder JSON updates + FlywaySchemaContractTest core-tables extension. Open questions resolved per recommendations: OQ-1 wire executeIdempotent in 2.11 (Task 8 lands the @Transactional answerClarification pipeline so replay works end-to-end in 2.11 tests rather than as a 2.13 behavioral change); OQ-2 question_text snapshot kept (FR10 stability); OQ-3 archived_at column shipped without operator surface (Epic 5 retention sweeps). Deferred to code-review batch: Testcontainers-based ClarificationRead/WritePersistenceAdapterTest, Spring-slice ClarificationServiceSubmitAnswerContractTest, dedicated logging-redaction pin (Logback ListAppender), per-column FlywaySchemaContractTest assertions for V8. WSL2 Ubuntu native verify (JaCoCo gate LINE 81.33% / BRANCH 62.74%) deferred to reviewer per project memory note — Windows-only verification path completed in this dev session. Prior 2026-05-25 entry: story 2.11 dev-story started -> `ready-for-dev -> in-progress`: Alex kicked off bmad-dev-story 2-11. Survey phase complete: confirmed all reference templates (ApprovalService, ApprovalSnapshot, ApprovalReadPort/WritePort, ApprovalRead/WritePersistenceAdapter, ApprovalEntity/Mapper/Repository, ApprovalResult, RejectSpecCommand, V7 migration, V1:167-204 approvals block, WorkflowCommandService, WorkflowCommandFingerprintFactory, PublicIdPrefixes, DomainErrorCode, WorkflowEventType lines 23-28 — all six `clarification.*` event types ALREADY registered as expected). Constructor-shape ripple smaller than the story warned: `new WorkflowCommandService(` returns 0 matches (Spring-only DI in production tests); `new WorkflowInspectionService(` returns 2 test files (WorkflowInspectionServiceTest, WorkflowInspectionServiceSpecTest). Proceeding with implementation: V8 migration, registries, command, fingerprint extension, full application.clarification + adapters.persistence slice, service orchestrator, WorkflowCommandService.answerClarification, WorkflowInspectionService.getClarifications[ForArtifact], test suite. Resolving OQ-1 as recommended (wire executeIdempotent in 2.11 — see story dev-notes). Prior 2026-05-24 entry: story 2.11 created via create-story -> `ready-for-dev`: backend Clarification domain entity + ClarificationService.submitAnswer writer. New `application.clarification` package mirroring `application.approval` end-to-end (Clarification projection + ClarificationReadPort + ClarificationWritePort + persistence adapters + entity + mapper + repository + ClarificationService + ClarificationResult). New Flyway V8 migration `V8__add_clarifications.sql` (clarifications table only — the two `workflow_runs` columns the epic AC bundled into "V2" already shipped in story 2.10's V7; the epic AC2 wording is obsolete). New `PublicIdPrefixes.CLARIFICATION` ("clr_"). Three new `DomainErrorCode` values (`CLARIFICATION_ARTIFACT_VERSION_MISMATCH`, `CLARIFICATION_NOT_FOUND`, `CLARIFICATION_TERMINAL_STATE`) wired into `ProblemDetailsCatalog`. New `SubmitClarificationCommand` (`WorkflowCommand` sealed member); `WorkflowCommandFingerprintFactory` extended with explicit switch branch (excludes `answerText`, mirrors `ApproveSpecCommand.reason` exclusion). `WorkflowCommandService.answerClarification(...)` plugs into the existing `executeIdempotent` pipeline (OQ-1 recommendation: wire in 2.11, not 2.13). `WorkflowInspectionService` extended with `getClarifications(workflowRunId)` + `getClarificationsForArtifact(artifactId)` (status-grouped, then `created_at ASC`). NO REST endpoint / CLI verb / UI in this story (deferred to 2.13 + 2.18). TWELVE traps baked in: (T1) Migration is V8 not V2 — the epic-asserted bundling of workflow_runs columns is obsolete; (T2) NO REST endpoint — 2.13 owns the controller; (T3) NO open-clarification CREATE path in the service — only `ClarificationWritePort.insertOpen` adapter-level (test fixtures + future spec runner Epic 3); (T4) only `CLARIFICATION_ANSWERED` event emitted — the five lifecycle events (accepted/incorporated/superseded/rejectedInvalid/noEffectReason) already registered at `WorkflowEventType:24-28` stay dormant until story 2.12; (T5) version-binding BEFORE terminal-state check (mirror 2.10 trap T3 — stale-version reviewer gets actionable mismatch error first); (T6) cross-run leak guard — clarification in a sibling run raises `CLARIFICATION_NOT_FOUND` not a leaky "wrong run" error (mirror 2.8 `getContextBundleForArtifact` cross-run guard); (T7) NO `@Transactional` on `ClarificationService.submitAnswer` — outer `WorkflowCommandService.answerClarification @Transactional` is the boundary (mirror 2.10 trap T4); (T8) fingerprint excludes `answerText` (symmetric with approve.reason / reject.reasonText exclusion); (T9) DB CHECK `ck_clarifications_answered_fields_paired` enforces "status=open ⇔ all three answer fields null" — `RecordAnswer` writes all three in one UPDATE; (T10) constructor-shape change ripple — grep `new WorkflowInspectionService(` + `new WorkflowCommandService(` (~10 callsites each) before refactoring; (T11) `getClarifications` JPQL uses `ORDER BY case status …, created_at asc` — status-grouped order pinned in contract test; (T12) `answerText` / `questionText` / `priorAnswerText` forbidden in log output (free-form, may carry product specifics — mirror 2.10 `reasonText` redaction). KEY FINDINGS: ALL six `clarification.*` `WorkflowEventType` values already registered (lines 23-28) — zero registry additions for events; `AllowedAction.ANSWER_CLARIFICATION` already registered at line 11; V1 `approvals` table provides the canonical shape (line 167-204) — composite `(artifact_id, artifact_version)` FK via `uq_artifacts_id_version` (V1:134); `application.approval` package + `adapters.persistence.{ApprovalEntity, ApprovalRepository, ApprovalEntityMapper, ApprovalReadPersistenceAdapter, ApprovalWritePersistenceAdapter}` is the verbatim template; constructor-shape ripple precedent set by 2.10 (`WorkflowRunSnapshot` 4→6 fields, 21 callsites updated). 3 open Qs to Alex (OQ-1 wire executeIdempotent in 2.11 vs defer to 2.13 — recommend wire here so idempotent replay works end-to-end in 2.11 tests; OQ-2 snapshot vs link question_text — recommend snapshot per FR10 stability; OQ-3 operator archive-clarification surface — recommend defer to Epic 5 retention sweeps). JaCoCo gate (LINE 81.33% / BRANCH 62.74%) applies — reproduce on WSL2 Ubuntu before pushing per project memory. Prior 2026-05-24 entry: story 2.10 review patch batch applied and verified: reject-spec replay pinned to Investigating, loop-count persistence made atomic, CLI history/OpenAPI/frontend contracts updated, focused backend verification green at 51 tests / 0 failures / 0 errors / 0 skipped; story 2.10 flipped review -> done.)
# last_updated: 2026-05-24 (story 2.9 bmad-code-review batch 1 complete: 3 adversarial layers → 48 raw findings triaged to 3 decisions + 10 patches + 5 defers + ~30 dismissed. Auditor verdict: ACs SATISFIED WITH CAVEATS. Edge Case Hunter surfaced 2 HIGH-severity gaps the Auditor missed: ApprovalService.approveSpec was not checking `artifact.artifactType() == SPEC` or `artifact.workflowRunId() == command.workflowRunId()` — a non-spec artifact OR a cross-run artifact could pass all guards and trigger the wrong run's transition. All 3 decisions resolved as patches: D1 (replay returns live state vs approval-time) → pin EXECUTING for ApproveSpecCommand replay path in WorkflowCommandService.replayStateChange; D2 (silent reviewer-role fallback) → resolver emits one-shot WARN at construction when default property is unset + DEBUG per blank-request resolve; D3 (no defense-in-depth pin on @Transactional T4) → `@Transactional(propagation = Propagation.MANDATORY)` on ApprovalService.approveSpec so direct callers fail with IllegalTransactionStateException instead of silently committing partial state. 10 additional patches applied: (1) artifactType==SPEC guard with new artifactTypeInvalid error returning INVALID_COMMAND_PAYLOAD; (2) workflowRunId-vs-artifact.workflowRunId mismatch guard with new artifactRunMismatch error; (3) fixed misleading "2 events" inline comment → "3 events"; (4) happy-path unit test now pins correlationId + idempotencyKey in event details map; (5) ApprovalWritePersistenceAdapter constraint-name resolution now layered — ConstraintViolationException.getConstraintName() preferred, substring-match fallback, SQLSTATE 23505 defense-in-depth; (6) uq_approvals_public_id collision branched as INTERNAL_ERROR (instead of raw DAE leakage); (7) IDEMPOTENCY_KEY_CONFLICT details sanitized — no longer echoes caller-private idempotencyKey verbatim (now emits source/conflictDetected only); (8) ApprovalWritePersistenceAdapterTest pins the WARN log line via Logback ListAppender; (9) new WorkflowCommandServiceContractTest.approveSpecSameKeyDifferentFingerprintRaisesIdempotencyKeyConflict pins AC10(e) end-to-end (same key, different reviewerRole → IDEMPOTENCY_KEY_CONFLICT before DB backstop); (10) new ApprovalServiceContractTest Spring-slice integration pins AC10(h) (write through ApprovalService → ApprovalReadPort.findLatestApprovedForArtifactLineage AND WorkflowInspectionService.getCurrentApprovedSpec both surface the just-written row). 5 defers logged to deferred-work.md: AllowedActionRegistryPinTest executor-wiring (defers to 2.14), ApprovalServiceTransactionalityTest named-test missing (functional equivalent ships), T8 shared SpecArtifactFixtures (revisit when 2.10 lands), idempotencyKey in event_details JSON (per-spec contract, cross-cutting policy belongs in a future operator-secret epic), checkstyle suppressions.xml line-number drift (pre-existing pattern). Focused test slice green: 38/38 unit + contract pass (ApprovalServiceApproveSpecTest, ApprovalResultTest, ApprovalReadPersistenceAdapterTest, WorkflowAdapterEquivalenceTest, ProblemDetailsContractTest, LoggingFieldNameContractTest, WorkflowCommandTypeTest, CommandModelSymmetryFoundationContract, AllowedActionRegistryPinTest). Story 2.9 flipped review → done. Prior 2026-05-24 entry: story 2.8 bmad-code-review batch 2 complete: 3 adversarial layers (Blind Hunter / Edge Case Hunter / Acceptance Auditor) → 49 raw findings triaged to 2 decisions + 9 patches + 5 defers + 33 dismissed. Auditor verdict: ACs SATISFIED WITH CAVEATS. D1 (silent canonicalization of spec `payloadRef`) resolved as patch: added `log.warn` when `ArtifactOperationService.canonicalSpecPayloadRef` rewrites a non-canonical value. D2 (JSON `schemaVersion=2` bump even when bundle unavailable) accepted current behavior (flag IS the version-negotiation switch). 9 patches applied: (1) `ApprovalRepository` all 3 JPQL queries now `join fetch a.workflowRun join fetch a.artifact` to kill the lazy-load N+1 in `ApprovalEntityMapper.toSnapshot`; (2) `SpecificationArtifact` compact ctor now `Objects.requireNonNull(createdAt)` — projection refuses to silently propagate a null persistence-mapper regression; (3) `ContextBundleService.createForSpecInvestigation` null-checks `ticketSummaryProvider.fetchByWorkflowRun` and throws `DomainException(INTERNAL_ERROR)` with `workflowRunId/runnerExecutionId/stage` details on null; (4) new `ArtifactOperationServiceContractTest.newVersionOnSpecParentEmitsExactlyOneArtifactVersionCreatedEvent` AC10 regression pin asserts `service.newVersion(spec parent, "spec.md", actor)` appends exactly one `artifact.versionCreated` event with `details.artifactId` and `details.version` matching the v2 spec; (5) `WorkflowCommands.textBundleReason` switch now `case null -> "context bundle unavailable (unknown reason)"`; (6) `ContextBundleService.createForSpecInvestigation` wraps `RedactionPolicyService.redact` in try/catch → `DomainException(INTERNAL_ERROR)` with stage context on policy-load failure; (7) `WorkflowCommands.spliceContextBundleJson` now `isObject()` guard before the `(ObjectNode)` cast → `DomainException(INTERNAL_ERROR)` on non-object rendered status (no more uncaught ClassCastException); (8) `ContextBundleService.assembleForSpecInvestigation` defense-in-depth — `priorSpec.artifactType() == SPEC` assertion inside the loop throws `INTERNAL_ERROR` on non-SPEC leak; (9) Completion Notes updated to remove the stale "adversarial redaction Deferred" + "ARTIFACT_VERSION_CREATED Deferred" entries — both are now actually delivered. 5 defers logged to `deferred-work.md`: `LocalRunnerScratchStore.tryReadContextBundle` defense-in-depth path-traversal guard (pre-existing); `ArtifactRepository` artifactType case-sensitivity (pre-existing); `ApprovalRepository` ordering non-determinism on concurrent writes (blocked on writer story 2.9/2.10); CLI bundle byte-size cap (orthogonal CLI hardening); `workflow-status.v2.schema.json` contextBundle required-field flag→schema mapping documentation (defer to docs increment 2.29). Focused unit-test slice green: 100/100 (`SpecificationArtifactTest, ArtifactOperationServiceUnitTest, WorkflowCommandsContextBundleFlagTest, WorkflowInspectionServiceSpecTest, WorkflowInspectionServiceTest, WorkflowCommandsStatusHistoryTest, WorkflowCliJsonSchemaContractTest, ApprovalReadPersistenceAdapterTest, ContextBundleServiceSpecInvestigationTest`). Story 2.8 flipped `review -> done`. Prior 2026-05-23 entry: story 2.8 review patch batch applied -> `review -> in-progress`: structured CLI status JSON moved to schemaVersion 2 with `contextBundle { status, artifactId, reason, bundle }`; text/JSON bundle lookup now preserves exact miss reasons instead of collapsing to scratch eviction; spec-investigation bundles are forced to `shareable-redacted`; prior spec versions remain represented in `artifactReferences` with explicit unavailable metadata; spec payload refs are canonicalized to `spec.md`; duplicate spec approvals now raise an integrity error instead of being overwritten; context-bundle lookup now rejects runner executions that belong to another run. Focused review tests passed in the backend/runner-contracts reactor. Remaining blocker for full contract verification: `ArtifactOperationServiceContractTest` could not start Testcontainers on this Windows host because Docker access to `\\\\.\\pipe\\docker_engine` was denied.)
# generated: 2026-04-26
# last_updated: 2026-05-23 (story 2.8 review-finding patch applied -> `in-progress -> review`: open review item "`getContextBundleForArtifact` returns a recomposed bundle instead of the bundle that actually produced the artifact" addressed by removing the recompose-fallback branch in `WorkflowInspectionService.getContextBundleForArtifact`. Scratch eviction now returns `Optional.empty()` with WARN `reason=bundleNotPersisted` — honest to AC7/FR55 ("the bundle used to produce the artifact"). `WorkflowInspectionService` constructor narrowed 10→9 args (dropped `ContextBundleService` dep + `SPEC_INVESTIGATION_RECOMPOSE_*` constants). Test `getContextBundleForArtifactRecomposesWhenScratchEvicted` rewritten as `…ReturnsEmptyWhenScratchEvicted`; redundant `…WhenRecomposeAlsoFails` test deleted. CLI's existing `# context-bundle: none (context bundle unavailable (scratch evicted))` sentinel handles the empty case unchanged. OQ-2 resolution updated in story file from "scratch-first, recompose fallback" to "scratch-only; persisted-bundle storage deferred to story 2.13+". Full backend verify on Windows JDK21 green: 608 unit + 295 IT tests pass, 0 failures / 0 errors / 5 skipped; JaCoCo gate met (LINE 81.33% / BRANCH 62.74% floor still satisfied); Spotless/Checkstyle/SpotBugs clean (spotless-apply auto-formatted one prior-patch test file). Reproducing on WSL2 Linux per project memory is the next reviewer step before story 2.8 moves to `done`. Prior 2026-05-23 entry: story 2.10 created via create-story -> `ready-for-dev`: backend spec rejection writer with structured feedback + escalation marker — story 2.10 file is `_bmad-output/implementation-artifacts/2-10-backend-spec-rejection-with-structured-feedback-and-escalation.md`. Mirrors story 2.9's ApprovalService.approveSpec pattern for the WRITE/REJECT path: extends RejectSpecCommand (adds `reviewerRole` NotBlank + `taggedFeedback` NotNull RejectionTaxonomy + widens `reasonText` cap 512→1024), adds new `ApprovalService.rejectSpec(...)` method (no @Transactional — relies on outer WorkflowCommandService.rejectSpec boundary), refactors WorkflowCommandService.rejectSpecInternal to delegate to ApprovalService (today it is a minimal `transition()` stub with NO approvals row + NO approval.rejected event), introduces new `RejectionTaxonomy` registry enum (`MISSING_SCOPE`/`UNCLEAR_SPECIFICATION`/`MISUNDERSTOOD_IMPLEMENTATION`), adds new `WorkflowEventType.ESCALATION_REQUIRED` registry value, adds new `WorkflowRunRejectionLoopPort` SPI + adapter (atomic `spec_rejection_loop_count` increment + `markEscalationOnce` returning row count for first-raise idempotency), adds new `SpecRejectionEscalationThresholdProvider` (config `deliveryline.workflow.spec-rejection-escalation-threshold` default `3`), extends WorkflowInspectionService surface + WorkflowSummaryResponse + CLI JSON output with `specRejectionLoopCount` + `escalationMarker`. TEN traps baked in: (T1) **CRITICAL MIGRATION-SEQUENCING TRAP** — the epic AC10 says the two `workflow_runs` columns land in story 2.11's "V2__add_spec_loop_and_clarifications.sql" migration, but actual existing Flyway history is V1→V1_1→V2(artifact_failure)→V3→V4→V5→V6, so the next available migration number is V7, and story 2.11 has NOT started; story 2.10 cannot wait for 2.11 without re-sequencing the sprint. Resolution adopted: story 2.10 ships its OWN migration `V7__add_spec_rejection_loop_columns.sql` adding both columns; story 2.11 keeps clarifications-only scope and renumbers its migration when it ships (likely V8). Surfaced as Open Question OQ-1 for Alex sign-off. (T2) REPLACE WorkflowCommandService.rejectSpecInternal — today it is a minimal stub that only calls transition() without writing an approvals row or APPROVAL_REJECTED event; naive field-addition leaves the stub alive. (T3) Version-binding BEFORE eligibility — mirror 2.9 trap T3; however eligibility check is OPTIONAL for rejection per OQ-3 recommendation (skip it — rejecting a stale-but-unavailable artifact is a valid PM decision). (T4) NO @Transactional on ApprovalService.rejectSpec — mirror 2.9 trap T4. (T5) Escalation event MUST be appended ONCE per run on the first `false → true` marker transition — uses `markEscalationOnce` JPQL UPDATE with `WHERE escalation_marker_set = false` guard + row-count check; subsequent rejections do NOT spam duplicate escalation events. (T6) Remove `reasonText` from WorkflowCommandFingerprintFactory's RejectSpecCommand branch (symmetric to 2.9 trap T9 — free-form reviewer text must replay idempotently); add `reviewerRole` + `taggedFeedback.value()` to the fingerprint. Behavioral change — surfaced as Open Question OQ-2. (T7) Do NOT introduce a new `MISSING_REJECTION_TAXONOMY` DomainErrorCode despite epic AC8's descriptive wording — jakarta-validation `@NotNull` on taggedFeedback + existing INVALID_COMMAND_PAYLOAD path already produces a precise per-field error; DB-level `ck_approvals_decision_taxonomy_paired` CHECK is the defense-in-depth backstop. (T8) Add `@NotNull` taggedFeedback on REST DTO + make CLI flag required, fail fast at controller/parse boundary not deep in service. (T9) Grep `grep -rn "new RejectSpecCommand"` before editing — record constructor-shape change from 9→11 fields ripples through CLI commands, REST controller, contract tests, and CommandModelSymmetryFoundationContract. (T10) WorkflowRunEntity adds both new columns as `int` + `boolean` primitives matching V7 DDL DEFAULT, no `insertable=false`/`updatable=false`. KEY FINDINGS: existing `ApprovalSnapshot.DECISION_REJECTED` constant + compact-constructor invariant (rejected ⇒ rejectionTaxonomy != null) already shipped by 2.8; `ApprovalWritePort.NewApproval.rejectionTaxonomy` field already exists (2.9 left it null for approve path; 2.10 populates it); `ApprovalWritePersistenceAdapter` already maps `uq_approvals_idempotency_key` to IDEMPOTENCY_KEY_CONFLICT for ALL decision values; `WorkflowEventType.APPROVAL_REJECTED("approval.rejected")` already registered; `WorkflowState.WAITING_FOR_SPEC_APPROVAL → INVESTIGATING` already permitted by WorkflowTransitionTable line 52; `ApprovalReviewerRoleResolver` reused as-is for the new reviewerRole plumbing; existing fixture event stream `spec-rejection-and-resubmit.{md,json}` covers 1-loop rejection (no escalation since default threshold is 3 — fixture stays under). 3 open Qs to Alex (OQ-1 V7 migration de-scoped from story 2.11 to break 2.10↔2.11 chicken-and-egg; OQ-2 remove `reasonText` from RejectSpecCommand fingerprint for symmetry with ApproveSpecCommand; OQ-3 skip ArtifactService.isApprovalEligible check for rejection path). JaCoCo gate (LINE 81.33% / BRANCH 62.74%) applies — reproduce on WSL2 Ubuntu before pushing per project memory. Prior 2026-05-23 entry: story 2.9 dev-story complete; status flipped `ready-for-dev → review`. ApprovalService writer (new `application.approval.ApprovalService` orchestrator + `ApprovalResult` record + `ApprovalWritePort` SPI + `ApprovalWritePersistenceAdapter`) shipped. WorkflowCommandService.approveSpec now delegates to ApprovalService (constructor extended; no surface change). ApproveSpecCommand extended with `reviewerRole` (NotBlank, max 128, included in fingerprint) + `reason` (max 1024, EXCLUDED from fingerprint — Trap T9). New `ApprovalReviewerRoleResolver` MVP-fallback wires the existing REST surface against new config property `deliveryline.approval.default-reviewer-role` (default `product_reviewer`) until story 2.13 rebuilds the controller. MdcKeys surface widened 5→6 with `APPROVAL_ID = "approvalId"` (LoggingFieldNameContractTest + logback-spring.xml updated). New ArchUnit rule `APPROVAL_SERVICE_LIVES_IN_APPLICATION_APPROVAL` confines the service to application/domain imports (Trap T4). DB-level `uq_approvals_idempotency_key` UNIQUE constraint mapped to `IDEMPOTENCY_KEY_CONFLICT` with WARN `source=db_unique_constraint` (Trap T5 backstop). All 9 traps honored end-to-end: version-binding runs BEFORE eligibility (T3), `approval.approved` event + approvals row + `workflow.stateChanged` + state update commit in one outer transaction (T6), REST shape preserved verbatim (T7 — `WorkflowController.approveSpec` request gained optional `reviewerRole`/`reason`), test fixtures reuse the JDBC-direct seed pattern from story 2.8's `ApprovalReadPersistenceAdapterTest` (T8). Open questions resolved: OQ-1 implemented as `ApprovalReviewerRoleResolver` + config property; OQ-2 bootstrap context-bundle = 1 when no `runner_execution_id`; OQ-3 fingerprint factory is explicit-switch (not reflection), `reviewerRole` appended, `reason` deliberately omitted. 8 new + 2 existing approve-spec tests in `WorkflowCommandServiceContractTest` re-seeded with `seedAvailableSpecArtifact` helper that writes real payload bytes via `LocalArtifactStore` + computes the SHA-256 checksum so `ArtifactService.isApprovalEligible` returns true. OpenAPI snapshot updated for new optional `ApproveSpecRequest` fields. Pre-existing 2 failing tests on `ContextBundleServiceUnitTest` / `ContextBundleServiceSpecInvestigationTest` are unrelated story 2.8 WIP (schema relaxation pending — story 2.8 status `review`, not `done`); 2.9 touches neither the schema nor `ContextBundleService`. JaCoCo gate (LINE 81.33% / BRANCH 62.74% from story 2.32) held on `mvn -pl deliveryline-backend verify -Dmaven.test.failure.ignore=true`. Recommendation: re-run full verify (without -Dmaven.test.failure.ignore) AFTER story 2.8 moves to `done`. Checkstyle suppression for pre-existing `Thread.sleep` in `WorkflowCommandService.pauseBeforeReplayLookup` updated for new line number (438→434). Prior 2026-05-23 entry: story 2.9 created via create-story -> `ready-for-dev`: backend ApprovalService.approveSpec writer in new `application.approval` package — wraps story 2.8's already-shipped read-side (ApprovalReadPort + ApprovalSnapshot + ApprovalEntity/Repository/Mapper + ApprovalReadPersistenceAdapter). Story 2.9 ships the WRITE path: new ApprovalWritePort SPI + ApprovalWritePersistenceAdapter, new ApprovalService orchestrator, new ApprovalResult record, extension of ApproveSpecCommand (adds `reviewerRole` NotBlank + optional `reason`), refactor of WorkflowCommandService.approveSpec to delegate to ApprovalService while keeping the existing executeIdempotent + replay pipeline intact, new MdcKeys.APPROVAL_ID constant, AllowedAction.APPROVE_SPEC registry-value pin test. NINE traps baked in: (T1) keep existing `artifactVersion`/`contextVersion` field names on ApproveSpecCommand — do NOT rename to `expectedArtifactVersion`/`expectedContextBundleVersion` (the AC wording is descriptive; renaming ripples into fingerprint factory + REST controller + 3 tests; document in Javadoc that they ARE the expected versions); (T2) do NOT widen the `DomainResult` sealed interface — `ApprovalResult` is the new ApprovalService return type, but WorkflowCommandService.approveSpec constructs `WorkflowStateChangeResult` from it (preserves the existing REST shape; story 2.13 will surface ApprovalResult directly); (T3) run AC4 version-binding BEFORE AC5 isApprovalEligible so reviewers learn "your version is stale" before "artifact unavailable"; (T4) `ApprovalService.approveSpec` has NO `@Transactional` annotation — relies on outer WorkflowCommandService.approveSpec @Transactional; nested REQUIRES_NEW would break rollback; ArchUnit boundary asserts; (T5) catch DataIntegrityViolationException in ApprovalWritePersistenceAdapter.insert and map `uq_approvals_idempotency_key` constraint hits to IDEMPOTENCY_KEY_CONFLICT (defense-in-depth; IdempotencyService should have rejected first); (T6) approvals-row insert + approval.approved event append + workflow.stateChanged event append + state update ALL commit in ONE outer transaction — Task 4 integration test simulates a transition failure and asserts rollback shape; (T7) preserve the existing WorkflowController.approveSpec REST response shape verbatim (story 2.13 rebuilds it with rich Problem Details); (T8) reuse story 2.8's SpecArtifactFixtures pattern for "WaitingForSpecApproval run with available approved-eligible spec artifact" — do NOT roll new fixtures; (T9) `reason` MUST be excluded from WorkflowCommandFingerprintFactory's fingerprint (reviewer edits to free-form text must replay as idempotent); `reviewerRole` IS in the fingerprint (semantic shift). KEY FINDINGS: ALL required registry values already present — WorkflowEventType.APPROVAL_APPROVED ("approval.approved"), DomainErrorCode.APPROVAL_VERSION_MISMATCH + IDEMPOTENCY_KEY_CONFLICT + ARTIFACT_PAYLOAD_UNAVAILABLE + ILLEGAL_TRANSITION, AllowedAction.APPROVE_SPEC ("approve_spec"), WorkflowState.WAITING_FOR_SPEC_APPROVAL + EXECUTING, PublicIdPrefixes.APPROVAL ("apr_") — zero registry additions. V1 `approvals` table already provisioned with all 7 attribution columns + `uq_approvals_idempotency_key` UNIQUE + paired decision/taxonomy CHECK — NO new Flyway migration. WorkflowTransitionTable already permits WAITING_FOR_SPEC_APPROVAL → EXECUTING. ArtifactService.isApprovalEligible (story 1.12 AC6) already does checksum verification — reuse, do not inline. WorkflowCommandService.executeIdempotent (story 1.7+1.9) already wraps idempotency-reserve/replay/complete — ApprovalService does NOT invoke IdempotencyService directly; it sits inside the existing pipeline. Critical pre-existing wiring: WorkflowController.approveSpec is the SOLE production caller of WorkflowCommandService.approveSpec today (story 2.13 will rebuild). 3 open Qs to Alex (OQ-1 default-reviewer-role config property for the existing controller until 2.13 wires the proper header; OQ-2 context-bundle version source for artifacts without a runner_executions row — bootstrap path; OQ-3 fingerprint factory extension policy for new command fields — reflection-based vs explicit). Prior 2026-05-23 entry: story 2.8 created via create-story -> `ready-for-dev`: backend SpecificationArtifact domain projection + spec-stage ContextBundle composition path + three WorkflowInspectionService read methods (getCurrentApprovedSpec, getSpecHistory, getContextBundleForArtifact) + CLI `--include-context-bundle` flag on `workflow status`. EIGHT traps baked in: (T1) reuse existing `RunnerStage.INVESTIGATION` — do NOT add a `spec-investigation` enum value (cascades through MockRunnerScenarioRegistry/RunnerProperties/tests/schema); the story's wording is descriptive, not a registry value; (T2) add a sibling `createForSpecInvestigation(...)` to ContextBundleService — do NOT mutate the existing `create(...)` that walks `parent_artifact_id` for executions stage; prior-feedback source diverges by stage; (T3) relax `context-bundle.v1.schema.json` `artifactReferences.minItems` from 1 → 0 to allow bootstrap spec-investigation bundles (zero prior artifacts); backward-compatible relaxation, in-place v1 patch, no v2 bump; (T4) story AC6 wording says `ArtifactService.newVersion(...)` but the actual implementation is `ArtifactOperationService.newVersion(parentArtifactId, payloadRef, actor)` — `ArtifactService` only owns `isApprovalEligible`; (T5) `ApprovalService` (story 2.9) + spec rejection writer (story 2.10) do NOT exist yet — story 2.8 ships the `ApprovalReadPort` + adapter + tests that seed approvals via repository inserts; no blocking on the writer; (T6) `getContextBundleForArtifact` requires deciding whether redacted-bundle bytes are persisted alongside runner_executions OR recomposed on demand — open question OQ-2 must be resolved before coding Task 4's third method; recompose-on-demand is not faithful to FR55; (T7) every `ApprovalReadPort` query must filter `WHERE archived_at IS NULL` (V1 schema allows tombstoning); (T8) reuse the project's existing seedDeepLineage / ListAppender fixture patterns rather than rolling new ones. KEY FINDINGS: V1 migration already provisions the `approvals` table with composite `(artifact_id, artifact_version) → artifacts(id, version)` FK, `decision IN ('approved','rejected')`, `rejection_taxonomy` CHECK, `context_bundle_version`, `actor_identity`/`actor_type`/`reviewer_role` — sufficient for AC8/AC9 with NO new Flyway migration; `WorkflowEventType.ARTIFACT_VERSION_CREATED` is already emitted by `ArtifactRecordPersistenceAdapter.createNextVersion` so AC6 inherits the event for free; `ArtifactType.SPEC` + `PublicIdPrefixes.ARTIFACT/WORKFLOW_RUN` already exist — no parallel constants. 3 open Qs to Alex (OQ-1 v1-patch vs v2-bump for schema relaxation, OQ-2 persisted-bundle storage shape, OQ-3 CLI text-vs-json bundle render format). Prior 2026-05-23 entry: story 2.7 code-review patches applied: tablet width contract corrected, collapsed-mode landmarks preserved, mobile run-identity parity restored, context drawer state preserved across reopen, invalid run-id shell queries suppressed, and description-list semantics fixed. Story 2.7 flipped review -> done.)
# last_updated: 2026-05-22 (story 2.32 bmad-code-review complete: 3 adversarial layers (Blind Hunter / Edge Case Hunter / Acceptance Auditor) -> 33 raw findings triaged to 1 decision + 4 patches + 2 defers + 17 dismissed. Decision resolved: drop the verified no-op `-Dsurefire.skip=true` flag from the backend-contract-tests verify command. 4 patches applied: (1) flag removed from ci.yml + step comment rewritten; (2) deliveryline-backend/README.md Thresholds section corrected to the authoritative Linux CI measurement (LINE 81.33% / BRANCH 62.74%) with an honest "floor below measurement" rationale; (3) docs/ci-pipeline.md backend-contract-tests row restored the dropped @Tag/class-name-suffix + AC7 detail and reconciled the surefire.skip wording; (4) pom.xml jacoco-merged.exec path extracted to a `<jacoco.merged.exec>` property (resolution verified via help:evaluate). 2 defers logged to deferred-work.md (stale exec data on non-clean local rebuilds; foundation-gate "Placeholder" doc staleness). Story 2.32 flipped review -> done. Prior entry: story 2.7 created via create-story -> `ready-for-dev`: tri-pane application shell (left nav rail + central main pane + right context panel) with artifact-primacy layout rules. Frontend-only; builds the structural SHELL only — composites that fill it ship later (2.15/2.16/2.17/2.18/2.19/2.20/2.22). Mounts in `__root.tsx` wrapping `<Outlet/>` (currently a bare Outlet). FIVE traps baked in: (T1) every leaf route renders its own `<Container py-8>` -> de-duplicate so there is exactly one `<main>` landmark; (T2) the right panel is a documented composition SLOT, not the Run Context Strip/Clarification Region composites; (T3) responsive overlaps story 2.26 — 2.7 builds the responsive STRUCTURE + creates `useResponsiveLayout`, 2.26 owns the full breakpoint matrix + Tailwind aliases + `RESPONSIVE.md` + mobile sticky decision-bar (do NOT pre-empt); (T4) artifact-primacy is a HARD CSS rule — main pane min-width so the right panel collapses first — and jsdom has no layout engine, so the AC2 "layout test" asserts the structural contract, not computed pixels (Playwright defers to 2.26/2.27); (T5) run-identity region reads `useWorkflowDetail` (TanStack Query), never `useState`. KEY FINDINGS: the epic's `UX-DR5/UX-DR6/UX-DR24` codes do NOT appear verbatim in the UX spec — decoded to real sections (Artifact Primacy Rule 1050-1057, Hybrid Coherence Rules 989-1002, Breakpoint Strategy 2092-2106). Planning-doc CONFLICT surfaced: epic AC1/AC10 place the shell at `src/features/workflows/AppShell.tsx`+`LAYOUT.md` but architecture.md:1183 says `components/layout` — recommended: follow the epic AC (shell is workflow-coupled), document the deviation in LAYOUT.md. Shell needs NO new npm deps (composes existing layout + shadcn primitives + native `matchMedia`). 4 open Qs to Alex (file location, right-panel slot mechanism, `useResponsiveLayout` ownership vs 2.26, dead-end routes inside vs outside the shell). Prior 2026-05-22 entry: story 2.32 review follow-up: CI/workflow/docs wording corrected to match the current build shape — backend-contract-tests now explicitly documents that `-Dsurefire.skip=true` is still passed but is currently a no-op in this build, so the Linux gate effectively runs the full backend verify shape. Additional Linux reproduction attempts recorded in the story notes: full reactor run in a Linux container blocked in deliveryline-frontend (`Could not determine Node.js install directory`), backend-isolated Linux run reached JaCoCo/report generation but Failsafe could not access a valid Docker/Testcontainers environment inside the container. The last open review action item (clean Linux + Docker reproduction) was then resolved — reproduced green natively in WSL2 Ubuntu 24.04 with real Testcontainers (exact CI command `verify -Dsurefire.skip=true`, BUILD SUCCESS, JaCoCo gate met, LINE 81.33% / BRANCH 62.74%), so story 2.32 moved `in-progress -> review`. Prior 2026-05-22 story 6.9 entry: review batch applied — dynamic OpenAPI server/schema fixes, fail-closed bind-guard hardening, workflow-event-stream contract enforcement, bundled-jar smoke payload validation; story moved to `done`.)
# review_note: 2026-05-17 story 1.12c bmad-code-review complete. Three adversarial layers (Blind Hunter / Edge Case Hunter / Acceptance Auditor) → Auditor verdict all 5 ACs satisfied; triage 4 decision-needed + 7 patches + 3 defers + 4 dismissed. Decisions: D1 cycle/depth-truncation → accept per spec dev-notes (schema invariants make >10000 unreachable; documented contract in ArtifactRepository javadoc); D2 ARTIFACT_OPERATION_INTENT_CONFLICT rollout → dismissed (pre-existing errorCode, existing REST/CLI mapping handles it); D3 reject-path idempotency persistence → deferred to another epic (cross-cutting policy across all pre-createPending typed rejections); D4 idempotencyKey cleartext logging → accepted (operational traceability; keys treated as opaque/non-sensitive per contract). Patches applied: CTE per-chain early-termination (added `AND c.cursor_public_id <> :lineageMemberPublicId` to recursive step — bounds row count to chains' depth-to-target, not full root-walk); `seedDeepLineage` rewritten to insert one workflow_events row per artifact version (production-shape parity) + per-call nanoTime nonce prevents cross-test public_id collisions + explicit IllegalStateException on null insert returns (replaces NPE); `hibernateStatistics()` simplified — removed redundant `setStatisticsEnabled(true)` that mutated global state (class-level `generate_statistics=true` property already enables tracking); assertion lower bounds added (`artifactLoads >= 1`, `delta >= 4`) to reject any regression that short-circuits the path to zero loads while preserving the upper-bound rejection of legacy O(D) behavior; CTE depth-cap contract documented in `ArtifactRepository#findActiveLineageLeaf` javadoc. One reviewer patch dismissed: "drop unused `leaf_version` in recursive step" was a reviewer error — `leaf_version` IS consumed by the outer `ORDER BY c.leaf_version DESC LIMIT 1` and must be propagated. Defers logged to `deferred-work.md`: native query un-hydrated lazy associations (theoretical for current callers), `JOIN workflow_runs wr ON wr.id` plan suboptimality (needs profiling), `createNextVersion` empty-CTE fallback grafts onto unrelated lineage (pre-existing). Focused verification: `mvn -pl deliveryline-backend -am test -Dtest='ArtifactOperationServiceContractTest,ArtifactLoggingContractTest,ArtifactPersistenceAdapterUnitTest'` → 30 tests / 0 failures / 0 errors / 0 skipped, BUILD SUCCESS. Story 1.12c flipped `review → done`. Prior 2026-05-15 entry preserved:
# review_note: 2026-05-15 story 1.18 final deferred review item resolved via append-only second-event audit. New `WorkflowEventType.RECOVERY_DISPATCH_FAILED("recovery.dispatchFailed")` registered + fixture updated; `RecoveryService.retry(...)` catch now appends the new event (REQUIRES_NEW) stamping `errorCode`, `errorClass`, `failedStage`, `recoveryActionId`, `recoveryRetriedEventId`, `idempotencyKey`, optional `correlationId` / `reason` / `compensationFailed`. The already-committed `recovery.retried` event is never mutated (NFR4 preserved). Targeted slice 119/0/0; full backend regression 560/1/0/4 (only pre-existing F17). +5 net tests, zero new regressions. Story 1.18 flipped `in-progress → review`. Prior 2026-05-15 entry preserved: story 1.18 review batch follow-up applied the accepted non-controversial fixes, aligned the story and ArchUnit guard to the 4-arg retry signature, and reverified the recovery-focused slice at 102/0/0.
# project: DeliveryLine
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog -> in-progress: Automatically when first story is created (via create-story)
#   - in-progress -> done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-04-26
project: DeliveryLine
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ============================================================
  # Epic 1: Foundation & First Governed Run (CLI) - 22 stories
  # ============================================================
  epic-1: in-progress
  1-1-initialize-maven-multi-module-project-scaffold: done
  1-2-unified-docker-compose-with-env-configurable-ports: done
  1-3-flyway-v1-core-schema-with-retention-and-measurement-columns: done
  1-4-central-registries-with-drift-tests: done
  1-5-workflow-state-transition-table-and-workflow-transition-service: done
  1-6-runner-context-result-schema-v1-with-artifact-variant-discriminators: done
  1-7-shared-application-command-model-pattern: done
  1-8-problem-details-mapper-with-stable-domain-error-codes: done
  1-9-idempotency-service: done
  1-10-redaction-classification-policy-and-adversarial-secret-fixture-set: done
  1-11-archunit-package-boundary-tests: done
  1-12-artifact-operations-skeleton: done
  1-12c-artifact-query-hardening-tech-debt: done
  1-13-runner-broker-and-deterministic-mock-runner-adapter: done
  1-14-mock-linear-adapter-and-real-linear-adapter-sharing-port: done
  1-15-spring-shell-cli-commands-submit-status-history: done
  1-16-doctor-service-and-doctor-command: done
  1-17-supported-environment-matrix-and-cross-platform-scripts: done
  1-18-cli-minimum-viable-recovery-baseline: done
  1-19-structured-logging-and-correlation-ids: done
  1-21-github-actions-ci-tiered-pipeline: done
  1-22-setup-and-cli-first-run-quickstart-documentation: done
  1-23-foundation-gate-ci-verification-and-deterministic-fixture-event-stream: done
  epic-1-retrospective: done

  # ============================================================
  # Epic 2: Specification Review & Product Approval - 32 stories
  # Split into 2a (early-usable slice) + 2b (full PM-loop UX) per
  # sprint-change-proposal-2026-05-26.md
  # ============================================================

  # ---- Epic 2a — Early-Usable PM Loop (active slice) ----
  epic-2a: in-progress
  # Done stories (foundation + backend mutations + tooling)
  2-1-frontend-module-scaffolding-vite-react-typescript-and-maven-wiring: done
  2-2-tailwind-and-shadcn-ui-setup-and-primitive-inventory: done
  2-3-design-tokens-color-palette-and-semantic-state-variables: done
  2-4-design-tokens-typography-and-spacing-and-layout-primitives: done
  2-5-tanstack-router-setup-and-typed-routes-and-deep-link-handling: done
  2-6-tanstack-query-and-key-factories-and-typed-api-client-from-openapi: done
  2-7-tri-pane-application-shell-with-artifact-primacy-layout-rules: done
  2-8-backend-specification-artifact-model-and-spec-stage-context-bundle: done
  2-9-backend-approval-service-core-approve-with-version-binding: done
  2-10-backend-spec-rejection-with-structured-feedback-and-escalation: done
  2-11-backend-clarification-domain-model-and-submission: done
  2-12-backend-visible-incorporation-lifecycle-states-and-event-wiring: done
  2-13-backend-rest-mutation-endpoints-and-openapi: done  # closed 2026-05-26 (round-5 pragmatic-triage); status flipped from `review` per change proposal cleanup
  2-30-backend-lint-and-format-spotless-checkstyle-spotbugs: done
  2-31-frontend-lint-and-prettier-and-custom-rules: done
  2-32-backend-coverage-reporting-and-maven-jacoco-threshold-gate: done
  # Active-slice pulls — frontend track order: 2-24 first (unblocks 2-15/2-17), then 2-14, 2-22, 2-20, 2-16, 2-15, 2-17
  2-24-artifact-content-sanitization-untrusted-runner-output: done  # closed 2026-05-26 — bmad-code-review walkthrough resolved 8 decisions + 12 patches; 6 minor items deferred. All test surfaces green.
  2-14-backend-allowed-actions-inspection-endpoint: done  # closed 2026-05-27 — bmad-code-review walkthrough resolved 16 patches + 2 decisions (both deferred); 7 items moved to deferred-work.md; 13 dismissed. 55/55 tests green across the four impacted classes after regen.
  2-22-navigation-and-empty-loading-error-states-infrastructure: done  # epic-2a; created 2026-05-28; dev-story 2026-05-30 completed all 10 tasks + 12 review patches (in-progress → review); bmad-code-review Round 2 2026-05-30 closed (review → done): 2 decisions (ErrorState ARIA role/live-region → deferred to 2.25; root errorComponent AC8.b half → accepted inline split) + 1 patch fixed (active-urgency focus-effect re-fire) + 14 defers + 6 dismissed, no Critical; post-fix gates green: vitest 30/30, tsc -b clean, eslint max-warnings=0; AC11.w axe + defers documented (coupled to 2.14/2.18/2.25)
  2-20-queue-shell-states-loading-empty-filtered-empty-error: done  # bmad-code-review 2026-05-31 review -> done (3 adversarial layers; 0 critical/high; ul role=list patch + 3 net-new tests, 1 reviewer-HIGH verified false-positive [retry already shows skeleton], 4 deferred; gates green via rtk proxy tsc/eslint/vitest 199)
  2-16-run-context-strip-component: done  # bmad-code-review 2026-06-01 review -> done: 7 patch findings batch-fixed (error containment, live stale rollover, missing artifact-version partial-context, artifact display labels, max-height coverage, aria-busy loading, long-value truncation/title). Verified: build, eslint --max-warnings=0, lint:rules-test 4/4, vitest 231/231, prettier check. Prior bmad-dev-story ready-for-dev -> review: RunContextStrip + toRunContextView mapper + shared WorkflowStateBadge/StateSignifierChip + Intl.RelativeTimeFormat formatters + AC11 fixture; mounted in WorkflowDetailRoute above review pane.
  2-15-run-review-queue-item-component: backlog  # next-active (epic-2a; depends on 2-24)
  2-17-artifact-review-panel-generalized-composite-spec-variant: backlog  # next-active (epic-2a; depends on 2-24, 2-14)

  # ---- Epic 2b — Full PM-Loop UX (deferred until 2a + 3a close) ----
  epic-2b: deferred
  2-18-clarification-region-with-visible-incorporation-lifecycle-wiring: backlog  # epic-2b
  2-19-approval-decision-bar-generalized-composite-spec-approval-mode: backlog  # epic-2b
  2-21-feedback-patterns-infrastructure-inline-persistent-toast-boundaries: backlog  # epic-2b
  2-23-modal-overlay-confirmation-patterns-and-button-hierarchy-infrastructure: backlog  # epic-2b
  2-25-wcag-2-1-aa-compliance-keyboard-focus-aria-contrast-audit-label-semantics: backlog  # epic-2b
  2-26-responsive-design-mobile-tablet-desktop-breakpoints-and-structural-collapse-rules: backlog  # epic-2b
  2-27-frontend-test-suite-component-route-query-hook-a11y-sanitization-cross-browser: backlog  # epic-2b
  2-28-spa-fallback-controller-and-maven-bundled-jar-smoke-and-packaging-integration: backlog  # epic-2b
  2-29-pm-loop-walkthrough-documentation-increment: backlog  # epic-2b
  epic-2-retrospective: deferred  # ran at end of 2b

  # ============================================================
  # Epic 3: Agent Execution, Implementation Output & Dev Review - 36 stories + 2 new (3a-1, 3a-2)
  # Split into 3a (real agent + repo stack for spec stage) + 3b (implementation
  # output + dev review + remaining infra) per sprint-change-proposal-2026-05-26.md
  # ============================================================

  # ---- Epic 3a — Real Agent + Repo Stack (active slice) ----
  epic-3a: in-progress  # flipped 2026-05-27 — first story 3-1 created via bmad-create-story
  # Active-slice pulls — backend track order: 3-1, 3-2, 3-5, 3-3, 3-4, 3-6, 3-13, 3-14, 3-9, 3a-1, 3a-2
  3-1-docker-runner-adapter-core-container-lifecycle-and-file-based-contract-invocation: in-progress  # next-active (epic-3a, 1st); returned review → in-progress 2026-05-28 via bmad-code-review batch: 10/12 patch findings fixed; AC10 lifecycle coverage + dedicated logging contract remain open.
  3-2-docker-runner-adapter-lifecycle-timeout-heartbeat-lease-expiry-cleanup-idempotent-restart: done  # closed 2026-05-29 at 3-2a close-out: 3-2a discharged the open review action items (HIGH stage-starvation, HIGH dangling-sweep safety, AC9/T13 retry anchor, detectHeartbeat atomicity+re-seed, event double-emit gate, processHeartbeatStale null) + the full AC10 test surface + real-boot OpenAPI verification; docker-runner-it tier green on WSL2 Ubuntu. 2 MED items (stale-boundary shared-now, non-atomic workspace delete) → deferred-work.md (accepted tech-debt, not in 3-2a ACs).
  3-2a-docker-runner-lifecycle-test-surface-and-review-hardening: done  # closed 2026-05-30 via bmad-code-review close-out pass: 3 adversarial layers (Blind Hunter + Edge Case Hunter + Acceptance Auditor) → 27 raw → ~20 distinct → 0 decision-needed, 0 patch (ZERO critical/automatic findings), 9 deferred → deferred-work.md, 11 dismissed. Per Alex's directive (process only critical/automatic; defer/move rest; close the story) no inline patches applied. Both HIGH-rated items are deliberate already-decided behaviors recorded for visibility: null-createdAt age-out re-opens the closed 2026-05-29 decision (mitigated — DB row inserted at dispatch so the rowless+null window is narrow); `unknown` engine-state folded into the running set is intentional + test-codified (force-rm of an exited container is a no-op). Belt-and-suspenders fresh WSL2 Linux parity re-run on 5ad3e17 still available to the reviewer (prior 8/0/0/0 was on 820a5af; production stop/timeout path unchanged). PRIOR: flipped in-progress → review 2026-05-30 via bmad-dev-story review-continuation close-out: the 2026-05-29 code-review's one open action item (AC6 Trap T1 negative test) was committed in 5ad3e17 + all findings ticked, but the forward status flip was never done. Re-verified fast tier 59/0/0 (BrokerUnit 29 incl. AC6 scanForTimeoutsHeartbeatExtendedDeadlineDoesNotInvokeDockerTerminate, StaleDetection 5, CleanupDangling 4, RunnerProperties 7, RunnerLoggingContract 9, DockerRunnerLifecycleLoggingContract 5) + re-ran the docker-runner-it tier on Windows Docker Desktop on 5ad3e17 (the IT bases changed: awaitNotRunning fail-fast + teardown WARN) → 8/0/0/0 (Timeout 1 STOPPED_GRACEFULLY→timed_out, Heartbeat 2, Recovery 2, Dangling 1, ContainerLifecycle 2). Fresh WSL2 Linux parity re-run on 5ad3e17 remains a belt-and-suspenders gate-to-done for the reviewer (prior WSL2 8/0/0/0 was on 820a5af; production stop/timeout path unchanged since). All 15 ACs + all patch/defer findings resolved. Story 3.2 stays done. PRIOR: returned review → in-progress 2026-05-29 via bmad-code-review: 4 decisions resolved (3→patch, 1 accepted-deviation) + 9 patch findings ALL fixed (HIGH timeout race-gate, null-createdAt age-out, redaction test, min-age upper bound, AC10(h) behavior assertion, awaitNotRunning fail-fast, TC teardown WARN-hardening, AC6 Trap T1 docker-path negative test) + 10 deferred → deferred-work.md + 4 dismissed. Fast-tier verified green: RunnerPropertiesTest 7/0, LoggingContractTest 5/0, StaleDetection 5/0, BrokerUnit 29/0, CleanupDangling 4/0; docker-runner-it tier compile-verified. REMAINING gate to `done`: commit the review fixes + re-run the review-modified docker-runner-it ITs on WSL2 (Trap T15) since the IT bases changed (awaitNotRunning fail-fast + teardown WARN). PRIOR: flipped in-progress → review 2026-05-29 via bmad-dev-story close-out. ALL 11 patch findings fixed (3 hard ones rewritten broker-driven on BrokerDrivenDockerLifecycleITSupport; latent findActiveStatuses @Transactional(readOnly=true) bug fixed). FRESH WSL2 Ubuntu-24.04 Linux parity trust gate (Trap T15) GREEN on a clean git clone of pushed 820a5af: 8/0/0/0 BUILD SUCCESS (Timeout 1, Heartbeat 2, Recovery 2, Dangling 1, ContainerLifecycle 2). CI cannot substitute — pom default-excludes docker-runner-it; real-docker CI tier is story 3-8. schema.d.ts confirmed drift-free (new field on application record, not REST DTO). All 15 ACs + Logging-instrumentation task complete. Story 3.2 stays done.
  3-5-runner-secrets-handling-secure-mount-of-agent-provider-api-keys: done  # 2026-05-30 bmad-code-review `review -> done`: 3 adversarial layers over the working-tree diff (40 files, +946/-30 + 737 new) -> 1 decision + 2 patch + 11 defer + 9 dismiss, ZERO critical. Decision (defaultKind dispatch/scan coupling) resolved option-1 (latent: dispatch also hardwires defaultKind() at :288, so detector compares against the right key today; RunnerExecutionSnapshot carries no runnerKind — column deferred to 3-19/4-9 per 3.1 OQ-7) -> documented coupling comment at RunnerBroker.java:515. 3 patches FIXED+verified: (1) leak branch now quarantines FIRST (best-effort) + guards recordFailed against ILLEGAL_TRANSITION re-entry mirroring the recordCompleted gate (edge-hunter HIGH: duplicate onResult on a leak-FAILED row threw uncaught + lost the marker); (2) RunnerBrokerUnitTest leak test gains verify(workflowTransitionService, never()).transition(...) pinning the no-run-state-drive invariant; (3) coupling comment. Post-fix focused green: RunnerBrokerUnitTest 30/0/0, RunnerSecretScanServiceTest 8/0/0, RunnerSecretsServiceTest 8/0/0, DockerRunnerAdapterUnitTest 24/0/0, RunnerWorkspaceCleanupJobDanglingUnitTest 5/0/0; spotless:apply + checkstyle:check clean. 11 defers -> deferred-work.md (onResult non-atomic, archived-vs-quarantined indistinguishable, substring no-min-length, doctor-all-kinds-FAIL, double-resolve log noise, AC11(f) tautological bundle test, readAllBytes no size cap, literal-not-regex no-leak tests, lean-ctx doctor/dispatch name divergence, gateway env char validation, String secret zeroing). Gate-to-done STILL PENDING (review could not run it in fast tier): real-boot OpenApiSnapshotContractTest (contract tier, Testcontainers) to verify the hand-edited openapi.json/schema.d.ts runner_secret_leak enum, plus WSL2 Linux parity per memory wsl-linux-ci-reproduction. Epic-3a next-active: 3-3 -> 3-4 -> 3-6 -> 3-13 -> 3-14 -> 3-9 -> 3a-1 -> 3a-2.
  3-3-codex-runner-image-dockerfile-and-entrypoint-and-contract-conformance: done  # 2026-05-31 bmad-code-review closed: 2 decisions fixed (Docker dispatch now injects DELIVERYLINE_RUNNER_STAGE; POSIX workspace perms now allow non-root runner to read input and write output/logs) + 6 patch findings fixed (spec payload materialized, Docker result/artifacts mirrored into scratch, broker ingests schema-native implementationPlan/prOutput refs, structured failure results for auth/command/Codex non-zero, safe command seam/no raw arg log, prompt under logs, self-test version check, conformance test uses env-stage/golden shapes/spec payload). Verified: focused unit slice 72/0/0; CodexRunnerImageConformanceIT docker-runner-it 4/0/0; spotless:check + checkstyle:check clean. Prior 2026-05-31 bmad-dev-story in-progress -> review: Codex runner image. Filled the runners/codex/ stubs: Dockerfile (node:22-slim, ARG CODEX_CLI_VERSION=0.135.0 + INSTALL_CODEX_CLI toggle for the mock build, ARG IMAGE_VERSION OCI label, non-root codex:1001 [1000 taken by base node user], exec ENTRYPOINT), POSIX entrypoint.sh, NEW lib/runner.mjs, NEW test/mock-codex.sh, full README, docker-compose codex-runner build-only service under `runners` profile, root .dockerignore, and CodexRunnerImageConformanceIT. Real Codex-API execution remains story 3.8. Epic-3a next-active rotates 3-4 -> 3-6 -> 3-13 -> 3-14 -> 3-9 -> 3a-1 -> 3a-2.
  3-4-claude-runner-image-dockerfile-and-entrypoint-and-contract-conformance: done  # 2026-05-31 bmad-code-review closed: 1 decision fixed (runner compose profiles removed so plain `docker compose build` includes codex+claude) + 4 patch findings fixed (CLAUDE_PROMPT_TEMPLATE applied via template insertion, API-key-only Docker conformance, exact self-test version token match, doctor remediation names CLAUDE_CODE_OAUTH_TOKEN). Deferred 1 architecture concern: real Claude API/subscription execution vs inherited --network=none contract to story 3.8/runner-network decision. Verified focused non-Docker 23/0/0, ClaudeRunnerImageConformanceIT 6/0/0 Docker-backed, spotless:apply + checkstyle clean. Prior bmad-dev-story 2026-05-31 ready-for-dev → review (Claude runner image mirrors codex 3.3; shared RUNNER_CONTRACT.md; dual-mode subscription-first auth; RunnerSecretsService matched-name injection [user-approved deviation]; verified 68/0/0 fast tier + 4/4 Dockerized conformance IT + production image claude@2.1.149 ~147MB/10 layers on WSL2)
  3-6-runner-logs-capture-and-redaction-and-classification: ready-for-dev  # next-active (epic-3a)
  3-13-mock-github-adapter: backlog  # next-active (epic-3a)
  3-14-real-github-adapter-pr-branch-commit-refs-and-pat-auth: backlog  # next-active (epic-3a)
  3-9-repository-workspace-service-git-clone-branch-management-commit-push: backlog  # next-active (epic-3a; scope extended to include spec-investigation stage per change proposal §2.2)
  3a-1-spec-stage-orchestration-dispatch-spec-generation: backlog  # next-active (epic-3a, NEW — analog of 3-11 for Inbox → Investigating; full ACs via bmad-create-story)
  3a-2-spec-stage-repo-context-bundle-extension: backlog  # next-active (epic-3a, NEW — extends story 2-8 spec bundle with repo workspace ref; full ACs via bmad-create-story)

  # ---- Epic 3b — Implementation Output + Dev Review (deferred until 3a + 2a close) ----
  epic-3b: deferred
  3-7-elk-stack-integration-centralized-log-capture-profile-gated: backlog  # epic-3b
  3-8-real-docker-runner-contract-integration-test: backlog  # epic-3b
  3-10-full-context-bundle-generation-for-implementation-stage: backlog  # epic-3b
  3-11-implementation-plan-artifact-generation-flow-orchestration: backlog  # epic-3b
  3-12-pr-output-artifact-generation-flow-orchestration: backlog  # epic-3b
  3-15-integration-link-service-extended-for-github-pr-linkage: backlog  # epic-3b
  3-16-linear-completion-sync-write-merge-ready-summary-back-to-source-linear-ticket: backlog  # epic-3b
  3-17-runner-execution-queue-and-configurable-worker-pool: backlog  # epic-3b
  3-18-workflow-batch-submission-cli-and-rest: backlog  # epic-3b
  3-19-queue-inspection-and-worker-status-and-queue-size-monitoring: backlog  # epic-3b
  3-20-technical-approval-service-accept-implementation-with-version-binding: backlog  # epic-3b
  3-21-technical-approval-service-reject-implementation-with-structured-technical-feedback: backlog  # epic-3b
  3-22-developer-takeover-service-takeover-workflow-with-preserved-context: backlog  # epic-3b
  3-23-rest-endpoint-accept-implementation-and-openapi: backlog  # epic-3b
  3-24-rest-endpoint-reject-implementation-and-openapi: backlog  # epic-3b
  3-25-rest-endpoint-takeover-workflow-and-openapi: backlog  # epic-3b
  3-26-artifact-review-panel-implementation-plan-variant-renderer: backlog  # epic-3b
  3-27-artifact-review-panel-pr-output-variant-renderer: backlog  # epic-3b
  3-28-approval-decision-bar-implementation-review-mode-activation: backlog  # epic-3b
  3-29-developer-takeover-ui-flow-initiation-confirmation-persistent-visibility: backlog  # epic-3b
  3-30-ui-minimum-viable-recovery-baseline-failed-stage-last-successful-retry-action: backlog  # epic-3b
  3-31-github-pr-linkage-display-in-run-context-strip-and-queue-item: backlog  # epic-3b
  3-32-ticket-source-adapter-abstraction-extract-from-linear-adapter: backlog  # epic-3b
  3-33-repository-host-adapter-abstraction-extract-from-github-adapter: backlog  # epic-3b
  3-34-ci-tier-real-docker-runner-image-build-and-compatibility-checks: backlog  # epic-3b
  3-35-test-suite-extension-runner-adapter-github-adapter-takeover-dev-review: backlog  # epic-3b
  3-36-execution-walkthrough-documentation-increment: backlog  # epic-3b
  epic-3-retrospective: deferred  # ran at end of 3b

  # ============================================================
  # Epic 4: Failure Handling, Recovery & Reconciliation - 28 stories
  # ============================================================
  epic-4: backlog
  4-1-cli-operator-inspection-deliveryline-operator-status: backlog
  4-2-ui-operator-workflow-owner-queue-and-filters: backlog
  4-3-audit-history-query-by-ticket-and-by-run-cli-and-rest: backlog
  4-4-failure-diagnostics-deep-dive-view: backlog
  4-5-recovery-service-resume-recovery-from-paused-state: backlog
  4-6-recovery-service-reconcile-reconcile-workflow-state-on-integration-conflict: backlog
  4-7-recovery-service-rerun-from-step-restricted-rerun-from-safe-step-boundaries: backlog
  4-8-recovery-service-pause-manual-pause-for-operator-intervention: backlog
  4-9-recovery-service-classify-failure-and-failure-taxonomy-registry-management: backlog
  4-10-rest-endpoint-resume-and-openapi: backlog
  4-11-rest-endpoint-reconcile-and-openapi: backlog
  4-12-rest-endpoint-rerun-from-step-and-openapi: backlog
  4-13-rest-endpoint-pause-and-openapi: backlog
  4-14-rest-endpoint-classify-failure-and-openapi: backlog
  4-15-artifact-reconciliation-db-file-drift-detection: backlog
  4-16-artifact-reconciliation-operator-driven-repair-actions: backlog
  4-16a-artifact-lineage-reconciliation-and-fork-governance: backlog
  4-17-integration-conflict-detection-linear-and-github-vs-internal-state: backlog
  4-18-integration-conflict-operator-action-surfacing-and-conflict-driven-workflow-pause: backlog
  4-19-compare-mode-backend-revision-delta-service-for-spec-plan-pr-output: backlog
  4-20-compare-mode-ui-component-ux-dr13: backlog
  4-21-compare-mode-mobile-bounded-state-ux-dr23: backlog
  4-22-decision-bar-recovery-operator-mode-full-activation-deeper-actions-beyond-retry-baseline: backlog
  4-23-operator-reconciliation-dialog-ui: backlog
  4-24-failure-taxonomy-classification-ui: backlog
  4-25-ci-tier-extension-recovery-integration-tests-and-fault-injection: backlog
  4-26-test-suite-extension-recovery-service-reconciliation-compare-mode-operator-ui: backlog
  4-27-failed-run-recovery-walkthrough-documentation-increment: backlog
  4-28-architecture-lift-remove-recovery-service-scope-protected-lock-and-adr: backlog
  epic-4-retrospective: optional

  # ============================================================
  # Epic 5: Shareable Run Export & Team-Visible Review - 12 stories
  # ============================================================
  epic-5: backlog
  5-1-run-export-service-composition-and-redaction-and-classification: backlog
  5-2-export-bundle-file-format-and-schema-json-and-markdown: backlog
  5-3-adversarial-redaction-enforcement-on-exports: backlog
  5-4-cli-deliveryline-export-run-and-rest-endpoints: backlog
  5-5-measurement-aggregation-queries-per-run-and-per-batch-and-per-team: backlog
  5-6-cli-deliveryline-metrics-report-and-rest-endpoints: backlog
  5-7-inclusion-of-measurement-data-in-exported-run-reports: backlog
  5-8-retention-enforcement-scheduled-job-and-ticket-closure-handling: backlog
  5-9-retention-configuration-and-per-run-archive-opt-out: backlog
  5-10-ci-tier-extension-export-contract-and-redaction-verification: backlog
  5-11-test-suite-extension-export-measurement-retention: backlog
  5-12-exported-report-walkthrough-documentation-increment: backlog
  epic-5-retrospective: optional

  # ============================================================
  # Epic 6: Adoption Polish & Pilot Documentation - 9 stories
  # ============================================================
  epic-6: backlog
  6-1-documentation-index-and-cross-reference-audit: backlog
  6-2-concept-vocabulary-and-glossary-completeness-audit: backlog
  6-3-end-to-end-happy-path-tutorial-validation: backlog
  6-4-end-to-end-failed-run-recovery-walkthrough-validation: backlog
  6-5-exported-report-walkthrough-validation-with-real-bundle: backlog
  6-6-pilot-dry-runs-coordination-and-report-with-named-validators: backlog
  6-7-nfr42-nfr44-nfr45-closure-audit: backlog
  6-8-pilot-launch-readiness-checklist-and-epic-close-gate: backlog
  6-9-localhost-rest-binding-and-workflow-read-endpoints: done  # pulled forward (2026-05-21) as story 2.6's prerequisite; built in the Epic 2 timeframe, id unchanged per epics.md:843
  epic-6-retrospective: optional
# last_updated: 2026-05-26 (sprint-change-proposal-2026-05-26: Epic 2 split into 2a/2b, Epic 3 split into 3a/3b. Epic 2a active slice = 2-14, 2-15, 2-16, 2-17, 2-20, 2-22, 2-24 (frontend track, view-only spec UI). Epic 3a active slice = 3-1, 3-2, 3-3, 3-4, 3-5, 3-6, 3-9, 3-13, 3-14 + new stories 3a-1 spec-stage-orchestration + 3a-2 spec-stage-repo-context-bundle-extension (real-agent + repo path). 2-13 status cleanup: review → done per round-5 pragmatic-triage close note. Story-cycle skills should pick next story by following the `next-active (epic-2a)` / `next-active (epic-3a)` markers. Recommended first frontend story = 2-24 (clears dependency-edges block for 2-15/2-17). Recommended first backend story = 3-1.)
# last_updated: 2026-05-26 (story 2.13 round-3 follow-up batch: resolved 7 outstanding [Review][Patch] items — P5 CLI delegation to LocalActorIdentityResolver, P9 multi-header reject pin on reject + answer endpoints, D3 legacy-body-field 400 pin on all 3 endpoints + jackson fail-on-unknown global flip, reject-endpoint entry-log pin, strengthened answer-clarification length-only telemetry guarantee, new focused LocalActorIdentityResolverTest, P14 MDC/DomainException import consolidation. Story 2-13 remains `review`; focused regression slice 106/106 pass.)
# last_updated: 2026-05-26 (story 2.13 round-4 follow-up dev-story: resolved all 9 outstanding round-4 [Review][Patch] items end-to-end → `in-progress → review`. D-R4-1 fail-closed unsafe-X-Actor-Identity (LocalActorIdentityResolver split into requireSafe+resolve; controller calls requireSafe BEFORE resolve on all 3 endpoints; CLI mirrors symmetrically; 9 new per-endpoint *EndpointContractTest cases pin the oversize/controlChar/formatCategory/commaFolded 400 shapes). P-R4-1 cross-run guard on clarificationReplayStatus legacy-ref fallback (CLARIFICATION_NOT_FOUND on workflowRunId mismatch). P-R4-2 comma-folded RFC 7230 §3.2.2 multi-value detection (`,` containment check after the count==1 guard). P-R4-4 read currentState BEFORE clarification write — answer response now reflects the answer-start snapshot, not whatever a concurrent reject/takeover happened to commit. P-R4-12 CliRestEquivalenceContractTest resolver stub delegates to a real LocalActorIdentityResolver. P-R4-13 3 new golden-hex fingerprint-stability tests in WorkflowCommandFingerprintFactoryTest pin field order + reason/reasonText exclusion + 2.13 DTO rename non-effect. P-R4-14 ProblemDetailsMapper.handleMissingRequestHeader uses a registry Map<String, DomainErrorCode> so future required headers can claim typed codes via a one-line addition. P-R4-15 new idempotentReplayAfterRunReachedTerminalStillRendersOriginalAnswerTimeState test on the answer endpoint pins the controller's faithful rendering of replay-from-resultRef state. P-R4-16 ClarificationAnswerResponse.clarificationStatus @Schema(description=...) documents the legacy `"unknown"` sentinel for hard-deleted rows during the migration window; OpenAPI snapshot + frontend schema.d.ts regenerated. VERIFIED: focused regression slice (126 tests across LocalActorIdentityResolverTest + WorkflowCommandFingerprintFactoryTest + 3 *EndpointContractTest + CliRestEquivalenceContractTest + ProblemDetailsContractTest + WorkflowAdapterEquivalenceTest + Logging contracts + ArchitectureBoundaryTest + CLI tests + WorkflowCommandServiceReplayRefTest) → 126/126 pass; mvn checkstyle:check clean (suppressions.xml ForbiddenThreadSleep line drift updated 662 → 684 after P-R4-4 patch); mvn spotless:apply clean (one Javadoc reflow in LocalActorIdentityResolver); OpenAPI snapshot regen via `OpenApiSnapshotContractTest -Dopenapi.snapshot.write=true` + re-verification with the drift check both green on Docker-backed Windows run. NOT re-run locally on Windows due to Docker constraints (separate Testcontainers fork sequencing): WorkflowMutationEndpointsContractTest, WorkflowControllerMdcCorrelationPropagationTest — reviewer should run on WSL2 Linux per memory `wsl-linux-ci-reproduction.md`. Story 2-13 flipped `in-progress → review`; all 10 tasks + Review Findings round 1–4 [Review][Patch] items checked.)
