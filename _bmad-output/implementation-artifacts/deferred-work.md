# Deferred Work

Items raised during reviews that are intentionally postponed. Each entry references the source review and the story it came from.

## Deferred from: code review of story-3-33 (2026-06-18)

- **Stray untracked frontend test in the working tree belongs to story 3b-3, not 3.33.** `deliveryline-frontend/src/routes/workflows/$workflowRunId/index.test.tsx` is untracked and its own docblock reads "Story 3b-3 (AC4 / AC6) — the WorkflowDetail route surfaces an 'Open the implementation output →' link." It is not in story 3.33's File List (3.33 is backend-only). **Follow-up:** exclude it from the 3.33 commit; it should land with story 3b-3's work. [`deliveryline-frontend/src/routes/workflows/$workflowRunId/index.test.tsx`]

## Deferred from: code review of story-3-32 (2026-06-17)

- **`kind` fail-fast validates the kind value, not profile-vs-kind consistency.** `assertSupportedTicketSourceKind` rejects an unsupported kind (e.g. `jira`) but does NOT assert a backing `linear-*` profile is active for `kind=linear`. A `kind=linear` deployment with no profile passes the assertion, then fails later with an opaque Spring no-bean error when the unconditional `IntegrationLinkService` requires a `TicketSourceAdapter` bean. Forward-looking: only the `linear` kind exists today (no mismatch possible) and the unconditional consumer already fails-fast at startup. **Follow-up:** add a profile-vs-kind cross-check when the second kind (3.33 GitHub / a future JIRA) lands. [`LinearConfiguration.java` assertSupportedTicketSourceKind]
- **Blank (non-null) `sourceStatusId` bypasses the null-only auto-ingest eligibility guard.** `LinearPollingHost.isEligible` checks `sourceStatusId() != null && allowList.contains(...)`; a blank `""` id would be eligible only if `""` is also in the configured allow-list. Pre-existing gate semantics carried over verbatim from `LinearTicket.statusId`; requires an implausible blank-id + blank-allow-list double-misconfig. **Follow-up:** treat blank as ineligible (`!isBlank()`) if a source can ever emit a blank status token. [`LinearPollingHost.java:339-343`]
- **Poll watermark trusts positional-last assuming ascending sort.** The watermark advances to `tickets.get(size-1).updatedAt()`, relying on the adapter returning ascending-sorted-by-`updatedAt`. The port Javadoc requires it and both Linear adapters sort, but the neutral port cannot enforce it — a future non-sorting `TicketSourceAdapter` could move the watermark backward or skip tickets. Pre-existing pattern; only the element type changed (`LinearTicket`→`Ticket`). **Follow-up:** compute `max(updatedAt)` defensively rather than trusting positional last. [`LinearPollingHost.java:204`]

## Deferred from: code review of story-3-22 (2026-06-15)

- **Split-transaction degraded mode strands the recovery_actions row + poisons the idempotency key.** If `DeveloperTakeoverService.markSucceeded` fails after the prep tx commits, the takeover effect (→ TakenOver + runner cancels) is durable but the `recovery_actions` row stays `pending` forever, and a same-key retry hits `IDEMPOTENCY_KEY_CONFLICT` (`resolveReplay` rejects `pending`) so it can never converge without operator action. Acknowledged design mirroring `RecoveryService.retry`. **Follow-up:** add a reconciliation sweeper for orphaned `pending` recovery_actions covering BOTH retry and takeover. [`DeveloperTakeoverService.java:285`]
- **`onResult` buckets a cancelled_for_takeover late result as `outcome=late_result`.** Conflates operator-cancelled rows with timed_out/orphaned stale results in `deliveryline.runner.completed.count`. **Follow-up:** add a distinct `cancelled_for_takeover` outcome tag (the counter itself is 3-19 infra). [`RunnerBroker.java:1154`]
- **getRunSummary event-fallback hard-codes `reviewerRole="developer"`.** When no `recovery_actions` takeover row exists but a → TakenOver event does, the fallback stamps `developer` regardless of the actual transition actor. Until 3.25 wires this orchestrator, real takeovers go through the dormant transition-only REST endpoint (no recovery row), so the fallback is the live path. Defensible under the AC2 takeover-invariant reading; app-internal field not surfaced yet (Trap T8). **Follow-up:** revisit when 3.25 wires the transition-only endpoint to the orchestrator. [`WorkflowInspectionService.java:885`]
- **MDC restoration asserted only on the happy path.** `takeoverStampsFullAuditContextInMdcAndRestoresAfter` proves a clean MDC after a successful call; no exception path (illegal-transition, completion-failure) asserts restoration. **Follow-up:** add a restoration assertion on a throwing path. [`DeveloperTakeoverServiceTest.java`]

## Deferred from: code review of story-3-24 (2026-06-16)

- **OpenAPI `reviewerRole` advertised optional while de-facto required.** The `RejectImplementationRequest` DTO mirrors `RejectSpecRequest` (`@Size(max=128)` without `@NotBlank`; role validated in-controller per R4), so springdoc emits `reviewerRole?: string` in `openapi.json` / `schema.d.ts` — yet any request omitting it always 400s (`INVALID_REVIEWER_ROLE_FOR_ENDPOINT`) at the controller boundary. The contract under-specifies a de-facto-mandatory field; generated clients can compile a request that always fails. Pre-existing convention shared by reject-spec/accept-implementation, not introduced here. **Follow-up:** revisit the optional-vs-required schema asymmetry API-wide if the Decision Bar client needs an accurate required-field contract. [`RejectImplementationRequest.java` / `openapi.json` / `schema.d.ts`]
- **CLI omitted required option yields a framework error, not a typed domain code.** `deliveryline reject-implementation` invoked without `--tagged-feedback` / `--reviewer-role` / `--reason-text` (all `required = true`) aborts with a Spring Shell "missing required option" error before any `DomainException` is built; REST returns a typed `INVALID_COMMAND_PAYLOAD` problem+json for the same omission. Pre-existing CLI/REST equivalence gap shared by all `required = true` Spring Shell commands. **Follow-up:** add CLI option bean-validation or a negative-parity test if strict required-field equivalence becomes a contract requirement. [`WorkflowCommands.java`]

## Deferred from: code review of story-3-23 (2026-06-15)

- **CLI vs REST first-error precedence diverges on multiply-invalid payloads.** REST `@Valid` on the request body surfaces `INVALID_COMMAND_PAYLOAD` (bad `artifactId`/version) before the in-body `requireDeveloperReviewerRole`; the CLI has no `@Argument`/`@Option` bean-validation, so reviewer-role is checked first → `INVALID_REVIEWER_ROLE_FOR_ENDPOINT`. For the same logically-bad payload the two surfaces report a different first-error code. The `CliRestEquivalenceContractTest` only tests the all-valid payload. **Follow-up:** add CLI command bean-validation or a CLI negative-parity test if strict first-error equivalence becomes a contract requirement. [`WorkflowCommands.java`]
- **Whitespace-padded `"  developer  "` acceptance (trim path) untested on both surfaces.** Both `requireDeveloperReviewerRole` copies do `reviewerRole.trim()` then `"developer".equals(...)`, so a padded value is accepted and the trimmed canonical is placed on the command — but no test pins this, and each surface owns its own `.trim()` copy (drift risk if the CLI later drops it). **Follow-up:** pin the trim-acceptance on at least one surface. [`AcceptImplementationEndpointContractTest.java`]
- **`reason` fingerprint-exclusion has no behavioral test.** No case proves two requests differing only in `reason` (same `Idempotency-Key`) replay idempotently rather than conflicting. A future `WorkflowCommandFingerprintFactory` regression appending `accept.reason()` would break the documented idempotency contract uncaught. **Follow-up:** add a same-key/different-reason replay test. [`WorkflowCommandFingerprintFactory`]
- **Multi-valued / duplicated `Idempotency-Key` and `X-Actor-Identity` rejection untested for the accept-implementation route.** The `rejectMultiValued*` guards are wired (shared static helpers, mirror approve-spec) but unpinned for this endpoint. **Follow-up:** add duplicate-header cases if route-level coverage is desired. [`AcceptImplementationEndpointContractTest.java`]
- **Unsafe `X-Actor-Identity` (`requireSafe` reject) untested for the accept-implementation route** on both REST and CLI (resolver wired with the real `requireSafe`, but every test sends a safe value). **Follow-up:** drive the reject branch with a control-char/over-length actor identity. [`WorkflowController.java` / `WorkflowCommands.java`]
- **`WORKFLOW_RUN_TERMINAL` (409) documented but untested.** Listed in `@ApiResponses` + the OpenAPI snapshot for accept-implementation, but only `ILLEGAL_TRANSITION` has a contract test. **Follow-up:** add a terminal-run acceptance case. [`AcceptImplementationEndpointContractTest.java`]

## Deferred from: code review of story-3-17b (2026-06-15)

- **`contextBundleVersion` minted at enqueue, consumed at worker-dispatch — narrow idempotency-fingerprint drift.** The synchronous path computed the version immediately before reserving; the queue freezes it at enqueue and the worker reserves/dispatches against the frozen value (`RunnerBroker.dispatchFingerprint(workflowRunId, stage, contextBundleVersion)` at `RunnerBroker.java:767`). If a row sits queued while another same-run execution bumps the version sequence, the worker reserves against a now-stale version, which can change replay-vs-conflict classification compared to the synchronous semantics. Narrow (needs interleaved same-run dispatches); the reserve-at-worker choice was deliberate. **Follow-up:** revisit if batch submission (story 3.18) or takeover (3.22) makes interleaved same-run dispatches common.
- **`enqueue` rejects a blank `idempotencyKey` with an unchecked `IllegalArgumentException`.** `RunnerExecutionQueue.enqueue` (`:114`) now throws `IllegalArgumentException` (not a typed `DomainException`) on a blank key, surfacing as a 500 rather than a retryable typed error and rolling back the submit/approve tx. Theoretical today — all 7 production callers pass a non-blank derived key and 3.17a was dormant (no legacy null-key rows). **Follow-up:** map to a typed error if a future caller can derive an empty key.

## Deferred from: code review of story-3-27 (2026-06-14)

- **Unified-diff parser has no input-size bound before fully parsing untrusted diff.** `parseUnifiedDiff` does `diff.split('\n')` over the entire untrusted diff string and accumulates one object per line; the `PR_DIFF_MAX_FILES` (50) / `PR_DIFF_MAX_LINES` (5000) caps are applied at render time only, so a multi-megabyte runner diff is fully parsed into memory regardless. **Follow-up:** add a parse-time size/line cap (truncate-with-note) when a live `prOutput` read model is wired — today the renderer is presentational/fixture-driven with no live diff source, so impact is bounded. [`deliveryline-frontend/src/lib/sanitization/unifiedDiff.ts:99-122`]

## Deferred from: code review of story-3-21 (2026-06-14)

- **Artifact-type guard excludes only `SPEC`; re-dispatch else-branch defaults to PR_OUTPUT with no allowlist.** `TechnicalApprovalService.rejectImplementation` rejects only `ArtifactType.SPEC` (line 354), and the re-dispatch branches `if (artifactType == IMPLEMENTATION_PLAN) retryPlanGeneration else retryImplementation` (line 498). Currently safe — `ArtifactType` has exactly 3 values (`SPEC`, `IMPLEMENTATION_PLAN`, `PR_OUTPUT`) — and it mirrors the sibling `acceptImplementation` pattern. **Follow-up:** if a 4th `ArtifactType` is ever added, it would pass the SPEC-only guard and be silently treated as a `prOutput` rejection (re-dispatching the implementation runner). Harden with an explicit `!= IMPLEMENTATION_PLAN && != PR_OUTPUT → throw` allowlist guard plus an `else throw illegalState` on the re-dispatch branch. [`deliveryline-backend/src/main/java/org/dradgo/application/approval/TechnicalApprovalService.java:354,498`]

## Deferred from: code review of story-3-20 (2026-06-14)

- **PR-link gate is a self-match tautology — true artifact-vs-link PR-ref comparison not enforced.** `TechnicalApprovalService.assertPrLinkPresentAndMatches` resolves `IntegrationLinkService.findActiveGitHubPrLink(runId).externalRef()` and passes that same ref into `assertArtifactPrLinkMatches(runId, sameRef)`, so the comparison is against itself: it enforces only that an active `github_pr` link *exists* (fail-closed when absent), never that the prOutput artifact's claimed PR ref matches the link. This is the Alex-confirmed OQ-1 pilot fallback (the artifact's true PR ref lives only in its payload JSON; no clean read port exists yet). **Follow-up:** when an artifact-prRef read port is available, tighten the gate to a true artifact-vs-link comparison against the canonical `owner/repo#number` form (see [[proutput-prref-validator-rejects-real-adapter]]).

## Deferred from: code review of story-3-26 (2026-06-14)

- **Placeholder controls are focusable no-op affordances** — the internal context-ref placeholder (`<button>` with title only, no `onClick`/`aria-disabled`) and the revision-history anchor (`aria-disabled` but still focusable, no handler) are keyboard-focusable controls that do nothing. Intentional OQ-4/D5 placeholders mirroring `SpecArtifactRenderer`. When D5 wires live in-app deep-links, also run the internal href through `validateUrlScheme` (internal refs emit no href today, so currently safe). [`deliveryline-frontend/src/features/workflows/components/ImplementationPlanArtifactRenderer.tsx:95,154`]
- **External context-ref with a relative/scheme-less href becomes a `target="_blank"` "external" link** — `validateUrlScheme` returns `ok:true` for scheme-less relative input (same-origin), so an `internal:false` ref with `href:"/foo"` renders `<a href="/foo" target="_blank" rel="noopener noreferrer">`. Latent — no live `contextReferences` source yet; fixtures use absolute URLs. Add an absolute-URL expectation when the live artifact-read mapping lands. [`deliveryline-frontend/src/features/workflows/components/ImplementationPlanArtifactRenderer.tsx:80,103`]
- **Empty/whitespace-only `summary`/`label`/`estimatedComplexity` render empty controls** — the guard accepts `''` (`typeof === 'string'`) and the renderer branches on `!== undefined`, so an empty `estimatedComplexity` shows a "Complexity" label with a blank badge, and empty summary/label produce label-less accordion triggers / empty interactive elements. Robustness nit; harden when a live wire source feeds these. [`deliveryline-frontend/src/features/workflows/components/ImplementationPlanArtifactRenderer.tsx:198,202`]

## Deferred from: code review of story-2-29 (2026-06-13)

- **Dual lifecycle representation never bridged** — the headline 3-pill indicator (`submitted › accepted › incorporated`, the literal `LIFECYCLE_STAGES`) and the per-item/Mermaid set (`Open → Answered → Accepted → Incorporated`) are both faithful to the live UI but a first-run PM must reconcile "Answered = first dot (submitted)" unaided; one bridging sentence would help. Clarity polish only. [`docs/pm-loop-walkthrough.md:135,154-163`]
- **Superseded "Safe to approve?" cell is a non-answer** — reads "See the note shown" while every other row gives Yes/No; could be strengthened to "No — check for a follow-up first". Wording, not incorrect. [`docs/pm-loop-walkthrough.md:148`]
- **ASCII mockup borders ragged** — right-edge `│` characters don't align in the tri-pane and queue `text` blocks; cosmetic. [`docs/pm-loop-walkthrough.md:51-61,77-83`]
- **Role note "anyone can perform any action" vs the two state gates** — the unqualified RBAC-disclaimer sits next to two documented state-machine refusals (blocked-approve, out-of-date); a clause distinguishing permission-gating from state-gating would prevent a double-take. [`docs/pm-loop-walkthrough.md:283-287`]

## Deferred from: re-review of story-2-27 (2026-06-11)

- **terminalState→currentState mapping makes the happy-path run report `Completed`** — `summary`/`detail` map `workflowRun.terminalState` straight to `currentState`; the happy-path fixture is `Completed`, so `allowedActions` returns `[]` and the approval bar renders structurally `blocked`. Reactivating the quarantined J1/J2 approve/reject steps (AC8) will require driving a NON-terminal run/fixture, not just shipping the 2.18/2.19 read-seams the quarantine rationale cites. [`deliveryline-frontend/e2e/support/mockApi.ts:54`, `src/test/handlers.ts:53`]
- **Playwright `--project=X` matching zero specs exits 0** — a project-name typo in the CI matrix makes a browser leg pass with zero tests; no leg asserts its browser binary actually launched. Silent cross-browser coverage loss. [`.github/workflows/ci.yml` frontend-e2e matrix]
- **`mobile-viewport` asserts only horizontal bounds** — a sticky decision bar pushed below the fold (vertical off-viewport) still passes. Add a `box.y + box.height ≤ viewport.height` in-viewport check. [`deliveryline-frontend/e2e/mobile-viewport.spec.ts`]
- **`mockApi` casts fixtures `as unknown as EventStream`** — the Playwright mock is untyped against the generated OpenAPI schema, so AC2's no-mock/prod-drift guarantee holds for the Vitest `handlers.ts` but NOT the E2E layer. Documented loader constraint (can't import the schema-typed handlers under Playwright). [`deliveryline-frontend/e2e/support/mockApi.ts:34`]
- **`useArtifact`/`useClarifications` "queryFn never runs" asserted by `fetchStatus==='idle'` + comment, not a spy** — a queryFn spy would prove the disabled-query contract directly. [`deliveryline-frontend/src/features/workflows/hooks/useArtifact.test.tsx`, `useClarifications.test.tsx`]
- **`routeCoverage` exercises only `state=Executing`** — the empty/unknown filtered-queue path is unasserted. [`deliveryline-frontend/src/features/workflows/__tests__/routeCoverage.integration.test.tsx`]
- **No uniqueness assertion on fixture `workflowRun.publicId` across STREAMS** — a duplicate id silently first-wins in `streamByRunId`/`eventStreamByRunId`. Add a load-time uniqueness assert. [`deliveryline-frontend/src/test/handlers.ts`, `e2e/support/mockApi.ts`, `src/test/fixtures/event-streams/index.ts`]
- **`tabUntilFocused` budget off-by-one / no focus-loop-wrap detection** — a >max-stop reachable control false-fails; the final Tab fires after the in-loop focus check. Low. [`deliveryline-frontend/e2e/keyboard-only.spec.ts`]

## Deferred from: code review of story-2-27 (2026-06-11)

- **No global coverage floor — only 3 per-path globs thresholded** — `src/lib/sanitization/**`, `src/lib/queryKeys/**`, `src/features/workflows/**` have floors; every other shipped path (`src/lib/api/**`, route files, etc.) can regress to 0% without redding CI. Matches the S4 spec intent (per-path was the design), but the "coverage thresholds enforced (AC10)" claim only covers three directories. Add a modest global floor when hardening. [`deliveryline-frontend/vitest.config.ts`]
- **Glob-keyed thresholds silently pass if a glob matches zero files** — a future rename (e.g. `src/lib/sanitization/` → `src/security/`) makes the threshold key bind to nothing and the floor evaporates with no CI signal. Add a meta-test asserting each threshold glob matches ≥1 instrumented file. [`deliveryline-frontend/vitest.config.ts`]
- **Fixture `runner.started` events carry a `failedStage` in `details`** — `evt_fix_happy_010` / `evt_fix_fail_009` record a `failedStage: "implementation"` on a *started* event (paste from a `runner.failed` event). Backend-owned (story 1.23 originals); 2.27 must not touch backend, and the drift gate locks the frontend copies to the backend. Fix in the backend fixture set, then re-vendor. [`deliveryline-frontend/src/test/fixtures/event-streams/happy-path-success.json`, `…/execution-failure-with-retry.json`]
- **`useWorkflowEvents` success test asserts positional `.at(-1)?.resultingState === 'Completed'`** — couples the assertion to the happy-path fixture ending on a `workflow.stateChanged`; a re-vendor that appends a non-state trailing event makes it silently read `null` and fail without a fixture-drift diagnostic. Assert the specific `workflow.stateChanged → Completed` event instead. [`deliveryline-frontend/src/features/workflows/hooks/useWorkflowEvents.test.tsx:1232`]
- **Shared `webServer` + chromium leg invoking `test:e2e` twice against `--strictPort 4173`** — desktop-chromium then mobile run as two serial process invocations, each spawning its own `vite preview --strictPort`; relies on full teardown before the second binds. Works in practice (sequential); low risk. Combine the mobile project into one invocation if tightening. [`deliveryline-frontend/playwright.config.ts`]
- **`summaryFromStream`/`detailFromStream` derive `currentState` from `terminalState` for the non-terminal clarification streams** — the two `clarification-*` streams carry `terminalState: "WaitingForSpecApproval"`; the `?? 'Inbox'` only guards `null`, so a non-terminal value flows through. Low confidence — `WaitingForSpecApproval` is plausibly the correct reported state for a run paused on a clarification. Revisit if a consumer asserts state↔last-event coherence. [`deliveryline-frontend/src/test/handlers.ts:2820`]

## Deferred from: code review of story-3-15 (2026-06-10)

- **V1 non-partial unique `(integration_type, external_ref, workflow_run_id)` collides when a retry returns to an earlier (now-superseded) PR ref** — active-row queries exclude `superseded`, so the same-ref idempotent no-op misses a superseded row and `insert` hits the constraint (`#42 → #43 → #42`). Documented constraint ([[integration-link-adapter-tx-and-supersede]] + the IT Debug-Log note). Swallowed as WARN on the broker path; surfaces untyped (`DataIntegrityViolationException`) to a future direct (OQ-1) caller. Fix when a direct caller lands: query same-run-same-ref including superseded and replay/reactivate, or make the V1 index partial. [`IntegrationLinkService.java:430-462`]
- **`findActiveByTypeAndWorkflowRunForUpdate` returns `Optional` with `order by` but no `LIMIT`/`findFirst`** — `@Query` ignores the name-based limit, so ≥2 active rows for `(type, run)` throw `NonUniqueResultException` instead of returning the earliest. Latent; guarded in practice by the supersede invariant + pessimistic lock + V6 unique index keeping active rows ≤1. Mirrors the pre-existing `findFirstActiveByWorkflowRunPublicId` pattern. Add `limit 1` if a defense-in-depth tightening is wanted. [`IntegrationLinkRepository.java:54-67`]
- **Broker swallows cross-run conflict / repo-mismatch with no durable signal** — spec-mandated best-effort (Task 7): a genuinely mislinked PR (`INTEGRATION_LINK_CONFLICT`) is logged WARN and the run still advances to `WaitingForReview` with the `integration_links` row absent; no event/status surfaces the missed linkage. Revisit surfacing an event/status when the live trigger (story 3.20) makes the seam reachable in prod. [`RunnerBroker.java:1359-1370`]
- **PR-not-found / transient adapter-failure map to `INTEGRATION_LINK_CONFLICT` (HTTP 409, non-retryable)** — mirrors the existing Linear `adapterFailure` precedent, but a transient rate-limit/5xx surfaced as a non-retryable 409 (and "not found" as a "conflict") is a debatable typed surface for a future direct REST caller. Broker path swallows it today. Reconcile the error-code semantics when a non-broker `linkGitHubPr`/`syncGitHubPr` caller is added. [`IntegrationLinkService.java:848-879`]
- **`assertArtifactPrLinkMatches` is non-`@Transactional` yet calls the `PESSIMISTIC_WRITE` (`ForUpdate`) read** — a read-only drift guard should not take a write lock and needs an ambient tx (pessimistic locking outside a tx is a no-op / `TransactionRequiredException` risk). Switch to a non-locking by-type-and-run read + ensure an ambient tx when wiring the guard at prOutput-approval (story 3.20). [`IntegrationLinkService.java:565-572`]

## Deferred from: code review of story-3-11 (2026-06-09)

- **In-flight plan guard does not distinguish EXECUTION sub-stages** — `inFlightPlanDispatch` returns the first `stage()==EXECUTION` active row regardless of sub-stage, so once story 3.12 adds pr-output dispatch an in-flight pr-output execution would make a legitimate plan dispatch a silent no-op (and vice versa). Not triggerable in 3.11 (PR_OUTPUT has no production originator). Harden the guard (sub-stage aware) when 3.12 lands. [`deliveryline-backend/src/main/java/org/dradgo/application/workflow/WorkflowOrchestrationService.java:~428`]
- **`dispatchPlanInternal` has no `EXECUTING` precondition guard** — unlike `dispatchSpecGeneration`'s `ensureInvestigating`, it reads the run only for logging then dispatches an EXECUTION runner unconditionally (T1 forbids transitioning). Safe today (only `approveSpec` calls it, right after transitioning to Executing; `retryPlanGeneration` has no production caller). Add a precondition guard when `retryPlanGeneration` gets a recovery/CLI caller (OQ-5). [`WorkflowOrchestrationService.java:~266`]
- **`PLAN_READY_OR_BEYOND` omits `EXECUTING` (asymmetric with `SPEC_READY_OR_BEYOND`)** — minimal `{WaitingForReview, Completed}` set; a late/duplicate plan-ready replay arriving while the run is legitimately back in `EXECUTING` would log WARN-anomaly instead of INFO-replay. Currently correct (no plan-stage path returns to Executing until the 3.20 reject loop). Revisit with story 3.20. [`WorkflowOrchestrationService.java:~194`]
- **PR_OUTPUT sub-stage success is an absorbing state** — the broker `else` branch ingests + completes the execution but drives no transition, leaving the run in `EXECUTING` with no auto-advance/failure/timeout. Documented 3.12 deferral; unreachable in 3.11's live scope (plan approval, story 3.20, is deferred so `deriveExecutionSubStage` always returns IMPLEMENTATION_PLAN in prod). Resolved by story 3.12. [`RunnerBroker.java:~1031`]
- **No plan-stage-specific failure / git-push-failure / correlationId integration test** — AC4/AC11 plan-stage failure→`Failed` and correlationId propagation are verified by reuse of pre-existing broker tests + the unit tier, not a new plan-flavored test (spec-sanctioned scoping, mirrors `SpecStageOrchestrationIT`). Optional coverage enhancement. [`deliveryline-backend/src/test/java/org/dradgo/application/workflow/ImplementationPlanOrchestrationIT.java`]

## Deferred from: code review of story-3a-5 (2026-06-08)

- **Submit / pre-check failures are absent from the batch-summary counters (no `failed` bucket)** — on a caught `DomainException`/`RuntimeException` from `submit` (or, once patched, the active-link pre-check), none of `ingested`/`autoIngestSkipped`/`ineligible` increments, so the `polling_batch` summary under-counts the eligible-and-attempted population. AC6 does not mandate a failure counter and the failures are already logged per-ticket at WARN (`auto_ingest_failed`), so the aggregate gap is observability-only. Optional hardening: add an `autoIngestFailed` counter and append it to the enabled-branch batch summary so the buckets are exhaustive over eligible tickets. [`deliveryline-backend/src/main/java/org/dradgo/infrastructure/config/LinearPollingHost.java:269-297`]
- **`auto_ingest_no_status_ids` WARN repeats every poll interval on a stuck cursor** — the AC2 fail-safe WARN is emitted once per poll batch (spec-compliant), but when a touch failure preserves the cursor (`touchFailures > 0` ⇒ `lastPollAt` not advanced) the same window is re-polled every interval, so a persistently-misconfigured + flaky-touch deployment emits the WARN unboundedly rather than once per distinct batch of new tickets. Spec-compliant; consider throttling/dedup if log volume becomes a problem in practice. [`deliveryline-backend/src/main/java/org/dradgo/infrastructure/config/LinearPollingHost.java:202-209`]
- **Partial Linear state (`name` present, `id` missing/blank) → ticket silently ineligible, diagnosable only at DEBUG** — `textOrNull` maps a missing/blank `state.id` to `null`, and `isEligible` treats a null `statusId` as ineligible (correct per T-STATUS-NULL). But a malformed/partial response that returns `state.name` without `state.id` makes a genuinely-active ticket indistinguishable at INFO/WARN from a backlog ticket — it is never ingested and the only trace is the DEBUG `auto_ingest_ineligible` line (which does carry both `statusName` and a null `statusId`). Rare; optional: emit a non-DEBUG signal when `status != null && statusId == null` (likely a query/response defect). [`deliveryline-backend/src/main/java/org/dradgo/adapters/integration/linear/LinearRealAdapter.java` state mapping + `LinearPollingHost.isEligible:326-331`]
- **Review-diff scope hygiene: shared `application.yml` / `.env.example` carry unrelated working-tree drift** — the 3a-5 review diff (scoped by the story File List) swept in non-3a-5 uncommitted changes that happen to live in the same shared resource files: `frontend.fail-on-missing-bundle`, an `integration.repos` rewording, and a `linear.api-token` entry in `application.yml`, plus unrelated `.env.example` lines. Not a code defect — a commit-hygiene caveat: when committing 3a-5, stage only the auto-ingest placeholder lines (hunk-level `git add -p`), not the co-located drift from other in-flight stories. [`deliveryline-backend/src/main/resources/application.yml`, `.env.example`]

## Deferred from: code review of story-3a-4 (2026-06-05)

- **No live-schema verification of the `project.id.eq` poll filter** — `team.key.eq` is byte-identical to the already-working `fetch-ticket-by-reference.graphql`, but `project.id.eq` is a net-new `IssueFilter` shape exercised only by `MockRestServiceServer`, which returns canned JSON and never validates the request against the real Linear GraphQL schema. The spec deliberately scoped out the Docker/integration tier (T-GATES, AC8). Smoke-test project scoping against a real Linear workspace before relying on it in production. [`deliveryline-backend/src/main/resources/graphql/linear/poll-tickets-since.graphql`, `LinearRealAdapter.buildPollFilter`]
- **Unit tests don't assert the loaded `.graphql` document matches the `$filter` variable** — `MockRestServiceServer` asserts the outbound `variables.filter.*` JSON but never parses the GraphQL query string, so a future drift between `poll-tickets-since.graphql` (`$filter: IssueFilter!`) and the variable map sent by the adapter would stay green locally and fail only at the Linear server. Optional hardening: add a body matcher asserting the request `query` contains `$filter: IssueFilter!`. [`deliveryline-backend/src/test/java/org/dradgo/adapters/integration/linear/LinearRealAdapterUnitTest.java`]

## Deferred from: code review of story-2.19 (2026-06-05)

- **(was decision-needed) Full UI-side stale-detection wiring** — `isStaleAgainst` + the `localUi.stale` field exist and are tested, but the container never computes `stale` from a refetch-compare source and the mutation handlers don't short-circuit against a known-stale stamp. **Deferred (Alex, Option 2):** the contextBundle-comparison correctness fix in `isStaleAgainst` is patched now; the proactive wiring (poll/focus refetch of the version stamp + mutation short-circuit) is a follow-up, since the 409 `APPROVAL_VERSION_MISMATCH` path already covers stale live and no refetch-compare source exists in the container yet. [`deliveryline-frontend/src/features/workflows/components/ApprovalDecisionBarContainer.tsx:109-111,113-164`]
- **Rejection dialog has no focus trap despite `aria-modal="true"`** — Tab can leave the region-local rejection dialog (no trap). Escape-inert is INTENTIONAL (AC8 — non-dismissible except explicit Cancel), and focus-in-on-open + focus-restore-on-close are implemented. Deferred to story 2.25 (no axe harness; full WCAG AA/focus-containment is 2.25 scope, OQ-4). [`deliveryline-frontend/src/features/workflows/components/ApprovalDecisionBar.tsx:141-150`]
- **Decision timestamp is a raw client-clock ISO string** — `decidedAt = new Date().toISOString()` rendered verbatim in the summary. `WorkflowStateChangeResponse` carries no server timestamp, so the client clock is the only source; relative/locale formatting (the 2.16 pattern) is polish. Deferred. [`ApprovalDecisionBarContainer.tsx:126`, `ApprovalDecisionBar.tsx:88`]
- **Untrusted actor identity rendered as markdown source, not plain text** — `buildDecisionContextLabel` concatenates `by ${actor}` into the string fed to `SafeMarkdownRenderer`, so a markdown-control-char actor (`[x](javascript:…)`, `*`, backticks) is interpreted as markdown within a trusted-looking line. The story explicitly sanctioned the sanitization barrel for this text; the real link/HTML policy lives in `SafeMarkdownRenderer` (out of this diff). Deferred to a 2.25 security pass — verify the renderer's link policy or render the actor as a plain text node. [`approvalDecisionView.ts:369-378`, `ApprovalDecisionBar.tsx:94,411`]
- **`currentState` empty-string fallback flows downstream** — `detail?.currentState ?? versionStamp?.workflowState ?? ''`; when both are absent `''` flows into the decision label/summary rather than being treated as not-ready. Low impact. Deferred. [`ApprovalDecisionBarContainer.tsx:81`]

## Deferred from: code review of story-2.18 (2026-06-04)

_All four are on the DORMANT path (no clarification-read endpoint yet); the live state today is only `no open questions`._

- **First-render lifecycle transitions never announced by the ARIA live region** — `useLifecycleAnnouncements` only announces on a `prior !== undefined && prior !== status` change, so a region that MOUNTS already populated (e.g. when the read endpoint ships and returns `answered`/`accepted` items) emits nothing to assistive tech for those states. Deferred: dormant until the read endpoint, and announce-everything-on-mount is itself a debatable a11y choice (chatty live region). [`deliveryline-frontend/src/features/workflows/components/ClarificationRegion.tsx ~1312`]
- **Only the last-in-array clarification is announced/logged when several advance in one render** — single `latest` slot (no queue, no timestamp comparison), so simultaneous lifecycle advances coalesce to one announcement + one `clarification.lifecycleAdvance` log. Deferred: dormant, low impact. [`ClarificationRegion.tsx ~1319`]
- **Off-chain (`superseded`/`rejected_invalid`) not visibly highlighted in the lifecycle indicator** — these resolve to `currentIndex:0, offChain:true`, so the indicator renders `submitted` as reached and no stage current (visually near-identical to a fresh `answered`); the off-chain state is conveyed only by the separate `NoEffectReason` callout and a `data-lifecycle-offchain` attribute, despite the indicator's doc-comment claiming a muted off-chain marker. Deferred: cosmetic, dormant. [`ClarificationRegion.tsx ~1007`]
- **`CompactSummary` CTA absent for an `unknown`-only view** — `firstActionable = grouped.open[0] ?? grouped.pending[0]` excludes `grouped.unknown`, so a compact view of only `unknown` sentinels shows "N pending" with no drill-in affordance. Deferred: dormant edge. [`ClarificationRegion.tsx ~1283`]

## Deferred from: code review of story-3a-3 (2026-06-03)

- **No CI test covers the entrypoint subscription branch or real-CLI `auth.json` consumption** — the conformance IT (`CodexRunnerImageConformanceIT.subscriptionAuthMaterializesAuthJsonWithoutLeakingTheValue`) runs the mock Codex CLI, which ignores env + auth files, so it pins only the in-scope materialize-log-line + negative-leak guarantees. The entrypoint's subscription-branch behaviors (skip `OPENAI_API_KEY` export, `cleanup()` rm of `$CODEX_HOME/auth.json`) and the real Codex CLI reading `$CODEX_HOME/auth.json` were verified only via local git-bash smoke. Deferred: real subscription execution + token refresh are explicitly story 3.8 scope (locked design decision #3); the dev already recommends a WSL2/Docker CI confirm before merge. [`CodexRunnerImageConformanceIT.java`, `runners/codex/entrypoint.sh`]

## Deferred from: code review of story-3a-2 (2026-06-03)

- **(was decision-needed, MED) Manifest/README detection silently bounded by the depth-2 tree cap** — `RepositoryWorkspaceService.summarize` runs `detectReadme`/`detectManifests` over the depth-2-truncated tree, so a manifest/README deeper than 2 path segments (e.g. `modules/foo/pom.xml`, `src/main/resources/...`) is collapsed to a `DIR` entry and never detected → `packageManifestRefs`/`repositoryReadmeRef` systematically incomplete for monorepos. **Deferred (Alex): pilot scope** — acceptable for the 1:1 root-manifest pilot; nested-manifest detection (e.g. an unbounded `git ls-files`-style scan independent of the embedded-tree depth bound, T9 still capping only the embedded tree) lands with the full Linear↔GitHub mapping work, stories 3.32/3.33. [`RepositoryWorkspaceService.java:347,479,531,552`]
- **Empty/unborn-HEAD repo fails the spec dispatch with a mislabeled category** — `CliGitAdapter.listTopLevelTree` throws `git ls-tree HEAD` failure as `IntegrationFailureCategory.GIT_NETWORK_FAILURE`, but an unborn HEAD (zero-commit repo) is a local read failure, not network; it also fails the whole INVESTIGATION dispatch rather than yielding an empty tree. Pilot repos always have commits; no `GIT_LOCAL` category exists (adding one is a three-sites fan-out). [`CliGitAdapter.java:31-34`]
- **Embedded-tree cap vs full-tree detection can produce refs outside the embedded summary** — when `fullTree > TREE_ENTRY_CAP (200)`, a README/manifest sorted past index 200 is emitted as `repositoryReadmeRef`/`packageManifestRefs` while being absent from the capped `repositoryTreeSummary`. By Decision D4 the refs are read off the `/workspace/repo` mount independent of the embedded tree, so the contract does not require co-occurrence — flagged as a consumer-correlation sharp edge. [`RepositoryWorkspaceService.java:479-486`]
- **Multi-repo config silently disables repo-context** — `resolveConfiguredRepositoryRef` returns empty for `repos.size() != 1`, so a deployment that adds a second repo to `deliveryline.workflow.repos` disables spec repo-context for ALL runs with only an INFO `reason=no_repo_resolved` log. Documented D2 1:1 pilot assumption; consider a WARN when >1 repo is configured. [`RepositoryWorkspaceService.java:515`, `RunnerBroker.java:413-417`]
- **Adapter materializes the full tree before any cap** — `listTopLevelTree` builds a `TreeMap` of every truncated ancestor for every tracked file with no early exit; the `TREE_ENTRY_CAP` is applied only later in `summarize`. O(tracked-files) allocation in the dispatch hot path; mitigated by the depth-2 collapse (bounded by distinct depth-2 prefixes). [`CliGitAdapter.java:43-67`]
- **`GitCommandException` cause not chained into the remapped `DomainException`** — the broker reuses `INTERNAL_ERROR` and carries `gitFailureCategory` in `details` but drops the underlying git exception as the `cause`, losing its stack for debugging. [`RunnerBroker.java:464`]
- **Schema has no `maxItems` on `repositoryTreeSummary`** — the 200-entry bound lives only in `RepositoryWorkspaceService.summarize`, not in `context-bundle.v1.schema.json`, so a future caller bypassing `summarize` (or a cap change) could emit an unbounded array that still validates. Deferred because adding `maxItems` would couple the contract to the app cap. [`context-bundle.v1.schema.json:117`]

## Deferred from: code review of story-3a-1 (2026-06-02)

- **(was decision-needed, HIGH) Spec artifact never reaches `available`; auto-advance fires on a `pending` artifact** — `RunnerBroker.handleSuccess` ingests the spec artifact via `recordOperation` but nothing in production calls `ArtifactOperationService.markAvailable`, so the run advances `Investigating→WaitingForSpecApproval` on COMPLETED ingest while the artifact is still `pending`. AC2's "→ markAvailable" and AC3's "becomes available" precondition are not enforced. **Deferred (Alex): documented KNOWN GAP; full availability needs out-of-scope checksum/storageRef artifact plumbing — accepted as tech debt for a follow-up.** (memory: markavailable-has-no-production-caller) [`RunnerBroker.handleSuccess`, `WorkflowOrchestrationService.onSpecStageSucceeded`]
- **(was decision-needed, HIGH) Synchronous runner dispatch runs inside the submit/reject DB transaction** — `dispatchSpecGeneration`/`retrySpecGeneration` → `RunnerBroker.dispatch` (PROPAGATION_REQUIRED) calls `runnerAdapter.dispatch` (Docker create+start container) while the `submit`/`rejectSpec` `@Transactional` is open; a rollback after the container starts orphans a running container with no `runner_executions` row, and the DB connection is held across Docker daemon I/O. **Deferred (Alex): deliberate atomic-rollback design (run + row commit/roll-back together); orphan-container / connection-hold risk rides on the pending architect ratification of the auto-dispatch=true deviation.** [`WorkflowCommandService.submitInternal`, `ApprovalService.rejectSpec`, `RunnerBroker.dispatch`]
- **Originating `correlationId` not propagated across the async poller boundary** — `RunnerBroker.resolveOutcomeCorrelationId` prefers the MDC correlationId but the `@Scheduled` poller thread carries none, so it falls back to a deterministic `rex-<runnerExecutionId>` (an improvement over the prior random UUID). The late-result harvest path still mints a random UUID. AC7's end-to-end propagation is partially met; full propagation requires persisting the correlationId on the runner-execution row — a migration this story forbids. [`RunnerBroker.java:590-595`, `resolveOutcomeCorrelationId`]
- **AC11 failure-mode / type-mismatch / correlationId coverage lives at the unit tier, not the integration tier** — `SpecStageOrchestrationIntegrationTest` covers happy / in-flight-idempotent / reject-redispatch; AC4 failure→Failed, AC8 type-mismatch, and correlationId propagation are delegated to `WorkflowTransitionTableTest`, `RunnerBrokerUnitTest`, `WorkflowOrchestrationServiceTest`. Behavior is covered; adding `@SpringBootTest`-tier cases is the literal-AC11 follow-up. [`SpecStageOrchestrationIT`]
- **AC9 auto-advance ArchUnit rule is scoped to success only and guards the callback, not the transition** — `ONLY_ORCHESTRATION_AUTO_ADVANCES_ON_SPEC_RUNNER_SUCCESS` forbids non-broker/orchestration calls to `onSpecStageSucceeded`, but (1) the failure path still mutates state via the broker's `driveWorkflowFailed`, and (2) a service could drive the `spec_ready` transition directly without tripping this rule (only the broader `ONLY_WORKFLOW_TRANSITION_SERVICE_MAY_MUTATE_WORKFLOW_STATE` would). Intentional OQ-2 minimal choice; AC9 invariant is materially weaker than the AC text — hardening opportunity. [`ArchitectureRuleCatalog.java`]
- **In-flight dispatch guard is read-then-act** — `inFlightSpecDispatch` queries for PENDING/RUNNING then dispatches with no row lock or unique constraint, so concurrent invocations could both pass and double-dispatch. Needs locking/constraint (migration-forbidden); not reachable from current callers (submit is idempotency-guarded, rejectSpec is MANDATORY-tx, recovery retry is unwired). [`WorkflowOrchestrationService.java`, `inFlightSpecDispatch`]
- **`ensureInvestigating` uses a run-fixed idempotency key** — `"spec-investigating:" + workflowRunId` carries no source-state or loop-count discriminator. Safe today (only `submitInternal` calls it once on a fresh Inbox run), but the table also legalizes `WaitingForSpecApproval→Investigating` and `Failed→Investigating`; a future caller routing those through this method would reuse the key for a semantically different transition. Latent. [`WorkflowOrchestrationService.java`, `ensureInvestigating`]
- **AC5 CLI/recovery `retry` baseline not wired** — only `ApprovalService.rejectSpec` invokes `retrySpecGeneration`; the story-1.18 CLI/recovery `retry` path for a `Failed` investigation-stage run is not wired. Intentional OQ-3 deferral; minor Task 5(c) traceability note (the "rejectSpec-only" half was done). [`ApprovalService.java:452`]
- **`MockRunnerAdapter.writeReferencedArtifactContents` catches only `IOException`** — a HAPPY fixture whose `contentReference` escapes `deliveryline.home` makes `writeArtifactContent` throw `DomainException(ARTIFACT_INVALID_FILENAME)`, which is not caught, so the mock dispatch fails instead of best-effort skipping. Test-infra only, controlled fixtures, not production-reachable. [`MockRunnerAdapter.java:91-118`]

## Deferred from: code review of story-3.4 (2026-05-31)

- **Real Claude execution still conflicts with the inherited `--network=none` runner contract** — Story 3.4 inherits the DockerRunnerAdapter/file-based contract that launches runners with `--network=none`, while the production Claude Code CLI path is expected to call Anthropic/Claude subscription services. Mock conformance can pass, but real API/subscription execution remains a story 3.8 / runner-network architecture decision rather than a safe patch inside this image-only story. [`runners/RUNNER_CONTRACT.md:26`, `runners/claude/entrypoint.sh:331`]

## Deferred from: code review of story-2.20 (2026-05-31)

- **`error` precedence blanks already-loaded rows on a background-refetch failure** — `resolveQueueState` returns `error` whenever `isError`, even when `query.data` still holds a prior successful list. Spec-correct for 2.20 (no refetch trigger ships yet — no filter control, first-load dominant). Revisit when story 2.15 (rows) + a filter control land: branch `isError && count > 0` to keep stale rows with an inline error instead of the full-page error state. [`deliveryline-frontend/src/features/workflows/queueState.ts:45`]
- **Clear-filters route-mount integration test deferred (OQ-1)** — the `navigate({search:{}})` route wiring is type-checked and the `onClearFilters` callback is unit-tested, but the AC4/AC10 "resets search and re-queries" path is not exercised end-to-end. Lands with story 2.15's real-row queue integration test. [`deliveryline-frontend/src/routes/workflows/index.tsx`]
- **Cold-load skeleton may be silent to screen readers (→ story 2.25)** — the only visible loading content is `aria-hidden`, and a polite live region is generally not announced for text already present at first mount (the common entry path), so SR users may get no "loading" cue on a cold load. AC8 baseline gap; full axe/WCAG sweep is story 2.25 (same owner as the `<ErrorState>` ARIA defer below). [`deliveryline-frontend/src/features/workflows/QueueShell.tsx:182-197`]
- **`filtersActive` defined twice with divergent predicates** — the route's `validateSearch` decides filter presence by key-presence (returns `{}` or `{state}`) while the shell's `hasActiveFilters` scans values (`Object.values(...).some(non-empty)`). They agree today (only a non-empty `state` survives validation) but will drift into a Trap-T6 misclassification once a non-string or multi-key filter is added. Consolidate onto one predicate when the filter control ships. [`deliveryline-frontend/src/features/workflows/QueueShell.tsx:53-57`]

## Deferred from: code review of story-2.22 (2026-05-30)

- **`<ErrorState>` ARIA role/live-region semantics (→ story 2.25)** — `role="alert"` + `aria-live="polite"` is contradictory for `passive` urgency, and the audit-only `permissionRestricted` variant is announced via `role="alert"` (vs Trap T7 "not a hard failure"). Current behavior matches literal AC9.b and is test-pinned. Deferred to the dedicated WCAG/axe sweep in story 2.25, which owns role/live-region refinement; fix candidate: `role="status"`/`role="region"` for passive + neutral, reserve `role="alert"` for active. [`deliveryline-frontend/src/components/feedback/states/ErrorState.tsx`, `src/components/ui/alert.tsx`]
- **Catastrophic deterministic-crash Dismiss→re-crash loop** — Round-1 reset wiring fixed the blank-app, but a component that throws on every render now loops Dismiss→re-mount→re-throw; Reload is the only real escape (acknowledged in code). Accepted MVP posture; revisit with an attempt-count cap or navigate-to-safety path. [`deliveryline-frontend/src/lib/navigation/CatastrophicErrorBoundary.tsx:21`]
- **`withRunContext` captures the destination's scroll, not the origin's** — The HOC wraps the destination component, so `RunContextBoundary` snapshots the destination's mount-time scroll (≈0) and restores that on exit, defeating the "return to prior run context scroll" intent; compounds the no-re-capture-on-identity-change defer and a dev-only StrictMode transient null. No real consumer until 2.16/2.18; capture-frame semantics need design. [`src/lib/navigation/RunContextBoundary.tsx:41`, `withRunContext.tsx`]
- **`useAssertRunContextLoaded` `loaded` branch type-unsafe** — Asserts `detail.data` as `WorkflowDetail` and `actions` as `unknown` with no `undefined` narrowing; unreachable in production today (`useAllowedActions` is `enabled:false`). Add a `detail.data !== undefined` guard when story 2.14 un-stubs the actions query. [`src/lib/navigation/guards.ts:50`]
- **ESLint `no-untyped-loading-state` blind to dynamic classNames** — Cannot detect `animate-spin` via `cn(...)`/variable className (only string/template literals resolve); matches raw spinners only by `*Spinner` suffix. AC7 is lint-enforced only for literal classNames; the TS `variant` type is the real backstop. Inherent static-analysis limitation. [`tools/eslint-rules/no-untyped-loading-state.js`]
- **ESLint rule trusted boundary silently widened to `src/dev/**`** — Beyond the two dirs AC7.b/Trap T16 name (`feedback/states`, `components/ui`); harmless while `src/dev/` is non-production but weakens the rule beyond what AC7 authorized. Tighten or document. [`tools/eslint-rules/no-untyped-loading-state.js:21`]
- **`useBreadcrumbAutoTrack.classify` uses substring route matching** — `String.includes(routeId, '$workflowRunId' / 'artifacts/$artifactId')` will mis-classify future nested routes containing those substrings (e.g. `/workflows/$workflowRunId/compare`). Switch to exact route-id equality as the route tree grows. [`src/lib/navigation/useBreadcrumbAutoTrack.ts:55`]
- **`useReturnToRunContext` walk-back edges** — An `artifact` breadcrumb with `undefined` `artifactId` silently downgrades to run-detail; a synchronous back-click before the tracker effect flushes can skip the wrong entry (callback reads render-captured `stack`). Narrow timing/edge windows, no current consumer triggers them. [`src/lib/navigation/useReturnToRunContext.ts:74`]
- **Breadcrumb `scrollY` is dead data** — Captured at classify time but the dedup `replaceLast` overwrites it with the navigation-time value (≈0) and nothing consumes it for restoration. Wire it when a consumer actually restores breadcrumb scroll. [`src/lib/navigation/useBreadcrumbAutoTrack.ts:53`, `breadcrumbReducer.ts:60`]
- **Catastrophic overlay logs possibly-stale route** — `window.location.pathname` (correct, outside the router) can log the prior URL for a crash mid-route-transition. Observability mislabel only; router-state read isn't available outside the provider. [`src/lib/navigation/CatastrophicErrorOverlay.tsx:23`]
- **Overlay doc claims `createPortal` but uses Radix Dialog Portal** — Functionally equivalent (defaults to `document.body`, AC11.q passes) but the documented mechanism is inaccurate; a future Dialog swap could silently break Trap T11 with no test pinning the portal target. Fix doc wording or add a portal-target assertion. [`src/lib/navigation/CatastrophicErrorOverlay.tsx`, `README.md`]
- **Trap T5 integration coverage thin** — No-dedup-on-different-search-params is pinned only at the reducer level via synthetic entries; no test exercises the auto-tracker turning two different `clarificationId` params into two entries. Add a `useBreadcrumbAutoTrack` integration test. [`src/lib/navigation/__tests__/NavigationBreadcrumbContext.test.tsx`]
- **Empty-string prop edges** — `title ?? defaultTitle` (nullish) renders a blank heading/body for `title=""`/`message=""` (icon-alone outcome AC9.d forbids); `ContactSupport href=""` is kept by `??`, suppressing the `VITE_SUPPORT_URL` env fallback into a permanently disabled button. No current caller passes empty strings; harden with truthiness/trim checks. [`src/components/feedback/states/EmptyState.tsx:836`, `ErrorState.tsx:990`]
- **`breadcrumbReducer.clear` returns a shared non-frozen constant** — Returns the module-level `INITIAL_BREADCRUMB_STACK` (not `Object.freeze`d); `readonly` is compile-time only, so the pure-reducer guarantee rests on the shared reference never being mutated. Freeze it or return a fresh `[]`. [`src/lib/navigation/breadcrumbReducer.ts:25,67`]
- **DeadEndState verbatim-copy test is a normalized substring check** — `toContain(normalize(body))` can't detect dropped punctuation, edits outside the fragment, or duplication; some fragments are short/generic. Test-strength nit; assert full-string equality. [`src/routes/-states/__tests__/DeadEndState.test.tsx:293`]

## Deferred from: code review of story-2.24 (2026-05-26)

- **`JSON_SECRET_FIELD_PATTERN` value capture broken with escaped quotes** — `[^"]+` stops at the first `"`, so values containing `\"` are truncated and the analyzer leaks fragments of escaped-quote-containing tokens. This story widened the field-name allowlist, but the escape-handling bug is older than 2-24. Structured-analyzer path covers properly-parsed JSON. [`deliveryline-backend/src/main/java/org/dradgo/application/security/SensitivePayloadAnalyzer.java:~106-108`]
- **`JSON_SECRET_FIELD_PATTERN` doesn't match non-string JSON values** — Numeric (`"client_secret": 12345`) or array values bypass the text-mode regex; `analyzeStructured` JSON traversal covers them. Pre-existing text-mode limitation. [`deliveryline-backend/src/main/java/org/dradgo/application/security/SensitivePayloadAnalyzer.java:~106-108`]
- **PEM label alphabet `[A-Z ]*` over-permissive** — Accepts garbage tokens like `BEGIN XYZZY PRIVATE KEY` as private-key blocks. Not exploitable (still redacts a key body that wouldn't decode); just imprecise pattern surface. [`SensitivePayloadAnalyzer.java:~62-67`]
- **CI step `mvnw -pl deliveryline-runner-contracts test` lacks `-am`** — Runner-contracts is a leaf module today, so this works. Adding `-am` would future-proof against a downstream story introducing a sibling dependency. [`.github/workflows/ci.yml:23-25`]
- **`everyDeclaredRegexCompiles` ignores JS flag semantics** — Java `Pattern.compile` passes flags-less; a regex that needs `(?m)` for correctness in Java (or possessive quantifiers `*+` invalid in JS) is not pinned cross-engine. Frontend `new RegExp(entry.regex, flags)` at module-load time acts as an implicit check; explicit cross-engine equivalence test would need a JS shim in Java tests. [`deliveryline-runner-contracts/src/test/java/org/dradgo/runnercontracts/RedactionPolicyContractTest.java:~70-84`]
- **`scanForRedactions` overlap drops shorter-category match from `detectedCategories`** — Two patterns matching the same span keep only the longer; the shorter category is not reported. Span is still redacted (no leak); only audit-log telemetry under-counts. Story doesn't require category-multiplicity in audit. [`deliveryline-frontend/src/lib/sanitization/redactionFilter.tsx:~80-93`]

## Deferred from: code review of story-2.13 round 5 (2026-05-26)

- **`ProblemDetailsMapper.problemResponse` writes `correlationId` to response body without `MdcKeys.sanitizeForLog`** — The handler's WARN log sanitizes `correlationId` (line 85), but the body-attached value at line 340-343 is the raw MDC value. JSON serialization escapes control characters on emit, and `CorrelationIdFilter` validates UUIDv7 shape upstream, so this is a no-op in practice. Cosmetic asymmetry; defense-in-depth pin would document the invariant. Related to the round-4 defer line 252. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/ProblemDetailsMapper.java:340-343`]
- **`answerClarificationInternal` P-R4-4 comment over-claims READ COMMITTED race protection** — The fix correctly reads `currentState` BEFORE the clarification write, so the response reflects the snapshot at the START of the answer operation. However, the comment's "READ COMMITTED still sees the latest committed value at this point" wording is slightly imprecise: under READ COMMITTED a concurrent reject/takeover committed BEFORE this read still becomes visible, so the response can still surface a non-`WaitingForSpecApproval` state if the workflow was concurrently transitioned before the answer attempt. Controller-level Trap T6 contract is satisfied; deeper service-layer race ("should answer have been ALLOWED if workflow already terminal?") is a separate concern bounded by the clarification's terminal-state check. [`deliveryline-backend/src/main/java/org/dradgo/application/workflow/WorkflowCommandService.java:240-258`]

## Deferred from: code review of story-2.13 round 4 (2026-05-26)

- **AC7 `--actor-type` flag missing on three new CLI commands** — Round-2 D2 intentionally hardcoded `ActorType.HUMAN` to mirror the REST surface, but the AC text was not amended. Story doc cleanup pass should reconcile. [`deliveryline-backend/src/main/java/org/dradgo/adapters/cli/WorkflowCommands.java`]
- **`AnswerClarificationRequest.answerText` byte-size cap missing** — `@Size(max=8192)` is UTF-16 char count; UTF-8 worst case = 32KB. DB column will reject oversized payloads but a boundary-level byte-size pre-check would surface as typed `INVALID_COMMAND_PAYLOAD` instead of a 500. Defense-in-depth; cross-cutting input-policy. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/AnswerClarificationRequest.java`]
- **Jackson silently coerces `"expectedArtifactVersion": 1.5` → `Integer 1`** — Affects all three version-bearing DTOs (Approve/Reject/Answer). Requires `DeserializationFeature.FAIL_ON_INTEGER_FOR_FLOATS=true` and carries the same global-flag blast radius as the round-3 `fail-on-unknown-properties` flip — route through a future strict-parsing epic. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/{Answer,Approve,Reject}*Request.java`]
- **No Unicode confusable / homoglyph canonicalization in `LocalActorIdentityResolver`** — Cyrillic `аlex` (U+0430) can impersonate Latin `alex` in the audit identity column with indistinguishable rendering in most UIs. Requires ICU4J or `Normalizer.normalize(NFKC)` + mixed-script detection; out of scope for MVP audit-only RBAC. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/LocalActorIdentityResolver.java`]
- **`parseClarificationReplayRef` does not validate legacy `|` separator absence inside workflowRunId portion** — Bounded blast radius — `IdValidator` constrains runIds and legacy refs are only read, never written by current code. Defense-in-depth pin. [`deliveryline-backend/src/main/java/org/dradgo/application/workflow/WorkflowCommandService.java parseClarificationReplayRef`]
- **`@PostConstruct warnOnBlankFallback` observability gap** — WARN line emitted at startup has no Micrometer counter or actuator health-indicator hook; operators monitoring metrics-only will miss the misconfiguration signal. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/LocalActorIdentityResolver.java @PostConstruct`]
- **No log rate-limit on `handleDomainException` WARN** — Hostile clients can probe `RUN_NOT_FOUND`/`INVALID_IDEMPOTENCY_KEY` at high rate and flood WARN logs. Needs infra-level log-throttling middleware. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/ProblemDetailsMapper.java handleDomainException`]
- **No hashed/masked `idempotencyKey` in correlation logs for cross-line debugging** — Tension with the round-2 P5/P6 explicit ban on logging any portion of caller-supplied keys; an HMAC-of-key would balance debuggability vs leakage. Observability enhancement. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/WorkflowController.java`]
- **AC10 "no duplicate row insert, no duplicate event" only pinned in live-stack `WorkflowMutationEndpointsContractTest` for `answerClarification`** — Approve/reject slice tests use mocked service so can only assert response-byte-equality; extending live-stack write-side assertions to approve/reject is a tracked debt item. [`deliveryline-backend/src/test/java/org/dradgo/adapters/rest/{Approve,Reject}SpecEndpointContractTest.java`]
- **`ProblemDetailsMapperLoggingContractTest` exercises only one `DomainErrorCode` branch** — Other ~20 codes traverse the same `handleDomainException` path unverified; parametric coverage enhancement. [`deliveryline-backend/src/test/java/org/dradgo/adapters/rest/ProblemDetailsMapperLoggingContractTest.java`]
- **Idempotency-Key two-empty-headers vs single-blank-header produce different typed error codes** — Two empty `Idempotency-Key:` headers → `INVALID_COMMAND_PAYLOAD` (multi-value guard); single blank → `MISSING_IDEMPOTENCY_KEY`. Documented behavior, not a correctness defect, but typing-precedence is undocumented. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/WorkflowController.java`]
- **Inconsistent header-parsing guard order across the three mutation endpoints** — No test pins precedence; under current paths there is no functional difference, but a future regression would slip through. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/WorkflowController.java`]
- **`WorkflowController` entry log fires AFTER `localActorIdentityResolver.resolve(...)`** — If the resolver ever becomes fail-closed (per D-R4-1 option b), no record of the attempted request is captured. Minor under current fail-open semantics. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/WorkflowController.java`]
- **`correlationId` sanitized by `MdcKeys.sanitizeForLog` before persistence onto the command** — Persisted value can diverge byte-for-byte from the wire `X-Correlation-Id` header echo. Current `CorrelationIdFilter` validates UUIDv7 shape upstream so sanitization is a no-op in practice; defense-in-depth pin would document the invariant. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/WorkflowController.java:922`]
- **`httpRequest.getHeaders(name)` case-sensitivity is servlet-container-dependent** — Tomcat case-insensitive; Jetty/Undertow may differ. Tomcat is the production target — portability concern only. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/WorkflowController.java rejectMultiValuedHeader`]

## Deferred from: code review of story-2.13 round 3 (2026-05-26)

- **AC10 per-endpoint coverage of `WORKFLOW_RUN_TERMINAL`, `ILLEGAL_TRANSITION`, `ILLEGAL_CLARIFICATION_TRANSITION`, `ARTIFACT_PAYLOAD_UNAVAILABLE`, `ARTIFACT_RECORD_NOT_FOUND` incomplete** — Documented in `@ApiResponses` but no per-endpoint contract test pin; only the live-stack `WorkflowMutationEndpointsContractTest` partially exercises these. Round-3 close-out advertised "deferred AC10 batch resolved" but the resolution covered only the 3 round-2-flagged codes (`INVALID_IDEMPOTENCY_KEY`/`INVALID_ID_PREFIX`/`RUN_NOT_FOUND`) plus the conflict/version variants. [`deliveryline-backend/src/test/java/org/dradgo/adapters/rest/*EndpointContractTest.java`]
- **`X-Correlation-Id` multi-value header not guarded in `CorrelationIdFilter`** — Filter reads first value via `http.getHeader(HEADER)`; a hostile proxy can hide a valid-shape value behind an invalid first one, forcing the filter's UUIDv7 fallback. Pre-existing surface; cross-cutting fix outside story 2.13. [`deliveryline-backend/src/main/java/org/dradgo/infrastructure/observability/CorrelationIdFilter.java:48`]
- **CLI `--correlation-id` bypasses the UUIDv7 shape gate enforced for REST** — REST: `X-Correlation-Id` is parsed by `UuidV7Generator.tryParse`; invalid → filter generates fresh. CLI: `--correlation-id` flag is pushed verbatim to MDC and into command's `correlationId` field. CLI/REST shape asymmetry; needs harmonization in a future story. [`deliveryline-backend/src/main/java/org/dradgo/adapters/cli/WorkflowCommands.java pushCorrelation`]
- **Idempotent replay correlation-id split between response body (first call) and response header (current call)** — Replay returns `result.correlationId()` (= original call's value) in the body, but `CorrelationIdFilter` echoes the CURRENT call's value in the `X-Correlation-Id` header. AC4 wording ambiguous; should be tightened. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/WorkflowController.java + IdempotencyService`]
- **`CorrelationIdFilter` bean registration not pinned by a dedicated bean-order test** — Production path covered by `WorkflowControllerMdcCorrelationPropagationTest` + `WorkflowMutationEndpointsContractTest`, but no isolated assertion guards the filter's place in the chain. [`deliveryline-backend/src/test/java/org/dradgo/infrastructure/observability/`]
- **ArchUnit rule scope refinement — `application.idempotency.spi`, `application.integration.spi` not in the new rule's exclude list** — Already-tracked AC8 refinement item; widening here. A controller transitively depending on idempotency.spi slips past today. [`deliveryline-backend/src/test/java/org/dradgo/architecture/ArchitectureRuleCatalog.java:4136-4151`]
- **`clarificationReplayStatus` reads `clarificationReadPort.findByPublicId` outside an explicit `@Transactional` boundary** — Called from outer `@Transactional` `executeIdempotent` pipeline, so safe today; defense-in-depth pin would be cleaner. [`deliveryline-backend/src/main/java/org/dradgo/application/workflow/WorkflowCommandService.java:1348-1349`]
- **OpenAPI `ClarificationAnswerResponse.currentState` typed as plain `string`, not `WorkflowState` enum** — Frontend TS clients lose enum narrowing; same gap exists for `WorkflowStateChangeResponse.currentState`. Cross-cutting schema modernization. [`deliveryline-backend/src/main/resources/openapi/openapi.json + ClarificationAnswerResponse.java`]
- **`SubmitClarificationCommand.idempotencyKey @Size(max=256)` vs `IdempotencyKeyValidator.MAX_KEY_LENGTH=128` mismatch** — 129–256 char key passes record validation and fails downstream as `INVALID_IDEMPOTENCY_KEY`. Pre-existing surface; not introduced by story 2.13. [`deliveryline-backend/src/main/java/org/dradgo/application/workflow/SubmitClarificationCommand.java:37`]
- **`requireNonBlankIdempotencyKey` may run AFTER `@Valid` body validation** — A request with both empty `Idempotency-Key` AND malformed body surfaces `INVALID_COMMAND_PAYLOAD` first, masking the typed `MISSING_IDEMPOTENCY_KEY` code. Spring parameter-resolution order; multi-round debugging UX nit. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/WorkflowController.java:489-496`]
- **`WorkflowStateChangeResult` 4-field record nullable `clarificationStatus` may serialize `"clarificationStatus": null` in approve/reject JSON responses** — Decide whether `WorkflowStateChangeResponse.from(...)` should suppress null with `@JsonInclude(NON_NULL)` for symmetry with `ClarificationAnswerResponse`. Not a contract break either way. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/WorkflowStateChangeResponse.java`]
- **`ClarificationAnswerResponse` lacks `@JsonInclude(NON_NULL)` so `"correlationId": null` is serialized as explicit JSON null in slice tests** — Style consistency with other responses; tests should not silently mask null-vs-missing distinction via `asText()`. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/ClarificationAnswerResponse.java`]
- **Request DTO version fields have no upper bound — `@NotNull @Positive Integer expectedArtifactVersion`** — `Integer.MAX_VALUE` passes validation and falls through to service-layer `APPROVAL_VERSION_MISMATCH`. Defense-in-depth — current behavior surfaces overflow as INVALID_COMMAND_PAYLOAD via Jackson coercion. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/AnswerClarificationRequest.java:29 (and approve/reject equivalents)`]
- **`clarificationReplayStatus` no prefix validation on `clarificationId` parameter** — Idempotency-record-bounded so attacker-controlled values cannot reach this path; defense-in-depth pin would mirror the controller's upfront `PublicIdPrefixes.require`. [`deliveryline-backend/src/main/java/org/dradgo/application/workflow/WorkflowCommandService.java:1349`]
- **`CliRestEquivalenceContractTest` only exercises the default `local-operator` fallback identity** — Does not parametrize across non-default property values; a parametrized test would catch wiring divergence in CLI/REST property-source lookups. [`deliveryline-backend/src/test/java/org/dradgo/foundation/CliRestEquivalenceContractTest.java`]

## Deferred from: code review of story-2.13 (2026-05-26)

- **Inconsistent transport policy across `WorkflowController` endpoints** — approve/reject/answer use `X-Actor-Identity` header + MDC correlation; submit/retry/takeover still read `actorIdentity`/`actorType`/`correlationId` from the request body. Acknowledged in story 2.13 dev notes line 156 as out-of-scope. Mixed model surfaces in OpenAPI without documented rationale; clients reading the spec alone will be confused. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/WorkflowController.java`]
- **`Idempotency-Key` required-true only on approve/reject/answer; submit/retry/takeover still default to required-false** — Same controller, two contracts. Tighten in the next mutation-endpoint epic. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/WorkflowController.java`]
- **OpenAPI emits `minLength: 0` for `@NotBlank` string fields** — springdoc-openapi limitation predating story 2.13. Affects `AnswerClarificationRequest.answerText`, `ApproveSpecRequest.artifactId`, `RejectSpecRequest.reasonText`. Can be mitigated with explicit `@Size(min=1)` per field if/when client-side validation matters. [`deliveryline-backend/src/main/resources/openapi/openapi.json`]
- **AC10 happy-path assertions use `jsonPath` field-by-field instead of schema match against the regenerated OpenAPI snapshot** — Partial coverage acceptable; promote to generic infra change rather than a per-story task. [`deliveryline-backend/src/test/java/org/dradgo/adapters/rest/*EndpointContractTest.java`]
- **New ArchUnit rule `REST_CONTROLLERS_STAY_THIN_AND_AVOID_SPI_OR_PERSISTENCE_OR_RUNNER` scope uncertain** — Possible false-positives for re-exported SPI types (e.g. if a controller transitively pulls a `workflow.spi` enum) and false-negatives for runner adapter subpackages beyond the constant. Refine on next iteration once build confirms green. [`deliveryline-backend/src/test/java/org/dradgo/architecture/ArchitectureRuleCatalog.java`]
- **CLI generated idempotency-key print may differ from validator-canonicalized value** — When caller-supplied `--idempotency-key` is non-null, the print shows the raw input but the validator may canonicalize. Diagnostics UX nit; doesn't affect correctness. [`deliveryline-backend/src/main/java/org/dradgo/adapters/cli/WorkflowCommands.java approveSpec`]

## Deferred from: code review of story-2.12 (2026-05-25)

- **`pendingClarifications` N+1 in `listRuns`** — `WorkflowInspectionService:614` calls `clarificationReadPort.countPendingByWorkflowRun(run.publicId())` per run inside the loop. Self-acknowledged in code as MVP-acceptable (<50 rows typical). Replace with a single batched `IN (...)` query when queue scale grows. [`deliveryline-backend/src/main/java/org/dradgo/application/workflow/WorkflowInspectionService.java:614`]
- **REST surface `pendingClarifications` not on `WorkflowSummaryResponse`** — Explicit comment in `WorkflowSummaryResponse.java` defers this to story 2.13 OpenAPI regen. Story 2.14 reads `WorkflowInspectionService.getRunSummary` directly so the gate logic doesn't need REST. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/WorkflowSummaryResponse.java`]
- **`ClarificationLifecycleSnapshot` does not validate `noEffectReason` vocabulary or status/timestamp consistency in compact constructor** — DB CHECK `ck_clarifications_status_fields_paired` catches this at the storage layer; in-memory defensive validation is lower priority. [`deliveryline-backend/src/main/java/org/dradgo/application/clarification/ClarificationLifecycleSnapshot.java`]
- **AC1 / AC6 method-signature deviations** — Implementation adds `workflowRunPublicId` parameter to `markAccepted`/`markIncorporated`/`markSuperseded`/`markRejectedInvalid`/`getClarificationStatus` for cross-run guard (Trap T11) per Task 6 body, but spec AC1/AC6 summary lists the un-augmented signatures. Intentional deviation; document in PR. [`ClarificationLifecycleService`, `WorkflowInspectionService`]
- **`payload_read_failed` `noEffectReason` token outside AC4's literal vocabulary** — Consistent with Dev Notes OQ-5 recommendation but not explicitly authorized in AC4 text. Document in PR; revisit if PM rejects the vocabulary extension. [`deliveryline-backend/src/main/java/org/dradgo/application/clarification/ClarificationLifecycleOrchestrator.java`]
- **`SubmitClarificationCommand` plumbing scope-creep into story 2.11** — `WorkflowCommandService.answerClarification*`, `WorkflowCommandFingerprintFactory` case, replay-ref encoding, and sealed-permits entry are story 2.11 finish-up work bundled into the 2.12 commit. Flag in PR description so 2.11 retro records it. [`deliveryline-backend/src/main/java/org/dradgo/application/workflow/`]
- **`markRejectedInvalid` accepted→rejected branch missing** — State machine allows only `answered → rejected_invalid`. PM may later want `accepted → rejected_invalid` for "realized question was malformed after accept". Confirm with PM and add if needed. [`deliveryline-backend/src/main/java/org/dradgo/application/clarification/ClarificationLifecycleService.java:1922`]
- **`replayStateChange` pins live state inside the writing tx** — Currently safe; fragile if a future refactor moves `findByPublicId` outside the tx, in which case state would be read post-mutation by an unrelated actor. Speculative. [`deliveryline-backend/src/main/java/org/dradgo/application/workflow/WorkflowCommandService.java:282-291`]
- **`getRunSummary` cross-tenant timing-leak speculation** — Depends on `runNotFound` implementation timing; not in diff. [`deliveryline-backend/src/main/java/org/dradgo/application/workflow/WorkflowInspectionService.java`]
- **`acknowledgesQuestion` no payload-size cap** — Epic 3 replaces the substring-scan stub with structured `clarification_acknowledgements`. Defer DOS-guard until then per Trap T6. [`deliveryline-backend/src/main/java/org/dradgo/application/clarification/ClarificationLifecycleOrchestrator.java:1552-1567`]
- **Contract test gap: resubmission case `[answered, accepted, answered, …]` not asserted** — Story 2.11 invariant should prevent resubmission upstream; contract-test gap acceptable. [`deliveryline-backend/src/test/java/org/dradgo/contract/ClarificationVisibleIncorporationContractTest.java`]
- **`executeIdempotent` 3-arg + 4-arg overload churn** — Overlapping responsibility increases chance a future command type forgets to pin its custom replay ref. Refactor opportunity. [`deliveryline-backend/src/main/java/org/dradgo/application/workflow/WorkflowCommandService.java:305-313`]
- **Checkstyle suppression hardcoded line number drift** — `lines="483"` for `ForbiddenThreadSleep` is brittle; every edit above the sleep call drifts the suppression. Pre-existing project pattern. [`config/checkstyle/suppressions.xml:38`]
- **`FlywaySchemaContractTest` CHECK assertion substring-weak** — Substring matches on CHECK definitions can't catch a removed `else false` clause. Generally weak; not a 2.12-specific regression. [`deliveryline-backend/src/test/java/org/dradgo/contract/FlywaySchemaContractTest.java:1192-1223`]
- **V9 partial index uses string status filter** — If `status` column type ever changes to enum, the partial-index predicate breaks silently. Defensive note only. [`deliveryline-backend/src/main/resources/db/migration/V9__add_clarification_lifecycle_columns.sql:2235-2237`]
- **`SKIPPED_NON_ACCEPTED` enum value is dead code** — `ClarificationLifecycleOrchestrator.Outcome.SKIPPED_NON_ACCEPTED` defined but never emitted; sweep filters to `accepted` upfront. Observation only. [`deliveryline-backend/src/main/java/org/dradgo/application/clarification/ClarificationLifecycleOrchestrator.java`]

## Deferred from: code review of story-2.9 (2026-05-24)

- **`AllowedActionRegistryPinTest` only pins the wire-value string** — The entire test is `assertEquals("approve_spec", AllowedAction.APPROVE_SPEC.value())`. Story 2.14 owns the `WorkflowInspectionService.getAllowedActions` extension that maps APPROVE_SPEC to ApprovalService as the canonical executor. Without that wiring, a future regression could surface APPROVE_SPEC in the allowed-actions surface with no executor handling it — undetected by this test. Track as a 2.14 entry-criterion. [`deliveryline-backend/src/test/java/org/dradgo/architecture/AllowedActionRegistryPinTest.java`]
- **`ApprovalServiceTransactionalityTest` named-test missing** — Spec Task 4 line 132 named a focused integration test that mocks `WorkflowTransitionService.transition` to throw and asserts rollback shape. Functional intent IS pinned by `WorkflowCommandServiceContractTest.approveSpecRejectsIllegalTransitionWhenRunIsNotWaitingForSpecApproval` (seeds INBOX run, real transition rejection, asserts zero approvals + zero events). Naming-only deviation; track for documentation alignment or rename the existing test. [`deliveryline-backend/src/test/java/org/dradgo/application/approval/`]
- **T8 — `seedAvailableSpecArtifact` is inline private helper, not a shared fixture** — Spec trap T8 (line 203) suggested reusing/extending story 2.8's `SpecArtifactFixtures` package builder. As shipped, the JDBC seed pattern is duplicated between `WorkflowCommandServiceContractTest:467-498` and `ApprovalWritePersistenceAdapterTest`. Story 2.10 (rejection writer) will duplicate it again unless refactored. Track as the moment-to-extract-a-shared-fixture when 2.10 lands. [`deliveryline-backend/src/test/java/org/dradgo/application/workflow/WorkflowCommandServiceContractTest.java`]
- **`idempotencyKey` is persisted into queryable `workflow_events.details` JSON** — `ApprovalService.approvalEventDetails` + `transitionEventDetails` both `details.put("idempotencyKey", command.idempotencyKey())`. AC6 explicitly requires this. Caller-supplied idempotency keys (session-scoped client tokens) now surface in any events-list API response that exposes `workflow_events`. The existing `WorkflowTransitionService.transition` already had the same pattern. Cross-cutting policy change (omit/hash idempotency keys in event details surface-wide) belongs in a future operator-secret-handling epic; not a 2.9 regression. [`deliveryline-backend/src/main/java/org/dradgo/application/approval/ApprovalService.java:309-340`]
- **`config/checkstyle/suppressions.xml` line-number magic number drift** — The `Thread.sleep` suppression in `WorkflowCommandService.pauseBeforeReplayLookup` is targeted by hard-coded line number (438 → 434 this commit). Every change above that method drifts the suppression. Flagged in story 1.21's review and accepted as the project's existing pattern; track for a `@SuppressWarnings`-based replacement that survives refactors. [`config/checkstyle/suppressions.xml:38`]

## Deferred from: code review of story-2.8 (2026-05-24)

- **`LocalRunnerScratchStore.tryReadContextBundle` defense-in-depth path-traversal guard** — `tryReadScratchFile(runnerExecutionId, filename, logTag)` resolves a Path under the scratch root without validating `runnerExecutionId` matches the `RUNNER_EXECUTION` public-id prefix. Production callers (`WorkflowInspectionService.getContextBundleLookupForArtifact`) already prefix-validate via `PublicIdPrefixes.require` before reaching the adapter, so this is defense-in-depth, not a current vuln. Track as a future adapter-hardening pass; consider validating the prefix inside the adapter itself. [`deliveryline-backend/src/main/java/org/dradgo/adapters/files/LocalRunnerScratchStore.java:181-204`]
- **`ArtifactRepository` queries pass `artifactType` as raw String — case-sensitivity drift not enforced** — `listByWorkflowRunIdAndArtifactType` / `findLatestByWorkflowRunIdAndArtifactType` accept `String artifactType` and compare with the column directly. A caller passing `"SPEC"` instead of `"spec"` would silently get zero rows. Production callers always pass `ArtifactType.value()` so the risk is theoretical, but the query signatures are weakly typed. Tracker for a query-style refactor to accept the enum or normalize at the repository boundary. [`deliveryline-backend/src/main/java/org/dradgo/adapters/persistence/repository/ArtifactRepository.java`]
- **`ApprovalRepository` ordering non-deterministic when multiple non-archived approvals exist for the same `(artifact_id, artifact_version)`** — `findLatestApprovedForArtifactLineage` orders `artifactVersion desc, decidedAt desc, id desc`. The schema permits multiple non-archived rows on the same `(artifact_id, artifact_version)` via the tombstoning model, so a re-approval of the same artifact version is theoretically possible. Today the writer (`ApprovalService.approveSpec`, story 2.9) hasn't shipped — the multi-row case is unreachable. Blocked on 2.9/2.10 shipping; revisit then with a `UNIQUE (artifact_id, artifact_version) WHERE archived_at IS NULL` partial-index proposal or an explicit "supersedes" tombstoning protocol. [`deliveryline-backend/src/main/java/org/dradgo/adapters/persistence/repository/ApprovalRepository.java:25-37`]
- **`ContextBundle.redactedPayload` has no size cap before being embedded in `workflow status --format json`** — `WorkflowCommands.appendBundleJsonField(...)` re-parses the redacted bytes and `contextBundle.set("bundle", bundleNode)` embeds the full bundle as a sub-document with no length warning. A multi-megabyte bundle is unbounded in the CLI splice path. Not a 2.8 regression — the bundle bytes are already bounded by the runner-contracts schema in practice. Track for a future CLI hardening pass (per-payload byte cap + truncation marker). [`deliveryline-backend/src/main/java/org/dradgo/adapters/cli/WorkflowCommands.java:421-484`]
- **`workflow-status.v2.schema.json` makes `contextBundle` required — mixed-flag callers may see v1 vs v2 inconsistency** — The v2 schema declares `contextBundle` as a required field, which only appears when callers pass `--include-context-bundle`. Without the flag, the CLI emits v1 (no `contextBundle`). Opt-in via flag is the version-negotiation boundary, but the flag→schema mapping is currently documented only by the constant `STATUS_WITH_CONTEXT_BUNDLE_SCHEMA_VERSION` inside `WorkflowCommands.java`. Document explicitly in the CLI docs increment (story 2.29) so jq/schema-validating consumers know which version to expect for which invocation. [`deliveryline-backend/src/main/resources/schemas/cli/workflow-status.v2.schema.json:6`]

## Deferred from: code review of story-2.1 (2026-05-21)

- **Static-wipe allowlist is hardcoded** — `wipe-static-before-copy` enumerates top-level dist files (`index.html`, `favicon.svg`, `icons.svg`, `vite.svg`) plus the `assets/` dir; future or renamed top-level dist outputs (`manifest.webmanifest`, `robots.txt`, renamed favicon) survive across incremental (non-`clean`) builds. Extends the already-accepted static-namespace hardening from the prior 2.1 review. [`deliveryline-backend/pom.xml:222-234`]
- **`-Dfrontend-maven-plugin.skip=true` can still ship a SPA-less jar** — the `clean`/wipe execution has no `<skip>`, so static is wiped even when the copy is skipped, and the skip path produces an empty `static/` with no warning. Documented local-dev escape hatch; harden if it becomes a footgun. [`deliveryline-backend/pom.xml:209-267`]
- **`${maven.multiModuleProjectDirectory}` fragility** — the enforcer and resources-copy paths can resolve incorrectly for non-`mvnw` or single-module `mvn -pl deliveryline-backend` invocations launched outside the reactor root, firing a misleading "frontend dist missing" error even when dist exists. [`deliveryline-backend/pom.xml:189,275`]
- **Reactor build-order coercion unguarded** — build order relies on the `<dependency type=pom scope=test>` on the frontend module; dependency mediation/analyzers could prune it and silently revert ordering. Add a build-order assertion test. [`deliveryline-backend/pom.xml:134-146`]
- **npm separate-download with no bundled-npm fallback** — `frontend-maven-plugin` downloads npm `10.8.2` as a separate artifact; an air-gapped/cold Windows runner could fail the `install-node-and-npm` step. Not exercised by current CI environments. [`deliveryline-frontend/pom.xml`]
- **Required-status-check names have no single source of truth** — the matrix check names are duplicated across `ci.yml`, both `configure-branch-protection.{sh,ps1}`, and `ci-branch-protection.md`; a future matrix-axis/`name:` rename silently desyncs branch protection (renamed check stops blocking). [`.github/workflows/ci.yml`; `scripts/ci/configure-branch-protection.{sh,ps1}`; `docs/ci-branch-protection.md`]
- **Vite `/api` proxy has no error/ws-down handler** — backend-down yields a raw `ECONNREFUSED` 500 and ungraceful WebSocket (`ws:true`) upgrade resets; no `configure` hook normalizes the error. Documented expected dev behavior. [`deliveryline-frontend/vite.config.ts`]

## Deferred from: dev-story of 2-3-design-tokens-color-palette-and-semantic-state-variables (2026-05-20)

- **Pixel-level visual-regression for blocker/warning dominance (AC6)** — true pixel snapshot tooling (Playwright/Chromatic) is not installed and was deliberately NOT added here (Q3 RESOLVED: defer). AC6 is satisfied in story 2.3 by a programmatic prominence proxy (`tools/contrast/__tests__/token-prominence.test.js`: saturation + lightness-delta-from-background score, asserting blocker/warning > informational/draft/empty + visible border) plus the human-reviewable token gallery in `PrimitivesPlayground.tsx`. Add real pixel VR when the frontend test suite (story 2.27) / a11y audit (story 2.25) lands. [`deliveryline-frontend/tools/contrast/__tests__/token-prominence.test.js`]

## Deferred from: code review of 1-21-github-actions-ci-tiered-pipeline (2026-05-17)

- **doctor-smoke Windows runner coverage** _(added 2026-05-18 during CI stabilization)_ — the original 1.17 AC8 matrix included `windows-latest` to exercise the `DOCTOR_DOCKER_MISSING` WARN-tolerance path (Windows runners have no Docker Desktop). With story 1.21's full-Spring-context doctor boot, the transitive bean graph (`WorkflowCommands` → services → JPA repositories) requires a real persistence layer — and Windows runners can't start the compose-managed Postgres. Doctor-smoke was collapsed to Ubuntu-only as a pragmatic unblock. Re-enabling Windows requires either: (1) decoupling doctor from the persistence bean graph (e.g., a separate Spring profile/sub-context that excludes service beans) or (2) wiring a non-Docker DataSource for Windows (HSQLDB embedded, with Flyway disabled). [`.github/workflows/ci.yml:doctor-smoke`]
- **`RedactionLayoutHolder` masks Spring startup-failure diagnostics** _(added 2026-05-18 during CI stabilization)_ — when context refresh fails before `RedactionLayoutInitializer.@PostConstruct` can wire the holder, every log message (including Spring's `LoggingFailureAnalysisReporter` output) becomes the `[redaction-pending]` sentinel. The fail-closed design is intentional (story 1.19 D1 resolution), but it makes CI failures un-diagnosable without a local repro. Fix candidate: wire the holder via `ApplicationContextInitializer` or `SpringApplicationRunListener` so it's set before any bean creation. [`deliveryline-backend/src/main/java/org/dradgo/infrastructure/observability/RedactionLayoutHolder.java`]
- **Maven dependency-cache strategy is per-job** — every tier re-resolves the same Maven dependencies (no shared warmup step). Optimization for a 9-tier pipeline; revisit when measurable cold-cache cost surfaces. Out of story scope.
- **Checkstyle uses JAR-bundled `google_checks.xml`** — `<configLocation>google_checks.xml</configLocation>` resolves from the Checkstyle JAR. Future Checkstyle version bumps will silently change the enforced rule set. Future hardening: vendor a pinned copy under `config/checkstyle/google_checks.xml`.
  - **CLOSED 2026-05-22 (story 2.30):** the root POM now points `<configLocation>` at the committed `config/checkstyle/checkstyle.xml` — a focused ruleset derived from Google's naming conventions plus project-specific deltas (forbidden-call bans, issue-referenced TODOs), NOT a verbatim `google_checks.xml` copy. Spotless already owns whitespace/formatting, so re-enforcing it via a full Google ruleset would create the double-enforcement noise story 2.30 AC7 forbids. Nothing resolves from the Checkstyle JAR any more, so a version bump can no longer silently change the enforced rules.
- **Default `actions/checkout@v4` shallow `fetch-depth: 1`** — fine for current tiers, but tiers that need git history (e.g., diff-vs-main for OpenAPI drift in story 6.9, or release-readiness tag scans in `bundled-jar-smoke`) will require `fetch-depth: 0` or a targeted unshallow step. Forward-looking concern.
- **Frontend cache uses `setup-node@v4` built-in `cache: npm` rather than `actions/cache@v4` with explicit `restore-keys`** — Task 5 sub-bullet 3 asked for `actions/cache` with `restore-keys` fallback. Functionally near-equivalent (`setup-node@v4` does cache `~/.npm` keyed by lockfile hash), low value to refactor before story 2.1 lands real frontend tooling.

## Deferred from: code review of 1-12c-artifact-query-hardening-tech-debt (2026-05-17)

- **Reject path bypasses idempotency persistence** — `ArtifactOperationService.createOrAdvanceArtifact` throws `DomainException(ARTIFACT_OPERATION_INTENT_CONFLICT)` before any `createPending` call records an `artifact_operations` row. Retries of the same `(workflowRunId, type, UPDATE, idemKey)` still reject deterministically, but a follow-up `CREATE` reusing the same `idempotencyKey` inside the dedup window can silently succeed with no audit trail linking the two attempts. Likely affects every typed `DomainException` thrown before `createPending` (not unique to AC3) — needs cross-cutting policy + possible schema support for a `REJECTED` status. Deferred: move to another epic; story 1.12c is already too large. [`ArtifactOperationService.java:155-174`]
- **Native query may return entity with un-hydrated lazy associations** — `ArtifactRepository#findActiveLineageLeaf` uses `nativeQuery=true` with entity mapping. Current callers in `ArtifactRecordPersistenceAdapter` only touch column accessors (`getPublicId`, `getClassification`), but `entity.setParentArtifact(lineageHead)` keeps a proxy reference. If a downstream consumer touches `parentArtifact.parentArtifact` in a detached state, `LazyInitializationException`. Risk is theoretical for current callers; broader audit needed. [`ArtifactRepository.java:50-58`, `ArtifactRecordPersistenceAdapter.java:170-194`]
- **`JOIN workflow_runs wr ON wr.id = a.workflow_run_id` plan suboptimality** — anchor filters by `wr.public_id` rather than first resolving the workflow_run id and filtering `a.workflow_run_id = ?`. May produce a worse query plan depending on indexes. Needs profiling; micro-optimization. [`ArtifactRepository.java:113-117`]
- **`createNextVersion` empty-CTE fallback can graft new version onto an unrelated lineage** — `findLatestActiveLineageMemberEntity(...).orElse(latestActiveArtifact)` silently merges lineages when the CTE returns empty. Pre-existing behavior, but the CTE introduces new ways to reach the empty-result branch (depth-truncation, archived-only lineage). [`ArtifactRecordPersistenceAdapter.java:170-171`]

## Deferred from: code review of 1-12-artifact-operations-skeleton (2026-05-07)

- **`ArtifactEventPersistenceAdapter.append` may double-allocate `WORKFLOW_EVENT.next()` ids** — adapter calls `PublicIdPrefixes.WORKFLOW_EVENT.next()` then forwards the record to `workflowEventWritePort.append(...)`, which (per prior-story patterns) likely also generates an id. The supplied id may be ignored, burning the prefix counter; the caller has no return path to discover the real persisted id. Verify against `WorkflowEventWritePort` impl and decide whether the id-generation responsibility belongs in the adapter or the port. [`deliveryline-backend/src/main/java/org/dradgo/adapters/persistence/ArtifactEventPersistenceAdapter.java:79-91`]
- **`ArtifactService.isApprovalEligible` performs a filesystem stat per call** — `artifactPayloadStore.isReadable(...)` is invoked on every eligibility check with no caching. Likely a hot path once approvals land. No AC violation; revisit if profiling shows latency. [`deliveryline-backend/src/main/java/org/dradgo/application/artifact/ArtifactService.java:1271-1279`]
- **Asymmetric event emission: `markAvailable` appends `ARTIFACT_AVAILABLE` event, `markFailed` appends nothing** — failure path is event-silent so downstream recovery/correlation consumers cannot react. Tied to decision-needed item H8 (whether reconciliation should also flip the artifact and emit `recovery.reconciled`); resolve together. [`deliveryline-backend/src/main/java/org/dradgo/application/artifact/ArtifactOperationService.java:1132-1137`]
- **System clock backwards-jump skews reconciler stale-pending threshold** _(re-review chunk 1, 2026-05-07)_ — `ArtifactReconciliationService` computes the stale-pending threshold via JVM clock subtraction; an NTP correction or VM-suspend can move the threshold into the past and either over-scan or skip windows. Skeleton-stage; deeper reconciliation belongs to Epic 4 per AC #5. [`deliveryline-backend/src/main/java/org/dradgo/application/artifact/ArtifactReconciliationService.java:531`]

## Deferred from: code review of 1-8-problem-details-mapper-with-stable-domain-error-codes (2026-05-01)

- **Field-name-only redaction allowlist gaps and false positives** — `Idempotency-Key` is not redacted, `credential`/`bearer`/`private` patterns are missing, and harmless names containing `token` get redacted unnecessarily. Story 1.10 owns adversarial redaction; Task 2 of 1.8 explicitly says "Keep the rule narrow and deterministic in this story." [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/ProblemDetailsMapper.java:258`]
  - **Status update 2026-05-19:** This is **F19** in retrospective discussion. Scheduled for closure by **Epic 2 story 2-24** (Artifact Content Sanitization + Redaction-Gap Closure) per `_bmad-output/planning-artifacts/sprint-change-proposal-2026-05-19.md`. Closure ACs: 2-24 AC13 (allowlist extension + Idempotency-Key) + AC14 (fixture additions) + AC16 (contract test against shared `runner-contracts/redaction-policy.json` spec) + AC18 (mark this entry closed when 2-24 ships).
  - **CLOSED 2026-05-26 (story 2-24):** redaction-policy allowlist + Idempotency-Key + bundle-JSON shapes closed via AC13/AC14/AC16. See `2-24-artifact-content-sanitization-untrusted-runner-output.md`.
- **`extractJacksonFieldName` returns innermost reference for nested errors** — `while (matcher.find()) { field = matcher.group(1); }` overwrites with each match. Current DTOs are flat so tests only cover shallow cases; revisit when a nested request body lands. [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/ProblemDetailsMapper.java:138`]
- **Out-of-scope drive-by additions inside the 1.8 commit** — new `PublicIdPrefixesTest`, two new `WorkflowCommandServiceContractTest` cases, `PublicIdPrefixes.java` import reorder, and a secondary `.thenComparing` in `WorkflowCommandService.validate(...)`. Useful additions but outside transport-adaptation scope; track for separation if scope-creep recurs.
- **`WorkflowAdapterEquivalenceTest` lost cli↔rest field-by-field response comparison** — replaced by jsonPath + captured-command equivalence; prior assertion that both adapters produce the same logical response payload was dropped. Restore in a follow-up if cross-adapter response divergence is observed. [`deliveryline-backend/src/test/java/org/dradgo/adapters/WorkflowAdapterEquivalenceTest.java`]
- **Future CLI commands need explicit `exitStatusExceptionMapper` wiring** — only `submit` exists today. New `@Command` methods will silently revert to Spring Shell defaults and break the unified 1xx/2xx/3xx/4xx bands. Add a `// REVIEW` comment when story 1.18+ adds more CLI commands; consider an ArchUnit rule once we have more than one. [`deliveryline-backend/src/main/java/org/dradgo/adapters/cli/WorkflowCommands.java:22`]

## Deferred from: code review of 1-2-unified-docker-compose-with-env-configurable-ports (2026-04-27)

- **Test class package convention** — `LocalDevelopmentContractTest` lives in root `org.dradgo` package; spec dev notes assign profile-wiring concerns to an `infrastructure` subpackage. Decide and apply once the broader package-layout convention is settled across modules. [`deliveryline-backend/src/test/java/org/dradgo/LocalDevelopmentContractTest.java`]
- **Compose `${POSTGRES_PASSWORD:?...}` will block future `@SpringBootTest` in CI without `.env`** — when the first context-loading backend test is added, set `spring.docker.compose.skip-in-tests=true` (Spring Boot 3.1+) or inject the env var at the CI level. No action required until the first such test exists. [`docker-compose.yml`, `application.yml`]
- **No datasource fallback for non-compose contexts** — `application.yml` has no `spring.datasource.*`; production packaging or any future test that disables docker-compose will fail with "Failed to determine a suitable driver class". Revisit when prod deployment shape is in scope (post-Epic 1 local-first phase). [`deliveryline-backend/src/main/resources/application.yml`]
- **Linux bind-mount volume-permission mismatch** — if a developer overrides the named volume to a host bind-mount (e.g., under `${DELIVERYLINE_HOME}`), Postgres UID 999 will not match host UID and startup fails. Pilot docs should explicitly warn against bind-mounting the Postgres data dir, or the compose file should pin `user: "999:999"` and pre-chown. [`docker-compose.yml`]
- **AC-7 (`docker compose down -v` removes volume) has no automated assertion** — manually verified per the story dev log. Consider adding a Testcontainers- or integration-level check that exercises the volume-removal contract.

## Deferred from: code review of 1-3-flyway-v1-core-schema-with-retention-and-measurement-columns (2026-04-27)

- **State enum duplicated across 3 CHECK constraints** in `workflow_runs.current_state`, `workflow_events.prior_state`, `workflow_events.resulting_state`. Drift risk when the canonical state list changes. Refactor to a `workflow_states` lookup table (FK from each column) or `CREATE TYPE workflow_state AS ENUM (...)` is story 1.4's territory (workflow state registry).
- **`workflow_events.event_type` lacks a CHECK constraint or registry FK** — accepts any text including empty string and case-mismatched values. Architecture mandates a central event-type registry; story 1.4 owns it.
- **No `COMMENT ON TABLE` / `COMMENT ON COLUMN` statements anywhere in V1** — schema is undocumented at the DB level. Categorical fields like `failure_category`, `classification`, `intervention_marker` have non-obvious semantics. Add in a follow-up doc pass.
- **`integration_links` lacks `idempotency_key` / `classification` columns** — discretionary per Task 3 spec ("any idempotency/classification fields needed"). Story 3.15 (GitHub integration-link metadata) may force a schema add.
- **`@SpringBootTest` is heavyweight for pure schema tests** — `@DataJpaTest` or plain Testcontainers + Flyway harness without Spring would be faster. Performance optimization for later when CI time matters.
- **`postgres:17` not pinned by digest** in `TestcontainersConfiguration` and the broken-migration test — tag mutability means CI could shift to a newer point release any night. Reproducibility nice-to-have; CI risk acceptable for now. Pin to `postgres:17.x@sha256:...` later.
- **Testcontainers `withReuse(true)` not enabled** — every test class spins up a fresh container; multi-second startup tax per class. Performance optimization for later.
- **Timezone consistency between Postgres container and JVM not pinned** — container default is UTC, JVM TZ may be local; flaky time-comparison tests later. Add `postgres.withEnv("TZ","UTC")` and `-Duser.timezone=UTC` to surefire when first time-sensitive test appears.
- **Index-name truncation rationale documented only in completion notes, not as in-SQL ADR comments** — the per-line comment patch in the story addresses the specific sites; the broader ADR/doc pass is a follow-up.

## Deferred from: code review of 1-4-central-registries-with-drift-tests (2026-04-27)

- **Per-column registry coverage for non-AC CHECK columns** — V1 has CHECK lists for columns the spec didn't explicitly require a Java registry for. Each is deferred to its owning story:
  - `recovery_actions.action_type` and `recovery_actions.result_status` → story 1.18 (CLI MVR baseline)
  - `idempotency_records.status` → story 1.9 (Idempotency Service)
  - `approvals.decision` and `approvals.rejection_taxonomy` (also `workflow_events.rejection_taxonomy`) → story 2.9 (Backend Approval Service)
  - `integration_links.integration_type` → story 1.14 (Mock/Real Linear Adapter)
  - `artifact_operations.operation_type` → story 1.12 (Artifact Operations Skeleton)
- **V1 schema asymmetries — columns without SQL CHECK** — Java registries are authoritative for `artifacts.artifact_type`, `artifacts.classification`, and `*.failure_category` (workflow_events / runner_executions / artifact_operations). V1 SQL has no enum CHECK on these (only `length(failure_category) > 0` for failure category). DB rows can carry registry-unknown values; bidirectional drift would require a future schema-tightening story to add the missing CHECKs as a V2 migration.
- **Missing workflow states / non-runner failure categories — story 1.5 territory** — Candidate `WorkflowState` values currently absent: `Cancelled`, `Aborted`, `Queued`, `Superseded`, `RetryScheduled`, `Retrying` (note: `Reconciled` and `TakenOver` exist as states but no transient `Reconciling`/`TakingOver` companion states — race-prone if either is non-instantaneous). Candidate `FailureCategory` values currently absent: `INFRASTRUCTURE_FAILURE`, `DEPENDENCY_UNAVAILABLE`, `CONFIGURATION_INVALID`, `PERMISSION_DENIED`, `RATE_LIMITED`, `INPUT_VALIDATION`. Story 1.5 owns transitions and should decide which to add (any addition requires a V2 migration extending the V1 SQL CHECK lists).
- **Self-rubber-stamp on placeholder fixtures** — `allowed-actions.placeholder.json`, `registry-api-schema-placeholders.json`, and `workflow-event-types.fixture.json` are authored alongside the enums in this same diff, so drift is structurally impossible until external owners take over (story 1.20/1.21 for the OpenAPI snapshot, 1.23 for foundation-gate fixtures, 2.14 for the frontend allowed-actions consumer).
- **AC-5(a) "drift vs domain enums"** — no parallel domain enums exist; the spec scope says story 1.4 should not yet build the domain model. The registry-vs-itself check (via `DomainRegistry`) is the practical interpretation; revisit when the domain model materializes in 1.5+.
- **`RunnerSchemaVersion` int-vs-string design** — current `value() = "1"` will require redesign for `1.1` / `2-beta`. Revisit when story 1.6 introduces real schema versioning.
- **`DomainRegistry` returns `Set<String>`** rather than `Set<RegistryValue>` (or sealed types). Stringly-typed external API; revisit when consumer call sites materialize.
- **Registry value collisions across enums** — `ArtifactStatus.PENDING("pending")` and `ArtifactOperationStatus.PENDING("pending")` collide on raw value across registries. Field-aware overload mitigates blast radius; structural duplicate-detection would belong in a future cross-registry validator.
- **AC-6 reconciliation queue seam** — current explicit-error path satisfies "fail fast", but no extension point exists for routing to a recovery queue. Revisit when story 1.18 (CLI MVR baseline) or recovery work needs it.
- **`RegistryContractTest` is `@SpringBootTest`** — heavyweight for what's mostly enum/string/regex assertions. Split fast unit tests from DB-touching ones in a follow-up performance pass.
- **`Map.copyOf` ordering** — replaced with `Collections.unmodifiableMap(LinkedHashMap)` in `RegistryParsers.index()` and `PublicIdPrefixes.prefixMap()` to preserve insertion order. (Not deferred — landed.) Logging `Map.copyOf` ordering as a learned-pattern note for future utilities.
- **Tabs vs spaces indentation** — every new `.java` file uses tabs; project lacks `.editorconfig`. Address via project-wide formatter setup, not in 1-4.
- **Hard-coded literal `https://deliveryline.local/problems/`** — promote into a single source of truth (constant) when the Problem Details mapper lands in story 1.8.
- **`details()` returns map of `Object` values that may be mutable collections** — defensive copying of values is a polish item.

## Deferred from: code review of 1-5-workflow-state-transition-table-and-workflow-transition-service (2026-04-28)

- **Optimistic-lock catch may miss commit-time `TransactionSystemException`** — `WorkflowTransitionService.java:74` only catches `OptimisticLockingFailureException`. With `saveAndFlush` the conflict surfaces at flush time, so current tests pass; if Hibernate ever defers detection to commit, the wrapper exception would slip past the catch and produce raw infrastructure errors instead of `CONCURRENT_TRANSITION_CONFLICT`. Hardening: also catch `TransactionSystemException` with optimistic-lock cause, or assert wrapping stability.
- **`WorkflowEventEntity.getDetails()` exposes the live mutable map** — `WorkflowEventEntity.java:174`. `WorkflowTransitionService` currently relies on `getDetails().put(...)` to inject the idempotency key into the JSON column. Refactor to immutable view + `addDetail(key, value)` would couple multiple files; revisit when details composition gets richer.
- **Event public id is non-deterministic on `idempotencyKey`** — `WorkflowTransitionService.java:235`. Tied to the unresolved idempotency seam; once narrow per-run idempotency lands, deterministic event ids derived from `(runId, idempotencyKey)` become a natural follow-up.
- **`WorkflowRunEntity.createdAt` is `insertable=false, updatable=false` with no refresh** — `WorkflowRunEntity.java`. The entity never inserts new runs in story 1.5 (JdbcTemplate is used in tests). When the first JPA-driven insert path lands, callers will read stale (null) `createdAt` until manual refresh.
- **In-memory `workflowRun.setCurrentState` mutation precedes flush** — `WorkflowTransitionService.java:94-95`. On rollback the in-memory entity carries the unpersisted target state. Callers do not re-use the entity after exception today; defensive refresh-or-detach would be an architectural change.
- **`OffsetDateTime.now(ZoneOffset.UTC)` event-ordering collisions** — `WorkflowTransitionService.java:229`. Two events on the same run within the same JVM clock tick share `created_at`; the index `idx_workflow_events_workflow_run_id_created_at` is non-unique. Audit consumers ordering by `created_at` see non-deterministic order between concurrent appends. Revisit by adding a monotonic sequence column on `workflow_events` when audit ordering becomes load-bearing.
- **`runId` empty string yields `RUN_NOT_FOUND` rather than `INVALID_ARGUMENT`** — `WorkflowTransitionService.java:66`. Diagnostic muddiness only; legitimate fix is normalize-and-reject at boundary alongside other input-validation tightening.
- **Narrow per-run idempotency seam (story 1.9 territory)** — Story 1.5 keeps `idempotencyKey` in the service API but the current implementation only writes the key into `event.details` JSON; there is no DB-level uniqueness on `(workflow_run_id, idempotency_key)` and no pre-insert lookup. Replays against a still-valid source state would currently append duplicate events. The contract test that nominally covered this only proved state-machine rejection (renamed accordingly). When story 1.9 builds `IdempotencyService` it should provide the deduplication seam this transition surface plugs into.

## Deferred from: code review of 1-7-shared-application-command-model-pattern (2026-04-29)

- **Idempotency replay** — `submit()` creates a fresh `WorkflowRunEntity` on every call (no DB-level uniqueness on the key, no pre-insert lookup). State-change commands route through `WorkflowTransitionService.transition`, which also has no per-key dedup, so replays against an unchanged state currently append duplicate events; replays after the state has advanced surface as `ILLEGAL_TRANSITION` instead of returning the original outcome. Story 1.9 (`IdempotencyService`) owns the dedup seam.
- **Missing/blank `Idempotency-Key` header → generic 400** — `@RequestHeader("Idempotency-Key") String idempotencyKey` raises Spring's `MissingRequestHeaderException` (default 400) rather than the project's stable `INVALID_COMMAND_PAYLOAD`. Story 1.8 (Problem Details mapper) maps it.
- **`DomainException` becomes HTTP 500 (no `@RestControllerAdvice`)** — entire REST surface returns 500 for any validation failure today. Story 1.8 explicitly defers HTTP serialization of stable error codes.
- **Jackson parse failures bypass `INVALID_COMMAND_PAYLOAD`** — malformed JSON, unknown enum strings, type-mismatched `Integer artifactVersion`, null `actorType`, etc. all fail at the framework layer before `WorkflowCommandService.validate(...)`. Story 1.8 territory.
- **Details JSON >64KB → `DataIntegrityViolationException`** — `ck_workflow_events_details_size` constraint surfaces as raw infrastructure error, not `INVALID_COMMAND_PAYLOAD`. Story 1.8 mapping territory.
- **CLI invalid `--actor-type` raises Spring Shell parse error** — Spring Shell's automatic enum coercion fails before reaching the application layer; CLI users see a different error shape than REST. CLI hardening + story 1.19 territory.
- **No newline/control-char guard on `event.setReason(...)`** — log-injection vector when audit pipelines render the string into log lines. Story 1.19 (structured logging + redaction) territory.
- **No injectable `Clock`** — `OffsetDateTime.now(ZoneOffset.UTC)` called directly in both `WorkflowCommandService.submit` and `WorkflowTransitionService.doTransition`; non-deterministic for time-sensitive tests. Pre-existing pattern.
- **In-place `LinkedHashMap` mutation on `@JdbcTypeCode(SqlTypes.JSON)` columns** — works at INSERT time but fragile if any future code path fetches an event and mutates `details` in place; Hibernate's dirty-check relies on reference change. Pre-existing pattern in `WorkflowEventEntity`.
- **`correlationId` not propagated through structured logs / MDC** — preserved in event details only. Story 1.19 explicitly.
- **CLI Spring Shell surface only exposes `submit`** — approve/reject/retry/takeover have no CLI command. Story 1.7 explicitly scopes only submit end-to-end; full CLI mutation surface is later-epic work.
- **`linearTicketReference` has no `@Pattern` format constraint** — accepts any 128-char `@NotBlank` string; arbitrary content propagates into `workflow_events.details`. Beyond story 1.7 scope.
- **`commandType()` derived from `getClass().getSimpleName()` is rename-fragile** — persisted into `workflow_events.details.commandType`; record renames silently corrupt the wire value. The story's review patch list adds a literal-pinning test as the minimum guard; a registry-backed enum (analogous to `WorkflowEventType`) is a follow-up if `commandType` becomes a routed value.
- **`WorkflowTransitionService.transition(...)` telescoping overloads (4 variants)** — `(... idempotencyKey)`, `(... failureCategory)`, `(... eventDetails)`, `(... failureCategory, eventDetails)`. Refactor to a request object/builder; not blocking.
- **No seams declared for `ArtifactOperationService` / `IdempotencyService` / `RunnerBroker`** — Task 3 wanted "clean seams that don't force premature implementations." Collaborators arrive in their own stories (1.9, 1.12, 1.13); fold the seam in there.
- **`WorkflowStateChangeResponse.currentState` is the static target literal** — `transition(...)` returns void, so the response cannot reflect the actual post-transition state without widening the transition signature. Acceptable today since `WorkflowTransitionTable` rejects illegal targets before commit.
- **`event.setReason("workflow submitted")` is hardcoded English** — i18n out of scope for foundation phase.
- **`details::text` Postgres-specific cast in tests** — ties contract tests to one dialect; Testcontainers always uses Postgres so non-blocking.
- **`@AfterEach cleanDatabase` deletes only `workflow_events` and `workflow_runs`** — sufficient for current test scope; expand when other FK tables get exercised.

## Deferred from: code review of story 1-7 (2026-04-30, second pass)

- **No authentication/authorization on `WorkflowController` POST endpoints** — no `@PreAuthorize`, no security config; `actorIdentity` is taken from request body, so any client can claim any identity for submit/approve/reject/retry/takeover and the audit log. Foundation phase has no auth story; track for a dedicated security epic.
- **`Map.of(...)` rejects null values — defensive only** (`WorkflowCommandService.java:428-431,446-449`) — today every value is `@NotNull`-validated upstream, so this is safe. A future validation relaxation or a refactor that reorders `validate()` after the map construction would NPE. Use `LinkedHashMap`-with-null-skip when convenient.
- **CLI `submit` return-string format is coupled with the equivalence test** (`WorkflowCommands.java:42`) — test asserts the literal `"<id> submitted (state: <STATE>)"`. Any UX tweak breaks the test, and the test arguably compares the wrong thing (formatted string vs structured result).
- **`validate()` does not cascade `@Valid` for nested types** (`WorkflowCommandService.java:503`) — all commands are flat records today; cascade is moot. When the first nested record-type lands, `@Valid` cascade convention must be applied.
- **ActorType enum bound directly at REST/CLI boundary** — adding/removing/renaming enum values is a breaking API change. Foundation phase ships internal enums at the boundary; a stable wire-format DTO mapping is a dedicated concern beyond story 1.7.
- **`RunnerContractValidator` path-traversal scope reduction (D1 from 2026-04-30 review)** — `walk(document, "$", errors)` was replaced with `inspectArtifactReferencePaths(document.path("approvedSpecificationReference"), ...)` + `inspectArtifactReferencePaths(document.path("artifactReferences"), ...)` [`deliveryline-runner-contracts/.../RunnerContractValidator.java:117-145`]. Companion test loosened from `assertEquals(4, ...)` to `assertTrue(... >= 4)` [`RunnerContractValidatorTest.java:60`]. **Reason:** out-of-scope for story 1.7's `application.workflow.commands` work — split into a separate commit/PR for the runner-contracts owner to review (likely a story 1.6 follow-up). Either accept as intentional design narrowing (and tighten the test back to exact count) or revert as a regression.

## Deferred from: code review of story 1-9-idempotency-service (2026-05-01)

- **W1: `System.console()` interactivity detection unreliable on JDK 21** [`CliInteractivityDetector.java`] — IDE run configurations and CI runners with redirected stdin both yield `null` for `System.console()`, so an interactive operator in IntelliJ who omits `--idempotency-key` gets `MISSING_IDEMPOTENCY_KEY` instead of auto-gen. JDK 22 brings `Console.isTerminal()` for clarity. Track in a future CLI hardening story; do not invent a custom heuristic in foundation phase.
- **W2: Transaction isolation level not pinned at app level** [`IdempotencyService.java`] — Race-handling correctness (loser blocks on winner via `SELECT FOR SHARE`) depends on PostgreSQL `READ COMMITTED` default. Under `REPEATABLE READ`/`SERIALIZABLE`, the loser may not see the winner's row → `IllegalStateException`. Add a startup assertion or contract test in a future infra story.
- **W3: `complete()` does not enforce idempotency-record state-machine transitions** [`IdempotencyService.java`] — All current callers are correct (only `RESERVED → COMPLETED`/`FAILED`). Adding a transition guard would protect against future caller mistakes overwriting finalized records, but is defensive only today. Defer to a state-machine-enforcement story.
- **W4: ProblemDetails missing-header tests mock the service rather than exercising the actual null-header path** [`ProblemDetailsContractTest.java` lines ~1551–1571, `WorkflowAdapterEquivalenceTest.java` lines ~1153–1169] — `when(workflowCommandService.submit(any())).thenThrow(...)` injects the exception; the end-to-end null-header flow through `WorkflowCommandService` is not exercised. Real-MVC coverage will land via P6; broader test-quality pass can revisit other mock-the-thing-under-test patterns.
- **W5: Replay returns *current* workflow state, not the original outcome** [`WorkflowCommandService.replaySubmit` / `replayStateChange`] — Edge Case Hunter flagged this as a forward-looking idempotency-semantics concern: a retried `submit` whose run has since advanced (e.g. to `Executing` or `TakenOver`) returns the new state, not the `(runId, Inbox)` shape the original call produced. Implementation matches the literal spec wording (Task 4.3: "reconstruct the typed result from the stored `resultRef`, the command type, and existing domain state"). Revisit in a future story considering whether to persist a full result snapshot in `idempotency_records` (extra column, schema migration) for stricter idempotency replay semantics.

## Deferred from: code review of 1-11-archunit-package-boundary-tests (2026-05-03)

- **`WorkflowEventPersistenceAdapter.append` re-fetches `WorkflowRunEntity` per event call** — N+1 risk only matters once batch event paths exist; not exercised by this story. [`deliveryline-backend/src/main/java/org/dradgo/adapters/persistence/WorkflowEventPersistenceAdapter.java`]
- **JPQL `:currentState` String coercion in `WorkflowRunRepository.updateCurrentState`** — Hibernate's enum AttributeConverter likely handles, but verify against Postgres `workflow_state` column type at runtime. [`deliveryline-backend/src/main/java/org/dradgo/adapters/persistence/repository/WorkflowRunRepository.java`]
- **`ArchitectureDiagnosticMetaTest` only validates one rule's diagnostic format** — AC8 only requires one such test; broader coverage (per-rule remediation message) would be additive. [`deliveryline-backend/src/test/java/org/dradgo/architecture/ArchitectureDiagnosticMetaTest.java`]
- **`ImportOption.DoNotIncludeTests` excludes test packages from architecture rules** — Test code can violate boundaries (e.g., touch JPA from REST tests) without detection; design choice, not a regression. Consider a separate test-only rule pass in a future story. [`deliveryline-backend/src/test/java/org/dradgo/architecture/ArchitectureBoundaryTest.java`]
- **AC10 — `RegistryContractTest` Surefire group routing not wired in pom.xml** — Tag `@Tag("architecture")` is added on the test, but no Maven Surefire `<groups>`/`<includes>` configuration includes it. Spec defers CI profile/group routing to story 1-21. [`deliveryline-backend/pom.xml`]
- **`IdempotencyService.complete` lock-ordering test gap** — Refactor moves the lock from the service to the adapter; no test exercises the lock-contention scenario. Test follow-up. [`deliveryline-backend/src/test/java/org/dradgo/application/idempotency/`]
- **`IdempotencyService.isReservationStale` trusts port to never report `false` for stale records** — Pre-existing repository contract carried over via the new port. [`deliveryline-backend/src/main/java/org/dradgo/application/idempotency/IdempotencyService.java`]
- **`IdempotencyPersistenceAdapter.tryReserve` SQL inline mechanically relocated from service** — Cleaner abstraction (e.g., `@Modifying` repository method) is a refactor follow-up; overlaps with the related `decision_needed` item. [`deliveryline-backend/src/main/java/org/dradgo/adapters/persistence/IdempotencyPersistenceAdapter.java`]
- **`IdempotencyRecordPort.tryReserve` six-`String`-parameters signature** — Easily transposable at call sites; refactor to a parameter object. [`deliveryline-backend/src/main/java/org/dradgo/application/idempotency/spi/IdempotencyRecordPort.java`]
- **`InvalidShellAdapter` test fixture in production-style package path** — `src/test/java/org/dradgo/adapters/cli/InvalidShellAdapter.java` exists purely to trip the architecture rule; add a class-level Javadoc clarifying its purpose so IDE users don't mistake it for a real adapter. [`deliveryline-backend/src/test/java/org/dradgo/adapters/cli/InvalidShellAdapter.java`]
- **`WorkflowEventRecord.details` Map exposed directly via record accessor** — Records can't enforce immutability of nested collections; broader API hardening pass. [`deliveryline-backend/src/main/java/org/dradgo/application/workflow/spi/WorkflowEventRecord.java`]

## Deferred from: code review of story 1-12-artifact-operations-skeleton (2026-05-08, round 5)

- **Tests still construct `ArtifactRecordSnapshot` via deprecated 11-arg convenience ctor (~26 sites)** — Known pre-existing footgun deferred per R9 to avoid a mechanical-edit churn bundle.
- **Tests still construct `ArtifactOperationService` via deprecated 5-arg ctor that bypasses the workflow-run terminal-state guard** — Same rationale as above; per C3-P4 resolution.
- **JVM-side `OffsetDateTime.now(UTC)` in `@PrePersist` vs DB `default now()` clock-skew** — Codebase-wide pattern across all entities, not specific to story 1.12. Defer to a project-level timestamp policy decision.
- **NFD vs NFC filename round-trip on macOS readers vs Linux writers** — `LocalArtifactStore.write` normalizes to NFC; `isReadable(...)` does not re-normalize on read. Cross-platform polish beyond skeleton scope.
- **Windows `MAX_PATH` (260-char) limit not validated against the resolved storage path** — Tests use short names; opt-in long-path policy is env hardening for later.
- **`ArtifactRecordPersistenceAdapter.findLatestByParentArtifactId` walks `parentArtifact` lazily** — Perf hazard for deep lineages (O(K²) lazy fetches). Note for Epic 4 read-side optimization.
- **`RegistryContractTest`/`ck_artifact_operations_operation_type` couples DB constraint to enum** — Will fire if a future enum value is added without a parallel migration. Known coupling, not a current bug.
- **V3 unique-constraint widening has no data-cleanup phase for legacy rows** — Operational guidance for upgraders; if pre-V3 data has dupes on the new key, the migration will fail at constraint-add. [`V3__widen_artifact_operation_idempotency.sql`]
- **`ArtifactReconciliationResult.orphanedOperations` returns a mutable `List`** — Minor encapsulation leak on the result type.
- **`version=Integer.MAX_VALUE` overflow surfaces as raw `DataIntegrityViolationException`** — Pathological; constraint `ck_artifacts_version` rejects but error type is untyped. Consider explicit pre-check if any real producer approaches the bound.

## Deferred from: code review of 1-12-artifact-operations-skeleton (2026-05-10) — Chunk A

- **W1: Inconsistent paired-validation between `markAvailable` and `markFailed` event-write paths** — Constraint-safe today; revisit on next event-table refactor.
- **W2: `isApprovalEligible` TOCTOU between metadata read and payload read** — Intrinsic to non-tx read; story 1.20 (REST endpoints) revisits read-side gating.
- **W3: `markLateOrStale` not under workflow-run pessimistic lock** — Marginal race; needs broader runner-attribution refactor (Epic 4 recovery).
- **W4: `ArtifactChecksum.canonicalAlgorithm` Locale.ROOT casing accepts Unicode lookalikes** — Theoretical attack surface; ASCII-only validator hardening pass.
- **W5: `version` overflow at `INT_MAX` within a `(workflow_run, artifact_type)` lineage** — Unreachable in practice (2.1B versions per lineage). Document.
- **W6: `isApprovalEligible` silently downgrades artifacts whose stored `checksum_algorithm` is no longer in `ALLOWED_ALGORITHMS`** — Data-migration concern after `ALLOWED_ALGORITHMS` is tightened.
- **W7: `ArtifactRecordSnapshot` 13-arg / 11-arg ctor pair is collision-prone for future field additions** — Refactoring task; acknowledge in next snapshot-schema change.
- **W8: `ArtifactEventRecord` nested mutable values (lists/maps inside `details`) not deep-copied** — Deep immutability requires a walker; punt unless concrete bug appears.

## Deferred from: code review of 1-12-artifact-operations-skeleton (2026-05-11)

- **F10: REPLACE/UPDATE silently creates draft on empty lineage** — `createOrAdvanceArtifact` falls back to `createDraft` when `latestArtifact` is absent and `operationType` is `UPDATE` or `REPLACE`. An update against a non-existent lineage should arguably fail with a typed error rather than bootstrapping a v1 draft. Pre-existing semantic; D1 (Bundle 3) handles the CREATE-against-existing-failed-leaf case. [`ArtifactOperationService.java:createOrAdvanceArtifact`]
- **F11: `findLatestActiveLineageMemberEntity` loads all artifact versions without LIMIT** — `findByWorkflowRunPublicIdAndArtifactTypeOrderByVersionDesc` has no row cap. For a long-lived run with hundreds of artifact versions this is an O(N) heap load and a slow query inside an advisory lock. Acceptable for skeleton stage; add a paginated or `LIMIT`-capped variant when lineage depth becomes measurable. [`ArtifactRecordPersistenceAdapter.java:findLatestActiveLineageMemberEntity`]
- **F12: N+1 lazy loads in `belongsToLineage` parent chain walk** — `cursor.getParentArtifact()` on a `@ManyToOne(fetch=LAZY)` field triggers a separate SELECT per ancestor hop. O(S×D) round trips (siblings × lineage depth) inside the write transaction while the advisory lock is held. C2-P6 added `@Transactional(readOnly=true)` to prevent `LazyInitializationException`; batch-fetch or a recursive CTE query would eliminate the N+1. Pre-existing pattern; deferred to a query-hardening story. [`ArtifactRecordPersistenceAdapter.java:belongsToLineage`]

## Deferred from: code review of 1-15-spring-shell-cli-commands-submit-status-history (2026-05-14)

- **F13: History query loads 1001 entities before throwing HISTORY_TOO_LARGE** — `WorkflowEventPersistenceAdapter` issues `PageRequest.of(0, HISTORY_CEILING + 1)` and only after materializing every column of every row checks `entities.size() > HISTORY_CEILING`. A `count(*)` precheck or `LIMIT 1000` plus a separate `EXISTS` probe would avoid the wasted bandwidth on the failure path. The diff also relies on the existing `idx_workflow_events_workflow_run_id_created_at` without a covering hint for the `archived_at IS NULL` filter. Pilot-acceptable for now; revisit when run-event volumes grow. [`WorkflowEventPersistenceAdapter.java:588–602`]
- **F14: WorkflowEventPersistenceAdapter `--since` query has driver-specific sub-microsecond precision rounding** — `OffsetDateTime` is forwarded as-is to JPA, but Postgres `timestamptz` only stores microsecond precision. An event at `09:00:00.000001Z` can be missed (or doubly included) when callers pass nanosecond-precision values like `09:00:00.0000019Z`. Realistic impact is null for pilot CLI flows. Truncate to microseconds (or document) when wider tooling consumes this surface. [`WorkflowEventPersistenceAdapter.java:50–72`]
- **F15: WorkflowInspectionService.getStatus issues N queries (one per ArtifactType)** — the per-type latest-artifact lookup is a `for (ArtifactType t : ArtifactType.values()) { ... }` loop with no early termination or batching. Linearly grows with every artifact type added in stories 1.16+, against the same indexed table. Replace with a single grouped query (`SELECT artifact_type, MAX(version) ... GROUP BY artifact_type`) when artifact types multiply. Sits with F11/F12 in a future query-hardening story. [`WorkflowInspectionService.java:75–83`]
- **F19 (TECH DEBT): `IntegrationLinkPersistenceAdapter` conflict warning log dropped `externalRef`, breaking operator triage and creating a redaction-policy contradiction with the CLI render allow-list** — the conflict log line is now `insert conflict integrationType={} workflowRunId={} cause={}` (no ticket disambiguation), while `WorkflowInspectionService.ALLOWED_DETAIL_KEYS` includes `linearTicketReference` and renders it in `status`/`history` output. The same value is treated as PII in service logs but as operator-safe in the CLI surface — pick one classification (restore raw `externalRef` in the log, or emit a `externalRefHash=<8 hex>` correlatable form). Bundle with F16/F17/F18 in the concurrency/observability hardening story. [`IntegrationLinkPersistenceAdapter.java:522–537`]
- **F18 (TECH DEBT): `IdempotencyRecordRepository.findWithLockByKey` `@Lock(PESSIMISTIC_READ)` silently removed while method name still advertises locking** — the diff drops the `@Lock` annotation plus the `Lock`/`LockModeType` imports, with no replacement (`SELECT ... FOR UPDATE`, advisory lock, or rename) visible. Either an accidental regression (callers depended on the row-lock for serialized state-machine transitions, now exposed to lost-update bugs) or an intentional shift to constraint-based contention via the new `REQUIRES_NEW` flow (in which case the method should be renamed `findByKey`). Resolve alongside F16/F17 in the concurrency-hardening story; either restore the annotation or rename the method to match its actual semantics. [`IdempotencyRecordRepository.java:4,7,40`]
- **F17 (TECH DEBT): `IdempotencyService.MAX_RESERVATION_ATTEMPTS` raised 3 → 200 with fixed 10ms blocking sleep, no jitter/backoff/metric** — losing callers block their thread for up to 2s while the outer transaction holds connection/locks; under burst contention on a single key (or with a stuck winner before the 10-minute staleness threshold) this saturates Tomcat / Spring Shell threads. Tightly coupled to F16 — the polling existence is partly a workaround for the eager-COMPLETED visibility window. Fix candidates: add jitter + exponential backoff capped at ~5s; lower the ceiling and surface a fast-fail `CONCURRENT_TRANSITION_CONFLICT`; or instrument retry exhaustion with a counter + WARN. Pair with F16 in the concurrency-hardening story. [`IdempotencyService.java:39–41,96–122`]
- **F16 (TECH DEBT): Eager-COMPLETED idempotency record committed before outer workflow_run transaction commits** — `WorkflowCommandService.completeWhenTransactionFinishes` writes COMPLETED via `REQUIRES_NEW` immediately, then registers an `afterCompletion` callback to flip COMPLETED→FAILED on rollback. Two windows: (1) concurrent replayers between the inner and outer commits see `status=COMPLETED, resultRef=run_xxx` but `findByPublicId` returns empty, prompting `findWorkflowRunForReplay` to poll 200×10ms = ~2s before throwing `RUN_NOT_FOUND`; (2) JVM crash between the two commits leaves the idempotency key permanently COMPLETED with a `resultRef` pointing at a non-existent workflow_run, and the read-path's "FAILED is terminal" semantics in `IdempotencyService.tryResolveExistingRecord` then turn the eventual rollback into a permanent lock-out for that key. Fix candidates: move COMPLETED into `afterCommit`; tighten the read path to tolerate "COMPLETED but pending"; or drop the eager commit and lose the cross-tx FAILED-survives-rollback property. Deliberate concurrency-design call deferred to a hardening story before any production load. [`WorkflowCommandService.java:122–134`] + [`IdempotencyService.java:83–88`]


## Deferred from: code review of 1-17-supported-environment-matrix-and-cross-platform-scripts (2026-05-14)

- **DoctorService.aggregate() returns PASS when zero checks ran** — `effectiveOrder` intersection with explicit `--exclude` (or future loosened validation) can yield an empty `checks` list; `anyPass=false, anyWarn=false` falls through to `DiagnosticsStatus.PASS`. Misleading green report when no probes actually executed. Pre-existing from story 1.16. [`DoctorService.java:261-281`]
- **DoctorService.runSingleProbe catches `RuntimeException` but not `Error`** — A `LinkageError`/`NoClassDefFoundError`/`OutOfMemoryError` from a single probe aborts the entire doctor run instead of degrading that one slot to FAIL. Pre-existing pattern from story 1.16; revisit when probes start touching native or heavy classpath surfaces. [`DoctorService.java:192-201`]
- **AC5 "no string-concat path construction" is audit-only** — `PathHandlingContractTest` exercises one consumer (`DoctorProbeAdapter.probeArtifactDirectory`); the broader claim across `application-local.yml` consumers rests on manual inspection. Add an ArchUnit / static-analysis rule that flags `String` concatenation of path-like literals when consumer surfaces multiply.
- **`probeSupportedEnvironmentEmitsDebugLogOnEntryExit` mutates global Logback state** — Attaches an appender and bumps level on a static logger; under `@Execution(CONCURRENT)` other tests writing through this logger would race. Refactor to a captured-events utility or use `LogCaptor` / per-test logger context. [`DoctorProbeAdapterTest.java:828-856`]
- **`parseLinuxRelease` keeps the last `ID=` line on pathological multi-ID files** — Niche; `/etc/os-release` spec doesn't strictly forbid duplicates, but the silent last-wins behavior is undocumented. Switch to first-wins (`if (id.isBlank())`) or warn-and-skip when maintainable. [`DoctorProbeAdapter.java:781-787`]

## Deferred from: code review of 1-18-cli-minimum-viable-recovery-baseline (2026-05-16)

- **Broker dispatch key `<key>:runner` may violate `IdempotencyKeyValidator.OPAQUE_KEY_PATTERN`** — the regex `[A-Za-z0-9-]{16,128}` rejects `:`, and the 7-char suffix can push a max-length user key to 135 chars. Today the broker only checks `isBlank`, but a future hardening that adds `requireValid` to broker dispatch would fail every retry after the prep audit has committed. Pre-existing namespacing decision (Dev Notes line 270-280). [`RecoveryService.java:442`]
- **`lastSuccessfulStage` field stores a `WorkflowState`, not a runner stage** — the field name advertises a stage (e.g., `execution`/`investigation`) but the value is a workflow state (e.g., `Executing`). Pre-existing from the original 1.18 design; the walkthrough doc renders the state name. Rename touches the JSON schema, walkthrough doc, and snapshot tests. [`RecoveryService.java:506`]
- **TOCTOU on pre-prep-tx state check** — `RecoveryService.retry` reads the run snapshot OUTSIDE the REQUIRES_NEW prep tx; a concurrent state flip between the check and the prep tx is possible. The inner `WorkflowCommandService.retryWorkflow` re-validates the legal transition under tx, so the breach is narrow. [`RecoveryService.java:248-260`]
- **`reason` and `errorClass` now leak through history via the extended allow-list** — `reason` is operator-supplied free text (capped 512 chars by `RetryWorkflowCommand`); `errorClass` is the JVM class simple name of any broker exception. F7's `escapeForText` neutralizes CR/LF/TAB; redaction policy is the only safety net for pasted secrets. Constrain `errorClass` to a typed-broker-error whitelist if Epic 4 expands the broker contract. [`WorkflowInspectionService.java:172`]
- **F528 native SQL has no integration test** — `findLatestCorrelationIdInDetails` correctness depends on real PostgreSQL JSONB semantics; only mock tests at the SPI boundary exist. Testcontainers (`\.\pipe\docker_engine`) unavailable. Lands with story 1.21 CI tiered pipeline. Same env block as F534. [`WorkflowEventRepository.java:81-92`]
- **`findLatestRunner` may pick a newer PENDING runner row from a subsequent retry** — surfacing a `lastActivityTimestamp` that disagrees with `failedStage`'s vintage in rare retry-after-retry-after-Failed interleavings. Narrow read-skew window. [`RecoveryService.java:539-541`]
- **`pickLatest` tiebreaker on lexical `publicId` is inconsistent with DB-side numeric `id DESC`** — when two rows share `created_at`, the in-memory pick uses string ordering while the DB-side queries use numeric BIGINT id. ULID prefixes are time-monotonic in practice, so the divergence is theoretical. Pre-existing helper. [`RecoveryService.java:546-549`]
- **`getStatus` triggers second `findByPublicId` via `describeFailure` inside the same readOnly tx** — JPA first-level cache absorbs the second call (no second SQL roundtrip), but the code shape suggests duplicated work. Resolved D3 (2026-05-16) → defer; revisit if profiling shows the SPI indirection matters. [`WorkflowInspectionService.java:101`, `RecoveryService.java:475`]

## Deferred from: code review of 1-19-structured-logging-and-correlation-ids (2026-05-16)

- **`CorrelationIdFilter` only reads the first `X-Correlation-Id` header value** [`CorrelationIdFilter.java:45`] — RFC 9110 allows multiple values; `http.getHeader(...)` returns only the first. Edge case unlikely in practice but breaks the "echo what the client sent" contract if a proxy duplicates the header.
- **`CorrelationIdFilter` response-header echo brittle if `tryParse` is later relaxed** [`CorrelationIdFilter.java:46-50`] — Defense rests entirely on `tryParse`'s exact-length + `UUID.fromString` validation. Worth a regression guard (sanitize CR/LF in the `setHeader` path or use a stricter response API).
- **`@PreDestroy clearHolder()` race with shutdown logs** [`RedactionLayoutInitializer.java:42-45`] — After `@PreDestroy` clears the `RedactionLayoutHolder` static, any further log line during graceful shutdown (Hikari close, JPA flush, Spring teardown) bypasses redaction. Defer pending production-deployment story.
- **`RedactingMessageConverter.convert` doesn't redact if `super.convert` throws** [`RedactingMessageConverter.java:20-22`] — Theoretical edge case where a log event's `getFormattedMessage()` recursively logs (e.g., `log.info("ctx={}", ctx)` where `ctx.toString()` calls `log.info`). Stack-overflow before reaching the redactor.
- **`ProblemDetailsMapper.correlationId` extension can be empty on async / error-controller re-dispatch** [`ProblemDetailsMapper.java:283-286`] — Spring's `DefaultHandlerExceptionResolver` or an `@ExceptionHandler` invoked outside the servlet filter chain (or async boundary) may see an empty MDC. Filter chain covers normal paths.
- **`ArtifactOperationService` validator logs run before MDC scope** [`ArtifactOperationService.java:255-258,357-360`] — `requireActor`, `validateStorageRef`, `validateChecksum`, `validateFailureReason` throw before `MdcKeys.beginScope(ARTIFACT_ID, ...)`. Lower-severity sibling of the `RecoveryService` issue addressed as P11.
- **`JsonSchemaStabilityTest` doesn't assert stale-MDC absence** [`JsonSchemaStabilityTest.java:69-76`] — Test only asserts `correlationId` and `workflowRunId` are present with expected values. Never asserts no stale `artifactId` appears, leaving cross-test MDC bleed in JSON output unpinned.
- **Doctor `--verbose` correlation-id surfacing (AC9 of story 1.19)** [`DoctorCommands.java:69-89`] — Story 1.19 AC9 lists `doctor` as needing `--verbose` correlation-id stdout output. Skipped because the doctor render path mixes `System.out.println` with the returned string and correlation-id surfacing requires the CLI output channel refactor owned by story 2.30.
- **AC6 four call-site discipline audit (D5 from story 1.19 review)** -- inspect logging in `LinearRealAdapter` (HTTP request/response handling), `DoctorProbeAdapter.probeConfigPermissions`, and `LocalArtifactStore.write/read` for any line that violates the permitted-field allow-list in AC6. The redaction layout safety net (now pinned by the rewritten `LoggingForbiddenPayloadContractTest`) covers known secret patterns, but raw bundle JSON / PEM private keys / full HTTP bodies will not be scrubbed by pattern matching alone.
- **Redaction policy coverage gap for raw PEM private keys and generic bundle JSON shapes** -- discovered while rewriting `LoggingForbiddenPayloadContractTest` (story 1.19 P1). `RedactionPolicyService` currently scrubs `Bearer`/`api_key=`-shaped tokens but not raw PEM blocks or attribute-shape JSON like `"contextBundle":`. Owner: story 1.10 follow-up.
  - **Status update 2026-05-19:** This is **F20** in retrospective discussion. Scheduled for closure by **Epic 2 story 2-24** (Artifact Content Sanitization + Redaction-Gap Closure) per `_bmad-output/planning-artifacts/sprint-change-proposal-2026-05-19.md`. Closure ACs: 2-24 AC13 (PEM pattern + bundle JSON pattern) + AC14 (PEM + bundle-JSON fixture additions) + AC15 (frontend second-pass filter as defense-in-depth) + AC18 (mark this entry closed when 2-24 ships).
  - **CLOSED 2026-05-26 (story 2-24):** PEM-block patterns + frontend defense-in-depth filter closed via AC13/AC14/AC15/AC16. See `2-24-artifact-content-sanitization-untrusted-runner-output.md`.

## Deferred from: code review of story-1.23 (2026-05-19)

- **AC4 state-names mismatch actual `WorkflowState` enum (spec defect)** — Spec lists `SpecDraftReady`, `SpecAvailable`, `ImplementationPlanReady`, etc.; actual enum is `Inbox`/`Planned`/`Investigating`/`WaitingForSpecApproval`/`Executing`/`WaitingForReview`/`Completed`. Fixtures correctly use real enum values. Owner: spec-edit follow-up; no code change needed.
- **`FoundationGateRegressionDetectionTest` one annotation-removal away from self-DoS** [`FoundationGateRegressionDetectionTest.java:1487`] — Removing `@Disabled` would actually run the documented proof-case mutations on main. Future ArchUnit or Checkstyle guard requiring `@Disabled` on this class is desirable.
- **No GHES authentication-host check in branch-protection helpers** [`scripts/ci/configure-branch-protection.{sh,ps1}`] — If `gh` is authenticated against a different host than the local remote, scripts silently write to wrong repo. Out-of-scope hardening for the 1.23 close.
- **`./mvnw` shell-dependency on Windows runner** [`.github/workflows/ci.yml:782 foundation-gate job`] — Pins `ubuntu-latest`; if the job ever migrates, single-quoted globs + backslash continuations break in PowerShell. Add `shell: bash` defensively later.
- **Generic `<!-- foundation-gate-status -->` PR-comment marker** [`.github/workflows/ci.yml:874`] — Low cross-tool collision risk; namespace with workflow filename in a future cleanup.
- **`Path.of("..")` CWD-dependent paths** [`BranchProtectionConfigSmokeContractTest.java:459`, `RunnerContractFixturesFoundationContract.java:44`] — CI safe (Maven runs from module dir); only matters for local IDE runs from repo root.
- **Spring TestContext cache thrash potential in foundation-gate aggregator** — Different `@SpringBootTest` configs across foundation-contract classes could multiply context-start cost; 20-min job timeout absorbs current load. Tune Failsafe fork settings if foundation-gate runtime grows.
- **`failedStage: "implementation"` on `runner.started` events** [`fixture-event-streams/happy-path-success.json:2921` and `execution-failure-with-retry.json`] — Semantically odd key reuse. Rename `failedStage` → `stage` on non-failure events in a future fixture-cleanup pass.

## Deferred from: code review of story-2.1 (2026-05-19)

- **Enforcer only checks `index.html`, not full `dist/assets/` tree** [`deliveryline-backend/pom.xml:113-135 require-frontend-dist`] — A partial or interrupted frontend build can leave `index.html` present while asset chunks are missing or stale. Backend then ships a jar pointing at deleted Vite chunks → blank page. Hardening: enforcer should also check `dist/assets/` exists and is non-empty, or hash-check against `package-lock.json` mtime.
- **Lockfile entries for React/types/scheduler annotated `"peer": true`** [`deliveryline-frontend/package-lock.json`] — npm 10.x metadata flags direct dependencies as peers in unusual peer-resolution conditions. Likely cosmetic but worth confirming `npm ci --strict` doesn't fail in CI's strictest mode. Follow-up investigation.
- **Windows long-path validation asserted in docs without diff-level evidence** [`docs/supported-environments.md:93-99`] — Story claim says no `node_modules/` path exceeds 260 chars. CI Windows matrix is the ongoing enforcement, but a deterministic check (script-validated) would prevent silent regressions when transitive deps add deeply-nested binding packages (e.g., `@rolldown/binding-*`, `lightningcss-win32-arm64-msvc`).
- **`mvn -pl deliveryline-backend -am` can resolve stale frontend POM from `.m2` cache** [`deliveryline-backend/pom.xml`] — Edge case where a previously-installed frontend artifact in `~/.m2` makes the build pass even though the on-disk `target/dist/` is stale or missing. Rare in practice; document in developer setup notes that `clean install` from root is the canonical path.
- **Interactive HMR / proxy round-trip verification deferred to manual review** [`deliveryline-frontend/vite.config.ts`] — Status update 2026-05-20: resolved. User confirmed manual verification in PowerShell and Git Bash; no follow-up story needed for this specific item.
- **Strip Vite/React template promo content** [`deliveryline-frontend/src/App.tsx`, `public/icons.svg`, `README.md`] — Scaffold ships unmodified Vite/React boilerplate UI ("Join the Vite community", links to `chat.vite.dev` / `x.com/vite_js` / `bsky.app/profile/vite.dev`). Decision (2026-05-19 code review): defer the strip to story 2.7 (real tri-pane app shell), but add a `console.warn` placeholder in this story so dev builds visibly flag the scaffolding state. Re-evaluate when story 2.7 begins.

## Deferred from: code review of story-2.1 (2026-05-20)

- **Backend static cleanup deletes from the shared `target/classes/static/assets` namespace** [`deliveryline-backend/pom.xml:228-299`] — Accepted/deferred. Story 2.1 currently ships only copied frontend assets there, so this is not a present regression. Revisit once story 2.28 or any later story introduces backend-owned static assets under the same `static/assets` path; cleanup should then target only frontend-generated outputs.

## Deferred from: dev-story of 2-5-tanstack-router-setup-and-typed-routes-and-deep-link-handling (2026-05-21)

Story 2.5 is the navigation skeleton only (route tree + typed/validated params + UI states + loader seam). Per the resolved Q2, the following AC sub-items ship as **structure + documented seam** here and are wired by the owning story when the data layer / SPA fallback exist. Each route loader carries a `// SEAM (...)` marker at the exact attach point.

- **AC3 — real loader query prefetch.** Route loaders return typed STUB data today; the real `queryClient.ensureQueryData(workflowKeys.detail(workflowRunId))` (and artifact equivalent) prefetch — so deep-linked detail/artifact views render without a loading flash — lands when TanStack Query + the key factories + the typed API client exist. Owner: **story 2.6**. [`deliveryline-frontend/src/routes/workflows/$workflowRunId/index.tsx`, `.../artifacts/$artifactId.tsx`]
- **AC4 — backend-404 → not-found state.** The dedicated **Run not found** / **Artifact not found** states are built and wired as each data route's `notFoundComponent`, but the `throw notFound()` that drives them on a real backend 404 lands with the loader data wiring. Owner: **story 2.6**. [same files]
- **AC9 — correlation-ID request header.** The seam exists (`src/lib/api/correlation.ts`: `CORRELATION_ID_HEADER = 'X-Correlation-Id'` + `newCorrelationId()`) plus a `// SEAM (story 2.6/1.19):` marker in each loader. The actual header attaches once the API client is built — there is no HTTP client yet. Owner: **story 2.6** (correlation IDs from **story 1.19**). [`deliveryline-frontend/src/lib/api/correlation.ts`]
- **AC5 — production deep-link SPA fallback.** Dev deep links already resolve via Vite's history fallback; production deep links require the `SpaFallbackController` (serve `index.html` for unmatched non-`/api` paths). Owner: **story 2.28**. [documented in `deliveryline-frontend/src/routes/README.md`]
- **Route / component / loader / a11y tests.** No Vitest/Playwright added here (Q3) — only the `node --test` param-validation + route-tree-generation gate (`npm run check:routes`). The component/route/a11y suite that exercises these routes is owned by **story 2.27** (its AC5 explicitly tests these routes).

## Deferred from: code review of story-2.32 (2026-05-22)

- **Stale `jacoco.exec` / `jacoco-it.exec` on non-clean local rebuilds** [`deliveryline-backend/pom.xml`] — The JaCoCo agent's default append behavior plus skipped-tier exec-file reuse can let a non-clean local `mvn verify` merge stale execution data into `jacoco-merged.exec`. Pre-existing story-1.21 exec-file behavior, not introduced by story 2.32. CI runs on fresh checkouts (empty `target/`) so it is unaffected; only local incremental builds are at risk. A future cleanup could document a `clean` recommendation in `deliveryline-backend/README.md` or bind a pre-merge cleanup.
- **`docs/ci-pipeline.md` `foundation-gate` row still labeled "Placeholder for story 1.23"** [`docs/ci-pipeline.md`] — The table cell still describes the `foundation-gate` job as a "Placeholder for story 1.23" with a "shell echo" body, although story 1.23 is `done`. Pre-existing documentation staleness outside story 2.32's scope; fold into a docs-accuracy pass or the Epic 1/2 retrospective.
## Deferred from: code review of 2-13-backend-rest-mutation-endpoints-and-openapi.md (2026-05-26)

- **Problem Details retryability metadata still contradicts AC5** [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/ProblemDetailsCatalog.java:43`] — `APPROVAL_VERSION_MISMATCH` is still published as non-retryable and `IDEMPOTENCY_KEY_CONFLICT` as retryable. This predates the 2.13 patch, but it still needs a contract-alignment fix before clients can safely automate retries from the machine-readable payload.
- **Validation-error details still expose `constraint` instead of the documented `code` key** [`deliveryline-backend/src/main/java/org/dradgo/adapters/rest/ProblemDetailsMapper.java:111`] — field-level validation details remain `{field, rejectedValue, constraint}` rather than the story’s promised `{field, code, message?}` shape. This is pre-existing mapper contract debt, not introduced by the 2.13 endpoint rebuild.

## Deferred from: code review of story-2.11-backend-clarification-domain-model-and-submission.md (2026-05-25)

- **Terminal-state write race is not guarded at the persistence boundary** [`deliveryline-backend/src/main/java/org/dradgo/adapters/persistence/ClarificationWritePersistenceAdapter.java:165`] вЂ” `ClarificationService.submitAnswer(...)` checks `clarification.isTerminal()` on an earlier read, but `ClarificationWritePersistenceAdapter.recordAnswer(...)` re-reads and overwrites the row without lock/version protection. Once story 2.12 introduces lifecycle writers (`incorporated`, `superseded`, `rejected_invalid`), this needs optimistic or pessimistic concurrency hardening so an answer cannot land after a terminal transition.

## Deferred from: code review of story-2.14-backend-allowed-actions-inspection-endpoint.md (2026-05-27)

- **`getRunSummary` null-state surfaces as 500 instead of typed error** [`deliveryline-backend/src/main/java/org/dradgo/application/workflow/WorkflowInspectionService.java`] — `WorkflowState.fromValue(summary.currentState(), "currentState")` will throw if the snapshot returns a non-null view with a null `currentState`, producing a 500 instead of `RUN_NOT_FOUND` / a typed 422. Pre-existing defensive gap shared by every inspection read endpoint.
- **`currentContextBundleVersion` type-chain audit** — DTO field is `Integer` (int32). If any upstream port in the chain (`RunnerExecutionSnapshot.contextBundleVersion()` → `RunnerExecutionRecordPort` → JPA entity → DB column) widens to `long`, silent truncation occurs. Audit the full chain and either pin `Integer` everywhere or widen the DTO.
- **`pendingClarifications` read-after-write race in inspection reads** — under READ COMMITTED, the count read by `getRunSummary` is not consistent with the spec/event reads that follow inside the same `@Transactional(readOnly=true)`. Affects every inspection endpoint that composes these signals; needs a project-wide answer (`REPEATABLE READ` for inspection reads, or accept the skew with explicit doc).
- **`contextBundleVersion = 0` sentinel ambiguity** — `RunnerExecutionSnapshot.contextBundleVersion()` is primitive `int`. A row written with `0` before any real bundle would surface as `currentContextBundleVersion: 0` in the stamp rather than `null` for "no bundle yet". Confirm whether `0` is a valid value in the schema and either reserve it or store `null` (boxed) at the source.
- **No defense against multiple SPEC artifacts with the same `version`** — `findLatestByWorkflowRunIdAndArtifactType` returns "latest" with no documented tiebreaker. If a future rerun-from-step path or a concurrent writer slipping the unique constraint produces two specs with the same version, the bundle resolution becomes adapter-order-dependent.
- **ArchUnit `ALLOWED_ACTION_DERIVATION_LIVES_ONLY_IN_WORKFLOW_INSPECTION_SERVICE` rule scope** — currently `.resideInAnyPackage(APPLICATION_PACKAGE, ADAPTERS_PACKAGE)` only. A future `infrastructure..` scheduled job or a `cli..` subcommand can directly reference `AllowedAction.APPROVE_SPEC` to make a decision without tripping the rule. Either widen the scope or document the policy choice in the rule's remediation message.
- **Dev-record Task 9 bullet overstates OpenAPI representation of `UNKNOWN_ACTOR_ROLE`** — it appears only in the 400 `description` prose, not as a discriminator/enum/oneOf schema. Doc-accuracy nit; pattern-consistent with `INVALID_ID_PREFIX` / `RUN_NOT_FOUND` elsewhere. Fix the story dev-note when re-touched.
- **ArchUnit `ALLOWED_ACTION_DERIVATION_LIVES_ONLY_IN_WORKFLOW_INSPECTION_SERVICE` rule shape: re-stringify at boundary vs. widen `AllowedAction` exemption** — current rule whitelists only `WorkflowInspectionService` (+ inner classes) and `AllowedActionsResponse`; any future application-layer consumer of `AllowedActionsView` whose `actions()` returns `List<AllowedAction>` will trip the rule even though they aren't deriving anything new. Deferred: no real consumer yet — revisit on first friction.
- **`Investigating` + open clarification on a run with no spec yet — should `answer_clarification` surface?** — current code short-circuits to `[view_only]` when `latestSpecPublicId == null`. Deferred to Epic 2b — revisit with clarification UX work (story 2-18 clarification-region) once UX confirms whether pre-spec clarifications are possible in the domain.

## Deferred from: code review of story-3.2 (2026-05-28)

- **AC10 test surface (17 entries a–q) split to a dedicated follow-up story** — no lifecycle/stale/cleanup/recovery/dangling/heartbeat tests were authored; `src/test/java/org/dradgo/adapters/runner/lifecycle/` not created. Also un-added: directed `RunnerPropertiesTest` positive-checks + `FlywaySchemaContractTest` `heartbeat_stale_emitted_at` column assertion; the claimed closure of the 3-1 `P-Logging-Contract` item is NOT delivered (re-opens that debt). Story 3-2 stays in-progress until the follow-up lands. Behavioral ACs 1–6 + 9 are currently automated-test-unverified.
- **OpenAPI snapshot + frontend `schema.d.ts` were hand-edited; full regen unverified against a real springdoc boot** — `OpenApiSnapshotContractTest` must pass on a real boot before merge. Needs WSL2+Docker clean-env per memory `openapi-regen-platform-shim.md` / `verify-ci-fixes-in-clean-env.md`. Plausibly green (insertion order matches enum declaration) but unconfirmed.
- **`markArchived` is non-atomic with `deleteWorkspace` and unguarded on status** — low risk: terminal rows don't revert and the `findCompletedBeforeAndNotArchived` SQL guard protects selection. A crash between delete and archive only re-WARNs on the next tick. Revisit if cleanup metrics show stuck rows.

## Deferred from: story-3.2a close-out (2026-05-29)

_Story 3.2a discharged the bulk of the 3.2 review action items (stage-starvation, dangling-sweep safety, AC9/T13 retry anchor, detectHeartbeat atomicity + recover re-seed, event double-emission gate, `processHeartbeatStale` null handling) and the 3.2 deferred test-surface debt above (full AC10 surface authored + green; `RunnerPropertiesTest` + `FlywaySchemaContractTest` V10 column added; 3-1 `P-Logging-Contract` closed via `DockerRunnerLifecycleLoggingContractTest`; OpenAPI verified on a real springdoc boot). The two items below were NOT in 3.2a's ACs and remain open as accepted MED tech-debt._

- **Stale two-pass boundary + same-tick double-emit (shared-`now` / operator alignment)** [`RunnerBroker.scanForStaleExecutions` / `processHeartbeatStale`] — Phase 1 and Phase 2 read `now()` independently and mix `isBefore` vs strict `<`. Largely mitigated already: `processHeartbeatStale` skips any row whose `lastActivityAt` is before the orphan cutoff, so a row past `2×timeout` never emits `runner.heartbeatStale` (the orphan phase handles it). Residual is only the exact-`2×timeout` microsecond boundary, where a heartbeatStale + orphaned pair could both fire in one tick. Fix by threading one shared `now` through both phases and aligning the comparison operators. Low practical impact (60s scan cadence; microsecond boundary).
- **Workspace delete is non-atomic & aborts on an unreadable entry** [`RunnerWorkspaceCleanupJob.sweepWorkspaces` / `LocalRunnerWorkspaceStore.walkAndDelete`] — a partial delete throws before `markArchived`, so the row is re-selected and retried on the next tick (retry-forever, but no data loss); `walkFileTree` has no `visitFileFailed` override. The fix needs a genuine give-up policy decision (archive-after-N-failures, which hides the problem, vs. alert) — deferred to a deliberate follow-up rather than chosen ad hoc at close-out.

## Deferred from: code review of story-3.2a (2026-05-29)

- **`onResult` runs artifact ingestion before the `recordCompleted` race-gate** [`RunnerBroker.java:~430-504`] — a duplicate/replay `onResult` re-applies the artifact `recordOperation` loop *before* discovering the terminal state and bailing at the gate. Benign iff artifact ingestion is idempotency-keyed; verify ingestion idempotency before relying on replay-safety.
- **`markArchived` now `@Transactional(REQUIRED)` couples FS-delete/archive atomicity to outer-txn rollback** [`RunnerExecutionPersistenceAdapter.java`] — the annotation fixes the "no active transaction" lock error, but if `markArchived` joins an outer transaction that later rolls back, the on-disk workspace delete is already committed while `archived_at` reverts → row re-selected, retried on an already-deleted tree. Overlaps the existing "Workspace delete is non-atomic" deferred item above.
- **Stale scan per-tick work scales `stageCount × 2 × batchSize` with no aggregate cap** [`RunnerBroker.scanForStaleExecutions`] — the stage-starvation fix issues one LIMIT query per stage per phase; correct, but per-tick work is now unbounded in the number of stages. Perf, not correctness; no regression test asserts a total-work bound.
- **`mergeObservationKeepingHighWaterMark` can synthesize a (maxBytes, latestTimestamp) pair that never co-occurred** [`DockerRunnerAdapter.java`] — taking max bytes from observation A and the later timestamp from observation B can suppress a genuine later-but-smaller-byte observation (`fresh.byteCount() <= previousBytes`), masking real activity. Inverse of the regression the merge prevents; low probability.
- **Heartbeat IT uses strict `isAfter` on wall-clock / file-mtime** [`DockerRunnerLifecycleHeartbeatIT.java`] — two 1.1s-separated polls compared with strict `isAfter` can flake on coarse-mtime filesystems (1–2s granularity) where the second observation equals the first.
- **`recordCompleted`/`recordOrphaned` race-gate keys only on `ILLEGAL_TRANSITION`** [`RunnerBroker.java:505`] — a genuine non-terminal illegal transition (e.g. `pending → completed`) would also surface as `ILLEGAL_TRANSITION` and be silently swallowed as "already_terminal", masking a real logic error. Theoretical given the current transition machine.
- **Retry anchor is silently null on a docker `Replayed` dispatch result** [`RecoveryService.java`] — `runnerDispatchedEventPublicId` is populated only on `instanceof Dispatched`; an idempotency-replayed docker dispatch yields a null anchor indistinguishable from the documented mock-path null. Audit-trail completeness.
- **Retry anchor captured pre-commit could dangle on partial-failure rollback** [`RecoveryService.java`] — the `evt_…` id is read from the in-memory `Dispatched` result; there is no check that the referenced `runner.dispatched` row actually persisted if a later step rolls back. Speculative; append-only semantics make this unlikely.
- **`detectHeartbeat` read→decide→emit is non-atomic (only the map write is atomic)** [`DockerRunnerAdapter.java:~257`] — two concurrent polls can both pass the `isAfter` guard on a stale snapshot and both emit `HeartbeatTouched`, double-advancing `last_activity_at`. Benign forward-progress; the AC8 fix closed the stored-value regression but not the emit gate.
- **AC10(n) "fields preserved verbatim" is under-verified** [`RecoveryServiceDockerRetryContractTest.java`] — the contract test asserts only `status` + `failure_category` on the original row, not the full field set (`last_activity_at`, `timeout_at`, `completed_at`, …). Append-only semantics make regression unlikely.

## Deferred from: code review of story-3.2a (2026-05-30)

_Close-out review pass (Blind Hunter + Edge Case Hunter + Acceptance Auditor). ZERO critical/automatic findings — story 3.2a flipped `review → done`. The two HIGH-rated items below are deliberate, already-decided behaviors recorded here for visibility. Items overlapping the 2026-05-29 list above (recoverHandle merge truncation, `onResult` ingestion-before-gate / terminate-in-race, `markArchived` `@Transactional`) are not duplicated._

- **Dangling min-age guard mixes engine `createdAt` clock with broker `Clock`** [`RunnerWorkspaceCleanupJob.sweepDanglingContainers`] — no skew tolerance between the two clock domains. Engine clock behind the broker can age-out a launching container early; ahead can preserve a leak past the grace window. Heuristic-grade; add a skew tolerance to `minAgeSeconds` or derive `now` from the engine.
- **`redactImageTag` under-redacts exotic ref forms** [`RunnerProperties.redactImageTag`] — `host:port/user:pass@…` (first colon = registry port → no redaction) and `user@host/img` (no colon → returned verbatim) leak userinfo. NOT valid Docker image-ref grammar (`@` denotes a digest; pull credentials live in docker config, never the tag) and the redaction is defense-in-depth, judged adequate 2026-05-29. Harden by redacting any `userinfo@` segment before the host if a defensive belt is wanted.
- **Stage-scoped stale query lacks a deterministic tie-break** [`RunnerExecutionRepository` `order by lastActivityAt asc`] — when >`batchSize` rows in one stage share an identical `last_activity_at`, the `LIMIT` cut among equal keys is non-deterministic and a subset can starve every tick — a narrow re-occurrence of the cross-stage starvation this story fixed. Add `, runnerExecution.id asc`.
- **`touchActivity` writes `last_activity_at` unconditionally** [`RunnerExecutionPersistenceAdapter.touchActivity`] — only `timeout_at` is clamped non-decreasing; a heartbeat with a backward filesystem mtime could regress `last_activity_at` and shrink the stale window. Mitigated upstream by `detectHeartbeat`'s `isAfter(previousActivity)` gate. Add a monotonic guard for belt-and-suspenders.
- **AC9 completed-path double-emit gate lacks a direct re-entry test** [`RunnerBroker.java:496-513` / `RecoveryServiceDockerRetryContractTest`] — the orphan path is covered by `orphanPathInvokedTwiceEmitsRunnerOrphanedExactlyOnce`; the symmetric `recordCompleted` gate is implemented but no test re-enters `onResult`/recovery on an already-`completed` row to assert a single `runner.completed` event.
- **Test-strength nits** — heartbeat IT asserts `last_activity_at` "advances ≥twice" (AC10 b) via a single strict advance across two polls; AC11 pins the heartbeat-stale-WARN / orphan-WARN / standalone timeout-stop branches by proxy (INFO "…appended" lines) rather than the named WARN/stop log lines. Both behaviors are covered in aggregate; strengthen the assertions if AC-literal coverage is wanted.
- **(HIGH) Null `createdAt` ages OUT in the dangling sweep** [`RunnerWorkspaceCleanupJob.sweepDanglingContainers`] — re-opens the closed 2026-05-29 decision (age-out chosen to avoid permanent leaks). A rowless container reporting no creation time is removed, including — in theory — one caught mid-`docker create` before its DB row exists. Mitigated: the DB row is inserted as part of dispatch, so the rowless + null-`createdAt` window is very narrow. Decision stands; recorded for awareness.
- **(HIGH/MED) `unknown` engine state folded into the running set** [`RunnerWorkspaceCleanupJob` + `DefaultDockerEngineGateway.listContainersByLabel`] — `unknown` (the gateway's null/blank-state fallback) triggers stop+force-rm, which AC4(b) does not enumerate (`running|exited|created|paused|dead`). Intentional and codified by `unknownStatusRowlessContainerPastMinAgeUsesStopThenForceRemove`; defensively safe (force-rm of an exited container is a no-op). Reconcile AC4 wording on the next touch of this file.
- **Test hygiene: modulo-derived artifact public id** [`RecoveryServiceDockerRetryContractTest`] — seeds `art_seedspec + (workflowRunId % 100000)`, a collision/flake risk across reruns sharing the same Postgres sequence. Use `PublicIdPrefixes.ARTIFACT.next()` as the production code does.

## Deferred from: code review of story-2.22 (2026-05-30)

- **(MED) `useAssertRunContextLoaded` `loaded.actions` typed `unknown` + `loaded` branch unreachable in production** [`src/lib/navigation/guards.ts:2162`] — AC2.b specifies `actions: AllowedActions`, but the guard erases it to `unknown`, and because `useAllowedActions` is an `enabled:false` stub the `loaded` discriminant never resolves at runtime (queries with `enabled:false` stay `isPending`). Both are coupled to story 2.14 finishing the allowed-actions stub; restore the typed contract and re-verify the `loaded` path when 2.14 lands.
- **(MED) `RunContextBoundary` publishes a stale mount-time snapshot when identity props change while mounted** [`src/lib/navigation/RunContextBoundary.tsx:1423`] — the capture `useLayoutEffect` depends only on `[setSnapshot]`, so it runs once at mount; `identityRef.current` is kept current each render but `setSnapshot` is never re-invoked, so `useRunContextSnapshot` consumers read the original `runId` even after the boundary's `runId` prop changes. No current consumer reads the snapshot (the Clarification Region is story 2.18) and the restore-on-unmount scroll semantics intentionally use the mount-time value, so the correct behavior on identity churn is a design question to settle when 2.18 wires a reader.

## Deferred from: code review of story-3.5 (2026-05-30)

_3 adversarial layers (Blind Hunter + Edge Case Hunter + Acceptance Auditor) over the working-tree diff. ZERO critical findings. Headline (2-layer convergence): the post-execution secret scan re-resolves the injected key via `defaultKind()` not the dispatched kind — held back as a decision (verified LATENT: the broker's own dispatch also hardwires `defaultKind()` at `RunnerBroker.java:288`, so dispatch+scan coincide today). Two patch items (leak-branch re-entry/quarantine-durability hardening; missing no-run-state-drive test assertion) handled separately. The 11 items below are non-blocking._

- **`onResult` leak-handling is not atomic** [`RunnerBroker.java:519-538`] — DB FAILED + event append + `.quarantine` marker run sequentially with no surrounding transaction; a mid-branch failure leaves inconsistent state (FAILED row without event, or FAILED+event without quarantine). Pre-existing pattern (`onResult` is non-transactional throughout). No compensation/rollback path.
- **Quarantined workspace marked `archived`, indistinguishable from a reaped one** [`RunnerWorkspaceCleanupJob.sweepWorkspaces`] — the leak path skips deletion but calls `markArchived(...)`, the same state used for cleaned/deleted workspaces. No query distinguishes a leak-preserved workspace (files on disk) from a normally reaped one; the only signal is the `.quarantine` marker file, which nothing surfaces. Deliberate per Completion Notes; operator-UI DB column deferred (OQ-5).
- **Substring leak detector has no minimum-length/entropy guard** [`RunnerSecretScanService`] — a short/low-entropy provider-key value (e.g. a dev `.env` placeholder) can false-positive every workspace file via `text.contains(value)` and force-quarantine a clean workspace. `resolveSecretsForRunner` rejects only blank values; real provider keys are long. Add a min-length/word-boundary guard if pilot placeholders are a concern.
- **Doctor `probeRunnerSecrets` hard-FAILs unless EVERY `RunnerKind` has a key** [`DoctorProbeAdapter`] — an operator who only runs Codex and never sets `ANTHROPIC_API_KEY` gets a permanent doctor FAIL for an irrelevant kind. Matches AC8 as written (per-kind FAIL on missing); a notion of "enabled kinds only" is future product scope. Dispatch is correctly per-kind fail-fast.
- **Scan re-invokes `resolveSecretsForRunner` purely to get the value** [`RunnerSecretScanService.resolveInjectedValues`] — doubles the resolve INFO log lines per execution and emits a spurious "secret missing" WARN (caught) if the key was rotated to blank mid-run. Log hygiene only; consider a quieter value-only resolution path.
- **AC11(f) bundle-redaction test is tautological** [`RunnerSecretScanServiceTest`] — it calls `redact()` on a literal string and asserts the placeholder; it never seeds a fake key into source data nor asserts the *persisted* context bundle contains the placeholder instead of the value. Real coverage gap vs AC11(f); a proper test needs the dispatch/persist path.
- **`readFilesForSecretScan` loads every workspace file fully into memory with no size cap** [`LocalRunnerWorkspaceStore`] — `Files.readAllBytes` + strict-UTF-8 decode to `String`, all files retained simultaneously in the returned list; logs (observed growing unbounded elsewhere in this class) and large outputs are held as both `byte[]` and `String`. OOM/memory-pressure hazard on a verbose or leaky runner. Stream or cap.
- **No-leak logging tests assert only the test's own literal value is absent** [`DockerRunnerAdapterUnitTest` / `RunnerSecretScanServiceTest`] — `doesNotContain(<constant secret>)` is weaker than the regex sweep AC2/AC11(g) specify; a future log line printing a different secret value, or a prefix/hash of the key, would pass. Strengthen to a secret-shape regex sweep over captured logs.
- **Doctor probe vs dispatch can disagree on env-var names in lean contexts** [`DoctorProbeAdapter`] — the adapter falls back to `RunnerProperties.defaults()` when the bean is absent, while real dispatch uses the bean-bound properties; an operator who customized `deliveryline.runner.secret-env-names` gets a doctor result against default names that does not reflect dispatch behavior.
- **Gateway flattens env to `key + "=" + value` with no character validation** [`DefaultDockerEngineGateway`] — a configured env-var name containing `=`/newline, or a resolved value containing a newline, yields a malformed/ambiguous docker env pair (`A=B=C` → name `A`, value `B=C`; a newline can split into a spurious second pair). Names/values are operator-config/`.env`-sourced and unvalidated along the path.
- **Plaintext provider key resides in multiple GC-retained `String` instances with no zeroing** [`RunnerSecretsService` / `CreateContainerSpec` / `DefaultDockerEngineGateway`] — the key flows as immutable `String` through resolution → `CreateContainerSpec.environment` → `key=value` pairs → docker-java, retained on heap until GC. Heap-dump residency is unaddressed; beyond the documented MVP env-var posture (ADR-0003). No `char[]`/zeroing strategy.

## Deferred from: code review of story-3.7 (2026-06-04)

- **ILM policy committed but inert** [`infra/observability/elasticsearch/ilm-policy.json` + `logstash/pipelines/deliveryline.conf`] — output writes date-named indices (`deliveryline-logs-%{+YYYY.MM.dd}`) incompatible with the policy's `rollover` action (needs a write alias / data stream); binding is a manual README `curl`. Only the 30-day `delete` phase is reachable, and only if an operator runs the step. AC9's "commit a minimal ILM policy" is met; effective retention is documentation-only.
- **`ElkPipelineRoundTripIT` robustness** — the local-only drop test relies on TCP send-ordering + ES refresh timing (a not-dropped doc lagging the kept doc could false-PASS the headline "local-only never indexed" guarantee); `hitCount` string-scans the first `"value":` in the ES `_search` response (functional for current ES, brittle to response-shape changes). Docker-tier; needs Linux/Docker CI verification (T11) regardless.
- **No size ceiling on a runner-log shipment** [`RunnerLogShipment` / `RunnerLogShippingService`] — a multi-MB redacted stream is fully materialized (StringBuilder + JsonNode + UTF-8 byte[]) with no upper bound; `byteSize()` re-allocates byte arrays purely to read length. OOM hazard on the very low-memory host the new doctor probe warns about. Dormant until the D5 seam gets a production caller.
- **`vm.max_map_count` ES precondition not auto-checked/set** [`scripts/start-all.{ps1,sh}`, `docker-compose.yml` elasticsearch] — on Linux/WSL2 without `sysctl vm.max_map_count=262144`, ES crash-loops and the `depends_on: service_healthy` gate blocks logstash/kibana, with no doctor signal (the new probe checks RAM only). AC12 requires only documentation, which is present; an auto-check/auto-set is a future hardening.
- **D5 runner-log shipping seam dormant — no production caller** [`RunnerLogShippingService.shipIfPermitted`] — nothing in the runner-capture path (`RunnerLogCaptureService`/`LocalRunnerLogStore`) constructs a `RunnerLogShipment` or calls the service, so AC3(b)'s file-ingest leg is wiring-only and never produces `runner-logs-ingest/*.ndjson` end-to-end. Accepted as a forward seam for story 3.7 (consistent with the markAvailable dormant-seam pattern); wiring a caller lands with a later runner-execution story (e.g. 3.8 / 3.17).

## Deferred from: re-review (code review) of story-3.7 (2026-06-04)

> Second-pass review (different LLM) after the first-pass P1–P5 patches. No new actionable defects; the items below are minor and ALL gated by the dormant D5 file-ingest seam above — resolve them together with the D5 production caller.

- **Read-only ingest mount + `mode => "read"` leaves consumed envelopes un-reaped** [`infra/observability/logstash/pipelines/deliveryline.conf:29-34` + `docker-compose.yml` logstash `…/runner-logs-ingest:/ingest/runner-logs:ro`] — the file input sets no `file_completed_action`; `mode => "read"` defaults to `delete`, which cannot run against the `:ro` mount (the read-only is the deliberate D5 fail-closed property). Once the seam is wired, `runner-logs-ingest/` grows unbounded and Logstash logs a delete error per consumed file. Fix when wiring the D5 caller: explicit `file_completed_action => "log"` (+ `file_completed_log_path`) or an out-of-band reaper.
- **Sink temp-file leak on crash** [`LocalRunnerLogShipmentSink`] — `Files.createTempFile(ingestRoot, runnerExecutionId + ".", ".tmp")` orphans a `.tmp` in the watched dir if the process is SIGKILLed between create and the atomic move; not ingested (glob is `*.ndjson`) but never swept. Add a boot-time `*.tmp` reconciliation when the seam goes live.
- **`deliveryline.home` default diverges from the compose ingest-mount default** [`application.yml` `./` vs `docker-compose.yml` `${DELIVERYLINE_HOME:-./deliveryline-data}`] — if an operator relies on defaults, the backend writes `./runner-logs-ingest` while Logstash watches `./deliveryline-data/runner-logs-ingest`; paths diverge and nothing ships. Moot today (dormant), bites when D5 gets a caller; no test guards the two defaults agreeing. Mirrors the same latent convention as story 3.6's `runner-logs/`.
- **Layer-2 gsub scans only `[message]`, not the sibling `mdc`/`stack_trace` JSON fields** [`deliveryline.conf` gsub chain + `LogstashRedactionParityTest`] — re-flagged independently by all 3 review layers; this is the already-accepted P2 residual. `mdc`/`stack_trace` are redacted by the layer-1 `Redacting{Mdc,StackTrace}JsonProvider`, so it is a defense-in-depth completeness gap (a layer-1 regression would not be caught by layer-2), not a live leak. Extending the gsub also requires making the parity test field-agnostic (it currently asserts every gsub field is `message`).
- **Shippable shipment with both streams empty writes empty `"message":""` docs** [`RunnerLogShippingService.shipIfPermitted`] — minor index/dashboard-count noise once the seam is wired; add an empty-content short-circuit (new SKIPPED reason) then.

## Deferred from: code review of story-3.7 (2026-06-04, THIRD pass)

> Third-pass re-review (different LLM). The headline finding — Logstash `gsub` uses Java/PCRE regex syntax (`$1` backreferences; `(?s)` flag) invalid in the JRuby/Joni engine, masked by a parity test that validates with `java.util.regex` — is a **decision-needed** item recorded in the story's Review Findings (not deferred). The one new minor item below is gated by the dormant D5 file-ingest seam; resolve it with the D5 production caller alongside the read-only-mount / completed-action item already listed above.

- **Re-shipping the same runner execution re-indexes (duplicate ES docs)** [`LocalRunnerLogShipmentSink.write` + `infra/observability/logstash/pipelines/deliveryline.conf` file input] — `write()` does `Files.move(…, ATOMIC_MOVE, REPLACE_EXISTING)` keyed on `{runnerExecutionId}.ndjson`; a second shipment for the same execution id replaces the file with a new inode, which the `mode => "read"` file input (path-keyed sincedb) treats as new content and re-reads → the same execution indexed twice into Elasticsearch. No guard against re-shipping the same id. Dormant until the D5 seam gets a production caller; address with the read-only-mount / `file_completed_action` reaping decision.

## Deferred from: dev-story of story-3.8 (2026-06-05)

> Story 3.8 (Real Docker Runner Contract Integration Test) is a TEST-AUTHORING story — `RealRunnerContractIT` wires the real Codex/Claude images to the broker + persistence + contract-validator end-to-end. It adds NO production code (the `--simulate-failure` / `--stage` args are injected via the proven test-owned direct-launch + `recoverHandle` + broker-reconcile pattern, so no product seam was needed — Decision D7/OQ-3 resolved "no seam"). The items below are deliberate scope boundaries documented for the follow-on stories.

- **`runner-image-build` + `runner-contract-real` GitHub Actions jobs are story 3.34's deliverables (Decisions D1/D10)** [`.github/workflows/ci.yml`] — 3.8's literal AC1/AC10 place the IT in a dedicated `runner-contract-real` job that `needs: runner-image-build` on `ubuntu-latest`. Those jobs do not exist yet; this story ships the IT runnable via the existing `-Pdocker-runner-it` profile (`mvn -Pdocker-runner-it -pl deliveryline-backend -am verify -Dit.test=RealRunnerContractIT`). Story 3.34 ("CI Tier — Real Docker Runner Image Build + Compatibility Checks", currently `backlog`) adds the dedicated CI job that builds both images and selects on `@Tag("real-runner-contract")`. Mirrors how 3.7 created the `docker-runner-it` Maven profile but left the dedicated CI line to a later story.
- **Foundation-gate "widening" deferred to 3.34 (Decisions D8/D9)** [`FoundationGateVerificationTest`] — 3.8 AC9 literally says the per-PR foundation gate should include "real-runner-contract test green on the branch", but the lightweight `@Tag("foundation-gate")` aggregator builds no Docker images / boots no Postgres; folding a runner-image-building, Postgres-booting test into the every-PR aggregator would defeat the tiering. The widening is realized as story 3.34's dedicated `runner-contract-real` required check, NOT by adding `RealRunnerContractIT` to `FoundationGateVerificationTest`. (Open question OQ-1 — confirm with Alex if a gate hook is wanted sooner.)
- **Full JVM-restart recovery / heartbeat-staleness / dangling-cleanup NOT re-run on the real images (Decision D5)** [`RealRunnerContractIT` vs `DockerRunnerLifecycle{Timeout,Heartbeat,Recovery}IT` + `DockerRunnerDanglingContainerCleanupIT`] — only the real-image **timeout** lifecycle is re-proven in 3.8 (via `--simulate-failure=timeout`, proving the real entrypoint's `sleep` + the broker's stop→kill path interoperate). Heartbeat staleness, broker-restart recovery, and dangling-container cleanup are image-agnostic (the broker kills any container the same way) and exhaustively covered against `alpine:3.20` by the 3.2a lifecycle suite; re-running them on the heavy real images adds build/run cost without new signal.
- **AC7 redaction asserted at the capture boundary, not through a full happy harvest** [`RealRunnerContractIT.runnerLogsAreCapturedAndAuthHeaderLeakIsRedactedBeforePersistence`] — a deliberate `Authorization: Bearer <sentinel>` seeded into the raw `runner.stdout` is (correctly) ALSO caught by the AC6 workspace secret-scan, which fail-closes the run to `runner_secret_leak`. The redaction-second-pass assertion (`redaction_count > 0`, sentinel absent from the persisted `runner-logs/{rex}/runner.stdout`) is therefore driven via `adapter.recoverHandle(rex)` (which runs `captureRunnerLogs` during its classify-exited poll) and deliberately stops before the full broker harvest. The fail-closed leak path is the correct secure behaviour and a separate concern from "logs redacted before persistence".
- **Mock-vs-real parity (AC5) pinned against the documented mapping, not a second live context** [`RealRunnerContractIT.mockBehaviourMapsToRealOutcome`] — booting a second `@SpringBootTest` under `!runners.docker` purely to read the `MockRunnerAdapter` outcome is heavy + profile-fragile. The `Behaviour → (status, failure_category, lifecycle event)` mapping is a fixed contract unit-tested in `MockRunnerAdapter`'s own suite; the real scenarios (`happyPath*`, `simulateCrash`, `simulateContractViolation`, `simulateTimeout`) assert exactly those triples via `assertTerminal`, so the real `RunnerAdapter` port outcome is proven identical to the mock's for each matched Behaviour. A future story could add a focused single-context mock-adapter comparison if a stronger guarantee than the contract table is wanted.

## Deferred from: code review of 3-8-real-docker-runner-contract-integration-test (2026-06-05)

- **`awaitNotRunning(..., 20s)` hardcoded vs configured broker graceful-stop timeout** [`RealRunnerContractIT.java:356`] — flake-hardening, not a correctness bug. The 20s kill-wait matches the 3.2a lifecycle pattern and is only a flake risk if the configured graceful-stop period grows to ≥ ~20s or SIGKILL delivery is slow (Windows vs WSL2). Derive the wait from the configured graceful timeout when this is next touched.
- **Dispatch-side container-env assembly (`DockerRunnerAdapter.dispatch`) uncovered by the real-image IT** [`RealRunnerContractIT` vs `DockerRunnerAdapter.dispatch`] — per Decision D7 (no product seam) the IT launches the real container via raw DockerClient and only drives the broker's reconcile/harvest path, so the production `dispatch()` env-assembly (resolved secrets + stage env) used on the real dispatch route is not exercised here. Accepted per D7; a future story wanting end-to-end dispatch-path coverage would need the adapter to expose an arg/env seam (or a real runner-API dispatch).

## Deferred from: code review of 2-23-modal-overlay-confirmation-patterns-and-button-hierarchy-infrastructure (2026-06-09)

- **`completed`/`submitting` `aria-live` nested inside an `aria-busy` button may not reliably announce the outcome** [`GovernedButton.tsx:148,153`] — a live region mounted already-populated (and inside an `aria-busy` control) is announced inconsistently across screen readers. Literal AC9 (`aria-busy` on submitting, `aria-live` on completed) is satisfied; full SR-announcement correctness belongs to the a11y/axe audit (story 2.25).
- **`ConfirmationDialog` manual focus-restoration unreliable for programmatically/composite-opened dialogs** [`ConfirmationDialog.tsx:901-912`] — restoration captures `document.activeElement` at open, which for a dialog opened from a side effect (no focused trigger) is often `<body>`, so focus can land nowhere on close. Baseline restoration is handled for the trigger-opened case; refine under 2.25.
- **`NonDismissibleCriticalWarning` focus-restoration-on-close not explicit** [`NonDismissibleCriticalWarning.tsx`] — relies on radix `FocusScope` default rather than the explicit capture/restore `ConfirmationDialog` uses. Verify it restores focus (not drops to `<body>`) under 2.25; auditor judged AC5 met.
- **`GovernedButton` blocked branch drops `...rest` passthrough props** [`GovernedButton.tsx:80-138`] — a caller's `id`/`aria-label`/`data-*` on a `priority="blocked"` button are silently swallowed (the blocked `<span>` tree never spreads `...rest`). No current call sites; fix needs a design call on which props to forward without re-introducing interactivity.
- **`single-primary-action` misses two literal primaries across nested `ButtonGroup`s within one `DecisionArea`** [`tools/eslint-rules/single-primary-action.js`] — `nearestContainer` stops at the first container, so a `DecisionArea` holding two `ButtonGroup`s each with a primary passes lint while violating UX-DR19. Rule-scope enhancement; `confirmationCatalog` + human review backstop, and the spec sanctions conservative detection.
- **`NonDismissibleCriticalWarning` renders `body` (`ReactNode`) inside `DialogDescription` (`<p>`)** [`NonDismissibleCriticalWarning.tsx:1056`] — block-level `body` content would be invalid DOM nesting (hydration/layout warnings). Fixtures pass inline content today; constrain the prop type or restructure when a block-content consumer appears.
- **`RationaleCaptureDialog` collapses duplicate field `name`s into duplicate DOM `id`s** [`RationaleCaptureDialog.tsx:98,145`] — breaks `htmlFor`/`aria-describedby` targeting. Consumer-contract edge (field names must be unique); add a dev guard if it recurs.
- **`BoundedDetailSheet` Escape-predictability + focus-restoration untested** [`__tests__/BoundedDetailSheet.test.tsx`] — radix-default behavior is correct but unverified; AC5 lists the sheet. Coverage thinness.
- **`RationaleCaptureDialog` missing focus-restoration + `consequence`-required type tests** [`__tests__/RationaleCaptureDialog.test.tsx`] — required-ness is enforced structurally and the `ConfirmationDialog` type-test exists; this is coverage thinness on the composed variant.
- **`RationaleCaptureDialog` ignores a `fields` prop changed while open** [`RationaleCaptureDialog.tsx:98-100`] — stale `useState` initializer; rare mid-open case. The common between-opens reset is handled by the patch in this review.

## Deferred from: code review of story-2.25 (2026-06-10)

- **`useLiveAnnouncement` cannot re-announce an identical consecutive message** [`src/lib/a11y/useLiveAnnouncement.ts:18`] — A→A transitions are not re-spoken because the `[message]` effect dep does not change (e.g. a repeated submit failure or a recurring identical state). Out of AC5 scope (which closes first-render + multiple-simultaneous-advance); a full fix needs a clear-then-`requestAnimationFrame` reset that risks introducing new test flakiness.
- **`BoundedDetailSheet` focus restoration no-ops when the opener was removed from the DOM before close** [`src/components/overlays/BoundedDetailSheet.tsx:71`] — `target.focus()` on a detached node silently drops focus to `<body>`, the WCAG 2.4.3 failure the effect set out to fix, for the detached-opener case. Mirrors the sibling `ConfirmationDialog` limitation already tracked from the 2.23 review; a full fix needs a `document.contains(target)` guard plus a fallback-focus target.
- **Keyboard-test rigor weaker than the test titles claim** [`src/test/a11y/keyboard.ts` + multiple `*.test.tsx`] — several suites assert activation via `.focus()` + key event rather than pure Tab traversal (a broken tab path still passes the activation half); disabled "excluded from Tab order" tests assert only `toBeDisabled()`; `expectTabReachesAll` never asserts the absence of *extra* focusables. Tests pass for the right reason today, but the assertions don't substantiate their stated claims.
- **`keyboard.ts` `TABBABLE_SELECTOR` is incomplete** [`src/test/a11y/keyboard.ts:37`] — omits `[contenteditable]`, `summary`, `[controls]`, and ignores positive-`tabindex` reordering. Not exercised by current components; relevant only if such elements are added to an audited surface later.
- **AC8/AC9 guard-rail ESLint rules cover only the canonical patterns** [`tools/eslint-rules/no-role-based-action-gating.js:41`, `no-bare-actor-role-text.js:102`] — `no-role-based-action-gating` matches only `===`/`!==` vs a role string-literal (misses `switch`, `.includes`/`Set.has`, comparison-vs-const); `no-bare-actor-role-text` misses role text in template substitutions, JSX attribute values, and dynamic vars. Accepted as-scoped (Alex, 2026-06-10): the AC examples are enforced and no consumers exist today — broaden when Epic-3a adds real role-rendering/gating code.
- **AC6 rendered-state contrast not exercised by axe in-story** [`src/test/setup.ts:25`] — `getContext` is stubbed to `null`, so axe `color-contrast` degrades to *incomplete*; only the token-pair `check:contrast` gate actually runs. Accepted for E2 close (Alex, 2026-06-10): jsdom has no layout engine; live rendered-state contrast is deferred to the 2.27 Playwright tier.
- **AC7 vocabulary gate enforces import-presence, not text-provenance, and is blind to role-only live regions** [`tools/a11y/__tests__/announcement-vocabulary.test.js`] — keys on the literal `aria-live=` attribute and a `PRIMITIVE_ALLOWLIST`; a composite could import the vocab yet inline a second bare live string and pass. Accepted as-scoped (Alex, 2026-06-10): reasonable static approximation, per-region provenance is hard to prove statically, and today's 3 composite announcers are clean.

## Deferred from: code review of story-3.12 (2026-06-10)

- **TOCTOU sub-stage re-derivation** [`RunnerBroker.java:1005`] — `deriveExecutionSubStage` is re-read at in-flight-guard, dispatch/compose, and `onResult` harvest time; a plan approved mid-flight can flip IMPLEMENTATION_PLAN→PR_OUTPUT and misclassify the result. No version pin enforces the asserted "stable mid-execution". Dormant seam (no live trigger until 3.20).
- **Clean-worktree push outcome empty → drift/enrich skipped, still advances** [`RunnerBroker.java:1271-1277`] — when `captureAndPush` returns `Optional.empty()` the run auto-advances on format-valid-but-unverified-against-actual refs (runner claims a commit while producing no diff). Dormant seam.
- **`PR_OUTPUT_BRANCH_PATTERN` over-permissive** [`RunnerBroker.java:1183`] — `^[a-zA-Z0-9._/-]+$` permits `..`, leading/trailing slash, `.lock` refs git would reject; reported branch is only validated/persisted (push uses deterministic `mount.branch()`). Harden alongside the 3.15 linkage consumer.
- **`PR_OUTPUT_PR_REF_URL_PATTERN` hardcodes github.com host** [`RunnerBroker.java:1188-1190`] — a GitHub Enterprise PR URL would be rejected as malformed; add a config seam if GHE is targeted.
- **Abbreviated-SHA format vs exact-equality drift** [`RunnerBroker.java:1184,1333`] — commit pattern accepts `{7,40}` but `prOutputDriftField` uses exact `equals` → a correct abbreviated SHA vs full 40-char actual → false `RUNNER_PR_REF_DRIFT`. Fold into the prRef-contract decision (require exact-40 or prefix-compare).
- **Unit-test gap: real-adapter prRef form untested** [`RunnerBrokerUnitTest.java:1980-2060`] — no drift/enrich case constructs `actual` with the real-adapter `owner/repo#number` prRef; add a regression test once the prRef-contract decision lands. (This gap masks the decision-needed finding.)
- **Drift bypass-by-omission** [`RunnerBroker.java:1330-1342`] — fields are drift-checked only when BOTH actual and reported are non-null; a runner under-reporting (null commitSha/prReference) skips that field's check. Low impact (enrichment overwrites branch/commitSha with actual regardless).
- **Retry IT weak assertion** [`PrOutputOrchestrationIT.java`] — `retryImplementation…` asserts `>=1` completed + `==2` total; the `>=1` is satisfied by the first execution alone, so the retry's execution is not proven to have done anything. Tighten.
- **prRef format contract mismatch (real adapter vs AC9 validator/AC3 drift)** [`RunnerBroker.java:1186-1190` vs `GitHubRealAdapter.java:591`] — real adapter `prRef="owner/repo#number"`; AC9 patterns accept only `PR-<n>`/canonical-URL. Once 3.20 wires the live trigger + real repo + real adapter: false `RUNNER_PR_REF_DRIFT`→`Failed` on every real PR, and enrichment writes a self-rejected value into the artifact. Masked today (IT has no repo → `captureAndPush` empty; unit tests hand-build `prRef="PR-1"` actuals). **Deferred to story 3.15 (Alex, 2026-06-10): 3.15 owns linkGitHubPr** + PR-ref canonicalization; seam dormant until 3.20. Carry as an explicit blocker note on 3.15.

## Deferred from: code review of story-3.16 (2026-06-11)

- Empty rendered completion-sync body trips `GovernedRunComment`'s blank-body guard → `IllegalArgumentException` recorded as the generic `failureCategory="unknown"` rather than a clear template/config diagnostic. Triggered only by a custom template whose tokens all resolve blank (default template is literal-bearing, so unreachable by default). [WorkflowOrchestrationService.java:1088]
- `WorkflowTransitionService.registerCompletionSyncHookIfApplicable` no-ops (WARN + return) when no transaction synchronization is active. Currently unreachable because `transition()` always executes inside `transactionTemplate`, but a future direct/non-transactional `COMPLETED` path would silently skip the completion sync with no event recording the skip. [WorkflowTransitionService.java:152-158]
- No `enabled <-> linear profile` consistency guard: `linear-completion-sync.enabled=true` with no `linear-mock`/`linear-real` profile active makes every completion silently no-op at WARN (adapter resolves null). The doctor `probeLinearCompletionSync` reports `enabled` but does not cross-check that a Linear profile is present. [WorkflowOrchestrationService.java:1066-1073]

## Deferred from: code review of story-2.28 (2026-06-12)

- Packaging contract test requires `assets/*.css` beyond AC6 scope (index.html + assets/ + entrypoint JS). A future Vite change that inlines CSS would red `backend-contract-tests` on a fully-valid bundle. Works today (build emits a separate CSS). [BundledJarPackagingContractTest.java:109-117]
- SPA fallback predicate misclassifies any deep-link route whose last segment contains a dot (e.g. `/users/john.doe`, `/tickets/ENG-1.0`) as a missing asset → JSON 404 instead of the shell; also treats `Accept: */*` as a navigation, so a programmatic `fetch('/non-api-typo')` receives a 200 HTML shell. Pre-existing (moved verbatim from ProblemDetailsMapper); low real impact since DeliveryLine route ids are `run_`-prefixed without dots. [SpaFallbackController.java shouldServeSpaShell/acceptsHtml]
- bundled-jar-smoke navigation curls use `curl -fsS` under `set -e`, so on an HTTP-error response the step aborts at the curl line before the hand-written `::error::` grep diagnostic runs (the grep still catches the 200-but-wrong-body case). Diagnostic-UX only — the smoke still fails correctly. [.github/workflows/ci.yml:777-787]
- bundled-jar-smoke greps for the literal `<div id="root">`, coupling the push:main smoke to the exact authored index.html formatting; a Vite/template change (unquoted attr, extra attributes) could red a healthy shell. [.github/workflows/ci.yml:779,786]
- AC11 packaging-invariant PR gate depends on `-am` building the `scope=test` deliveryline-frontend module so the AC6 packaging contract test ASSERTS (not `Assumptions.abort`/skips) inside `backend-contract-tests`. Correct today but undefended: a future `-DskipTests` or `frontend-maven-plugin.skip=true` leak would silently turn the gate green on an SPA-less jar. Consider a CI-env guard that fails-instead-of-skips when running in CI. [.github/workflows/ci.yml backend-contract-tests + BundledJarPackagingContractTest.java:82-89]

## Deferred from: code review of story-3-30 (2026-06-13)

- FailureEventSurface relative times don't tick — `const nowMs = Date.now()` captured per-render with no interval (RunContextStrip ticks via state). Consistent with the same static pattern in `RunReviewQueueItem.tsx:344`, so low priority. [FailureEventSurface.tsx:168]
- `humanizeNextSafeAction`/`titleCaseToken` lowercases the tail of each token — an unknown forward-compat value containing an acronym (`runner_OOM` → "Runner Oom") renders lossily. Affects only unmapped values. [failureCategoryView.ts:39]
- `humanizeFailureCategory` guards `undefined`/blank but not `null` — the wire type is `… | null`; safe today only because every caller coerces `?? undefined`. Guard the helper itself for robustness. [failureCategoryView.ts:54]
- Failure events rendered in raw stream order with no sort/dedup — a fail→retry→fail run yields multiple unordered `runner.failed`/`recovery.retried` rows under a "Failure timeline" heading; only the single-failure fixture is tested. [FailureEventSurface.tsx]
- No client-side staleness gate in recovery mode — a stale "Retry failed step" can be clicked; the server guards it (RETRY_NOT_APPLICABLE / ILLEGAL_TRANSITION → error+refresh), but unlike the spec-approval bar there is no version-stamp pre-empt. [RecoveryDecisionBarContainer.tsx]
- Idempotency-replay 409 surfaced as a generic error — a duplicate-submission 409 (the first attempt actually succeeded) shows "could not be submitted (CODE). Refresh and try again.", mislabeling a succeeded retry as failed. [useRetryWorkflow.ts / ApprovalDecisionBar.tsx:494-505]
- Success visible text duplicates the `retryRecorded` announcement constant — the `<p>` literal is hand-copied from `announcements.ts`; editing one silently drifts the other. Source both from the constant. [ApprovalDecisionBar.tsx:478-480]
- `useRetryWorkflow` spreads optionals on `!== undefined`, not blank — an empty-string `reasonText`/`correlationId` would be sent on the wire as present-but-blank; latent because no caller passes blank today (`retry.mutate({})`). [useRetryWorkflow.ts]
- AC11 retry-mutation query-invalidation asserted only at the `useWorkflowMutation` factory level — inherited+tested via `useWorkflowMutation.test.tsx` (mirrors `useApproveSpec`); no retry-hook-specific `detail`/`lists` invalidation assertion. [useRetryWorkflow.test.tsx]
- Run Context Strip tooltips show raw snake_case stage values vs humanized category/next-action — `failedStage`/`lastSuccessfulStage` `title=` carry raw tokens while category/next-action are humanized; minor SR/tooltip inconsistency. [RunContextStrip.tsx]

## Deferred from: code review of 3a-6-openspec-cli-in-runner-images (2026-06-13)

- Codex self-test loose substring version match [runners/codex/entrypoint.sh:161-162] — `*"$EXPECTED_OPENSPEC_VERSION"*` accepts substring-supersets (e.g. `1.4.10` for pin `1.4.1`). Pre-existing codex compare style (spec-mandated mirror); practically unreachable (install + expected pin share one ARG). Future hardening: align codex to claude's exact-token compare if/when the mirror rule is revisited.
- No `set -f` glob guard around `set -- $_openspec_version` [runners/claude/entrypoint.sh:153] — a version token with a glob metachar would pathname-expand. Mirrors the existing claude CLI version line.
- No timeout on `openspec --version` capture [runners/codex/entrypoint.sh:160, runners/claude/entrypoint.sh:152] — a hung CLI stalls `--self-test`. Pre-existing across both entrypoints' version probes.
- `<unset>` entrypoint default vs `0.0.0-mock` mock default mismatch [both entrypoints + both mock-openspec.sh] — latent (Dockerfiles always set `ENV OPENSPEC_VERSION=1.4.1`), fails-safe.
- Conformance ITs assert openspec summary labels, not the pinned version value [Codex/ClaudeRunnerImageConformanceIT.java] — optional hardening; exit-0 self-test gate already enforces the pin.
- Legacy "AC9"/"AC4 mock" AC numbering carried into 3a-6 docs from stories 3.3/3.4 [both READMEs, codex Dockerfile comment] — cosmetic.

## Deferred from: code review of story-3a-7 (2026-06-13)

- Production-CLI image's skills layer never built by conformance ITs [ClaudeRunnerImageConformanceIT.java / CodexRunnerImageConformanceIT.java] — ITs always build the offline (`INSTALL_*_CLI=false`) variant; the real-CLI image is never assembled in CI. Low risk: the `COPY`+`mv`+`ln` skills block sits after the CLI `if/else` and is branch-independent.
- `SUPERPOWERS_PIN` is a reported label, not verified against vendored bytes [runners/codex/entrypoint.sh, runners/claude/entrypoint.sh] — `--self-test` echoes the ENV pin; nothing ties that SHA to the COPY'd tree (no checksum/commit file). Same class as the 3a-6 OpenSpec "IT asserts label not pin value" deferral.
- No build-time guard that the vendored `skills/` dir exists [runners/claude/Dockerfile, runners/codex/Dockerfile] — a botched re-vendor with no `skills/` yields a dangling symlink; failure surfaces at runtime `--self-test` rather than at `docker build`. A `RUN test -d .../superpowers/skills` would shift it left.

## Deferred from: code review of 3a-8 (2026-06-14)

- **Fence-convention authoring robustness** [runners/{codex,claude}/lib/runner.mjs commandSplitFenced] — body containing a literal `=== FILE:` line mis-parses as a new section; relpaths with spaces / embedded `===` pass `isSafeRelPath` and write oddly-named files; a duplicate relpath silently overwrites the earlier file; a whitespace-only body writes a zero-byte file. All are facets of "does a single-prompt agent reliably honor the fence convention + OpenSpec delta format" — the authoring-reliability risk the story explicitly defers to the story 3.8 live-run spike.
- **pr-output retry idempotency** [runners/{codex,claude}/entrypoint.sh openspec_assemble_proutput] — on a retried pr-output run a stale `openspec/changes/<id>/` mixes with newly split output; a repo path component that exists as a file silently no-ops the split (mkdir WARN, nothing written). Real-run hardening; feature is default-off and the first real run is story 3.8.
- **`openspec validate` has no timeout** [runners/{codex,claude}/entrypoint.sh:191] — the offline mock exits 0, but a real CLI that hangs/prompts would block the pr-output stage despite the best-effort/never-blocks intent. Wrap validate in `timeout` when story 3.8 enables real egress.
- **Read-side referencePath not traversal-guarded (defense-in-depth)** [runners/{codex,claude}/entrypoint.sh openspec_split_carried] — `$INPUT_DIR/$DL_SPEC_REF_PATH` / `$DL_PLAN_REF_PATH` are read verbatim. The values are backend-composed (trusted) and match the pre-existing referencePath trust model, so this is not agent-exploitable, but the write side IS guarded — add an `isSafeRelPath`-equivalent check on the read side for symmetry.

## Deferred from: code review of story-3a-9 (2026-06-14)

- Read endpoint (`WorkflowInspectionService.getArtifactDetail`) decodes the full artifact payload as UTF-8 with no upper size bound and silently substitutes replacement characters for non-UTF-8 bytes. Deferred — pre-existing project-wide pattern (the payload store is unbounded everywhere; bodies are write-time-redacted markdown). Revisit if large/binary artifacts become a concern.

## Deferred from: code review of story-3-17a (2026-06-14)

- **`dequeueNext` lease + JPA re-read not atomic without an ambient tx** [RunnerExecutionPersistenceAdapter.java:286] — the native `UPDATE … RETURNING` + `findByPublicId` re-read rely on the caller's transaction; the method itself has no `@Transactional`. Safe today (the only path, `RunnerExecutionQueue.dequeue`, is `@Transactional`). When 3.17b wires direct callers, annotate `@Transactional` on `dequeueNext` so the lease + re-read are guaranteed one tx.
- **`QUEUED` rows absent from the timeout/stale-scan + broker active-status sets** [RunnerExecutionStatus.java + findStaleByStatusInAndTimeoutAtBefore callers] — a `queued` row never times out or gets reclaimed; harmless while the queue is dormant, but in 3.17b a stuck `queued` row would permanently consume backpressure depth. Add queued-row lease reclamation/timeout (extend the 3.2 stale scan) during 3.17b activation.
- **21-arg positional `RunnerExecutionSnapshot` ctor (+16-arg shim) is brittle** [RunnerExecutionSnapshot.java] — accepted tech-debt ([[runnerproperties-record-component-fanout]]); a mid-list field insertion shifts positional meaning across call-sites with no compile error. Consider a builder/named factory for the snapshot.

## Deferred from: code review of story-3-31 (2026-06-15)

- **AC5 unreachable `useEffect` keeps the time-derived `prStaleForMs` in its deps + logs `staleForMs: NaN` on an unparseable `lastSyncedAt`** [RunContextStrip.tsx:329-338] — bounded (can fire one extra warn only on the run-activity stale-timer transition; `nowMs` is pinned state, not a per-render tick) and the `NaN`-on-unparseable matches the existing sibling `runContext.stale` log (line 281), so it is consistent with current code. Revisit when the live PR-linkage wire lands (6.9): guard `Date.parse` → `undefined` and key the effect on `[prUnreachable, prState]`.
- **`toPrLinkageView` selects the FIRST `github_pr` `integration_links` row arbitrarily** [prLinkageView.ts:138] — dormant today (no live wire). When story 6.9 projects multiple linkage rows onto the workflow-detail read model, define a recency/active-state ordering so a stale/superseded PR row cannot mask the active one.
- **`prState` enum gating is strict + un-normalized (no case fold)** [prLinkageView.ts:122,144] — any unanticipated or case-variant state from the future backend projection drops the WHOLE PR cluster (AC8 graceful absence) rather than degrading to ref+badge. Reconcile against the actual 6.9 projected `prState` values (and decide whether to `.toLowerCase()`-normalize) when the live wire lands.

## Deferred from: code review of 3-18-workflow-batch-submission-cli-and-rest (2026-06-15)

- **Batch-row insert failure after per-ticket commits orphans runs + burns the idempotency key** [WorkflowBatchSubmissionService.java:116-126,197-207,256-268] — per-ticket `submit()` commit independently; if `batchSubmissionWritePort.insert(...)` then throws, `completeFailed` marks the idempotency record FAILED so a same-key retry hits `priorAttemptFailed` permanently, and the already-queued runs have no `batch_submissions` row and can never be replayed. Inherent to the best-effort + non-`@Transactional` design (narrow trigger; runs still execute). Needs partial-commit reconciliation.
- **`stampBatchSubmissionId` implicitly depends on synchronous `submit()` commit** [WorkflowBatchSubmissionService.java:227-239] — works today (enqueue is synchronous inside `submit`'s tx); if dispatch becomes async/after-commit the native UPDATE matches 0 rows and silently swallows (best-effort) → permanent loss of the batch trace link.
- **Test hardening: central best-effort tx-isolation + idempotency conflict** — (a) `WorkflowBatchSubmissionServiceTest` mocks the tx manager so `independentTransactionTemplate` runs inline; the REQUIRES_NEW isolation that makes best-effort work is never exercised, and `WorkflowBatchSubmissionIT` is happy-path only (no real per-ticket rollback while others commit). (b) AC11 "idempotency conflict" only stubs `checkAndReserve` to throw (vacuous). (c) No CLI blank-only (`--tickets ",,,"`) or unknown-JSON-field rejection test.
- **CLI logs `resolvedCorrelation` but passes raw `correlationId` to the service** [WorkflowCommands.java:253-267] — when `--correlation-id` omitted, the success-log id differs from the id the service generates and stamps on runs. Pre-existing house pattern (identical to `submit-workflow` :193-204); affects the whole CLI family, not 3.18-specific. AC8 still satisfied.

## Deferred from: code review of 3-19-queue-inspection-and-worker-status-and-queue-size-monitoring.md (2026-06-15)

- **`findLeasedRunning` N+1 + per-worker/scalar skew** [RunnerExecutionPersistenceAdapter.java findLeasedRunning] — one JPA `findByPublicId` per leased id (cap 1024, not pool-size); a row completing between the id-select and the re-read is silently dropped, so `workers.size()` can fall below the `activeWorkers` scalar (separate aggregate query, READ COMMITTED). Low impact — bounded by pool size in practice, observability-only inconsistency. Fix would be a single `IN (...)` fetch + accepting the inherent two-query skew or a snapshot read.
- **`staleDispatchedCount` uses the slowest-stage window uniformly** [WorkflowInspectionService.java maxStaleWindow] — a hung fast-stage dispatched row is under-counted until the slowest stage's window elapses. Currently INERT: both stage timeouts are 600s so max == per-stage; only becomes a real under-count if the stage timeouts are configured to diverge. Revisit if per-stage timeouts diverge.

## Deferred from: code review of story-3.28 (2026-06-16)

- **Takeover ConfirmationDialog focus-on-unmount (a11y)** — on takeover confirm the bar flips to `submitting`, force-unmounting the shared `ConfirmationDialog` without radix's close-focus-restore, dropping focus to `<body>` (AC8 focus-predictability concern). Shared with the existing `spec_approval`/`recovery_operator` confirm flows — fix belongs to the shared dialog pattern, not story 3.28. [ApprovalDecisionBar.tsx:752-784]
- **Stale state hides the still-valid `Take over` action** — when accept/reject go stale on `APPROVAL_VERSION_MISMATCH`, the stale branch replaces the whole impl-review action area, hiding takeover (which can never version-mismatch) until the user refreshes. Minor UX; refresh recovers. [ApprovalDecisionBar.tsx:836-861]

## Deferred from: code review of story-3b.2 (2026-06-16)

- **Production PR-link "match" gate compares a ref to itself** — `TechnicalApprovalService.assertPrLinkPresentAndMatches` → `assertArtifactPrLinkMatches` → `IntegrationLinkService` (~:596-600) compares the active link's `external_ref` to itself (`active.externalRef().equals(active.externalRef())`), so a mismatched artifact-claimed PR reference can never be detected — the gate only fails on an absent/empty link. Surfaced while auditing story 3b.2's AC5 (the seeded-link assertion is therefore tautological for the "binds" half). Pre-existing (3.12/3.23), not introduced by the test-only 3b.2 change. Worth a follow-up to either feed the artifact's own claimed PR ref into the comparison or document the gate as presence-only.

## Deferred from: code review of 3b-3 (2026-06-17)

- AC5 live `WaitingForReview` run verification was not performed headlessly (self-disclosed in the story Dev Notes). A reviewer must confirm on a running app that the "Open the implementation output →" link renders and the ingested `implementationPlan`/`prOutput` artifact reads `available` in the read model.
- Frontend impl-output link (`index.tsx:402`) is gated on `implArtifactId !== undefined`, not on the artifact's AVAILABLE status. If the best-effort enrich mark fails, `resolveImplementationArtifact` returns the highest-version (PENDING) `prOutput` id and the link renders to a non-acceptable artifact. Minor UX; partly subsumed by 3b-5 (the real `prOutput` PR/diff renderer).

## Deferred from: code review of story-3.29 (2026-06-17)

- Unrelated story-3b-3 test file present untracked in the working tree (`deliveryline-frontend/src/routes/workflows/$workflowRunId/index.test.tsx` — its own header declares "Story 3b-3 (AC4 / AC6) implementation-output link"). Not part of the 3.29 changeset; ensure it is committed under 3b-3, not bundled into the 3.29 commit.

## Deferred from: code review of 3b-4 (2026-06-17)

- AC7 live manual verify was not performed headlessly (self-disclosed in the story Task 6 / Completion Notes). A reviewer/operator must confirm on a live `WaitingForReview` run that the decision bar shows Accept/Reject/Take over (not `blocked`) and that a plan-phase accept/reject or a takeover transitions the run. Note: a `prOutput` *accept* additionally needs the 3b-1/3b-2 `github_pr` link to pass the backend PR-link gate — verify accept against a plan-phase artifact, or exercise takeover/reject (no link needed).

## Deferred from: story-3b-5 (2026-06-18) — BLOCKING OQ-1 resolved to "runner does not write the diff file"

- **RUNNER FOLLOW-UP (blocks live diff rendering): the `pr-output` stage must write the unified-diff FILE to scratch under the path it names in `diffReference`.** Verified in Task 0: `runners/{claude,codex}/lib/runner.mjs` `commandBuild` emits `diffReference: artifacts/{workflowRunId}/pr.diff` for the prOutput artifact but — unlike the `spec` stage, which `writeAtomically`s its `contentReference` file — it **never writes the `pr.diff` file**. So `RunnerScratchStore.tryReadArtifactContent(rex, diffReference)` returns `Optional.empty()` in production, and 3b-5's ingest diff-resolution always omits the diff today (the panel renders the graceful "No diff content was produced." empty-state). The PR link + `PrStateBadge` render real (they come from `branch`/`commitSha`/the `github_pr` link, not scratch). 3b-5's backend plumbing is **forward-compatible**: the moment the runner writes the file, the diff flows end-to-end with **no backend change** (AC1's absent-scratch branch is the only thing exercised until then). The fix belongs in the runner two-phase `pr-output` contract (3b-1 family / a runner story), NOT in 3b-5. Real runner emit points: `runners/claude/lib/runner.mjs:246-254`, `runners/codex/lib/runner.mjs:253-261`. (3b-5 AC1/AC2/AC3 + the `RunnerBrokerUnitTest` diff cases prove the resolution/cap/dual-embed logic against a stubbed-present scratch read, independent of the runner.)
- AC9/AC12 live `WaitingForReview` PR-render verify was not performed headlessly (human-in-the-loop). On a live run with a pushed PR an operator should confirm the panel shows the real PR link + `PrStateBadge` (and, once the runner follow-up above lands, the unified diff) and that a decision transitions the run. The automated mapper + render + contract pins (`queryOptions` mapper tests, the `ArtifactReviewPanel` live-prOutput render test, `OpenApiSnapshotContractTest`) stand in for the wiring proof.
- `WorkflowInspectionService.getArtifactDetail` success-log `bodyLength` reflects the **decoded payload** length, not the blanked response `body` (which is `""` for a prOutput). Intentional (it records bytes read), but a debugger reading the log alongside an empty wire `body` could be momentarily confused. Cosmetic; revisit if it ever misleads triage.

## Deferred from: code review of story-3b-5 (2026-06-18)

- **Combined line+byte cap ordering in `resolvePrOutputDiff` (`RunnerBroker.java`).** The 5000-line cap and the 1 MB byte cap are applied as two sequential `if`s, not mutually exclusively. When a diff trips BOTH, `truncateToBytes` re-truncates the already-marked string and the final stored length is `MAX_BYTES + marker` (the "absolute backstop" is soft by the marker length). Only one truncation marker survives (the byte path cuts off the first one), so it is not corrupting — but the combined-overflow path is untested (the oversize unit test is ~12 KB, only the line branch fires). Low; new-code hardening — make the caps exclusive (or subtract the marker from the byte budget) and add a combined-overflow test. (blind)
- **`prState` semantic fidelity — the real GitHub adapter emits only `open`/`closed`.** `GitHubRealAdapter.toPullRequest` (`:587`) passes GitHub's raw REST PR `state` verbatim, which is only ever `open` or `closed`. The declared/contract values `draft` and `merged` are unreachable from the live source, so a merged PR badges as `closed` and a draft as `open`. Pre-existing (3.15), surfaced — not regressed — by 3b-5; out of this story's literal scope. Lossy badge UX; revisit if/when draft/merged states are wanted. (auditor)
- **Truncation/cap log + cap-semantics cosmetics (`RunnerBroker.resolvePrOutputDiff`).** When only the byte cap fires, the resolved-diff `INFO` log reports `lineCount = min(originalLines, 5000)` even though byte-truncation discarded lines — the logged count can overstate the stored content. Separately, the backend caps **total** diff lines (5000) while the frontend `PR_DIFF_MAX_LINES` caps **changed** lines per file, so the "mirrors the frontend cap" comment is only approximate. Observability/comment accuracy only; no functional impact. (blind+edge)
- AC5 reconciliation: `prReference` is sourced **co-presently with `prState` from the `github_pr` link** (its `external_ref`), NOT parsed from the prOutput payload as the AC literally wrote. Reason: `findActiveGitHubPrLink` returns an `IntegrationLink` that carries no `external_metadata`, so reading `prState` needed a new projection anyway; sourcing both from the same link guarantees DD3's co-presence invariant without a separate null-coordination. A new **non-locking** typed projection (`findActiveTicketSummaryByTypeAndWorkflowRun`) was added (port+adapter+repo) rather than reusing AC5's locking `findActiveGitHubPrLink`, so the artifact-read path never takes the `PESSIMISTIC_WRITE` lock the enrich/sync writers hold.
