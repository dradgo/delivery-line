# Story 3f.5: Split Commit Fan-Out

Status: done

<!-- 2026-06-29 bmad-create-story context-engine pass. Target sprint key: 3f-5-split-commit-fan-out. Prereqs 3f-1 (subticket capability) + 3f-2 (lineage + non-terminal SPLIT) + 3f-3 (run-dependency edges + gated dispatch) + 3f-4 (persisted open proposal + 3 advisory actions) all DONE/review. 3-18 (best-effort batch fan-out) is the transaction-pattern precedent. Validation optional: run validate-create-story before dev-story. -->

> **⚠️ READ FIRST — this is the OPERATOR-FACING back half of split. 3f-4 produced the advisory *proposal*; 3f-5 COMMITS it.** On `approve_split` over a current open proposal, `SplitCommitService` performs a **best-effort, non-transactional fan-out** (the 3-18 pattern): per subtask it (a) creates a source sub-ticket where the connector supports it else proceeds **internal-only** linking the child to the parent's ticket, (b) mints a **child run** in the parent's project with `parentRunId` set, (c) records the proposed **dependency edges**; then it decomposes the parent into the **non-terminal `SPLIT`** state and appends the `workflow.split` event **only if ≥1 child was created**. A child with no unmet dependencies dispatches immediately; a dependent child parks in `WaitingForDependencies`.
>
> **3f-5 creates NO new schema, NO new WorkflowState, NO new WorkflowEventType, NO new transition-table edge, and NO new runner contract.** Every substrate it needs already shipped: the `approve_split` action is the *only* new `AllowedAction` (3f-4 deliberately left it out). `SPLIT` (state), `workflow.split` (event + `childRunIds` detail key), `parent_run_id`, the `SPLIT` into-edges from both gates, `run_dependencies` + `WaitingForDependencies`, `split_proposals` (with an `approved` status already in its CHECK), and the subticket capability are all in place. 3f-5 is **pure orchestration** over them — this is the integration story, not a substrate story.
>
> **What 3f-5 does NOT own (forward stories):** the `SPLIT → COMPLETED` **rollup** (parent stays in non-terminal `SPLIT` here — it rolls up only when all descendants complete) is **3f-7**; the **depth cap** / `SPLIT_DEPTH_LIMIT_EXCEEDED` / `complex-ticket-flow.max-split-depth` / deep-split override is **3f-7**; cascade-cancel/cascade-fail of dependents is a documented forward option (not this epic).
>
> **Seven reconciliations the live codebase forces (read ALL before coding):**
>
> 1. **`SplitCommitService.commit` MUST NOT be `@Transactional` (the 3-18 trap, verbatim).** If it were, every per-subtask `@Transactional(REQUIRED)` call (`workflowRunCreatePort.create`, `runDependencyService.declareDependencies`) would JOIN one batch transaction, so a single subtask's rollback-marked `DomainException` would poison the whole fan-out and destroy best-effort. With no ambient transaction each subtask opens its own physical transaction; one subtask's failure rolls back only that subtask. Mirror `WorkflowBatchSubmissionService` (`WorkflowBatchSubmissionService.java:48-53` — the documented "central trap"): top-level method un-annotated; use `TransactionTemplate` with `PROPAGATION_REQUIRES_NEW` for the idempotency reserve/complete, the proposal-status flip, and the parent decomposition. See R1.
> 2. **Fan-out is TWO passes — all child runs first, then all dependency edges — because `run_dependencies` FKs reference sibling child run public_ids.** A dependency edge `(run_id, depends_on_run_id)` has `ON DELETE RESTRICT` FKs to `workflow_runs.public_id` (`V28`); you cannot declare an edge to a prerequisite child that does not exist yet. So: **Pass A** — for each subtask, create the sub-ticket (or internal-only) + mint the child run in `INBOX` with `parentRunId`, building an `ordinal → childRunId` map. **Pass B** — for each child that has prerequisites, `declareDependencies` (parks it in `WaitingForDependencies`); then `dispatchWhenUnblocked` every child (no-dep children dispatch, parked children no-op). Doing edges inside Pass A would FK-fail on forward references. See R2.
> 3. **`declareDependencies` parks dependent children but does NOT dispatch zero-dependency children — you must also call `dispatchWhenUnblocked` per child.** `RunDependencyService.declareDependencies` (`RunDependencyService.java:76`) takes the graph advisory lock, validates acyclicity, persists edges idempotently, and — if prerequisites are unmet and the run is in `INBOX` — parks it. It deliberately does **not** dispatch when prerequisites are already satisfied/absent (that path logs "left unparked"). The actual dispatch is `dispatchWhenUnblocked(runId, correlationId)` (`RunDependencyService.java:167`): `blockedOn.isEmpty()` → `dispatchSpecGeneration` → `DISPATCHED`; unmet + `INBOX` → `PARKED`; otherwise `SKIPPED`. So 3f-5's uniform Pass-B step per child is: `declareDependencies(...)` (only if the subtask has incoming edges) **then** `dispatchWhenUnblocked(...)`. The latter is idempotent and safe to call on an already-parked child (returns `SKIPPED`). See R3.
> 4. **Map the proposal's `dependencies[]{fromOrdinal,toOrdinal}` to `declareDependencies(dependentRunId, [prerequisiteRunIds])` — and PIN THE DIRECTION with a test, because getting it backwards silently inverts execution order with no error.** `declareDependencies(runId, dependsOnRunIds)`: `runId` is the **dependent**, `dependsOnRunIds` are its **prerequisites**. The proposal edge `{fromOrdinal, toOrdinal}` direction is defined by 3f-4's `split-proposal.v1` semantics (the runner prompt: "Task 3 starts after Task 1+2"). **Before coding, confirm against `split-proposal.v1.schema.json` + the 3f-4 runner decomposition prompt which ordinal is the dependent and which is the prerequisite**, then assert the mapping in a focused test (e.g. proposal `{from:3,to:1}` ⇒ run_dependencies row `(child3, child1)` meaning child3 depends on child1). A backwards mapping passes every type check and every smoke test but runs the subtasks in the wrong order — a silent disaster. See R4.
> 5. **The internal-only fallback (no subticket) must STILL link the child run to the parent's ticket — and there is no existing helper, 3f-5 owns it.** `TicketSourceSubticketService.createSubticportIfSupported(...)` returns `CREATED` (child sub-ticket made), `INTERNAL_ONLY_SKIPPED` (connector can't create — `supportsTicketCreation=false`), or `NO_TICKET_SOURCE` (project has no connector). On `CREATED` the child run links to the **new sub-ticket's** `childRef`; on `INTERNAL_ONLY_SKIPPED`/`NO_TICKET_SOURCE` the child run links **internal-only to the parent's own `TicketRef`** (epic AC2/AC7: "linking the child to the parent's ticket"). Establishing that child↔ticket linkage uses the existing `IntegrationLink` seam (story 3-32) — **re-grep `IntegrationLinkService` for the create/associate signature and reuse it; do NOT hand-roll a link row** ([[integration-link-adapter-tx-and-supersede]], [[integration-link-mapper-lazy-proxy-on-worker-thread]]). See R5.
> 6. **The parent decomposition is a guarded, idempotent two-step appended AFTER the fan-out, only when ≥1 child was created; the `workflow.split` event is appended SEPARATELY from the SPLIT transition.** `WorkflowTransitionService.transition(...)` hardcodes `WorkflowEventType.WORKFLOW_STATE_CHANGED` — it does not emit `workflow.split`. So decomposition = (i) flip the proposal `open → approved` (compare-and-set guard), (ii) `transition(parentId, WorkflowState.SPLIT, actor, reason, key)` (legal only from `WaitingForSpecApproval`/`WaitingForReview` — the 3f-2 into-edges), (iii) `workflowEventWritePort.append(new WorkflowEventRecord(..., WorkflowEventType.SPLIT, ..., Map.of(WorkflowEventDetailKeys.CHILD_RUN_IDS, childRunIds)))` carrying the allow-listed `childRunIds`. On replay, the proposal CAS (already `approved`) and the SPLIT into-edge guard (parent already in `SPLIT` → transition rejected) both short-circuit; swallow + return the prior children. **Zero children created ⇒ skip all three steps entirely: parent untouched at its gate, proposal stays `open`, return an explicit aborted outcome (R7).** See R6.
> 7. **Zero-child abort is a RESULT STATE, not a thrown error (consistency with 3-18 best-effort) — no new `DomainErrorCode`.** When every subtask fails, the commit returns a `SplitCommitResponse` with `parentDecomposed=false` + `outcome=ABORTED_NO_CHILDREN` + the per-subtask failure list; HTTP 200/207 (partial), parent and proposal untouched (operator may re-propose or retry). Do **not** add a new three-sites `DomainErrorCode` for this (the epic's "a clear error is returned" is satisfied by the explicit aborted result, mirroring how `WorkflowBatchSubmissionService` reports all-rejected via the result, not an exception). If product later wants a hard 4xx ProblemDetails, that is an additive follow-up. See R7.

## Story

As an operator who approved a split proposal,
I want the system to create the subtasks — as real sub-tickets where possible, otherwise internal-only — wire their dependencies, and decompose the original run,
so that the decomposition is committed as governed child runs with preserved lineage, on a best-effort basis that never half-terminates the parent.

## Acceptance Criteria

> Reconciled against the live codebase (the subticket service, lineage/SPLIT state, dependency edges + gated dispatch, and the persisted open proposal all exist and are reused). Where epic wording assumes machinery owned by 3f-7 or superseded by the shipped 3f-1..3f-4 code, the reconciled AC below is authoritative; rationale in Dev Notes (R1–R7).

1. **Given** the new `APPROVE_SPLIT("approve_split")` `AllowedAction` (the ONLY new action this story — 3f-4 left it out), **Then** it is registered (enum + `allowed-actions.placeholder.json` + an `AllowedActionRegistryPinTest` wire pin) and surfaced by `WorkflowInspectionService.appendSplitOverlay` **alongside** `REPROPOSE_SPLIT`/`DECLINE_SPLIT` when an **open** proposal exists at the gate role (`product_reviewer`/`workflow_owner` at `WaitingForSpecApproval`, `developer` at `WaitingForReview`). No new gate state; the overlay is additive (existing gate actions intact).

2. **Given** `APPROVE_SPLIT` over a run's current **open** proposal, **Then** `SplitCommitService.commit` runs **best-effort + NON-transactional** (the 3-18 pattern, R1): each subtask is processed so one subtask's failure does not poison the rest; the top-level approve is idempotent under its `Idempotency-Key` (reuse `IdempotencyService.checkAndReserve/complete` in `REQUIRES_NEW`, mirroring `WorkflowBatchSubmissionService`).

3. **Given** per subtask (two passes, R2), **Then** the commit: **(Pass A)** creates a sub-ticket via `TicketSourceSubticketService.createSubticketIfSupported(project, parentTicketRef, draft)` — `CREATED` links the child to the new sub-ticket, `INTERNAL_ONLY_SKIPPED`/`NO_TICKET_SOURCE` links the child **internal-only to the parent's ticket** (R5) — then mints a child run via `workflowRunCreatePort.create(childId, INBOX, parentProjectId, parentRunId)` (3f-2), building an `ordinal → childRunId` map; **(Pass B)** for each child with proposed prerequisites, records the dependency edges via `runDependencyService.declareDependencies(...)` (3f-3) with the edge direction PINNED (R4), then calls `runDependencyService.dispatchWhenUnblocked(childId, correlationId)` for every child (R3).

4. **Given** dependency-gated dispatch, **Then** a child with **no** unmet dependencies dispatches its spec generation immediately (`dispatchWhenUnblocked → DISPATCHED`); a child **with** unmet dependencies is parked in `WaitingForDependencies` (`declareDependencies` parks it; `dispatchWhenUnblocked → SKIPPED`) — e.g. spec=Task1/Task2 dispatch, Task3 (depends on 1+2) parks.

5. **Given** the parent-decomposition guard (R6), **Then** **only if ≥1 child run was created** the commit: flips the proposal `open → approved` (CAS), transitions the parent to **non-terminal** `SPLIT` (`WorkflowState.SPLIT`, "Split"; legal only from the two gates, 3f-2 into-edge), and appends `workflow.split` (`WorkflowEventType.SPLIT`) carrying `childRunIds` via the allow-listed `WorkflowEventDetailKeys.CHILD_RUN_IDS`. The parent later rolls up to `COMPLETED` via 3f-7 (not this story). **If zero children were created, the split aborts: the parent stays at its gate, the proposal stays `open`, and an explicit aborted result is returned — the parent is never left half-decomposed (R7).**

6. **Given** idempotency, **Then** the commit is keyed on parent run + proposal id (`split-commit:<parentRunId>:<proposalId>`) and per-child keyed on parent + ordinal (subticket key `split:<parentRunId>:<proposalId>:<ordinal>`, child-run submit key, and deterministic dependency-declare key) so a replayed approve neither double-creates sub-tickets/children nor double-decomposes the parent. Defense in depth: the top-level `Idempotency-Key`, the proposal-status CAS (`open → approved`), and the `SPLIT` into-edge guard all independently short-circuit a replay.

7. **Given** the result + FE, **Then** the `POST /split/approve` response reports per-subtask outcome (`created` / `internal_only` / `failed`) + `parentDecomposed` + the child run ids; the FE Split Proposal Panel (3f-4) shows the commit result and links parent → children; the new lineage (`parentRunId` + `childRunIds`) renders in the run view (3f-2 read model). `GET /split-proposal` reflects the approved status.

8. **Given** a connector with no creation support, **Then** the whole split still succeeds **internal-only** (children link to the parent ticket) — proving the 3f-1 capability fallback end-to-end.

9. **Given** tests, **Then** a full IT proves request → (re-propose) → **approve** → N children created (mixed sub-ticket + internal-only) with lineage + dependency edges, the dependent child parked while others dispatch, parent → `SPLIT` + `workflow.split`; the **zero-child guard** (all subtasks fail → parent + proposal untouched); idempotent replay (no double children/sub-tickets/decomposition); the **dependency edge direction** pin (R4); `application.*` ≥ 80% line coverage.

## Tasks / Subtasks

- [x] **Task 1 — `APPROVE_SPLIT` action + extend the proposal-aware matrix** (AC: 1)
  - [x] `domain/registry/AllowedAction.java` — add `APPROVE_SPLIT("approve_split")` (the 3f-4 comment placeholder marks the exact spot; mirror the `REQUEST_SPLIT` style). This is the only new enum value in 3f-5.
  - [x] Drift: add `"approve_split"` to `src/test/resources/contracts/frontend/allowed-actions.placeholder.json`; add `approveSplitWireValueIsPinned()` to `architecture/AllowedActionRegistryPinTest.java`. (No DB CHECK for allowed-actions — enum ↔ placeholder JSON only.)
  - [x] `application/workflow/WorkflowInspectionService.java#appendSplitOverlay` — in the `hasOpenSplitProposal == true` branch (currently adds `REPROPOSE_SPLIT` + `DECLINE_SPLIT`), also add `APPROVE_SPLIT`. Keep the gate/role guard unchanged (spec gate: `product_reviewer`/`workflow_owner`; review gate: `developer`). Extend the existing both-gate × open/no-open matrix unit tests to assert `APPROVE_SPLIT` appears exactly when an open proposal exists at the gate.
  - [x] Update the `AllowedActions` OpenAPI `@Schema` if it pins allowable values (3f-4 found it is a free-form `List<String>`, so likely no edit — verify; regen in Task 5).

- [x] **Task 2 — `SplitCommitService` (best-effort, non-transactional fan-out)** (AC: 2, 3, 4, 5, 6, 8)
  - [x] `application/workflow/SplitCommitService.java` (new) — **NOT `@Transactional`** (R1). Inject a `PlatformTransactionManager` and build a `REQUIRES_NEW` `TransactionTemplate` (copy `WorkflowBatchSubmissionService.requiresNewTemplate`). `commit(ApproveSplitCommand)`:
    1. `IdempotencyService.checkAndReserve` (REQUIRES_NEW) on the approve key; REPLAY → return the prior result (re-derive children via `findByParentRunId`).
    2. Load the run's **open** proposal via the `SplitProposalReadPort` open-lookup (same one `WorkflowInspectionService` uses for `hasOpenSplitProposal`); guard gate state (`WaitingForSpecApproval`/`WaitingForReview`) + role. No open proposal → reject (existing precondition error / `IllegalState`, no new code).
    3. Resolve `parentProjectId` (parent run's `projectId`, 3c-7) and the parent's `TicketRef` (via the parent's `IntegrationLink`, R5).
    4. **Pass A** — for each `SplitSubtaskView{ordinal,title,scope}`: build a `SubticketDraft(parentRunId, proposalId, subtaskId, ordinal, title, scope, key=split:<parentRunId>:<proposalId>:<ordinal>)`; call `TicketSourceSubticketService.createSubticketIfSupported(project, parentTicketRef, draft)`; then in a REQUIRES_NEW tx mint the child run (`workflowRunCreatePort.create(childId, INBOX, parentProjectId, parentRunId)`) + establish the child↔ticket `IntegrationLink` (sub-ticket `childRef` on `CREATED`, else parent `TicketRef`); record `ordinal→childId` + outcome. Catch `RuntimeException` per subtask → `failed` outcome, continue.
    5. **Pass B** — for each child with proposed prerequisites: `runDependencyService.declareDependencies(new DeclareRunDependenciesCommand(childId, [prereqChildIds], actorIdentity, actorType, key, correlationId))` (R4 direction); then `runDependencyService.dispatchWhenUnblocked(childId, correlationId)` for **every** child (R3).
    6. **Parent decomposition (R6)** — if `createdCount >= 1`: CAS proposal `open → approved` (via `SplitProposalWritePort`); `workflowTransitionService.transition(parentId, SPLIT, actor, "approve_split", "split-commit:<parentRunId>:<proposalId>")`; `workflowEventWritePort.append(WorkflowEventRecord(... SPLIT ..., Map.of(CHILD_RUN_IDS, childRunIds)))`. Else skip all three (R7).
    7. `IdempotencyService.complete` (REQUIRES_NEW); return `SplitCommitResult`.
  - [x] `ApproveSplitCommand(workflowRunId, actorIdentity, actorType, idempotencyKey, correlationId)` (mirror `SplitProposalCommands`).
  - [x] `SplitCommitResult` / per-subtask `SubtaskCommitOutcome(ordinal, status ∈ {created|internal_only|failed}, childRunId, childTicketRef, reason)` + `parentDecomposed` + `outcome ∈ {decomposed|aborted_no_children}` (mirror `BatchSubmissionResult`/`TicketBatchResult` shape).

- [x] **Task 3 — REST + CLI approve surface** (AC: 7)
  - [x] `adapters/rest/WorkflowController.java` — `POST /{workflowRunId}/split/approve` (`Idempotency-Key` + `X-Actor-Identity`, HUMAN actor via `resolveHumanActor`, fail-closed). Mirror `/split/decline` (`WorkflowController.java:429-457`). New `SplitCommitResponse` DTO (per-subtask outcomes + `parentDecomposed` + child ids). Adding the `SplitCommitService` dep fans out to the `@WebMvcTest(WorkflowController.class)` slices (~17 — add `@MockitoBean SplitCommitService`).
  - [x] `adapters/cli/WorkflowCommands.java` — `split-approve` via the **optional-setter injection** pattern (3f-3/3f-4 `setSplitProposalService`) + a `requireSplitCommitWired()` guard.

- [x] **Task 4 — Parent ticket resolution + internal-only child linkage** (AC: 3, 5, 8)
  - [x] Resolve the parent run's `TicketRef` from its `IntegrationLink` (re-grep `IntegrationLinkService` / the integration-link read seam — confirm the lookup-by-run signature; [[integration-link-mapper-lazy-proxy-on-worker-thread]] — reads off the request thread must join-fetch).
  - [x] Establish each child run's ticket linkage through the existing `IntegrationLinkService` create/associate seam (NOT a hand-rolled row): sub-ticket `childRef` on `CREATED`; parent `TicketRef` on `INTERNAL_ONLY_SKIPPED`/`NO_TICKET_SOURCE`. Reuse the supersede/tx discipline ([[integration-link-adapter-tx-and-supersede]] — SPI writes need ambient `@Transactional`; here the per-subtask REQUIRES_NEW tx provides it).
  - [x] Redaction: subtask `title`/`scope` already redacted in the persisted `proposal_json` (3f-4); do not re-expose. Logs carry ids/ordinals/counts only — never titles/scope/feedback.

- [x] **Task 5 — OpenAPI + frontend client + Panel approve affordance** (AC: 1, 7)
  - [x] Regenerate the OpenAPI snapshot (`OpenApiSnapshotContractTest -Dopenapi.snapshot.write=true`, Failsafe + Testcontainers, full context) → `openapi.json` gains `POST /split/approve` + `SplitCommitResponse`/per-subtask payloads; `npm run generate-api` → `schema.d.ts`; `npm run check:api` GREEN ([[openapi-regen-frontend-client-drift-cascade]], [[openapi-regen-platform-shim]]).
  - [x] FE `approvalDecisionView.ts` — add `approve_split` to `DecisionAction` + `KNOWN_ACTIONS` + `CONSEQUENCE_HINTS`; wire the governed approve button in `SplitActionBar` (allowed-actions-driven; `useSplitActions`). Honor the 3f-4 follow-up: at `WaitingForReview` the split actions are matrix-gated to `developer`, so the review-gate approve button needs the developer-role allowed-actions wired ([[tanstack-validatesearch-strips-unparsed-param]] is unrelated; this is the role-scoped allowed-actions the 3f-4 dev flagged).
  - [x] `SplitProposalPanel.tsx` — render the commit result (per-subtask created/internal-only/failed) + parent→children links after approve. Vitest + axe cases for the approve action set + commit-result render + zero-child aborted render.

- [x] **Task 6 — Tests** (AC: 9, all)
  - [x] **Full loop IT** (Failsafe + Testcontainers + offline Docker mock-runner): bound project (Linear mock, `supportsTicketCreation=true`) + a no-creation connector path → `request_split` (mock emits 2-subtask/1-dep proposal) → `approve_split` → assert: 2 children created (one via sub-ticket, the internal-only path via a no-creation project), `parentRunId` set on both, dependency edge present with the **correct direction** (R4), the dependent child in `WaitingForDependencies` while the independent child dispatched, parent → `SPLIT`, `workflow.split` event with `childRunIds`. Best run in the `docker-runner-it` CI tier ([[docker-it-needs-exact-docker-runner-it-tag]]).
  - [x] **Zero-child guard IT**: force every subtask to fail (e.g. child-create stubbed to throw / no ticket source + create failure) → `parentDecomposed=false`, parent stays at gate, proposal stays `open`, no `workflow.split` event.
  - [x] **Idempotent replay IT**: replay `approve_split` with the same `Idempotency-Key` → no second sub-tickets, no second children (per-ordinal keys), no second `workflow.split` (proposal already `approved` + SPLIT into-edge guard).
  - [x] **Dependency-direction unit/contract test** (R4): a proposal edge maps to the run_dependencies row asserting which child is the dependent.
  - [x] Unit: `SplitCommitService` happy-path (mixed outcomes), zero-child abort, per-subtask failure isolation (one throws, others commit), CAS-already-approved replay; action-matrix `APPROVE_SPLIT` both gates × open/no-open.
  - [x] Drift: `APPROVE_SPLIT` (placeholder + pin); OpenAPI + `schema.d.ts` regen (NOT byte-identical). **No** Flyway / state-machine / event-type / transition-table drift — 3f-5 adds none (all owned by 3f-2/3f-3/3f-4).
  - [x] Naming/tier: Testcontainers tests are `*IT` (Failsafe) ([[springboot-testcontainers-test-must-be-IT]], [[maven-arglineation-goal-crash]]).

- [x] **Logging instrumentation** (cross-cutting; required)
  - [x] SLF4J parameterized logs at `SplitCommitService` entry (`approve_split` received, proposalId, subtaskCount), per-subtask decision (`created`/`internal_only`/`failed` with ordinal + childRunId + ticket outcome), per-child dispatch decision (`DISPATCHED`/`PARKED`/`SKIPPED`), the parent decomposition (`INFO transitioned parent → Split, childCount=N`), the zero-child abort (`WARN split aborted no children parentRunId=...`), idempotency replay (`WARN`), and every swallowed per-subtask `RuntimeException` (`WARN`).
  - [x] Context keys: `workflowRunId`(parent), `splitProposalId`, `childRunId`, `ordinal`, `idempotencyKey`, `actorIdentity`, `subtaskCount`, `createdCount`, `status` — **never** subtask titles/scope, feedback text, ticket bodies, or credentials.
  - [x] Pin ≥1 log assertion per new branch (list-appender / `OutputCaptureExtension`): parent-decomposed, zero-child abort, per-subtask failure isolation, replay.

## Dev Notes

### Reconciled Scope

In scope: the single `APPROVE_SPLIT` action + its matrix overlay; `SplitCommitService` best-effort fan-out (subticket-or-internal-only → child run → dependency edges → gated dispatch); the two-pass ordering; parent decomposition to non-terminal `SPLIT` + `workflow.split`; the zero-child guard; idempotent replay; REST `POST /split/approve` + CLI `split-approve`; FE commit-result render.

Out of scope (do NOT build — owned elsewhere):
- `SPLIT → COMPLETED` **rollup** + recursive split + depth cap + `SPLIT_DEPTH_LIMIT_EXCEEDED` + `complex-ticket-flow.max-split-depth` + deep-split override — **3f-7** (sequenced after this story).
- Any new `WorkflowState` / `WorkflowEventType` / transition edge / Flyway migration / runner contract — all substrate already shipped (3f-2/3f-3/3f-4).
- Per-subtask proposal editing; manual subtask entry; cascade-cancel/cascade-fail; GitHub/GitLab `createSubticket` — deferred forward options (ADR-0029).

### Why these ACs are reconciled (epic wording vs. the live codebase)

| Epic wording | Reality (verified 2026-06-29) | Reconciliation |
|---|---|---|
| "parent transitions to non-terminal `SPLIT` … + appends `workflow.split`" | `WorkflowTransitionService.transition` hardcodes `WORKFLOW_STATE_CHANGED`; it does not emit `workflow.split`. | Transition for the state change, then `workflowEventWritePort.append(... SPLIT ...)` separately, same tx (R6). |
| "records the proposed dependency edges" then "child dispatches immediately" | `declareDependencies` parks dependents but does NOT dispatch zero-dep children. | Add a `dispatchWhenUnblocked` call per child after declaring edges (R3). |
| "create … children … (c) records the proposed dependency edges" (implied one pass) | `run_dependencies` FKs (RESTRICT) reference sibling child `public_id`s — forward refs FK-fail. | Two passes: all children first, then all edges (R2). |
| "a clear error is returned" (zero children) | 3-18 best-effort reports all-rejected via the result object, not an exception; no error code exists for this. | Aborted **result state** (`aborted_no_children`), parent/proposal untouched; no new `DomainErrorCode` (R7). |
| "create a sub-ticket … else proceeds internal-only linking the child to the parent's ticket" | No existing "link child to parent ticket" helper. | 3f-5 establishes the child↔ticket `IntegrationLink` (sub-ticket ref or parent ref) via `IntegrationLinkService` (R5). |

### R1 — `SplitCommitService.commit` MUST NOT be `@Transactional`
This is the 3-18 "central trap" verbatim (`WorkflowBatchSubmissionService.java:48-53`). The per-subtask seams (`workflowRunCreatePort.create`, `runDependencyService.declareDependencies`, the transition) are each `@Transactional(REQUIRED)`. With an ambient batch tx they'd join it and one subtask's rollback-marked `DomainException` would poison the whole fan-out. Un-annotated top level + `REQUIRES_NEW` `TransactionTemplate` for the idempotency reserve/complete, the proposal CAS, and the parent decomposition gives each unit its own physical tx. **Pin this with an IT** where one subtask throws and the others still commit — the regression-catcher.

### R2 — Two-pass fan-out (children, then edges)
`run_dependencies(run_id, depends_on_run_id)` has `ON DELETE RESTRICT ON UPDATE CASCADE` FKs to `workflow_runs.public_id` (`V28`). Declaring an edge to a prerequisite child that hasn't been minted yet FK-fails. So Pass A mints every child (building `ordinal → childRunId`), Pass B declares edges + dispatches. Per-subtask Pass-A failures simply drop that ordinal from the map; a Pass-B edge that references a dropped ordinal must be skipped (its prerequisite never got created — log and continue, do not FK-fail the dependent's whole subtask).

### R3 — `declareDependencies` parks; `dispatchWhenUnblocked` dispatches
`RunDependencyService.declareDependencies` (`:76`) takes the graph advisory lock (`pg_advisory_xact_lock`, key `0x52444550` "RDEP"), validates acyclicity (`RUN_DEPENDENCY_CYCLE`), persists edges (`on conflict do nothing`), and parks the run in `WaitingForDependencies` if prerequisites are unmet **and** the run is `INBOX`. It does **not** dispatch the satisfied/zero case. `dispatchWhenUnblocked(runId, correlationId)` (`:167`) is the dispatcher: empty `blockedOn` → `dispatchSpecGeneration` → `DISPATCHED`; unmet + `INBOX` → `PARKED`; else `SKIPPED`. Uniform Pass-B per child: `declareDependencies` only if the subtask has incoming edges, **then** `dispatchWhenUnblocked` for every child. The advisory lock is taken internally by both — **do not** call `lockDependencyGraph()` yourself, but DO stay aware that these are the graph's serialization points ([[story-3f-3-run-dependency-graph-waiting-for-dependencies-reconciliations]]).

### R4 — PIN the dependency edge direction
`declareDependencies(runId, dependsOnRunIds)`: `runId` = dependent, `dependsOnRunIds` = prerequisites. The proposal's `dependencies[]{fromOrdinal,toOrdinal}` direction is set by 3f-4's `split-proposal.v1` + the runner decomposition prompt. **Confirm which ordinal is the dependent before mapping**, then assert it in a test. A backwards mapping (running prerequisites as dependents) type-checks, smoke-tests green, and silently inverts execution order — exactly the class of bug this story-context exists to prevent. Reference: `split-proposal.v1.schema.json`, `SplitDependencyView(fromOrdinal,toOrdinal)`, and the 3f-4 runner prompt.

### R5 — Internal-only child links to the parent's ticket (3f-5 owns the wiring)
`TicketSourceSubticketService.createSubticketIfSupported(Project, TicketRef, SubticketDraft)` returns `TicketSourceSubticketOutcome` (`CREATED` / `INTERNAL_ONLY_SKIPPED` / `NO_TICKET_SOURCE`). On `CREATED` link the child to `result.childRef()`; otherwise link the child internal-only to the **parent's** `TicketRef`. There is no pre-built "link child to parent ticket" helper — reuse `IntegrationLinkService` (3-32). `SubticketDraft` fields: `(parentRunId, proposalId, subtaskId, ordinal, title, description, idempotencyKey)`; idempotency key `split:<parentRunId>:<proposalId>:<ordinal>` (the Linear adapter's marker-scan replay backstop keys off this — same key on replay returns the existing child, `replay=true`). Note the documented residual race in the Linear adapter (crash between issue-create and marker-comment) — acceptable, not 3f-5's to solve.

### R6 — Parent decomposition: guarded, idempotent, event appended separately
Only when `createdCount >= 1`: (i) CAS proposal `open → approved` via `SplitProposalWritePort` — a replay finds it already `approved` and short-circuits; (ii) `workflowTransitionService.transition(parentId, WorkflowState.SPLIT, actor, "approve_split", "split-commit:<parentRunId>:<proposalId>")` — the 3f-2 into-edge from `WaitingForSpecApproval`/`WaitingForReview` is the only legal path; a replay finds the parent already in `SPLIT` and the transition table rejects it (catch/treat as no-op); (iii) `workflowEventWritePort.append(new WorkflowEventRecord(PublicIdPrefixes.WORKFLOW_EVENT.next(), parentId, WorkflowEventType.SPLIT, priorGateState, /*resulting*/ null-or-SPLIT, actorIdentity, actorType, "approve_split", null, false, now, Map.of(WorkflowEventDetailKeys.CHILD_RUN_IDS, childRunIds)))`. The `childRunIds` detail key is already allow-listed (3f-2). Steps (i)-(iii) belong in one `REQUIRES_NEW` tx so a partial decomposition can't strand the parent ([[caught-idempotency-conflict-poisons-shared-tx]] — prevent the conflicting flush, don't catch it after).

### R7 — Zero-child abort is a result state, not an exception
All-subtasks-fail ⇒ `SplitCommitResult{parentDecomposed=false, outcome=ABORTED_NO_CHILDREN, subtasks=[...failed]}`, HTTP 200/207, parent + proposal untouched. This mirrors `WorkflowBatchSubmissionService` reporting all-rejected via the result. No new three-sites `DomainErrorCode` ([[new-domainerrorcode-three-sites]] — avoid the foundation-gate cost when a result state suffices). The operator can re-propose or retry the still-`open` proposal. If product later wants a hard 4xx, that's an additive follow-up (out of scope).

### Previous Story Intelligence (3f-1..3f-4, 3-18)
- **3f-4** (immediate predecessor, in `review`) deliberately left `APPROVE_SPLIT` to this story — the enum spot, placeholder JSON line, and pin-test file all already host the sibling actions; just add the one value + the matrix branch. The `split_proposals.status` CHECK already includes `'approved'` (no migration needed). `SplitProposalService`/`SplitProposalReadPort`/`SplitProposalWritePort`/`SplitProposalView`(+`SplitSubtaskView{ordinal,title,scope}`/`SplitDependencyView{fromOrdinal,toOrdinal}`) all exist — reuse, don't rebuild.
- **3f-3** established the Epic-3f service shape: one `application.workflow` service owns preconditions + idempotency; read views live in `application.workflow` (NOT `.spi`); controller deps fan out to ~17 `@WebMvcTest` slices; CLI optional-setter; assert FK referential actions in `FlywaySchemaContractTest` (N/A here — no new table). The advisory-lock serialization (review fix) means consumers keep taking the lock — `declareDependencies`/`dispatchWhenUnblocked` do it internally.
- **3f-2** made `SPLIT` non-terminal with the sole `SPLIT → COMPLETED` out-edge **driven only by 3f-7 rollup** — 3f-5 must NOT add or fire that out-edge; it only drives the *into*-edge. `workflow.split` has two fixture sites already mirrored ([[new-workfloweventtype-fixture-sites]]) — 3f-5 doesn't touch them.
- **3f-1** built `TicketSourceSubticketService` + `SubticketDraft`/`CreateSubticketResult`/`TicketSourceSubticketOutcome` + the Linear sub-issue + parent-link marker. The `supportsTicketCreation=false` adapters (GitHub/GitLab) prove the internal-only path — AC8's end-to-end.
- **3-18** is the best-effort/non-transactional/per-item-own-tx/per-item-idempotency-key/aggregated-result precedent — copy its `requiresNewTemplate`, its loop-catch-aggregate shape, and its "NOT @Transactional" discipline (R1).

### Architecture & Boundary Guardrails
- Keep commit orchestration in `application.workflow`; reach the runner/dispatch, ticket source, and integration links via existing SPI ports/services — **application cannot import adapters** ([[application-cannot-import-adapters]]). `TicketSourceSubticketService` and `RunDependencyService` are the application-layer seams; `IntegrationLinkService` for ticket linkage.
- REST/CLI stay thin (`ArchitectureBoundaryTest`); the `SplitCommitResponse` is assembled from the application result.
- No Flyway, no new registry/state/event/edge — keep the foundation-gate surface to the single `APPROVE_SPLIT` action (+ placeholder + pin). [[transition-table-change-fans-to-contracts]] does NOT apply (no new edge), but the IT is the first real producer of the `WaitingForReview/WaitingForSpecApproval → SPLIT` transition — exercise it.
- If any response renders nested proposal JSON in a detail view, use `Collections.unmodifiable*` not `List.copyOf`/`Map.copyOf` to survive nested nulls ([[listcopyof-mapcopyof-reject-nulls-in-detail-render]]).

### Transaction & Idempotency Notes
- Top level un-annotated (R1). REQUIRES_NEW templates for: idempotency reserve/complete, per-subtask child mint + link, proposal CAS + parent transition + `workflow.split` append.
- Three independent replay guards (defense in depth): the approve `Idempotency-Key` (IdempotencyService), the per-ordinal keys (subticket marker scan + child-run submit key + dep-declare `on conflict do nothing`), and the proposal CAS + SPLIT into-edge guard. Any one alone makes a replay safe; together they make double-creation/double-decomposition impossible.
- Child completion later flows through the **unchanged** `RunDependencyReleaseService` (3f-3) and the 3f-7 rollup — 3f-5 wires nothing for release.

### Latest Technical Context
- No new external library, no new runner contract, no new schema. Pure Spring orchestration over shipped seams. `npm run check:api` after the additive OpenAPI op.

### Testing Standards
- Unit via Surefire; `@SpringBootTest`/Testcontainers ⇒ `*IT` via Failsafe ([[springboot-testcontainers-test-must-be-IT]], [[maven-arglineation-goal-crash]]); the full-loop runner IT in the `docker-runner-it` tier ([[docker-it-needs-exact-docker-runner-it-tag]]).
- `application.*` coverage ≥ 80% (CI `backend-contract-tests`; verify in a clean env — [[verify-ci-fixes-in-clean-env]]).
- OpenAPI: write snapshot → review additive diff → green → `npm run generate-api` → `check:api` ([[openapi-regen-frontend-client-drift-cascade]], [[openapi-regen-platform-shim]]).
- Hand-edited Java needs `spotless:apply` before push ([[spotless-apply-before-pushing-java-edits.md]]); FE: prettier/eslint before push ([[prettier-gate-cascades-ci]]).
- No Claude co-author trailer on commits ([[commit-no-claude-coauthor]]).

### References
- Epic: `_bmad-output/planning-artifacts/epic-03f-complex-ticket-flow.md` — Story 3f-5 + Cross-Cutting Notes (foundation-gate widening, NFR16, FR70/FR71).
- ADR: `docs/adr/0029-complex-ticket-flow.md` — parent→child lineage, the non-terminal `SPLIT` (rollup is 3f-7), run-dependency DAG, NFR16 "explicit reconciliation" hatch (`approve_split` IS the reconciliation).
- Previous stories: `3f-4-llm-split-proposal-channel-and-three-action-loop.md` (the open proposal + actions this story commits), `3f-3-run-dependency-graph-and-waiting-for-dependencies-gating.md` (declare/dispatch seam + advisory lock), `3f-2-parent-child-run-lineage-and-terminal-split-state.md` (`SPLIT` + `workflow.split` + create seam), `3f-1-ticket-source-subticket-creation-capability.md` (subticket service + outcome).
- Live seams (re-grep before editing — anchor semantically):
  - Subticket: `application/integration/ticketsource/TicketSourceSubticketService.java#createSubticketIfSupported`; `domain/integration/ticketsource/{SubticketDraft,CreateSubticketResult,TicketRef}.java`; `domain/integration/ticketsource/TicketSourceSubticketOutcome.java`.
  - Lineage/state: `application/workflow/spi/WorkflowRunCreatePort.java#create(publicId,initialState,projectId,parentRunId)`; `domain/registry/WorkflowState.java#SPLIT`; `application/workflow/WorkflowTransitionTable.java` (into-edges ~:73/:92); `application/workflow/WorkflowTransitionService.java#transition`; `application/workflow/spi/WorkflowEventWritePort.java#append`; `domain/registry/{WorkflowEventType.java#SPLIT, WorkflowEventDetailKeys.java#CHILD_RUN_IDS}`; read: `WorkflowRunPersistenceAdapter.findByParentRunId`.
  - Dependencies: `application/workflow/RunDependencyService.java#{declareDependencies(:76),dispatchWhenUnblocked(:167)}`; `DeclareRunDependenciesCommand`; `RunDependencyReleaseService` (unchanged).
  - Proposal: `application/workflow/{SplitProposalService.java,SplitProposalView.java,SplitSubtaskView.java,SplitDependencyView.java}`; `application/workflow/spi/{SplitProposalReadPort,SplitProposalWritePort}.java`; `db/migration/V29__add_split_proposals.sql` (status CHECK incl. `approved`).
  - Fan-out precedent: `application/workflow/WorkflowBatchSubmissionService.java:48-53` (the non-transactional trap) + `requiresNewTemplate` (~:409) + `{BatchSubmissionResult,TicketBatchResult}.java`.
  - Action matrix: `application/workflow/WorkflowInspectionService.java#appendSplitOverlay` (~:937); `domain/registry/AllowedAction.java` (the `approve_split` placeholder comment); `architecture/AllowedActionRegistryPinTest.java`; `src/test/resources/contracts/frontend/allowed-actions.placeholder.json`.
  - REST/CLI: `adapters/rest/WorkflowController.java` (`/split/decline` ~:429-457; `resolveHumanActor` ~:460); `adapters/cli/WorkflowCommands.java` (optional-setter); integration link: `IntegrationLinkService` (re-grep for the create + by-run lookup signatures).

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Opus 4.8, 1M context)

### Debug Log References

- **R5 reconciliation overridden by user decision.** The story's R5 ("reuse `IntegrationLinkService`, do NOT hand-roll a link row") is infeasible as written: migration `V6__integration_link_active_uniqueness.sql` enforces one active link per `(integration_type, external_ref)` across all runs, so multiple internal-only children cannot all link to the parent's same ticket ref via the standard `linkTicket`. Surfaced to the user (AskUserQuestion); user chose the **"supersede-aware shared link" + V-migration** option. Implemented as a new `internal_subtask` integration type (V30) exempt from the cross-run uniqueness index, plus a new `IntegrationLinkService.linkSplitChildTicket` seam. The `CREATED` and internal-only paths both use this seam (childRef vs parentRef).
- **Production bug caught by `SplitCommitServiceIT`: "No active transaction".** `resolveParentTicketRef` first used `findActiveLinearTicketLink` (a `SELECT … FOR UPDATE` locking read) — but `commit()` is non-transactional (R1), so a pessimistic lock threw `TransactionRequiredException` in production too. Switched to the NON-locking `findActiveLinkByWorkflowRun` (returns the `linear` variant by convention). This change required updating the `SplitCommitServiceTest` stub (caught by the full Surefire suite, not isolation).
- **`spec-stage.auto-dispatch=false` in the test profile** means `dispatchSpecGeneration` is a no-op, so an independent child stays `Inbox` (not `Investigating`) in the IT. The IT asserts the observable gating (dependent PARKS in `WaitingForDependencies`, independent is NOT parked); the dispatch DECISION (`DISPATCHED`/`PARKED`) is unit-tested.

### Completion Notes List

- **Task 1 — `APPROVE_SPLIT` action.** Added the enum value + `allowed-actions.placeholder.json` entry + `approveSplitWireValueIsPinned()` pin; `WorkflowInspectionService.appendSplitOverlay` now adds `APPROVE_SPLIT` alongside `REPROPOSE_SPLIT`/`DECLINE_SPLIT` in the open-proposal branch. Matrix unit tests extended (both gates × open/no-open). `DomainRegistry.allowedActions()` derives from the enum, so `RegistryContractTest` stays aligned automatically.
- **Task 2 — `SplitCommitService`.** Non-`@Transactional` best-effort two-pass fan-out (R1/R2); `REQUIRES_NEW` `TransactionTemplate` for idempotency reserve/complete, per-subtask child mint + link, and the parent decomposition. Pass B maps proposal `{fromOrdinal→toOrdinal}` edges with the direction PINNED (R4: `fromOrdinal` depends on `toOrdinal`), `declareDependencies` then `dispatchWhenUnblocked` per child (R3). Parent decomposition (R6): CAS proposal `open→approved` (replay-safe guard) → `transition(… SPLIT …)` → separate `workflow.split` event with `childRunIds`. Zero-child abort is a result state (R7), no new `DomainErrorCode`. Added `ApproveSplitCommand`, `SplitCommitResult`/`SubtaskCommitOutcome`, and `SplitProposalWritePort.approveOpenForRun` (+ adapter).
- **Task 3 — REST + CLI.** `POST /{workflowRunId}/split/approve` + `SplitCommitResponse` DTO (mirrors `/split/decline`); `SplitCommitService` added to `WorkflowController` ctor (fanned out `@MockitoBean SplitCommitService` to all 17 `@WebMvcTest(WorkflowController.class)` slices). CLI `split-approve` via the optional-setter pattern + `requireSplitCommitWired()` guard + JSON/text renderer.
- **Task 4 — Linkage + V30.** `V30__add_internal_subtask_integration_link_type.sql` widens the integration_type CHECK and re-types the V6 cross-run uniqueness index to exempt `internal_subtask`; `linkSplitChildTicket` records the child↔ticket association (per-run uniqueness keeps replay idempotent). Parent ref resolved NON-locking.
- **Task 5 — OpenAPI + FE.** OpenAPI snapshot regenerated (`OpenApiSnapshotContractTest -Dopenapi.snapshot.write=true`, +`/split/approve` + `SplitCommitResponse`); `schema.d.ts` regenerated; `check:api` green. FE: `approve_split` added to `DecisionAction`/`KNOWN_ACTIONS`/`CONSEQUENCE_HINTS`; `useApproveSplit` mutation; `SplitActionBar` governed approve primary; new `SplitCommitResultPanel` (per-subtask outcomes + parent→child links + zero-child aborted render). Vitest + axe extended.
- **Task 6 — Tests + logging.** `SplitCommitServiceTest` (7 unit, incl. R4 direction pin, zero-child abort, per-subtask isolation, CAS-replay, top-level replay, + 4 list-appender log assertions); `SplitCommitServiceIT` (3 scenarios: fan-out+lineage+dependency-direction+gated-dispatch+decomposition, zero-child guard, idempotent replay); `IntegrationLinkSplitChildIT` (V30: many children share parent ref + replay no-op); `SplitProposalPersistenceAdapterIT.approveOpenForRunFlipsOpenToApprovedAndIsReplaySafe`.
- **Verification.** Backend Surefire unit tier 1406/1406; targeted Failsafe ITs/contracts green (SplitCommitServiceIT 3/3, IntegrationLinkSplitChildIT 2/2, SplitProposalPersistenceAdapterIT 7/7, OpenApiSnapshot 1/1, Flyway 29/29, Registry 22/22, AllowedActionsEndpoint 10/10, RunDependencyEndpoint 4/4, WorkflowControllerLogging 6/6, CliRestEquivalence 7/7, ProblemDetails 11/11). Static gates: spotless ✓, checkstyle 0 violations, spotbugs ✓. FE: 1220 vitest + tsc + eslint + check:api ✓.

### File List

**Backend — main (new):**
- `deliveryline-backend/src/main/java/org/dradgo/application/workflow/SplitCommitService.java`
- `deliveryline-backend/src/main/java/org/dradgo/application/workflow/ApproveSplitCommand.java`
- `deliveryline-backend/src/main/java/org/dradgo/application/workflow/SplitCommitResult.java`
- `deliveryline-backend/src/main/java/org/dradgo/adapters/rest/SplitCommitResponse.java`
- `deliveryline-backend/src/main/resources/db/migration/V30__add_internal_subtask_integration_link_type.sql`

**Backend — main (modified):**
- `deliveryline-backend/src/main/java/org/dradgo/domain/registry/AllowedAction.java` (+`APPROVE_SPLIT`)
- `deliveryline-backend/src/main/java/org/dradgo/application/workflow/WorkflowInspectionService.java` (overlay)
- `deliveryline-backend/src/main/java/org/dradgo/application/workflow/spi/SplitProposalWritePort.java` (+`approveOpenForRun`)
- `deliveryline-backend/src/main/java/org/dradgo/adapters/persistence/SplitProposalPersistenceAdapter.java` (+`approveOpenForRun`)
- `deliveryline-backend/src/main/java/org/dradgo/application/integration/IntegrationLinkService.java` (+`linkSplitChildTicket` + `internal_subtask` type)
- `deliveryline-backend/src/main/java/org/dradgo/adapters/rest/WorkflowController.java` (`/split/approve` + ctor dep)
- `deliveryline-backend/src/main/java/org/dradgo/adapters/cli/WorkflowCommands.java` (`split-approve` + setter + guard + renderer)
- `deliveryline-backend/src/main/resources/openapi/openapi.json` (regenerated)

**Backend — test (new):**
- `deliveryline-backend/src/test/java/org/dradgo/application/workflow/SplitCommitServiceTest.java`
- `deliveryline-backend/src/test/java/org/dradgo/application/workflow/SplitCommitServiceIT.java`
- `deliveryline-backend/src/test/java/org/dradgo/application/integration/IntegrationLinkSplitChildIT.java`

**Backend — test (modified):**
- `deliveryline-backend/src/test/java/org/dradgo/architecture/AllowedActionRegistryPinTest.java`
- `deliveryline-backend/src/test/java/org/dradgo/application/workflow/WorkflowInspectionServiceAllowedActionsTest.java`
- `deliveryline-backend/src/test/java/org/dradgo/adapters/persistence/SplitProposalPersistenceAdapterIT.java`
- `deliveryline-backend/src/test/resources/contracts/frontend/allowed-actions.placeholder.json`
- 17 `@WebMvcTest(WorkflowController.class)` slices — added `@MockitoBean SplitCommitService` (AcceptImplementation/AllowedActions/AnswerClarification/ApproveSpec/ArchiveRun/Clarifications/ManualArtifact/ManualBundle/RejectImplementation/RejectSpec/RunDependency/Takeover EndpointContractTest, WorkflowControllerLoggingContractTest, WorkflowAdapterEquivalenceTest, ProblemDetailsContractTest, CliRestEquivalenceContractTest, CommandModelSymmetryFoundationContract)

**Frontend (modified):**
- `deliveryline-frontend/src/lib/api/schema.d.ts` (regenerated)
- `deliveryline-frontend/src/features/workflows/approvalDecisionView.ts`
- `deliveryline-frontend/src/features/workflows/hooks/useSplitActions.ts`
- `deliveryline-frontend/src/features/workflows/components/SplitProposalPanel.tsx`
- `deliveryline-frontend/src/features/workflows/components/SplitProposalPanel.test.tsx`

## Change Log

| Date | Version | Change |
|------|---------|--------|
| 2026-06-29 | 0.1 | Created ready-for-dev story for 3f-5: single new `APPROVE_SPLIT` action + matrix overlay; `SplitCommitService` best-effort non-transactional two-pass fan-out (subticket-or-internal-only → child run with `parentRunId` → dependency edges → gated dispatch); parent decomposition to non-terminal `SPLIT` + `workflow.split` only when ≥1 child; zero-child abort as a result state; idempotent replay; REST `POST /split/approve` + CLI `split-approve`; FE commit-result render. Reconciliations: non-`@Transactional` fan-out (R1); two-pass children-then-edges for sibling FKs (R2); `declareDependencies` parks but `dispatchWhenUnblocked` dispatches (R3); PIN dependency edge direction (R4); internal-only child links to parent ticket via `IntegrationLinkService`, 3f-5 owns it (R5); decomposition guarded + `workflow.split` appended separately from the SPLIT transition (R6); zero-child abort is a result state, no new error code (R7). No Flyway / state / event / edge / runner contract — all substrate shipped by 3f-1..3f-4. |
| 2026-06-29 | 1.0 | Implemented 3f-5 → review. All 6 tasks + logging complete. **R5 superseded by user decision:** the V6 cross-run uniqueness index makes the prescribed "reuse `linkTicket`, no new schema" infeasible for multiple internal-only children sharing the parent ref, so (per AskUserQuestion) added migration **V30** introducing an `internal_subtask` integration type exempt from the cross-run uniqueness index + a new `IntegrationLinkService.linkSplitChildTicket` seam (used by BOTH the `CREATED` childRef path and the internal-only parentRef path). Caught + fixed a real production bug: `resolveParentTicketRef` must use the NON-locking `findActiveLinkByWorkflowRun` (not the `FOR UPDATE` `findActiveLinearTicketLink`) since `commit()` is non-transactional. Backend 1406/1406 unit + targeted ITs/contracts green; static gates clean; FE 1220 vitest + tsc + lint + check:api green; OpenAPI snapshot regenerated. |

### Review Findings

<!-- Adversarial code review 2026-06-29 (Blind Hunter + Edge Case Hunter + Acceptance Auditor). Diff = uncommitted working tree (tracked diff HEAD + 8 untracked new files). All findings independently verified against source. -->

- [x] [Review][Patch] (FIXED in review 2026-06-29 — `requireAcyclicDependencies` rejects cycles before Pass A; pinned by `cyclicDependencyGraphRejectedBeforeAnyChildMinted`) **Pass B fan-out is not best-effort — an invalid/cyclic dependency graph aborts the whole commit AFTER children are minted + dispatched** [SplitCommitService.java:207] — RESOLVED (decision 2026-06-29): **validate dependency-graph acyclicity BEFORE Pass A** — if the proposal's edges contain a cycle, reject the approve up front (parent + proposal untouched, nothing minted); do NOT silently drop edges. Combine with P3 (per-child isolation) for transient non-graph Pass-B failures. — The 3f-4 `SplitProposalHarvester` filters self-edges, dangling ordinals and duplicates but NOT transitive cycles, so a cyclic edge set survives onto the proposal. `dispatchPassB` (line 207) is NOT wrapped in try/catch (unlike Pass A's per-subtask catch at 316-325); `declareDependencies` throws `RUN_DEPENDENCY_CYCLE` (or `prerequisite_not_pre_execution`), unwinds to `commit()`'s catch (152), `completeFailed` marks the idempotency record FAILED, and the exception is rethrown as 500. By then Pass A has minted child runs and earlier Pass-B iterations have already synchronously dispatched some (INBOX→INVESTIGATING + real runner enqueue). Net: orphaned/running children, parent + proposal untouched, run un-cleanly-retryable. Decision required: **reject the approve up-front on an invalid graph (validate acyclicity before Pass A → nothing created)** vs **best-effort skip bad edges and proceed**. Recommendation: validate-before-Pass-A (fail fast, mint nothing) — silently dropping a cycle edge risks R4's "silent execution-order inversion."

- [x] [Review][Patch] (FIXED in review 2026-06-29) NUL byte (0x00) embedded in source binarizes the load-bearing file [SplitCommitService.java — `FIELD_SEPARATOR` literal, byte offset ~4355] — the `private static final String FIELD_SEPARATOR = "<NUL>"` literal contains a raw 0x00 byte instead of the escape `" "`. It compiles, but git treats the whole file as binary (unreviewable in diffs) and it is a cross-platform/encoding hazard ([[literal-nul-byte-binarizes-source]]). Fix: replace the raw NUL with `" "`.
- [x] [Review][Patch] (FIXED in review 2026-06-29 — 17 files round-trip-repaired) Mojibake (CP1251-misdecoded UTF-8) committed in comments across ~15 modified files [WorkflowAdapterEquivalenceTest.java:48 + ~14 others] — em-dash `—`→`вЂ"`, `→`→`в†'`, `↔`→`в†"`, `§`→`В§`, `⇒`→`в‡'` on the new comment lines; HEAD versions are clean UTF-8. Comment-only (no behavior change) but pollutes a dozen-plus files and signals an editing-toolchain encoding bug. Fix: restore proper UTF-8 in the affected comments.
- [x] [Review][Patch] (FIXED in review 2026-06-29 — per-child try/catch in both Pass-B phases) Pass-B per-child operations not isolated [SplitCommitService.java:344-383] — wrap `declareDependencies`/`dispatchWhenUnblocked` per child in try/catch so one child's edge/dispatch failure does not abort the fan-out (consistent with Pass A + the best-effort contract). (Implements the chosen direction of the Decision above.)
- [x] [Review][Patch] (FIXED in review 2026-06-29 — declare-all then dispatch-all; edge-failed dependents held back; pinned by `passBEdgeFailureLeavesDependentUndispatchedButCommitsTheRest`) Pass B should be two-phase: declare ALL edges, then dispatch ALL children [SplitCommitService.java:344-383] — the single ascending loop dispatches a low-ordinal prerequisite (synchronous INBOX→INVESTIGATING) before a higher-ordinal dependent declares its edge; if the prerequisite advances past pre-execution first, `declareDependencies` throws `prerequisite_not_pre_execution`. Split Pass B into declare-all then dispatch-all.
- [x] [Review][Patch] (FIXED in review 2026-06-29 — pre-Pass-A reconcile: if the parent already has children, reconcile from durable state instead of re-minting; pinned by `existingChildrenReconcileWithoutRecreating`. Chose a per-parent guard over a per-ordinal key — stronger and schema-free) Child-run creation is not per-ordinal idempotent (AC6's named "child-run submit key" missing) [SplitCommitService.java:286] — `createChildForSubtask` mints a fresh `WORKFLOW_RUN.next()` with no per-`(run,proposal,ordinal)` guard, so a different-key retry (or a new-key retry after a Pass-B abort) double-creates children. Add a deterministic per-ordinal child-create guard.
- [x] [Review][Patch] (FIXED in review 2026-06-29 — re-reads live state; only a genuine `SPLIT` is a no-op, otherwise rethrows; pinned by `decomposeRethrowsWhenParentLeftItsGateDuringFanOut`) `decomposeParent` swallows ANY `ILLEGAL_TRANSITION` as success after CAS-flipping the proposal [SplitCommitService.java:427-434] — if the parent left its gate to a non-`SPLIT` state during the slow fan-out, the transition throws `ILLEGAL_TRANSITION`, the catch returns `true` (comment assumes "already Split"), no `workflow.split` event is appended, yet the result reports `parentDecomposed=true`/`decomposed`. Confirm the live state is actually `SPLIT` before treating it as an idempotent no-op.
- [x] [Review][Patch] (FIXED in review 2026-06-29 — reservation now keys on the deterministic `split-commit:<run>:<proposal>` per AC6, not the client key, so concurrent distinct-key approves serialize on the in-flight reservation and the loser replays; pinned by `reservationUsesDeterministicPerProposalKey`. Also moved preconditions before the reserve so a precondition failure no longer poisons the key — fixes the Edge-F2 retry wedge) No proposal claim/lock before Pass A → concurrent distinct-key approves double the fan-out [SplitCommitService.java:162-166, 408-415] — two concurrent `approve_split` calls with different Idempotency-Keys both pass the gate, both run Pass A; sub-tickets dedupe via the marker key but child runs are minted fresh, internal_subtask links are V30-exempt from cross-run uniqueness, and the CAS only serializes decomposition (the loser gets `flipped==0` and still returns `decomposed`). Claim the proposal (CAS open→approved or advisory lock) before Pass A.
- [x] [Review][Patch] (FIXED in review 2026-06-29) CLI `split-approve --format json` omits `childTicketRef` and `reason` per subtask [WorkflowCommands.java:1053-1061] — the REST `SubtaskOutcomePayload` carries all five fields; the CLI emits only `ordinal`/`status`/`childRunId`, so CLI consumers can't see the created sub-ticket ref or a failure's cause. Add the two missing fields.

- [x] [Review][Defer] `replay()` fabricates ascending ordinals + `created` status [SplitCommitService.java:477-483] — deferred: same-key replay reconstructs each surviving child as `created` with synthetic ordinals and `childTicketRef=null`, can't reproduce `internal_only`/`failed`, causing CLI/REST shape drift on replay. Acknowledged best-effort in code; a real fix needs durable per-subtask outcome storage.
- [x] [Review][Defer] AC9 test-completeness deviations [SplitCommitServiceIT] — deferred: no single full-loop IT driving `request_split`→`approve` (seeds the open proposal via `insertOpen`); the "mixed sub-ticket + internal-only" assertion is split across the `mixedOutcomes` unit test + `IntegrationLinkSplitChildIT` rather than one IT; the full-loop IT is tagged `@Tag("integration")` not the prescribed `docker-runner-it` tier; `application.* ≥80%` coverage could not be independently verified from the diff (claimed green in Completion Notes). Partially reconciled in the Debug Log (`spec-stage.auto-dispatch=false`). Verify coverage in CI.
