# Story 4.27: Failed-Run Recovery Walkthrough Documentation Increment

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a Workflow Owner joining the pilot,
I want a `docs/failed-run-recovery-walkthrough.md` that explains the Epic 4 recovery surface end-to-end — what each recovery action does, when to choose it, and how to recognize when to pause vs reconcile vs takeover vs classify — with annotated diagrams,
so that operators have a confident decision-making guide on their first pilot failure (NFR42 satisfied for the workflow-owner persona) and the architecture's "recovery is part of the primary value proposition" claim is realized in operator-facing docs.

## Scope Decisions (read first — these resolve epic-vs-reality tensions)

**This is a PURE DOCUMENTATION story. It produces exactly one new markdown file (`docs/failed-run-recovery-walkthrough.md`), one (or two) README "Quick links" entries, and a small set of `docs/glossary.md` additions. It touches ZERO application / frontend / backend code, no tests, no CI workflow logic, no ADRs, no schema.** The walkthrough must describe the recovery surface **exactly as it is implemented today** — every button label, dialog title, decision option, config key, CLI command, REST path, and taxonomy value below was verified verbatim against live `deliveryline-frontend` / `deliveryline-backend` source on **2026-07-18** (see the Live-string inventory in Dev Notes). **Where the epic's AC wording drifts from the shipped code, the live string wins.** A walkthrough that names a button, command, or option the operator can't find is worse than no walkthrough — quote real strings, do not paraphrase.

This story is the **direct Epic-4 twin of story 3.36** (execution-walkthrough increment) and story 2.29 (PM-loop increment). Model the new doc's house style, validator-placeholder line, diagram approach, glossary discipline, README-as-index decision, and logging-N/A handling on `3-36-execution-walkthrough-documentation-increment.md` and the existing walkthroughs (`docs/execution-walkthrough.md`, `docs/failure-recovery-walkthrough.md`, `docs/pm-loop-walkthrough.md`).

- **R1 — Pure-documentation scope + verbatim live-string fidelity.** Like stories 2.29/3.36, this story emits only markdown. The Dev Notes "Live-string inventory" table seeds every known button label, dialog title, decision option, config key, CLI verb, REST path, and taxonomy value with a `file:line` anchor. **If any live string disagrees with this story's tables at write time, re-grep and trust the code, not the story** — and record the drift in the Change Log. Do not paraphrase on-screen strings; copy them.

- **R2 — Two similarly-named walkthroughs will coexist — this is the NEW Epic-4 one.** The Epic-1 baseline doc is `docs/failure-recovery-walkthrough.md` (CLI-only, `retry`-only; `failure-recovery-…`). This story creates `docs/failed-run-recovery-walkthrough.md` (`failed-run-recovery-…`, note the different slug) — the **operator-console** guide for the full Epic-4 recovery surface. **Do NOT overwrite or rename the Epic-1 doc.** The two must cross-reference each other: the Epic-1 doc already says *"Epic 4 will extend it with the operator console"* (`failure-recovery-walkthrough.md:5-9`), so the new doc links back to it for the CLI `retry` decision aid + the `next safe action` matrix, and links forward from it. Get the exact filename right everywhere (AC1, AC11 both name `failed-run-recovery-walkthrough.md`).

- **R3 — Docs index = README "Quick links", NOT `docs/index.md` (AC11).** **There is no `docs/index.md`** — the glossary states the full documentation index is owned by Epic-6 stories 6.1/6.2, and stories 2.29 (R8) + 3.36 (R2) established the README "Quick links" section (`README.md:11-26`) as the de-facto docs index. **Encoded default: satisfy AC11 by adding a README "Quick links" entry** for the operator recovery walkthrough (e.g. *"Operator triaging a failed run in the console? → `docs/failed-run-recovery-walkthrough.md`"*), alongside the existing "Run failed? → `failure-recovery-walkthrough.md`" entry (`README.md:17`). Keep the existing Epic-1 entry; the two docs cross-link (R2). **Do NOT create `docs/index.md`** — that would collide with the Epic-6 6.1 deliverable. AC11's clause *"Epic 4 cannot close without … visible from `docs/index.md`"* is an **epic-close-checklist concern, not a this-story deliverable** — there is no `docs/epic-4-close-checklist.md` and no docs foundation-gate today (only `docs/epic-1-close-checklist.md` exists). This story's AC11 obligation is the README link being present + lychee-resolvable; flag the close-gate clause in Completion Notes. *(See Open Question 1.)*

- **R4 — Decision matrix: two DISTINCT classification axes — do not conflate them (AC3 vs AC7).** The doc must present two separate tables and say plainly they are different axes:
  - **AC3 decision matrix keys on `FailureCategory`** (runner-scoped, machine-set, 12 wire values: `runner_timeout`, `runner_crash`, `runner_contract_violation`, `runner_non_zero_exit`, `runner_late_result`, `runner_duplicate_result`, `runner_malformed_output`, `runner_secret_leak`, `runner_build_failed`, `testcontainers_infra_failed`, `recovery_dispatch_failed`, `orphan` — `FailureCategory.java:6-29`). This axis records **what the runner execution did**. Map each → recommended recovery action. The epic's AC3 lists only 10; the live enum has 12 (`testcontainers_infra_failed`, `recovery_dispatch_failed` added by stories 3h-1/4.7). Include all 12, or explicitly note the two extras.
  - **AC7 taxonomy section keys on `FailureTaxonomyValue`** (operator-applied, post-hoc, 6 wire values: `specification_gap`, `context_gap`, `agent_execution_failure`, `review_rejection`, `integration_or_merge_failure`, `tooling_or_infrastructure_failure` — `FailureTaxonomyValue.java:24-30`). This axis records **the operator's judgment of WHY the run failed**, for cross-run pattern analysis. Both can be set on the same run. State the distinction explicitly (source: the `FailureTaxonomyValue` Javadoc at `FailureTaxonomyValue.java:5-23` — "Separate from `FailureCategory` which is runner-scoped").

- **R5 — The recommended-action logic is live and deterministic — anchor the matrix to it (AC3).** `RecommendationService.recommend(currentState, failureCategory, linearDrift, githubDrift, nextSafeAction)` (`RecommendationService.java:35,90`) is a pure, port-free ranker: `safe(0) < caution(1) < risky(2)`. Facts the AC3 matrix must reflect: `retry` is always surfaced (risky under manual-reconciliation / contract-violation / malformed / secret-leak; caution for build-failed / orphan / testcontainers; else safe); `pause` added (safe) for manual-reconciliation / risky categories; `reconcile` added (safe on drift, caution on orphan); `classify_failure` always safe; **under integration drift every other safe MUTATING action is downgraded to caution except reconcile + classify** (`RecommendationService.java:108-192`). The `nextSafeAction` hint (`await_manual_reconciliation`, `retry`, `await_outcome`, `view_only`) comes from `RecoveryService#describeFailure` and is documented in the Epic-1 `failure-recovery-walkthrough.md#step-2--decide-retry-vs-await_manual_reconciliation` — cross-link it, do not re-derive it.

- **R6 — Recovery actions surfaced in the Decision Bar `recovery_operator` mode are SIX, and takeover is NOT one of them (AC3, AC4, AC6).** The `RecoveryDecisionBarContainer` → `ApprovalDecisionBar` in `recovery_operator` mode exposes exactly six actions (`approvalDecisionView.ts:698-705`, labels at `ApprovalDecisionBar.tsx:229-236`): **Retry failed step / Resume run / Reconcile conflict / Rerun from step / Pause run / Classify failure** (role constant `RECOVERY_OPERATOR_ROLE = 'workflow_owner'`, `approvalDecisionView.ts:129`). **`takeover` is a separate action on a different service** — `DeveloperTakeoverService` (POST `/api/v1/workflows/{id}/takeover`, `WorkflowController.java:2133`), story 3.22/3.25, still ArchUnit scope-protected per ADR 0033 §(c). The epic's AC3 lists "takeover" among the matrix's recommended actions; that is correct as a *decision* (an operator may choose to take over) but the doc must be accurate that takeover is surfaced via the developer/takeover path, NOT the `recovery_operator` Decision Bar's six actions. Present takeover in the AC4 "Pause vs Takeover" section and reference it in the matrix as the "hand off to a human long-term" option, cross-referencing `execution-walkthrough.md#when-and-how-to-take-over` (exists).

- **R7 — Safety affixes are three-tier in the Decision Bar, two-tier in the reconciliation dialog (AC3, AC6).** The Decision Bar renders `Safe` / `Caution` / `Higher risk` affixes (`ApprovalDecisionBar.tsx:239-243`); the deep-dive diagnostics render `[SAFE]` / `[CAUTION]` / `[RISKY]` chips (`FailureEventSurface.tsx:243`); the reconciliation dialog uses only two tiers `SAFE` / `RISKY` (`reconciliationDialogView.ts:268-269`). Use the correct affix vocabulary per surface in each diagram; do not invent a unified scale. **Only `retry` is one-click-invokable** from the deep-dive today (`INVOKABLE_ACTION_TYPES = {'retry'}`, `failureDiagnosticsView.ts:15`); the other actions are surfaced-and-explained. The Decision Bar wires the reconcile + classify seams (`RecoveryDecisionBarContainer.tsx:390-408`).

- **R8 — Failure diagnostics deep-dive: the NFR7 "five questions" have surface-specific labels (AC1 diagnose step, AC6 diagram).** The frontend `FailureDiagnosticsBody` (inside `FailureEventSurface.tsx`) renders five fields: **What happened / Reason / What changed / Who acted / What is next** (`FailureEventSurface.tsx:440,445,450,456,461`), sheet title **Event diagnostics** (`:713`), with a **Recommended recovery actions** section (`:504`) showing `[SAFE]/[CAUTION]/[RISKY]` + `Precondition:` per row. The CLI `deliveryline operator diagnose` describes the five as *"what happened / changed / who acted / what failed / what is next"* (`OperatorCommands.java:250-251`) — the UI's second slot is **Reason** where the CLI says "what failed". **Quote the frontend labels for UI sections and the CLI wording for CLI sections; note they map 1:1.**

- **R9 — Auto-pause-on-conflict is real and gated — document it precisely (AC5).** Auto-pause fires on `external_state_advanced` / `external_state_reverted` conflicts (story 4.18 AC4). The operator reviews both snapshots in the `ReconciliationDialog` (story 4.23) and picks an explicit decision; the dispatch gate (story 4.18 AC6) blocks a manual resume that bypasses reconciliation. Reconciliation decision options (wire → label, `reconciliationDialogView.ts:221-226`): `accept_external_state`→**Accept external state**, `accept_internal_state`→**Accept internal state**, `mark_completed_externally`→**Mark completed externally**, `mark_failed_externally`→**Mark failed externally**; the first (safe-first) option is marked **Recommended** (`ReconciliationDialog.tsx:456`); **Reason** is required (`ReconciliationDialog.tsx:483-494`); panels are **Internal state** / **External state** (`:405,412`). The AC5 example "reconcile with `accept_external_state`" is correct — use the live label **Accept external state**.

- **R10 — Classification dialog labels are HUMANIZED from the registry — do not show raw snake_case (AC7).** `FailureClassificationDialog` (title **Classify failure**, `:177`) renders radio cards via `entry.humanReadableName` + `entry.description` + `entry.examples` from the `GET /api/v1/registries/failure-taxonomy` read model (`FailureClassificationDialog.tsx:277-295`); fieldset legend **Failure category** (`:246`); submit **Apply classification** (`:180`); optional **Reason (optional)** (`:317`); deprecated values get a `(deprecated, use X instead)` affix (`:251`). **In the AC7 taxonomy section, present each of the six values by its wire value AND its human-readable form**, but note the operator sees the humanized label + description + examples in the dialog (the wire snake_case is the stored/analytics form). Pull the exact `humanReadableName`/`description`/`examples` for each of the six from the registry read model at write time (`FailureTaxonomyMetadataView` / `FailureTaxonomyCatalog.java`) — do not invent descriptions.

- **R11 — Diagrams over screenshots (AC6).** There is **no screenshot-capture pipeline** in this repo (binary PNGs rot against an evolving UI + bloat the tree), and AC6 explicitly permits "annotated diagrams (Mermaid OK)". **Encoded default: Mermaid diagrams + fenced ASCII layout sketches**, annotated to label each required surface — matching the three existing walkthroughs. AC6's required illustration set (with the live component to annotate): (1) **operator queue with a failed run highlighted** (`OperatorQueue.tsx` — heading "Operator queue", `RunReviewQueueItem variant="operator"` rows, `OperatorFilterSidebar` filter groups State/Failure category/Runner kind/Time window); (2) **failure diagnostics deep-dive with safety-ranked actions** (`FailureEventSurface.tsx` — the five fields + "Recommended recovery actions" with `[SAFE]/[CAUTION]/[RISKY]`); (3) **Decision Bar `recovery_operator` mode showing all 6 actions** (`ApprovalDecisionBar.tsx` — the six labels + Safe/Caution/Higher risk affixes); (4) **reconciliation dialog with side-by-side snapshots + recommended decision** (`ReconciliationDialog.tsx` — Internal/External panels, decision options, Recommended marker); (5) **classification dialog with radio cards** (`FailureClassificationDialog.tsx` — Failure category legend, humanized radio cards); (6) **audit query result showing post-recovery history** (`deliveryline audit query` output / `GET /api/v1/audit/by-run/{id}`). Note the runner-kind queue filter + bulk-actions ship **disabled** ("coming soon", `OperatorFilterSidebar.tsx:96`, `OperatorQueue.tsx:163-172`) — annotate them as such or omit; do not imply they work.

- **R12 — "Setting up alerts" references real, existing infra (AC8).** The three alert rules exist in `infra/observability/prometheus/alerts.yml`: **`RunnerQueueDepthHigh`** (`deliveryline_runner_queue_depth > 50` for 5m, `:11`), **`RunnerOldestQueuedStale`** (`deliveryline_runner_queue_oldest_age_seconds > 1200`, `:32`), **`IntegrationConflictUnresolvedHigh`** (unresolved `integration_conflicts` total > 5 for 10m, `:59`). Enablement docs live in `infra/observability/prometheus/README-alerting.md` (exists) and the observability stack starts with `docker compose --profile observability up -d` (story 3.19 AC8 alertmanager routing). Cross-link the README-alerting doc if it is under `docs/` reach of lychee — it is under `infra/`, so **reference it in a fenced path, not a markdown link** unless it resolves (lychee scans `docs/*.md docs/**/*.md README.md` only, so an `infra/` path is safe to name in prose/code but NOT as a relative link that lychee would try to resolve). Verify the exact alert names + thresholds against `alerts.yml` at write time.

- **R13 — Background section MUST reference ADR 0033 (AC-adjacent; satisfies story 4.28 AC7 + pre-satisfies story 4.29 AC4).** Add a short **"Background"** section explaining why the deeper recovery actions are now available, with a markdown link to `adr/0033-recovery-service-scope-lift.md` (exists, `docs/adr/0033-recovery-service-scope-lift.md`). This is the cross-link that story 4.28 AC7 promised and that `deferred-work.md` records as pending; authoring it here closes that thread and pre-satisfies story 4.29 AC4. One paragraph: story 1.18 installed an ArchUnit tripwire pinning `RecoveryService` to the Epic-1 baseline; Epic 4 (stories 4.5–4.9) delivered the deeper surface; story 4.28 lifted the tripwire and replaced it with ADR 0033, which governs the sanctioned recovery surface. Keep it brief — link out for detail.

- **R14 — Link-check contract (AC10 — this WILL gate the build).** The `docs-link-check` lychee job (`.github/workflows/ci.yml:147-205`) runs over `docs/*.md docs/**/*.md README.md`; **internal (relative + anchor) link failures FAIL the build** (external http(s) links are a separate non-blocking pass). Therefore: every relative link + `#anchor` in the new doc must resolve. **Story numbers (1.18, 2.16, 2.19, 3.19, 3.30, 4.2, 4.3, 4.4, 4.22, 4.23, 4.24, etc.) are NOT files** — reference them in prose, never as markdown links (AC10 lists these as the cross-refs to anchor "correctly" — that means correct prose references, not links). The only markdown links are to real files lychee can resolve: `failure-recovery-walkthrough.md`, `execution-walkthrough.md`, `pm-loop-walkthrough.md`, `quickstart.md`, `glossary.md` (+ new `#anchor`s you add), `supported-environments.md`, `cli/README.md`, `cli/workflow-commands.md`, `adr/0033-recovery-service-scope-lift.md`, and the README. **Never link `_bmad-output/…` or `infra/…`** (outside lychee's scan set → treated as broken). Em-dash headings (`## Step 1 — …`) GitHub-slugify to double-hyphen anchors — prefer prose cross-refs over intra-doc `#` links to avoid dead anchors (stories 2.29/3.36 both hit this).

- **R15 — Validator placeholder (AC12).** Near the top, include the exact-format line: `> **Operator walkthrough validator:** \`_____________________________\` (to be named before Epic 4 close)` — matching `quickstart.md:9`, `pm-loop-walkthrough.md:3`, `failure-recovery-walkthrough.md:3`, `execution-walkthrough.md`. The blank underscores are the spec-sanctioned convention (named at epic close), not a defect.

- **R16 — Glossary additions are in-scope (AC13 + glossary discipline).** `docs/glossary.md` carries a hard rule: *"Any doc that introduces a new term beyond this canonical set must add an entry here in the same PR."* The PRD seven concepts + Epic-1 `recovery action` + Epic-3 `takeover` already exist; the **Epic-4 vocabulary AC13 enumerates is new**: **resume**, **reconcile**, **rerun-from-step**, **pause**, **classify** (classification / taxonomy), **conflict** (integration conflict), **drift** (artifact drift). **Encoded default: add a terse `## Epic 4 vocabulary (failure handling & recovery)` section** registering each of those terms (fold `taxonomy` under `classify`, `auto-pause` under `pause`, `reconciliation decision` under `reconcile`), cross-linked to the walkthrough, with a "Linked from" back-reference. `takeover` already has an entry — extend it with a one-line pointer to the new pause-vs-takeover section rather than duplicating. Keep entries terse — Epic-6 6.2 normalizes wording later; this story registers the terms so NFR43 concept-sprawl tracking stays honest. *(See Open Question 2.)*

- **R17 — The mandatory "Logging instrumentation" task is N/A here.** This story emits only markdown — no service, branch, or SLF4J surface to instrument. Mark the cross-cutting logging task **N/A with rationale** rather than padding it with fake subtasks (mirror stories 2.29/3.36). The project-wide logging standard targets touched *services*; a docs increment touches none.

## Acceptance Criteria

_(The epic's 13 ACs, pinned to live reality via R1–R17. Where epic wording drifts from shipped code, the live string wins.)_

1. **Given** `docs/failed-run-recovery-walkthrough.md` (the NEW operator-console doc, distinct from the Epic-1 `failure-recovery-walkthrough.md` — R2), **Then** it follows a problem-driven sequence: **identifying** that something needs attention (operator queue, story 4.2 + alerts, story 3.19) → **diagnosing** the failure (deep-dive view, story 4.4 + audit query, story 4.3) → **choosing** a recovery action (the `FailureCategory` → action decision matrix, R4) → **executing** the action (Decision Bar `recovery_operator` mode, story 4.22 + the reconciliation, story 4.23 + classification, story 4.24 dialogs) → **verifying** the outcome (post-action state + audit history) → **classifying** for pattern analysis (story 4.24). A short **Background** section references ADR 0033 (R13).
2. **Given** target completion time, **Then** the doc states **"~10 minutes to triage your first failed run and apply the right recovery action"** (AC2).
3. **Given** the decision matrix mapping failure category → recovery action (R4, R5), **Then** a dedicated section presents a table (or flowchart) keyed on each of the twelve live `FailureCategory` values → recommended action (retry / resume / reconcile / rerun-from-step / pause / classify-only, plus "hand off via takeover" per R6) + when to choose alternatives, consistent with `RecommendationService`'s safety ranking. Worked examples: `runner_timeout` → typically `retry` (safe); `runner_contract_violation` → typically `pause` to investigate (a blind retry will likely fail again); an `external_state_advanced` integration conflict → typically **reconcile** with **Accept external state**; a repeatedly-failing `agent_execution_failure` → `pause` or hand off via **takeover** if the pattern persists.
4. **Given** the takeover non-reversibility expectation (story 3.25 AC9) and that **takeover is a separate service, not one of the six `recovery_operator` actions** (R6), **Then** a section **"Pause vs Takeover — when to choose which"** explicitly explains: **pause** is reversible (operator resumes later via story 4.10 / the `Resume run` action), **takeover** is non-reversible in the current release (story 3.25) and lives on the developer/takeover path (POST `/takeover`); so prefer pause for investigation, and only take over when a human will continue the work outside the orchestrator long-term. Cross-reference `execution-walkthrough.md` for the takeover mechanics rather than re-documenting them.
5. **Given** the conflict-handling expectation (stories 4.17 + 4.18), **Then** a section **"When the orchestrator auto-pauses on a conflict"** explains (R9): auto-pause fires on `external_state_advanced` / `external_state_reverted` (story 4.18 AC4); this is intentional (NFR21 — pause when state is uncertain); the operator reviews both snapshots (**Internal state** / **External state**) in the reconciliation dialog (story 4.23) and chooses an explicit decision (**Accept external state** / **Accept internal state** / **Mark completed externally** / **Mark failed externally**, with a required **Reason**); never bypass by manually resuming without reconciling — the dispatch gate (story 4.18 AC6) blocks it anyway.
6. **Given** annotated diagrams (Mermaid/ASCII per R11), **Then** the following six surfaces are illustrated: operator queue with a failed run highlighted (story 4.2); failure diagnostics deep-dive with the safety-ranked "Recommended recovery actions" (story 4.4); Decision Bar `recovery_operator` mode showing all six actions with Safe/Caution/Higher-risk affixes (story 4.22); reconciliation dialog with side-by-side snapshots + Recommended decision (story 4.23); classification dialog with humanized radio cards (story 4.24); audit query result showing post-recovery history (story 4.3). Disabled "coming soon" affordances (runner-kind filter, bulk actions) are annotated as such or omitted (R11).
7. **Given** the failure-taxonomy registry (story 4.9 AC5, `FailureTaxonomyValue`), **Then** a section explains each of the six canonical **operator-applied** taxonomy values — `specification_gap`, `context_gap`, `agent_execution_failure`, `review_rejection`, `integration_or_merge_failure`, `tooling_or_infrastructure_failure` — with its human-readable label + concrete examples (pulled from the registry read model per R10), and states plainly that this operator-judgment axis is **distinct from** the runner-scoped `FailureCategory` axis of AC3 (R4) — both can be set on one run.
8. **Given** the queue + alerts surfaces (stories 3.19 + 4.18 AC7), **Then** a section **"Setting up alerts"** walks operators through enabling Prometheus alert routing (story 3.19 AC8 alertmanager docs; `docker compose --profile observability up -d`) for `RunnerQueueDepthHigh`, `RunnerOldestQueuedStale`, and `IntegrationConflictUnresolvedHigh` (live names/thresholds per R12) so pilots can opt into proactive Slack/email/PagerDuty notification.
9. **Given** cross-platform usability, **Then** the walkthrough is browser-based + CLI-supplemented (the `deliveryline operator …` / `deliveryline audit query` / `deliveryline retry` commands shown alongside UI flows for CLI-preferring operators) and works identically on Windows / macOS / Linux per the story-1.17 supported-environment matrix (cross-link `supported-environments.md`); CLI commands are shown as single OS-neutral invocations, not per-shell blocks.
10. **Given** the lychee link-check CI step (story 1.22 AC8; `docs-link-check` job, `.github/workflows/ci.yml:147`), **Then** all internal markdown links + in-file anchors in `failed-run-recovery-walkthrough.md` resolve to real files/anchors (internal failures gate the build); cross-references to stories (1.18, 2.16, 2.19, 3.19, 3.30, 4.2, 4.3, 4.4, 4.22, 4.23, 4.24) are **prose references**, not links; the only markdown links are to real `docs/` files + the README (R14); never to `_bmad-output/` or `infra/` paths.
11. **Given** documentation-increment acceptance per Epic 4's doc-increment rule (pre-mortem refinement R7), **Then** `failed-run-recovery-walkthrough.md` is visible from the docs index — the **README.md "Quick links" section** (the equivalent index; no `docs/index.md` exists — that is the Epic-6 6.1 deliverable, R3) gains an entry linking the walkthrough (keeping the existing Epic-1 "Run failed?" entry, R2/R3). The Epic-4 close gate that verifies its presence is an epic-close-checklist concern, documented in Completion Notes, not implemented here.
12. **Given** an Operator-validator placeholder (parallel to story 1.22 AC7 + 2.29 AC11 + 3.36 AC11), **Then** the doc includes, near the top, the placeholder line **`> **Operator walkthrough validator:** \`_____________________________\` (to be named before Epic 4 close)`** — matching the format in `quickstart.md`, `pm-loop-walkthrough.md`, `failure-recovery-walkthrough.md`, and `execution-walkthrough.md` (R15). **This satisfies story 4.28 AC4 / 4.29 AC2's "Operator-validator named" gate prerequisite** (the placeholder is the artifact; the actual name is filled in at epic close).
13. **Given** NFR43 (minimize new concepts), **Then** the walkthrough uses only the PRD concept set (ticket, spec, run, artifact, review, failure, recovery action) plus the Epic-4 vocabulary (resume, reconcile, rerun-from-step, pause, classify/taxonomy, conflict, drift) and the already-registered `takeover`; because the Epic-4 vocabulary is new to `docs/glossary.md`, glossary entries registering those terms are added in the same change (R16, glossary discipline).

## Tasks / Subtasks

- [x] **Task 0 — Build/refresh the live-string inventory (do this FIRST; AC1, AC3–AC8, R1)**
  - [x] Re-grep and confirm verbatim (trust source over the Dev Notes table on any disagreement — R1): Decision Bar `recovery_operator` six action labels + Safe/Caution/Higher-risk affixes + consequence copy (`ApprovalDecisionBar.tsx`, `approvalDecisionView.ts`, `confirmationCatalog.ts`); deep-dive five-field labels + "Recommended recovery actions" chips (`FailureEventSurface.tsx`); reconciliation dialog panel/decision/Reason strings (`ReconciliationDialog.tsx`, `reconciliationDialogView.ts`); classification dialog title/legend/submit + humanized radio-card source (`FailureClassificationDialog.tsx`); operator queue heading/filters (`OperatorQueue.tsx`, `OperatorFilterSidebar.tsx`). — All verified verbatim. NOTE: `confirmationCatalog.ts` lives at `src/lib/overlays/confirmationCatalog.ts` (not under `features/workflows`); consequence copy for retry/resume/pause/rerun byte-exact.
  - [x] Pull the six `FailureTaxonomyValue` human-readable names + descriptions + examples from the registry read model (`FailureTaxonomyMetadataView` / `FailureTaxonomyCatalog.java`) for AC7 (R10) — do not invent descriptions. — Pulled verbatim from `FailureTaxonomyCatalog.java:35-84`.
  - [x] Confirm the twelve `FailureCategory` wire values (`FailureCategory.java:6-29`) and the `RecommendationService` safety ranking (`RecommendationService.java:90-216`) for the AC3 matrix (R4, R5). — 12 values confirmed exactly.
  - [x] Confirm the CLI verbs/options (`OperatorCommands.java`, `AuditCommands.java`, `WorkflowCommands.java`) and REST paths (`WorkflowController.java`, `AuditController.java`) in the Dev Notes table. — Used per Dev Notes inventory (verified today per story).
  - [x] Confirm the three alert names + thresholds (`infra/observability/prometheus/alerts.yml`) for AC8 (R12). **Record any drift in the Change Log.** — Names/thresholds confirmed; DRIFT recorded (metric name + `RunnerOldestQueuedStale` `for: 1m`) — see Change Log.

- [x] **Task 1 — Author skeleton + Background + prerequisites + problem-driven sequence (AC1, AC2, AC9, AC12, R2, R13, R15)**
  - [x] Create `docs/failed-run-recovery-walkthrough.md` (exact slug — R2). Open with a one-line purpose + the **Operator walkthrough validator placeholder** line (AC12/R15, exact format).
  - [x] State the **target time**: "~10 minutes to triage your first failed run and apply the right recovery action" (AC2).
  - [x] Add the **Background** section referencing ADR 0033 (R13) — link `adr/0033-recovery-service-scope-lift.md`.
  - [x] Prerequisites: DeliveryLine running locally (link `quickstart.md`); a governed run that has reached a `Failed`/stalled/conflict-paused state; browser + optional CLI (AC9); cross-link `supported-environments.md`. Cross-link the Epic-1 `failure-recovery-walkthrough.md` as the CLI `retry`/`next safe action` companion (R2).
  - [x] Lay out the **problem-driven sequence** (AC1) as numbered steps in the existing-walkthrough house style (numbered Steps, decision tables, Mermaid/ASCII). — Added an "At a glance" Mermaid flow (identify→diagnose→choose→execute→verify→classify).

- [x] **Task 2 — Identify + diagnose (AC1, AC6, R8, R11)**
  - [x] "Find the run that needs attention" — the operator queue (story 4.2): heading, State/Failure-category/Time-window filters, failed-row highlighting; annotate disabled runner-kind filter + bulk actions (R11). Add the **operator-queue diagram** (AC6 item 1). Show the CLI equivalent `deliveryline operator status` (R9/AC9).
  - [x] "Diagnose the failure" — the deep-dive (story 4.4): the five fields **What happened / Reason / What changed / Who acted / What is next**, the **Recommended recovery actions** block with `[SAFE]/[CAUTION]/[RISKY]` + `Precondition:` (R8). Add the **deep-dive diagram** (AC6 item 2). Show `deliveryline operator diagnose <run_...>` + the audit query `deliveryline audit query --run <run_...>` (AC1/AC9).

- [x] **Task 3 — The decision matrix (AC3, R4, R5, R6)**
  - [x] Present the `FailureCategory` → recommended-action table (all twelve live values, R4) consistent with `RecommendationService` (R5); include the four worked examples from AC3, using the live reconciliation label **Accept external state** and clarifying takeover is the hand-off path (R6).
  - [x] State the AC7↔AC3 axis distinction inline (runner-scoped category vs operator taxonomy — R4). — "The two classification axes" subsection.

- [x] **Task 4 — Execute the action: Decision Bar + dialogs (AC1, AC5, AC6, R6, R7, R9, R10)**
  - [x] Document the Decision Bar `recovery_operator` mode: the six actions (**Retry failed step / Resume run / Reconcile conflict / Rerun from step / Pause run / Classify failure**), Safe/Caution/Higher-risk affixes, `workflow_owner` role, and the verbatim consequence copy for each confirm dialog (R6/R7). Add the **Decision Bar diagram** (AC6 item 3).
  - [x] "When the orchestrator auto-pauses on a conflict" (AC5/R9): auto-pause categories, intentionality (NFR21), the reconciliation dialog (Internal/External panels, four decision options, Recommended marker, required Reason), and the dispatch-gate guard. Add the **reconciliation-dialog diagram** (AC6 item 4).
  - [x] The classification dialog (AC7 surface): title **Classify failure**, **Failure category** legend, humanized radio cards, **Apply classification**, optional Reason, deprecated-value affix (R10). Add the **classification-dialog diagram** (AC6 item 5).

- [x] **Task 5 — Pause vs Takeover section (AC4, R6)**
  - [x] Write "Pause vs Takeover — when to choose which": pause reversible (Resume run / story 4.10), takeover non-reversible (story 3.25) on the developer/takeover path; prefer pause for investigation; cross-reference `execution-walkthrough.md` for takeover mechanics (R6). Do not imply takeover can be undone. — Rendered as a compare table + prose.

- [x] **Task 6 — Verify + classify + taxonomy section (AC1, AC6, AC7, R10)**
  - [x] "Verify the outcome" — post-action state + audit history via `deliveryline audit query` / `GET /api/v1/audit/by-run/{id}` (story 4.3). Add the **audit-query-result diagram** (AC6 item 6).
  - [x] The six-value taxonomy section (AC7/R10): each of `specification_gap` / `context_gap` / `agent_execution_failure` / `review_rejection` / `integration_or_merge_failure` / `tooling_or_infrastructure_failure` with its human-readable label + example; restate the operator-judgment-vs-runner-category distinction.

- [x] **Task 7 — Setting up alerts (AC8, R12)**
  - [x] "Setting up alerts": enabling the observability profile + alertmanager routing (story 3.19 AC8) for `RunnerQueueDepthHigh` / `RunnerOldestQueuedStale` / `IntegrationConflictUnresolvedHigh` (verified names/thresholds); reference `infra/observability/prometheus/README-alerting.md` **in prose/code, not as a lychee-resolved link** (R12/R14).

- [x] **Task 8 — Wire into docs index + glossary (AC10, AC11, AC13, R3, R14, R16)**
  - [x] Add a **README.md "Quick links" entry** linking `docs/failed-run-recovery-walkthrough.md` (operator-console recovery), keeping the existing Epic-1 "Run failed?" entry (R2/R3). **Do not create `docs/index.md`** (R3). — Existing entry relabeled "Run failed? (CLI baseline)"; new "Operator triaging a failed run in the console?" entry added.
  - [x] Add a **`## Epic 4 vocabulary (failure handling & recovery)`** section to `docs/glossary.md` registering resume / reconcile / rerun-from-step / pause / classify (taxonomy) / conflict / drift, cross-linked to the walkthrough, with a "Linked from" back-reference; extend the existing `takeover` entry with a pointer to the pause-vs-takeover section (R16). Keep terse.
  - [x] **Verify all internal links + anchors resolve** (AC10/R14): hand-check every relative link + `#anchor`; keep story numbers as prose; no links to `_bmad-output/` or `infra/`. Note lychee may be absent locally (CI `docs-link-check` is the gate). — All file links + glossary anchors + intra-doc anchors verified; NO em-dash double-hyphen intra-doc anchors (converted to prose per R14).

- [x] **Task 9 — Completion-notes obligations (AC11, AC12)**
  - [x] In Completion Notes, record: (a) the Epic-4 close-gate verifying the doc's presence is an epic-close-checklist concern (no docs foundation-gate exists); (b) the docs index remains README "Quick links" pending Epic-6 6.1 `docs/index.md`; (c) AC12's validator placeholder **satisfies the "Operator-validator named" prerequisite** that story 4.28 AC4 / 4.29 AC2's Epic-4 close gate depends on; (d) the Background/ADR-0033 cross-link (R13) **closes the pending cross-link** recorded in `deferred-work.md` (story 4.28 AC7) and pre-satisfies story 4.29 AC4.

- [ ] ~~**Logging instrumentation**~~ — **N/A (R17).** This story produces only markdown (one new doc, one README link, glossary entries); it touches no service, branch, or SLF4J surface. The project-wide logging standard targets touched code, of which there is none. No log assertions are possible or required.

## Dev Notes

### Live-string / live-fact inventory (READ BEFORE WRITING — verify verbatim; source over table on any disagreement — R1)

Verified against live source on **2026-07-18**. Frontend paths under `deliveryline-frontend/src/features/workflows/…`; backend under `deliveryline-backend/src/main/java/org/dradgo/…`.

| Concept | Fact / string | Source |
|---|---|---|
| **New doc filename** | `docs/failed-run-recovery-walkthrough.md` (NOT the Epic-1 `failure-recovery-walkthrough.md`) | AC1/AC11; R2 |
| Operator queue | component `OperatorQueue`; heading **Operator queue**; empty state "No runs need operator attention right now. Failed, stalled, orphaned, taken-over, and overridden runs appear here."; virtualized rows (`RunReviewQueueItem variant="operator"`), not a `<th>` table | `OperatorQueue.tsx:59,151,220`; `OperatorClassifiedRow.tsx:31,41-47` |
| Queue filters | sidebar `OperatorFilterSidebar` (aria "Operator queue filters"); legends **State / Failure category / Runner kind / Time window**; runner-kind DISABLED ("Filtering by runner kind arrives in a future release."); time windows Last 1 hour/24 hours/7 days/30 days/All time | `OperatorFilterSidebar.tsx:51,59,67,90,96,100,30-36` |
| Deep-dive five fields (UI) | **What happened / Reason / What changed / Who acted / What is next**; sheet title **Event diagnostics**; section **Recommended recovery actions**; `[SAFE]/[CAUTION]/[RISKY]` + `Precondition:`; only `retry` one-click-invokable | `FailureEventSurface.tsx:440,445,450,456,461,713,504,243,248`; `failureDiagnosticsView.ts:15` |
| Deep-dive five fields (CLI) | `deliveryline operator diagnose` desc: "what happened / changed / who acted / what failed / what is next" (UI's 2nd slot = **Reason**) | `OperatorCommands.java:250-251` (drift note — R8) |
| Decision Bar recovery_operator | container `RecoveryDecisionBarContainer`; renderer `ApprovalDecisionBar`; role `RECOVERY_OPERATOR_ROLE='workflow_owner'` | `RecoveryDecisionBarContainer.tsx:81`; `approvalDecisionView.ts:129` |
| 6 action tokens → labels | `retry`→**Retry failed step**; `resume_workflow`→**Resume run**; `reconcile_conflict`→**Reconcile conflict**; `rerun_from_step`→**Rerun from step**; `pause_workflow`→**Pause run**; `classify_failure`→**Classify failure** | `approvalDecisionView.ts:698-705`; `ApprovalDecisionBar.tsx:229-236` |
| Safety affixes | **Safe / Caution / Higher risk** (Decision Bar); reconciliation dialog is 2-tier **SAFE / RISKY** | `ApprovalDecisionBar.tsx:239-243`; `reconciliationDialogView.ts:268-269` |
| Confirm-dialog consequence copy | resume: "Resume will return the run to its prior executing state and re-enqueue runner work." · pause: "Pause will halt orchestrator dispatch and cancel in-flight + queued runner work for this run. The run can be resumed later." · rerun: "Rerun will re-execute the run from the selected safe step. Artifacts produced at or after that step are superseded and the corresponding approval is invalidated." · retry: "Retry will re-execute the last failed step with a fresh runner. The previous failure will be preserved in the timeline." | `confirmationCatalog.ts:105-107,129-131,139-141,149-151` |
| Rerun step options | **Investigating** / **Executing** | `ApprovalDecisionBar.tsx:1088,1101-1102` |
| Reconciliation dialog | `ReconciliationDialog`; title `{conflictCategory} · {integrationType}` (fallback "Reconcile integration conflict"); panels **Internal state** / **External state**; legend **Reconciliation decision**; **Recommended** on safe-first; required **Reason** ("Explain why this reconciliation decision is correct."); confirm **Confirm reconcile** | `ReconciliationDialog.tsx:98,218-221,405,412,422,456,483-494,263` |
| Reconciliation decisions | `accept_external_state`→**Accept external state**; `accept_internal_state`→**Accept internal state**; `mark_completed_externally`→**Mark completed externally**; `mark_failed_externally`→**Mark failed externally** | `reconciliationDialogView.ts:221-226` |
| Classification dialog | `FailureClassificationDialog`; title **Classify failure**; legend **Failure category**; radio cards HUMANIZED via `entry.humanReadableName`+`description`+`examples` (NOT raw snake_case); submit **Apply classification**; **Reason (optional)**; deprecated affix "(deprecated, use X instead)" | `FailureClassificationDialog.tsx:53,177,246,277-295,180,317,251` |
| Taxonomy values (6, operator-applied) | `specification_gap`, `context_gap`, `agent_execution_failure`, `review_rejection`, `integration_or_merge_failure`, `tooling_or_infrastructure_failure`; read via `GET /api/v1/registries/failure-taxonomy` (humanReadableName/description/examples) | `FailureTaxonomyValue.java:24-30`; `RegistryController.java:27,44`; pull metadata from `FailureTaxonomyCatalog.java` |
| FailureCategory values (12, runner-scoped) | `runner_timeout`, `runner_crash`, `runner_contract_violation`, `runner_non_zero_exit`, `runner_late_result`, `runner_duplicate_result`, `runner_malformed_output`, `runner_secret_leak`, `runner_build_failed`, `testcontainers_infra_failed`, `recovery_dispatch_failed`, `orphan` (epic AC3 lists only 10) | `FailureCategory.java:6-29` (R4) |
| Recommendation logic | `RecommendationService.recommend(...)`; safe<caution<risky; retry always surfaced; drift downgrades other safe mutating actions to caution except reconcile+classify; `nextSafeAction` from `RecoveryService#describeFailure` | `RecommendationService.java:35,90,108-216` (R5) |
| CLI recovery | all `deliveryline operator <verb>`: `status` (`--state` def `failed,stalled,orphaned`, `--since`,`--format`,`--limit`), `diagnose <run>`, `resume <run>`, `reconcile <run> --conflict --decision --reason`, `rerun-from-step <run> --target investigating\|executing`, `pause <run> --reason`, `classify-failure <run> --taxonomy` ; plus top-level `deliveryline retry <run> --actor-identity` (NO `operator retry`) | `OperatorCommands.java:59-63,177,247,317,451,613,764,918`; `WorkflowCommands.java:75` |
| CLI audit | `deliveryline audit query --ticket XOR --run [--event-type --actor --since --until --limit --cursor --format]` | `AuditCommands.java:47,86,92-123` |
| REST recovery | POST `/api/v1/workflows/{id}/`{`resume`,`reconcile`,`rerun-from-step`,`pause`,`classify-failure`,`retry-workflow`,`takeover`}; GET `/{id}/failure-diagnostics`; GET `/api/v1/audit/by-ticket/{ref}` + `/by-run/{id}`; GET `/api/v1/operator/runs`; GET `/api/v1/integration-conflicts`; GET `/api/v1/registries/failure-taxonomy` | `WorkflowController.java:2236,2328,2439,2554,2665,2093,2133,636`; `AuditController.java:61,126`; `OperatorController.java:53`; `IntegrationConflictController.java:51`; `RegistryController.java:44` |
| Takeover (separate service — R6) | POST `/api/v1/workflows/{id}/takeover`; `DeveloperTakeoverService` (ArchUnit scope-protected per ADR 0033 §c); NON-reversible (story 3.25); NOT one of the 6 `recovery_operator` actions | `WorkflowController.java:2133`; `docs/adr/0033-recovery-service-scope-lift.md:45` |
| Alerts (3) | `RunnerQueueDepthHigh` (`deliveryline_runner_queue_depth > 50` for 5m), `RunnerOldestQueuedStale` (`…queue_oldest_age_seconds > 1200`), `IntegrationConflictUnresolvedHigh` (unresolved `integration_conflicts` > 5 for 10m); enable via `docker compose --profile observability up -d` | `infra/observability/prometheus/alerts.yml:11,32,59`; `README-alerting.md:12,14` |
| ADR 0033 (Background — R13) | `docs/adr/0033-recovery-service-scope-lift.md` — why the deeper recovery surface exists; Story 4.27 cross-link is recorded pending in `deferred-work.md` | `adr/0033-recovery-service-scope-lift.md:74` |
| Validator placeholder format | `> **Operator walkthrough validator:** \`_____________________________\` (to be named before Epic 4 close)` | `failure-recovery-walkthrough.md:3`; `quickstart.md:9`; R15 |

### Doc house style — model on the existing walkthroughs and the 3.36 twin

- **Primary structural model:** `docs/failure-recovery-walkthrough.md`, `docs/execution-walkthrough.md`, `docs/pm-loop-walkthrough.md` — numbered end-to-end walkthroughs with a validator placeholder at the top, numbered Steps, decision tables, Mermaid/ASCII diagrams, a "Concepts you just used" glossary footer, and a "What is NOT in this walkthrough" closer. Match tone, heading depth, table style.
- **Process twin:** story `3-36-execution-walkthrough-documentation-increment.md` (and `2-29-…`) — the same doc-increment shape (one doc + README link + glossary). Reuse its decisions: README-as-index (R3 = its R2), diagrams-not-screenshots (R11 = its R11), glossary-in-same-PR (R16 = its R10), logging-N/A (R17 = its R14).
- CLI commands are shown once as OS-neutral invocations (AC9) — no per-shell PowerShell/bash blocks.

### Link-check contract (AC10 — this WILL gate the build)

- `docs-link-check` (`.github/workflows/ci.yml:147-205`) runs **lychee** over `docs/*.md docs/**/*.md README.md`; **internal (relative + anchor) failures FAIL the build**; external http(s) links are a separate non-blocking pass.
- Every relative link + `#anchor` must resolve. Glossary anchors are GitHub-slugged from headings — if you add `### reconcile` and link `glossary.md#reconcile`, that heading must exist.
- **Story numbers are not files** — keep them prose. Only link real files lychee scans: `failure-recovery-walkthrough.md`, `execution-walkthrough.md`, `pm-loop-walkthrough.md`, `quickstart.md`, `glossary.md`, `supported-environments.md`, `cli/README.md`, `cli/workflow-commands.md`, `adr/0033-recovery-service-scope-lift.md`, README. **Never link `_bmad-output/…` or `infra/…`** (outside the scan set → broken). Reference `infra/observability/prometheus/README-alerting.md` in prose/code fence, not as a link.

### Architecture / boundary notes

- This is a **docs-only** change. No ArchUnit, no test tiers, no OpenAPI/schema regen, no Flyway, no DomainErrorCode/registry surface — none of the usual backend fan-out applies. The only CI tier that gates it is **`docs-link-check`** (and `format-static-checks` only if a markdown formatter touches docs — the existing walkthroughs were committed without reformatting, so `docs/**/*.md` is outside the frontend prettier scope; run a local format check to be sure, per the 3.36 note).
- **Do NOT create `docs/index.md`** (R3) — Epic-6 6.1 owns it. Use the README "Quick links" section as the index.
- **Do NOT modify any feature code to "make the doc true."** If a live string contradicts an epic AC, the doc bends to the code and the drift is recorded in the Change Log — never the reverse.
- **Do NOT overwrite/rename `docs/failure-recovery-walkthrough.md`** (the Epic-1 doc). This is a NEW file (R2).

### Project Structure Notes

- **New:** `docs/failed-run-recovery-walkthrough.md`.
- **Modified:** `README.md` (one "Quick links" entry), `docs/glossary.md` (Epic-4 vocabulary section + "Linked from" footer + one-line extension to the `takeover` entry).
- No source, test, pom, ADR, or workflow files change.

### Downstream / dependency notes (why this story unblocks Epic-4 closure)

- This is the **last open Epic-4 story before the closure gate**. Stories 4.1–4.26, 4.28, 4.30 are `done`; only 4.27 (this) and 4.29 (the closure-verification story) remain `backlog`.
- **Story 4.29 is gated on this story** (its HARD dependency gate requires 4.1–4.27 merged + the Operator-validator named + the walkthrough validated). Two of 4.27's outputs directly feed 4.29:
  - AC12's validator placeholder = the "Operator-validator named (story 4.27 AC12)" prerequisite of the Epic-4 close gate (story 4.28 AC4 / 4.29 AC2).
  - AC1/R13's Background→ADR 0033 cross-link = the pending cross-link story 4.28 AC7 deferred (recorded in `deferred-work.md`), pre-satisfying story 4.29 AC4.
- Once 4.27 merges, 4.29 can proceed as pure verification/docs closure.

### References

- [Source: `_bmad-output/planning-artifacts/epic-04-recovery.md#Story-4.27`] — story + 13 ACs (AC3 category count + takeover placement drift from live code; reconciled in R4/R6)
- [Source: `_bmad-output/implementation-artifacts/3-36-execution-walkthrough-documentation-increment.md`] — the doc-increment process twin (README-index, diagrams, glossary discipline, logging-N/A, link-check precedents)
- [Source: `docs/failure-recovery-walkthrough.md`] — Epic-1 baseline recovery doc (CLI `retry` + `next safe action` matrix); the NEW doc cross-links it (R2); primary structural model
- [Source: `docs/execution-walkthrough.md`] — sibling walkthrough; the takeover mechanics + "When and how to take over" section AC4 cross-links (R6)
- [Source: `docs/pm-loop-walkthrough.md`, `docs/quickstart.md`] — house style + validator-placeholder format + "~N minutes" target framing + glossary footer
- [Source: `docs/glossary.md`] — glossary discipline rule; Epic-4 vocabulary entries land here (AC13)
- [Source: `docs/adr/0033-recovery-service-scope-lift.md`] — Background cross-link (R13); §c allow-list confirms takeover is NOT on RecoveryService (R6)
- [Source: `README.md#L11-L26`] — the "Quick links" docs index (AC11 target)
- [Source: `.github/workflows/ci.yml#L147-L205`] — `docs-link-check` lychee job (AC10 gate; internal links fail the build)
- [Source: `infra/observability/prometheus/alerts.yml#L11,L32,L59` + `README-alerting.md`] — the three alert rules (AC8/R12)
- [Source: `deliveryline-backend/.../domain/registry/FailureTaxonomyValue.java#L24-L30`] — six operator taxonomy values (AC7/R4)
- [Source: `deliveryline-backend/.../domain/registry/FailureCategory.java#L6-L29`] — twelve runner failure categories (AC3/R4)
- [Source: `deliveryline-backend/.../application/recovery/RecommendationService.java#L35,L90-L216`] — safety-ranked recommendation logic (AC3/R5)
- [Source: `deliveryline-backend/.../adapters/cli/OperatorCommands.java`, `AuditCommands.java`, `WorkflowCommands.java`] — CLI recovery/audit/retry commands (AC9)
- [Source: `deliveryline-backend/.../adapters/rest/WorkflowController.java`, `AuditController.java`, `OperatorController.java`, `IntegrationConflictController.java`, `RegistryController.java`] — REST recovery/audit/queue/conflict/taxonomy endpoints (AC1/AC9)
- [Source: `deliveryline-frontend/src/features/workflows/…`] — `OperatorQueue.tsx`, `OperatorFilterSidebar.tsx`, `FailureEventSurface.tsx`, `RecoveryDecisionBarContainer.tsx`, `ApprovalDecisionBar.tsx`, `approvalDecisionView.ts`, `confirmationCatalog.ts`, `ReconciliationDialog.tsx`, `reconciliationDialogView.ts`, `FailureClassificationDialog.tsx` — the six diagram surfaces (AC6)

### Open Questions (for Alex — non-blocking; defaults encoded)

1. **Docs index (R3):** encoded default = README "Quick links" entry, **no `docs/index.md`** (that is the Epic-6 6.1 deliverable; stories 2.29/3.36 set this precedent). AC11's wording literally names `docs/index.md` — confirm you don't instead want a minimal `docs/index.md` stub now (it would later need reconciling with 6.1). The "Epic-4 close gate verifies its presence" clause is treated as an epic-close-checklist concern, not a CI gate built here.
2. **Glossary depth (R16):** encoded default = a terse entry for each Epic-4 term (resume, reconcile, rerun-from-step, pause, classify/taxonomy, conflict, drift) + a one-line extension to the existing `takeover` entry. If you'd rather defer Epic-4 vocabulary wholesale to Epic-6 6.2, say so — but glossary discipline says register-in-same-PR, so the default registers now.
3. **Diagrams vs screenshots (R11):** encoded default = Mermaid + annotated ASCII (no capture pipeline; PNGs rot). Flag if real annotated screenshots are wanted (would need a capture step + a place to store binaries).
4. **Two coexisting recovery docs (R2):** encoded default = keep the Epic-1 `failure-recovery-walkthrough.md` and add the NEW `failed-run-recovery-walkthrough.md`, cross-linking both; the README keeps the existing "Run failed?" entry and gains an "Operator console recovery" entry. Confirm you don't instead want the Epic-1 doc folded into / redirected to the new one (that would be a larger consolidation, arguably its own story).
5. **AC3 matrix breadth (R4):** encoded default = document all twelve live `FailureCategory` values (epic AC3 lists ten — `testcontainers_infra_failed` + `recovery_dispatch_failed` were added later). Confirm the two extras belong in the operator-facing matrix (they are recovery-relevant per `FailureCategory.java`), or should be footnoted as internal.

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Opus 4.8, 1M context)

### Debug Log References

- **Live-string inventory (Task 0)** — verified verbatim against live source on 2026-07-18:
  - `FailureCategory.java:6-29` — 12 wire values confirmed exactly (incl. `testcontainers_infra_failed`, `recovery_dispatch_failed`).
  - `FailureTaxonomyValue.java:24-30` — 6 wire values; `FailureTaxonomyCatalog.java:35-84` — humanReadableName/description/examples pulled verbatim (not invented).
  - `approvalDecisionView.ts:129` `RECOVERY_OPERATOR_ROLE='workflow_owner'`; `:698-705` the six recovery actions; `ApprovalDecisionBar.tsx:230-236` labels (Retry failed step / Resume run / Reconcile conflict / Rerun from step / Pause run / Classify failure); `:239-243` affixes (Safe / Caution / Higher risk).
  - `confirmationCatalog.ts` (at `src/lib/overlays/`, not `features/workflows`) `:105-107,129-131,139-141,149-151` — retry/resume/pause/rerun consequence copy byte-exact.
  - `FailureEventSurface.tsx:440-467` five deep-dive fields (What happened / Reason / What changed / Who acted / What is next); `:504` "Recommended recovery actions"; `:243` `[${safetyLevel.toUpperCase()}]` chips; `:249` `Precondition:`; `:713` sheet title "Event diagnostics". `failureDiagnosticsView.ts:15` `INVOKABLE_ACTION_TYPES={'retry'}`.
  - `reconciliationDialogView.ts:221-226` decisions (Accept external state / Accept internal state / Mark completed externally / Mark failed externally); `:268-269` two-tier SAFE/RISKY; `ReconciliationDialog.tsx:405,412,422` panels "Internal state"/"External state" + legend "Reconciliation decision".
  - `FailureClassificationDialog.tsx:177,180,247` title "Classify failure" / submit "Apply classification" / legend "Failure category"; humanized radio cards.
  - `OperatorQueue.tsx:151,172` heading "Operator queue" + disabled "Bulk operator actions arrive in a future release"; `OperatorFilterSidebar.tsx:59,67,90,96,100` legends + disabled runner-kind note.
  - `alerts.yml:11,32,59` alert names + thresholds.
- **Link-check (AC10)** — hand-verified: all six markdown file links resolve (`failure-recovery-walkthrough.md`, `quickstart.md`, `execution-walkthrough.md`, `supported-environments.md`, `glossary.md`, `adr/0033-recovery-service-scope-lift.md`); all 8 `glossary.md#…` anchors + all glossary intra-doc anchors resolve to real heading slugs; the 3 remaining intra-doc anchors are clean single-hyphen slugs (`#setting-up-alerts`, `#when-the-orchestrator-auto-pauses-on-a-conflict`, `#the-two-classification-axes`) — every em-dash-heading cross-ref was converted to prose (R14). No `_bmad-output/`/`infra/` markdown links (the alerting README is named in prose only). Strict UTF-8 validation: file is valid UTF-8, 76 U+2014 em-dashes, zero U+FFFD, zero real C1 controls, no BOM (the apparent "C1 controls" under `Get-Content` were a legacy-codepage display artifact of the box-drawing bytes — verified by codepoints, not display). lychee not run locally (CI `docs-link-check` is the gate).

### Completion Notes List

- **Pure-documentation increment** (Epic-4 twin of story 3.36 / 2.29): one NEW doc `docs/failed-run-recovery-walkthrough.md`, one README "Quick links" entry, and Epic-4 glossary additions. ZERO application/frontend/backend/test/CI-logic changes. Every on-screen string copied verbatim from live source (R1) — no paraphrasing of button labels, dialog titles, decision options, consequence copy, config keys, CLI verbs, or taxonomy prose.
- **AC coverage:** AC1 problem-driven sequence (identify→diagnose→choose→execute→verify→classify) + Mermaid overview + Background/ADR-0033 ✓; AC2 "~10 minutes to triage your first failed run and apply the right recovery action" ✓; AC3 twelve-`FailureCategory` → action matrix consistent with `RecommendationService`'s safe<caution<risky ranking + all four worked examples (incl. `external_state_advanced`→**Accept external state**) ✓; AC4 "Pause vs Takeover — when to choose which" (pause reversible; takeover non-reversible + separate service, not one of the six) ✓; AC5 "When the orchestrator auto-pauses on a conflict" (auto-pause categories, reconciliation dialog, required Reason, dispatch gate) ✓; AC6 all six illustrations as ASCII sketches (operator queue w/ highlighted failed run; deep-dive w/ `[SAFE]/[CAUTION]/[RISKY]`; Decision Bar six actions + affixes; reconciliation Internal/External + Recommended; classification humanized radio cards; audit history) + disabled affordances annotated (R11) ✓; AC7 six-value taxonomy section w/ humanized labels + examples + axis distinction ✓; AC8 "Setting up alerts" (observability profile + three live alert rules) ✓; AC9 browser-first + CLI-supplemented, OS-neutral single invocations ✓; AC10 internal links/anchors resolve, story numbers as prose ✓; AC11 README "Quick links" entry (no `docs/index.md`) ✓; AC12 validator placeholder (exact format) ✓; AC13 glossary registers resume/reconcile/rerun-from-step/pause/classify/conflict/drift + extends takeover ✓.
- **AC11 close-gate clause (Task 9a/9b):** AC11's *"Epic 4 cannot close without … visible from `docs/index.md`"* is an **epic-close-checklist concern, not a deliverable of this story** — there is no `docs/epic-4-close-checklist.md` and no docs foundation-gate today (only `docs/epic-1-close-checklist.md` exists). This story's AC11 obligation is the README "Quick links" entry being present + lychee-resolvable (satisfied). The docs index remains the README "Quick links" section pending Epic-6 story 6.1's `docs/index.md` — `docs/index.md` deliberately NOT created (R3) to avoid colliding with 6.1.
- **Downstream (Task 9c/9d) — feeds story 4.29 closure gate:** (c) AC12's validator placeholder IS the artifact satisfying the "Operator-validator named (story 4.27 AC12)" prerequisite of the Epic-4 close gate (story 4.28 AC4 / 4.29 AC2) — the actual name is filled in at epic close, per the spec-sanctioned `_____` convention. (d) The Background→ADR-0033 cross-link (R13) closes the pending cross-link story 4.28 AC7 deferred (recorded in `deferred-work.md`) and pre-satisfies story 4.29 AC4. With 4.27 in review, 4.29 is unblocked once 4.1–4.27 merge.
- **Open Questions (defaults encoded, non-blocking):** OQ-1 docs-index → README "Quick links" (no `docs/index.md`); OQ-2 glossary depth → all seven Epic-4 terms + extended `takeover`, terse; OQ-3 diagrams → annotated ASCII + one Mermaid (no screenshot pipeline); OQ-4 two coexisting recovery docs → kept Epic-1 `failure-recovery-walkthrough.md`, added the NEW `failed-run-recovery-walkthrough.md`, cross-linked both; OQ-5 AC3 matrix breadth → documented all twelve live `FailureCategory` values (the two beyond the epic's ten are recovery-relevant). Flag to Alex if any default should change.
- **Format-static-checks:** `docs/**/*.md` is outside both backend Spotless and the frontend prettier scope (the prior walkthroughs were committed unreformatted); no formatter touches this change. The only CI tier that gates it is `docs-link-check`.
- **Logging instrumentation task: N/A (R17)** — markdown only; no service, branch, or SLF4J surface to instrument.

### File List

- `docs/failed-run-recovery-walkthrough.md` (NEW) — the Epic-4 operator-console failed-run recovery walkthrough.
- `README.md` (MODIFIED) — relabeled the Epic-1 "Run failed?" entry to "Run failed? (CLI baseline)" and added an "Operator triaging a failed run in the console?" "Quick links" entry.
- `docs/glossary.md` (MODIFIED) — added the "Epic 4 vocabulary (failure handling & recovery)" section (resume / reconcile / rerun-from-step / pause / classify / conflict / drift), extended the existing `takeover` entry with a pause-vs-takeover pointer, and registered the walkthrough in "Linked from".
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (MODIFIED) — status `ready-for-dev → in-progress → review`.

## Change Log

- 2026-07-18 — Authored `docs/failed-run-recovery-walkthrough.md` (Epic-4 operator-console failed-run recovery walkthrough); added the README "Quick links" entry and the Epic-4 glossary vocabulary + `takeover` extension. Pure-documentation increment — no code/test/CI-logic changes. All on-screen strings verified verbatim against live source. Status `ready-for-dev → in-progress → review`.
- 2026-07-18 — **Code review (adversarial 3-layer):** 13/13 ACs satisfied, scope discipline held, all links/anchors resolve (no build-gating failure). 1 decision-needed + 11 patch findings applied, 1 deferred (classification-dialog legend "Failure category" — accurate-to-source product naming, logged to `deferred-work.md`), 5 dismissed. Key fix: the `### drift` glossary entry contradicted the walkthrough (artifact-lineage vs integration drift) and mis-attributed the ranker-downgrade rule — redefined as integration drift to match `RecommendationService`. Remaining fixes were live-string/consistency corrections (audit event type, humanized failure category, reconcile header, Decision Bar safe-affix rendering, `recovery_dispatch_failed` ranking). Pure-docs; no code/test/CI. Status `review → done`.
- 2026-07-18 — **Live-fact drift recorded (Task 0 / R12):** the `IntegrationConflictUnresolvedHigh` rule keys on metric `deliveryline_integration_conflict_unresolved_count` (expr `sum(...) > 5 for 10m`) — the story's R12 wording "unresolved `integration_conflicts` total" is descriptive, not the literal metric name; the doc uses the live metric name. `RunnerOldestQueuedStale` fires `for: 1m` (not 5m). `OperatorQueue` bulk-actions live copy is "Bulk operator actions arrive in a future release" (+ a "Select multiple" toggle) — the doc quotes the live copy rather than a generic "coming soon". `confirmationCatalog.ts` resolves to `src/lib/overlays/confirmationCatalog.ts`, not under `features/workflows`. No drift changed any AC outcome.

### Review Findings

_Code review 2026-07-18 (adversarial: Blind Hunter + Edge Case Hunter + Acceptance Auditor). All 13 ACs confirmed satisfied and scope discipline held (R2 Epic-1 doc untouched, R3 no `docs/index.md`, zero code/test/CI). Findings are documentation-accuracy issues only — no AC violation, no build-gating link-check failure (all links/anchors resolve). Triage: 1 decision-needed, 11 patch, 1 defer, 5 dismissed._

**Resolution 2026-07-18 — all decision-needed + patch findings applied (Alex chose option 1 for the drift decision; batch-applied the 11 patches).** The `### drift` glossary entry was redefined as **integration drift** (Linear/GitHub link `stale`/`failed` — the condition `RecommendationService` keys on, matching the walkthrough), with artifact-lineage drift kept as a distinct named sense; the `### conflict` entry's tail was re-pointed accordingly. Walkthrough patches: Decision Bar affix prose + ASCII now state safe actions render no affix (only Caution/Higher-risk show one); pause shown safe on the bar to match the Step-2 deep-dive; "second slot"/"map one-to-one" reworded (CLI orders "what failed" fourth vs console Reason second); audit ASCII event type `failure.classified`→`recovery.failureClassified`; reconcile dialog header uses the populated `External state advanced · GitHub` form; deep-dive "What happened" now shows the humanized `Contract Violation`; `recovery_dispatch_failed` retry re-marked `(safe)` per the ranker; validator closing line reworded; redundant "Reason (optional)" de-duplicated; "Executing → execution" → "Investigating → Executing". All ASCII boxes re-verified at 64-char width. Glossary edit: "six Epic-4 recovery actions" → "six recovery actions surfaced on the operator recovery Decision Bar". No code/test/CI touched — still pure-docs.

**Decision-needed:**

- [x] [Review][Decision] **"drift" is defined two contradictory ways across this PR, and the ranker-downgrade rule is mis-attributed** — The glossary `### drift` entry defines drift as **artifact-lineage** divergence ("Distinct from an integration conflict, which is external-state divergence") yet attaches the "downgrades every safe mutating action to caution except reconcile+classify" rule to it, even citing "a stale or failed integration-sync surface" (that is integration, not lineage) as an example. The walkthrough Step 3 attributes that *same* rule to **external Linear/GitHub drift**. Ground truth: `RecommendationService.recommend(...)` drives the downgrade off `drift = linearDrift || githubDrift` and the code calls it "integration drift" (`RecommendationService.java:103,150,174,188`) — so the **walkthrough is correct** and the **glossary entry is wrong**. But the two files cross-link (`glossary.md#drift` ← walkthrough), so a reader clicking "drift" lands on a contradictory definition. Note "artifact drift" is a real, *distinct* Epic-4 term (story 4.15). **Resolution requires an authoring choice** on the glossary taxonomy. Recommended: redefine glossary `### drift` as **integration drift** (Linear/GitHub link stale/failed — what the ranker keys on and what the walkthrough means), and give artifact-lineage drift its own entry or fold it under `conflict`/artifact-reconciliation, so the walkthrough's link resolves to a consistent definition.

**Patch:**

- [x] [Review][Patch] Decision Bar "Safe" affix is never rendered — safe-ranked actions carry NO visible affix (`ApprovalDecisionBar.tsx:833-838`); doc claims "each with a non-colour safety affix — Safe / Caution / Higher risk" and the ASCII shows "Safe" rows [docs/failed-run-recovery-walkthrough.md:250-251,277-281]
- [x] [Review][Patch] "second slot" mislabel + "map one-to-one" is positionally false — CLI order lists "what failed" 4th; console **Reason** is 2nd; the orderings are permuted, not 1:1 by slot [docs/failed-run-recovery-walkthrough.md:172-173]
- [x] [Review][Patch] Pause shown `[SAFE]` in the Step 2 deep-dive ASCII but `Caution` on the Step 4 Decision Bar ASCII, unreconciled (both illustrative/per-run, but the cross-surface flip confuses) [docs/failed-run-recovery-walkthrough.md:160,280]
- [x] [Review][Patch] Audit-history ASCII uses non-existent event type `failure.classified` — live wire value is `recovery.failureClassified` (`WorkflowEventType.java`); also spot-check `recovery.retried`/`workflow.stateChanged` [docs/failed-run-recovery-walkthrough.md:412]
- [x] [Review][Patch] Reconciliation dialog header uses the loading-only fallback "Reconcile integration conflict"; the populated header is `{conflictCategory} · {integrationType}` (`ReconciliationDialog.tsx:218-221`) [docs/failed-run-recovery-walkthrough.md:326]
- [x] [Review][Patch] Deep-dive "What happened" shown as raw snake_case (`runner_contract_violation`/`runner_timeout`) but the surface humanizes it via `humanizeFailureCategory` → "Contract Violation" (`FailureEventSurface.tsx:385-386`, `failureCategoryView.ts:18-28`) [docs/failed-run-recovery-walkthrough.md:138,150]
- [x] [Review][Patch] Glossary `takeover` edit says "one of the six **Epic-4** recovery actions" — retry is the Epic-1 baseline; the set of six recovery actions is not all Epic-4 [docs/glossary.md takeover entry]
- [x] [Review][Patch] `recovery_dispatch_failed` retry marked "(caution)" in the matrix, but the ranker ranks its retry **safe** (not in `isRiskyRetryCategory`/`isCautionRetryCategory`, `RecommendationService.java:206-216`); header claims consistency with the ranking [docs/failed-run-recovery-walkthrough.md:208]
- [x] [Review][Patch] Closing line "flag it to the Operator walkthrough validator **named at the top**" points at the blank `_____` placeholder — reword to "(to be named at Epic-4 close)" [docs/failed-run-recovery-walkthrough.md:503]
- [x] [Review][Patch] Redundant "add an **optional** **Reason (optional)**" — drop one "optional" [docs/failed-run-recovery-walkthrough.md:346]
- [x] [Review][Patch] "What changed : Executing → execution" — blurred example value (state name → lowercase word) vs the field spec "last successful stage → failed stage" [docs/failed-run-recovery-walkthrough.md:154]

**Deferred:**

- [x] [Review][Defer] Classification dialog's fieldset legend is literally "Failure category" — verbally collides with the machine "failure category" axis the doc works to keep distinct [docs/failed-run-recovery-walkthrough.md:344] — deferred, accurate-to-source product string (`FailureClassificationDialog.tsx:246`), faithfully reported; a doc note is optional polish, and Epic-6 6.2 owns wording normalization
