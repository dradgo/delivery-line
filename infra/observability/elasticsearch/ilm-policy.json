{
  "//": "Story 3.7 (AC9 / Decision D1, OQ-4) — minimal index lifecycle policy for DeliveryLine ELK indices. Referenced from docs/adr/0023-elk-replaces-loki.md. 30-day retention bounds index growth on a dev host. Apply once after the stack is up: PUT _ilm/policy/deliveryline-logs with this body, then attach via an index template (see infra/observability/README.md). Edit `max_age` to change retention.",
  "policy": {
    "phases": {
      "hot": {
        "min_age": "0ms",
        "actions": {
          "rollover": {
            "max_age": "1d",
            "max_primary_shard_size": "5gb"
          }
        }
      },
      "delete": {
        "min_age": "30d",
        "actions": {
          "delete": {}
        }
      }
    }
  }
}
