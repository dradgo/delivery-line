# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# TanStack Router generated route tree (story 2.5) — regenerated on every
# `npm run build` / `vite dev`; never committed (AC7).
src/routeTree.gen.ts

# Node + Vite build outputs
node_modules
dist
dist-ssr
.vite
coverage
*.tsbuildinfo
*.local

# Maven + frontend-maven-plugin output
target
.frontend-node

# Playwright (story 2.27) — HTML report + per-test traces/screenshots/videos.
# Browser binaries install to the user cache (npx playwright install), not here.
playwright-report
test-results
.playwright

# Env files
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
